# FORGE/SKILL/ · 内部循环协议（与根 SKILL/ 无关）

> ⚠️ **本目录不是对外 FDE Skill**——它是 FORGE 内部循环（fresh-eyes-loop、release-gate-loop）的协议定义，仅 sofagent 项目维护者使用，单独复制出去无法运行。
>
> 与仓库根 [`SKILL/`](../../SKILL/) 同名但职责完全不同：
>
> | 目录 | 身份 | 读者 |
> |------|------|------|
> | `SKILL/`（仓库根） | 对外 FDE Skill 资产（单一真相源，随 install.sh 分发给用户） | sofagent 用户 / 外部贡献者 |
> | `FORGE/SKILL/`（本目录） | FORGE 内部两循环协议（审查/发版流程定义） | 项目维护者（内部） |
>
> 误读排查指引：如果你是从 Agent 加载链（`~/.sofagent/skills/sofagent/SKILL.md`）找过来的，你要找的是根 `SKILL/`，不是这里。
