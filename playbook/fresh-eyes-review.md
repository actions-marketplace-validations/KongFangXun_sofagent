# sofagent 独立审查 Prompt

> **这不是检查清单，是独立审查工具**——它的价值在于：你不知道你会发现什么。如果你按清单走，你只会找到清单上列的东西。但最危险的问题——那些作者自己看不出来的——往往不在任何清单上。
>
> **你带进来的最值钱的东西不是你的技术能力，而是你的"第一印象"。** 作者在这个项目里待了太久，对一切都有了解释。你没有。你觉得"不对劲"的地方，往往就是真的不对劲。
>
> **举例是锚，不是灯。** 下文每个视角的"你可能会注意到的事"是举例不是清单——但举例天然吸走注意力：一条声明的约束力抵不过几十条举例的密度。所以每个视角开工前，先完成该视角的**必答题**（把这个身份最关心、但举例完全没提的事先写下来），把直觉带到举例没覆盖的地方，再回头看举例补漏。

---

## 审查对象

sofagent（https://github.com/KongFangXun/sofagent）。不管当前处于什么阶段——发版前、文档治理、架构重构——都以**完全独立的视角**重新审视。你的起点是空白：不预设"之前已经修好了 X"，只看交付物本身。

交付物不只是 TS 源码与测试——**含运行时 UI 与门面资产**：`tools/dashboard/dashboard.html`（2900+ 行单文件，CSS/HTML/JS 混合体——颜色分级、类化纪律、静默失效形态见 `docs/guides/frontend-design-standard.md`；结构性缺陷另有 `check-dashboard.sh` 机械门禁，审查只补语义面）、双语 README、install.sh 用户体验。这些资产没有单测兜底，审查就是它们的主防线。

---

## 审查纪律

1. **零上下文**。不预设"之前已经修好了 X"。你的起点是空白。如果有人告诉你"这个版本改了 A、B、C"——忘掉它。你只看交付物本身。
2. **相信直觉**。第一反应"不对劲"就是不对劲——不需要找到理论依据。你后续可以验证，但那个"不对劲"的感觉本身就是信号。
3. **实际动手**。想到什么就去跑、去读、去试。不要停在"我觉得可能有问题"。
4. **不修改代码**。只报告，不修复。
5. **逐轮清空**。每完成一个视角后，丢掉上一个视角的记忆。每一个视角的你都是"全新的你"。
6. **反臆造**。观察项不得升级为 P0/P1 结论——报告显式列出「未确认事项」；自动合成/降级产物须标注质量受限，防止低置信结果被当高置信。
7. **版本双核对**。核对版本时同时看入口材料（README/tag/安装器/SKILL）与内部声明（CHANGELOG/package.json）——入口滞后是独立信号，不因"内部对"而放过。
8. **工作树残留扫描**。看交付物结构时扫一眼未跟踪残留（stranger-test/ 类含 .env/嵌套 .git 的目录），并确认 .gitignore 是否覆盖目录本身——残留进 `git add .` 即污染提交。
9. **文件自述核对**。文件头自述（行数上限/零依赖/职责边界）与实际实现不符是独立信号——自述漂移 = 维护纪律信号。
10. **占位 ≠ 缺陷**。CHANGELOG 里「已排期·尚未实现」的后续版本规划文件、ROADMAP 迭代表的占位条目是**正常内容**，不是空壳 bug——报告前先读条目标注（「已排期」「规划中」「尚未实现」），区分「占位规划」与「真实缺陷」，避免误报。
11. **环境限制 ≠ 产品缺陷**。node_modules/dist 缺失、npm registry 不可达、构建未跑（发版前中间态）等环境/时序问题**不是产品级 finding**——报告须标注「环境限制」并说明不阻塞交付，不列为 P0/P1。
12. **接线存在性可判定面交给门禁**。「声称已交付的能力在 engine/ 有无生产调用点」属正则可判定项——check-docs §13 接线断言已是其单点版，泛化方向（@public 导出零生产消费的批量检测）排期机械化。审查中撞到这类发现照报，但备注「建议收口 check 脚本」——LLM 视角只留语义发现，机械面不重复人肉（对齐「机械检查前移」既有校准）。

---

## 审查视角

以下每个视角给你一个**身份**和一种**心态**。具体看什么、怎么看、看到哪里为止——**由你自己决定**。每个视角后面有一些"你可能会注意到的事"——那些是举例，不是清单，你发现的东西完全可以跟它们毫无关系。**开工先答必答题**（每个视角的第一行）：用一句到三句话写下"这个身份最关心、但下方举例完全没提的一件事"，答完再开始审查——这不是仪式，它是把你从举例的锚地里拉出来的缆绳。

**视角分六层，按场景取用：**

| 层 | 视角 | 什么时候用 |
|----|------|-----------|
| **常规发版（1-12）** | 陌生人 / 企业 IT / 竞品维护者 / npm 用户 / 开源审查员 / 用户旅程 / 红队 / 数字侦探 / 感知层 / 文档一致性 / 代码审读者 / 文件结构陌生人 | 每次发版的 fresh-eyes-loop 标准配置（单盲四角色流水线：A 按 12 视角审 → B 修 → C 验 → D 复核；工具预算 36 次；legacy 双盲 A/B 并行走 `FORGE_ENABLE_B_CHECK=1` 逃生门） |
| **文档治理（13-14）** | 技术编辑 / 对外形象分析师 | 大规模文档重构、跨文档迁移、来源标注清理等非发版场景手动使用 |
| **全文档通读（15-16）** | 外部开发者通读 / 资深架构师 | 不设焦点、从头读到尾，抓"整体读完后的不对劲"——适合任何需要全局视角的场景 |
| **动态面（17-19）** | 跨组件契约审查者 / 构建产物审查者 / 执行证据审查者 | 补静态视角的三个系统性盲区；发版审查建议在常规 12 视角外追加，手动使用 |
| **深度专项（20-21）** | 死代码猎人 / 未来架构师 | 大版本收口、季度全仓体检等预算富余场景追加；21 必须最后执行（依赖前面视角积累的理解深度） |
| **发现面（22）** | GitHub 发现优化审查员 | 门面/入口改动时使用：README 重构、topics/description 调整、双语对齐、发版前门面体检；与视角一（陌生人）互补——一管「读到什么」，22 管「怎么被找到」 |

**体系外挂（不在上表序列内）**：设计熵勘测走 `playbook/deep-module-review.md`——它审的是**模块形状**（该深不深、缝的真假、注册漂移），不是交付物对错。它与视角二十（死代码猎人：管存亡）、视角二十一（未来架构师：管方向）分工互补——本审查管「对不对」，它管「好不好改」。

**方法论的源头在别处**：`deep-module-review.md` 是**适配层**，只写本地差异与编排；方法论本体是上游开源技能 `improve-codebase-architecture` 及其依赖闭包，vendored 在 `playbook/vendor/improve-codebase-architecture/upstream/`。执行前按其 §二「读取顺序」读 vendored 原文，**不凭记忆复述方法论**——复述即开第二条真相源。

挂载进本审查时，它扫出的形状问题**直接并入本次审查报告**（与其它视角 findings 同载体），**不另出独立文档**，**也不落 HTML**（上游默认输出 HTML，本地覆盖为对话直出）；去留由人裁定：转版本条目施工，不在发版窗口内即兴重构。

---

### 🧑‍💻 视角一 [1]：陌生人

你在 GitHub Explore 或技术群里看到了这个项目。你没听过它，不知道作者是谁。你只是好奇——点进去了。

**你的心态**：我是一个普通的开发者，不是来审代码的。我在判断"这东西对我有用吗？我愿意花时间装吗？"

**✏️ 必答**（先写下再开工）：作为一个时间有限、选择很多的陌生开发者，我最关心但下面举例没提的一件事是什么？（方法举例：从"我为什么点进来"倒推——我带着什么需求/痛点来的？项目首页回答它了吗？）

你可能会注意到的事（举例，不是清单）：
- 3 屏内你能搞清楚"这是干什么的"吗？还是越看越糊涂？
- 如果要装，你的第一步是什么？有障碍吗？
- README 说的东西，你信吗？有没有让你觉得"这太夸大了"的声称？
- 你会用一句话怎么跟同事介绍它？
- 装完跑 `--help` 或 `--doctor`——输出给你信心还是让你想卸载？
- 你看到的输出，你知道是谁给你的吗？还是觉得"这不就是普通的 git hook"？

---

### 👔 视角二 [2]：企业 IT

你管理一家 200 人公司的 IT 基础设施。开发团队在用 AI 写代码，你担心安全和合规。上级问你：这东西能进我们公司吗？

**你的心态**：我关心的不是"有没有 bug"，而是"出事了谁负责"和"这东西能进公司吗"。我读任何一页文档的出发点都是：这让我更放心了，还是更担心了？

**✏️ 必答**（先写下再开工）：作为要对"出事了谁负责"签字的人，我最担心但下面举例没提的一件事是什么？（方法举例：想象采购审批会上最挑剔的那位同事会问什么——数据出境？审计留痕？责任边界？）

你可能会注意到的事（举例，不是清单）：
- SECURITY.md 让你信任还是不安？
- LIMITATIONS.md 诚实吗？有没有避重就轻？
- 审计日志推送到企业协同平台后，能追溯是哪个模块判的吗？
- 知识库访问控制——财务 Agent 和人事 Agent 用同一个底座，数据会串吗？
- 如果在 20 台机器上部署，文档够你用吗？
- 审计拦截了密钥后，拦截结果存在哪？会不会成为第二个泄漏点？

---

### 🏗️ 视角三 [3]：竞品维护者

你是另一个开源项目（Cursor Rules / Claude Code hooks / pre-commit 工具链）的维护者。你在研究竞品，想搞清楚它跟你有什么区别、有没有致命弱点。

**你的心态**：我要找到它站不住脚的地方。不是恶意——是竞争本能。README 每一句话我都会问"真的吗？"

**✏️ 必答**（先写下再开工）：作为最懂这个领域的人，我认为它最大的软肋在哪——但下面举例没提的那个？（方法举例：想想你自己的项目踩过什么坑，然后问"它会踩同一个坑吗"。）

你可能会注意到的事（举例，不是清单）：
- README 声称的差异化，跟你的项目比——是真不同还是换了个说法？
- 声称的功能在代码里能找到对应实现吗？有没有夸大——wrapper 叫"引擎"、CLI 调用叫"集成"？
- 如果写一篇《为什么不用 sofagent》，你的核心论据是什么？
- 它自称"正式版"和"可生产使用"——以你的标准够格吗？
- README 对比表里把竞品写得很弱——公平吗？去竞品官方文档核实一下。
- CHANGELOG 历史里有没有审查过程元信息？CHANGELOG 应该只写产品变更。

---

### 📦 视角四 [4]：npm 用户

你是前端/全栈开发者。你不读 README（太长不看），你只做一件事：装上它，跑起来。**你的心态**：从敲下 `npm install` 到觉得"这东西有用"，中间花了多长时间？超过 5 分钟我就走了。

**✏️ 必答**（先写下再开工）：装完第一次真实使用时，我最怕遇到但下面举例没提的一件事是什么？（方法举例：回忆你自己上一次 rage-uninstall 一个包的原因——是文档烂？报错懵？还是依赖树吓人？）

你可能会注意到的事（举例，不是清单）：
- `--help` 输出——你马上知道怎么用还是要再查文档？
- `--init` 体验顺滑吗？有没有让你困惑的提示？
- 故意制造一次失败（不在 git 目录、Node 版本太低）——报错信息让你知道怎么办吗？
- `--doctor` 输出有用吗？有没有凑数的检查项？
- 包的依赖树干净吗？有没有让你皱眉的依赖？

---

### 🔍 视角五 [5]：开源审查员
> 📌 **校准**：审查 worker 自己的测试 fixture（假密钥/空目录/测试 commit）可能污染被审仓库——发现「根目录可疑文件」时先辨别是不是上轮审查残留（作者 t@t.com / AWS 官方示例 key——`AKIA` 开头 + `EXAMPLE` 结尾的文档示例串是典型指纹）。worker 残留不是仓库的 bug，是审查工具的债（worktree 隔离根治）。

你在 GitHub 上 review 过 500+ 个开源项目。你有一套快速判断方法：先看目录结构和 git log，几分钟内形成第一印象。

**你的心态**：这个项目的代码组织方式让我觉得它是认真维护的，还是一团乱麻？

**✏️ 必答**（先写下再开工）：审过 500+ 项目后，我淘汰一个项目最快的信号是什么——但下面举例没提的那个？（方法举例：你心里的"三秒关闭"触发器是什么？用它先扫一遍。）

你可能会注意到的事（举例，不是清单）：
- 只看根目录 `ls`——你猜得到项目的结构吗？合理吗？
- 有没有"不该在这里"的文件？
- `git log` 的提交节奏健康吗？有诡异的大提交吗？
- 从 README 点进 3-5 个链接——有 404 吗？有引用了但不存在的章节吗？
- 先读 `docs/WIKI.md`——3 分钟之内能不能搞懂这个项目的全貌？能不能找到你需要的文档？有没有你想知道但 WIKI 没写的东西？
- 文档头部日期跟当前版本对得上吗？
- 这是一个"作者自嗨"项目还是有社区活性的？

---

### 🛤️ 视角六 [6]：用户旅程

你不是站在一个角度看——你是**走一条路**。你假装真实用户，从零开始完整走一遍使用流程。

**你的心态**：我不是在审查，我在用。哪里让我卡住、困惑、想放弃？

**✏️ 必答**（先写下再开工）：走这条路之前，回想我自己作为真实用户最后放弃某个工具的那一刻——是什么触发的？我会带着那个触发器的敏感度走这条路。

走完这条路（发现 → 安装 → 初始化 → 第一次 commit → 第一次拦截 → 探索更多），每一步标注体验评分（🟢 顺滑 / 🟡 小障碍 / 🔴 卡住）。**放弃阈值**：如果前 3 步出现 2 个🔴，标放弃点并停止——这个体验已经不及格了。

你也可以走**独立产品旅程**：从 FDE/ 或 FORGE/ 入口进来，不装主包。这条路真的能走通吗？还是"独立"只是话术？

---

### 🐛 视角七 [7]：红队

前面所有人都在"正常使用"——你反过来。**你是来搞破坏的。**

**你的心态**：不测正常路径，只测不正常的。我的目标是找出审计模块在极端、边缘、恶意场景下的弱点。

**✏️ 必答**（先写下再开工）：作为一个不看书本的安全研究员，我第一个想打的攻击面是什么——但下面举例没提的？（方法举例：问自己"如果我是内鬼员工/被入侵的依赖包/恶意 fork 作者，我从哪进门？"——攻击者的第一直觉比任何清单准。）

**🩺 常驻子项·错误处理四分类**（「静默失效」是历史痛点，本视角顺手做）：预算紧抽 10 个 `catch` 块、预算富余全仓分类，逐个判定——吞错（空块/仅 console）/ 降级（有 fallback）/ 转译（rethrow 包装）/ 正确。报**吞错率最高的三个文件**；对每个吞错点问：这个错误发生时，用户知道吗？数据会丢吗？专盯其他视角没人盯的静默失效面。

**🩺 常驻子项·安全面测绘**（与注入攻击互补的另一面）：不攻击，画地图。列出完整信任边界——哪些输入来自用户 / Agent / 网络 / git 历史 / 配置文件——逐条边界核校验点在哪、漏了哪；crypto 全家桶的密钥生命周期（生成/存储/轮换/销毁）各环节现状；供应链面——npm 依赖、bootstrap 下载、CI 之外的第三方输入点；再把 SECURITY.md 声称的防线与代码实际防线逐条对照。攻击找的是防线上的洞，测绘找的是**图上没有的防线**——声称了但不存在，比存在但有洞更危险。

你可能会尝试的方向（举例，不是清单）：
- 边缘情况：二进制文件、超大 diff、非 ASCII 文件名、重命名+修改
- 篡改：不合法 config、删关键字段、不存在的规则名
- 绕过：`--no-verify`、删 hook 文件、直接改 history.jsonl
- 注入：commit message 注入、代码注释注入、联邦 peer 返回恶意数据
- 加密攻击面：公钥替换、token 泄露、IV 复用、签名绕过
- 止损路径社工：紧急回滚/止损操作通常免人审、压力大、验证松——恰是最好下手的面。判据：任何「紧急/止损」语义路径的校验强度不得弱于常规路径（版本切换全路径同强度校验，无一旁路）

**但要探索清单之外的东西。** 你作为攻击者的创造力比这份清单值钱得多。

---

### 🤖 视角八 [8]：数字侦探

> 📌 **校准**：发版前跑审查时版本一致性天然处于中间态——registry 落后本地 / tag 未打 / URL 指向未来 tag 这类 finding 在「push+tag+publish」后自动消失，**默认 SKIP 不报**（判别口径见 releasing/03-quality-loop.md）。把中间态当发现 = 给修复者制造噪音。
> ⚠️ **但中间态豁免只限 registry / 远端 tag / URL 指向**——文档与脚本内的**字面版本 tag 残留**（如安装用法示例里写死的旧版本号）不是中间态，是实打实的 bug，必须报。

前面几轮都靠直觉和经验。这一轮换一种脑子：**你只看数字。** 文档里的每一个数字声称，跟代码里的实际数字——对得上吗？

**你的心态**：人类审查会跳过"数字对不上"这种枯燥检查，但这是最容易暴露问题的地方。大数字有人盯，小数字（"11 条""3 处"）往往写完就没人再看。

**✏️ 必答**（先写下再开工）：开查前，先自己列出三处你认为最可能数字漂移的位置——凭直觉，别看下面的举例。（方法举例：数字漂移最爱藏在"引用了别的文档的数字"和"写完后再没人跑过的数字"里。）

你可能会对账的数字（举例，不是清单）：
- README 说"X 条规则"——代码里实际注册了多少条？
- CHANGELOG 说"N 个测试"——实际 `npm test` 跑出多少？
- 文档里的规则 ID——在代码里逐个存在吗？（注意跳号不是遗漏）
- 版本号——所有文件、所有 package.json、所有注释里的版本号一致吗？
- 文档头部日期——跟当前发版日期对得上吗？
- exit code 声称 vs 实测——报告说「EXIT=0 全绿」，你亲自跑 `cmd > /tmp/log 2>&1; echo $?` 看是不是真的 0。管道取 `$?`（如 `cmd | tail; echo $?`）取到的是 tail 的退出码不是脚本的——这是假绿的温床

工具可以帮你（`check-version.sh` / `check-docs.sh`），但**不要只信工具**——工具自身也有盲区。用你的眼睛去看。

---

### 👁️ 视角九 [9]：感知层

你做完前面所有审查后，退一步问一个不同的问题：**用户能感知到这个产品的存在吗？** **你的心态**：如果用户用了三周还不知道自己装了什么，这个产品就是失败的——哪怕它工作得再好。

**✏️ 必答**（先写下再开工）：站在用户立场，"装了但没感知"最不可原谅的一个场景是什么——下面举例没提的？（方法举例：想象用户向老板汇报"我们装了什么"时的尴尬时刻。）

你可能会关注的方向（举例，不是清单）：
- 所有面向用户的输出——带产品签名吗？还是只是通用的"PASS""FAIL"？
- Agent 做对了的时候，用户知道"这个对，是经过验证的"吗？
- 你能区分"模型自己生成的结果"和"经过审计模块验证的结果"吗？

---

### 📉 视角十 [10]：文档一致性

你专门盯着那些"不起眼的小东西"——概念口径、叙事一致性、数字漂移、编码损坏。

**你的心态**：重大概念重构或物理搬迁后，跨文档的叙事口径一致吗？旧框架表述有没有残留？

**✏️ 必答**（先写下再开工）：这个项目最近做过什么大的改名/搬迁/叙事调整？先从 git log 或文档头嗅出来——最深的残留往往藏在那次调整的"没人想到要跟的地方"。（方法举例：查最近 10 条 commit message 里的"改名/迁移/重构"字样。）

你可能会关注的方向（举例，不是清单）：
- 产品身份的两层叙事——对外"FDE Harness"、底层"约束层"——一致吗？有没有该说 A 却说了 B？
- 🔴 **对外声称的精确性**——"审计日志按 git 仓库隔离"这种话，读者会以为所有审计日志都隔离了，但实际只有 runtime-audit 隔离、history.jsonl 仍全局。声称需要精确限定范围（口径先例："审计日志" → "运行时审计日志"）
- 同一个概念在多文档里描述一致吗？有没有矛盾？
- 术语统一吗？（比如同类对象用了多个不同的称呼）
- 文档里有编码损坏吗？（乱码、U+FFFD、null byte、mojibake）
- 文档里的链接——目录跳转、跨文件引用、代码路径——跟着点，每一步都通吗？有没有点进去是空的？
- 有没有读完什么信息也没拿到的段落？有没有三遍重复的内容？一节读完，你拿到了什么？如果什么也没拿到，为什么作者写了它？
---

### 🔬 视角十一 [11]：代码审读者

你是一个刚入职的高级工程师，第一天报到。导师让你"先把代码熟悉一下"。你打开编辑器，从头开始读。

**你的心态**：我在代码里漫游，不设路线。读着读着，我会产生各种"这不对吧？"的感觉——我相信这些感觉。

**✏️ 必答**（先写下再开工）：开读前，先写下一个你自己写过/见过的同类系统里最阴的 bug——然后专门留意这个项目有没有同款土壤。（方法举例：路径拼接、错误吞噬、时序竞态——你的私人伤疤清单就是最好的探针。）

你可能会问自己的问题（举例，不是清单）：
- 这个文件为什么在这里？存在合理吗？
- 这段类型断言为什么放弃了保护？必须的还是偷懒？
- 这个 catch 块吞了错误——出事了用户知道吗？
- 两个包里有几乎一样的文件——为什么不共享？
- 这个 import 方向对吗？底层包为什么引用了上层包？
- 这个路径是硬编码的——结构变了会断吗？
- 这个测试测了什么？行为还是字段存在？
- 这段代码搬迁过吗？痕迹干净吗？
- 环境变量命名——Unix 约定全大写+下划线（如 SOFAGENT_HOME），驼峰命名（如 SOFAgent_HOME）在 shell 脚本里是红灯。这不是品味问题，是可移植性陷阱
- 文档声称的命令真的接线了吗？CHANGELOG/README 声称的任何命令，CLI 子命令注册 / MCP tool-registry / daemon 巡检三面 grep 调用链——「声称已交付、代码零接线」是最隐蔽的交付谎言（入口函数仅有导出无调用方 = 断链实锤）
- 注释里的理由是假设还是校验？「因为 X 所以跳过校验」类注释——X 必须有运行时验证兜底，否则按校验旁路处理（注释不能替代不变量）

---

### 🏗️ 视角十二 [12]：文件结构陌生人

你刚 `git clone` 完这个仓库。`cd` 进去，`ls` 了一下。从这个 ls 开始，你的第一印象就形成了。**你的心态**：我在文件树里散步。这个结构让我觉得舒服还是别扭？合理还是混乱？

**✏️ 必答**（先写下再开工）：只看目录名（不进目录），先猜"这个项目的模块边界怎么划的"——再进去验证你的猜想。猜错的地方就是结构信号。（方法举例：两个名字相似的目录/文件，赌一把它们职责有重叠，去验证。）

你可能会自然产生的感觉（举例，不是清单）：
- 目录结构让我一眼看懂"项目由什么组成"吗？
- 有没有文件待在不合理的位置？
- 有没有两个目录看起来在做同一件事？
- 文档里的路径链接——我跟着走，每一步都通吗？
- install 脚本引用的路径——真的存在吗？
- 根目录干净吗？有没有碍眼的文件？
- 如果我是新贡献者，能从结构推断出"新文件该放哪"吗？

---

### ✏️ 视角十三 [13]：技术编辑

你不是开发者、不是用户、不是安全专家。你是一名**技术出版社的资深编辑**，你的工作是让一份手稿变成可出版的书。

**你的心态**：我不关心代码跑不跑得通，我关心**这份文档能不能让读者顺畅地从头读到尾**。我找的是：结构断裂、叙事跳跃、逻辑空洞、行文不一致、信息堆砌没有主线。

你可能会注意到的事（举例，不是清单）：
- 打开任意一份文档，3 分钟之内你能抓住它的主线吗？还是感觉"信息很多但不知道作者想说什么"？
- 同一个概念在不同章节用了不同的名字，读者会困惑吗？
- 有没有"突然出现"的段落——前面没有铺垫，后面也没有呼应，像是临时塞进来的？
- 一级标题读一遍，能不能串成一句话概括全文？串不起来 = 结构有问题
- 有没有信息密度极低的长段落——读了三遍也抓不住重点？
- 内部措辞（"不对用户宣传""负责人拍板"）泄漏到公开文档了吗？
- 来源标注——有 URL 的引用保留，没有 URL 的个人笔记引用当自含表述处理了吗？
- Mermaid 图 / 表格的结构跟正文描述一致吗？

---

### 🎯 视角十四 [14]：对外形象分析师

你不在 sofagent 团队里，你在一家竞品公司做竞争分析。老板让你评估这个开源项目的**对外形象**——它的定位清晰吗？品牌一致吗？对外人来说可信吗？

**你的心态**：我把自己当成一个潜在客户或投资人，第一次接触这个项目。我不深入读代码，我只看"对外呈现"。我要回答老板：这个项目的对外形象有没有自相矛盾的地方？

你可能会注意到的事（举例，不是清单）：
- 品牌名——一个项目对外用了几个名字？（sofagent / FDE Harness / 中间件 / FORGE…）读者会搞混吗？
- 开源 vs 商业——MIT 开源仓库里有没有暴露私有商业仓的路径或内部决策？
- 对外声称 vs 实际状态——文档里说的能力，跟代码实际实现的状态对得上吗？有没有"规划中的东西写得像已经实现了"？
- 定位一致性——README 说"平台无关"，但文档里是不是还在暗示某个特定平台？
- 目标读者——这份文档是写给开发者、企业 IT、还是投资人？三种读者需要的叙事节奏完全不同，混在一起会谁都不满意
- 进化模块说"越用越好"——实际状态支撑这个声称吗？（Dream Cycle 重启即丢、evolve 需外部依赖）

---

### 📖 视角十五 [15]：外部开发者通读

你是一个刚听说 sofagent 的开发者，你决定花 30 分钟从头到尾把主要文档读一遍。你不设焦点，不带着特定问题——你就是想搞清楚"这东西到底是什么、怎么用、值不值得投入"。**你的心态**：我在读一本技术书。如果我读到第三章还搞不清主线，或者每个章节都像独立的文章而不是一本书的一部分，那就是结构问题。我记录的是**整体阅读体验**——哪里让我停下来困惑、哪里让我想跳过、哪里读完发现什么也没记住。

你可能会注意到的事（举例，不是清单）：
- 读完 README → ARCHITECTURE → PHILOSOPHY，三条叙事线能接上吗？还是三个独立的故事？
- 跨文档引用——点进去跟上下文对得上吗？还是点进去发现"这说的不是刚才提到的那个事"？
- 有没有概念在文档 A 里定义了，文档 B 直接用了但没引回定义？
- 读完一份文档，你能用一句话向同事概括它说了什么吗？如果概括不出来，是信息太多还是主线不清？
- 哪些段落你读了两遍还是没懂？是技术复杂还是表达不清？
- 有没有"看起来很重要但读完不知道要做什么"的段落？

---

### 🏛️ 视角十六 [16]：资深架构师

> 📌 **校准**：driver 的 a-consolidate 可能 stall 降级（fallback 拼接代替完整合并）——降级轮的 finding 清单不完整是工具降级不是仓库干净，终判要回原始 per-perspective 报告核对，别把「合并报告说无发现」当成「无发现」。

你是有 15 年经验的架构师，评审过几十个系统的设计文档。你不是来挑错别字的——你在评估**架构决策的合理性和论证质量**。

**你的心态**：我不关心文档好不好读，我关心**技术判断站不站得住**。每个设计决策，我都问三个问题：为什么这么选？替代方案考虑过吗？有什么证据支撑这个选择？如果论证链断裂——结论对了但理由错了，或者理由对了但结论没跟——我标记。

你可能会注意到的事（举例，不是清单）：
- 架构决策有没有说清"为什么不选 X"——如果只说"我们选 Y"但没说为什么不是 X，论证不完整
- 行业对标公平吗——只挑了对自己有利的参照，还是也列了反面证据？
- 术语一致性——同一层架构在不同文档用了不同名字，读者会以为这是两个东西
- 状态机 / 流程图的边界条件——有没有"正常路径画了但异常路径没画"的图？
- 性能声称有数据支撑吗——"快""轻量""零依赖"这些词，有量化对比吗？
- 技术债标注——哪些是"知道但还没做"，哪些是"做了但文档没说"？有没有技术债被伪装成了设计选择？

---

### 🔗 视角十七 [17]：跨组件契约审查者

你不是在看单个文件好不好——你在看**包与包之间、模块与模块之间的隐形契约**还成不成立。每个 import、每个跨包调用、每个"我假设你在那里"的路径引用，都是一份契约。代码审查通常停在"这个文件对不对"；你问的是"这两方对彼此的假设还同时成立吗"。

**你的心态**：单点全绿的系统可能整体是断的。最深的 bug 藏在两个各自正确的组件中间——A 以为 B 会这样，B 以为 A 知道。

**✏️ 必答**（先写下再开工）：先列出这个项目里你最怀疑的 2-3 条跨组件假设（哪个包引用了哪个包的什么？哪个脚本假设了哪个文件的布局？），再去逐条验证。

你可能会注意到的事（举例，不是清单）：
- 一方引用另一方的文件路径——源码态成立，构建后还成立吗？安装态呢？（`__dirname` 相对路径在三种形态下可能指向三个地方）
- A 包升级改了导出面，B 包的调用跟上没有——版本号声明对齐了吗（peerDependencies / workspace 版本）？
- 两个包各写了一份"几乎一样"的逻辑（工具函数/评分器/校验器）——是历史复制还是有意隔离？复制的那份还在漂移吗？
- 配置文件字段——写入方写的字段名和读取方读的字段名，全程一致吗？
- 某个功能横跨三个包——删一处，另外两处会静默坏掉吗？谁在守护这个不变式？

---

### 🏗️ 视角十八 [18]：构建产物审查者

你不看源码——你看**源码变成运行物的每一次变换**：TypeScript → dist、仓库 → npm 包、仓库 → 安装态。每次变换都是一次失真机会：源码里对的东西，编译后可能错；编译后对的，安装到用户机器上可能又错。你专抓"源码视角审查永远看不到"的那一层。

**你的心态**：开发者看源码，用户跑产物。源码 ≠ 产物 ≠ 安装态——任何"在源码里验证过"的结论，到你这里都要重新问一遍：产物里也成立吗？

**✏️ 必答**（先写下再开工）：先画出这个项目从源码到用户手里要经过几次形态变换（编译？打包？安装脚本拷贝？），每一跳列出"最可能在这一跳丢失或变形的东西"。

你可能会注意到的事（举例，不是清单）：
- install 脚本/打包配置拷贝了哪些文件到安装态？没拷的里面，有没有运行时会被引用的（静态资源、模板、docs）？
- 源码里的动态 import / 运行时路径解析——在产物目录结构下还能解析到吗？
- package.json 的 files 字段 / bin 字段——声称的入口在 tarball 里真实存在吗？
- 编译产物里的 `__dirname` 指向——与源码假设的目录布局一致吗？
- 安装态跑一次真实入口（`--help` / doctor / 首次初始化）——报错信息里有没有"找不到文件"的影子？

---

### 🩺 视角十九 [19]：执行证据审查者

前所有视角都在审"代码写得对不对"——你反过来问：**这些代码被执行过吗？** 功能"存在"不等于"运行过"：定时任务可能从未触发、监听可能从未收到事件、工具调用可能静默失败 exit 0。你不读代码找 bug——你找"运行过的痕迹"（日志、产物、时间戳、状态文件），找不到痕迹的活代码就是嫌疑犯。

**你的心态**：静态审查的天花板是"代码看起来对"。但"从来没跑过的正确代码"和"跑起来就崩的代码"对用户是同一回事——零价值。我找的是那些活在仓库里、却从未真正活过的功能。

**✏️ 必答**（先写下再开工）：先列出这个项目里"理论上应该在运行/定期执行"的东西（cron / watcher / 后台任务 / 事件监听），然后逐个问：它在哪留下运行痕迹？那个痕迹真实存在吗？

你可能会注意到的事（举例，不是清单）：
- 定时/调度类功能——配置存在、代码存在，但你找得到它**跑过的日志或产物**吗？（execFileSync 静默 exit 0 是这类问题的经典伪装）
- 事件监听类功能——触发条件实际会发生吗？还是监听了一个永远不来的事件？
- 写入类功能——它该产出的文件/目录，在任何环境（仓库/测试/文档记录）里出现过吗？
- 错误处理路径——如果它一直静默失败，谁会知道？有任何告警/日志/退出码暴露它吗？
- 测试给这个功能的执行证据背书了吗——还是只测了"函数存在、返回类型对"？

- **等待条件必须覆盖断言条件**：`waitFor(A)` 之后断言的谓词若非 A 且与 A 不同步（等到 `done` 事件却断言 `isClosed()`，而 close 来自子进程 exit、晚于 done）——高负载 CI 必假红；查法 = 逐对对齐每个 `waitFor` 与紧随 `expect` 的谓词。
- **放宽类改动须附负向探针证据，且回退不得取中性值**：容差/豁免/跳过/回退属削弱检测力改动，报告须给出三探针（超阈值仍红 / 阈内不误伤 / 还原干净）；`取不到值 → 0/空` 参与比较 = 判据恒成立（fail-open），合规形态是回退到更严判据。
- **日期比对须与时区解耦、外部对账须识别错误体**：`TZ=` 取日期在 CI 与本地给出不同真值（ref 过滤器不认 TZ 前缀 / `date -r` BSD 语义与 GNU `--reference` 相左 / TZ 生效性依赖 runner）；凭证失效时错误体常打到 stdout（`2>/dev/null` 拦不住）须识别特征归 SKIP，而非解析出假红。
- **发版态是语义族、不是单点**：状态标记在 README 双语版本段标题 / ARCHITECTURE·WIKI 状态行 / API 版本头与变更表 / CHANGELOG 索引行各自可就地残留，同一行还可能「⏳ 待发版 · 已发版」自相矛盾；查法 = 全仓 grep 状态词后逐条判「真残留 vs 通用说明」。
---

### 🪦 视角二十 [20]：死代码猎人

前面所有视角都在审"活着的代码对不对"——你反过来：**找那些看起来活着、其实已经死了的代码。**

**你的心态**：代码的死不是 syntax error，是「没有调用者、没有消费者、没有未来」。接线断链是这个仓库的历史雷区——功能声称已交付，入口却在某次重构中断了线，实现成了无人祭拜的尸骸。你专找这类"活死人"。比完全断链更隐蔽的是温和版：只被测试引用、无生产调用点的模块——测试是它唯一的观众。

**✏️ 必答**（先写下再开工）：开工前先列 2-3 个你怀疑"看起来活着其实死了"的功能、导出或配置名，再逐个验证。（方法举例：从文档里挑一个你不熟的功能名——它在你心里有印象但没有使用记忆，这种就是头号嫌疑。）

**与机械门禁的分工**：export 后搜零引用是正则可判定项，机械面已排期收口进 check 脚本——你的价值在正则判不了的**意图判断**。你可能会注意到的事（举例，不是清单）：
- export 了但零生产消费——是「等待接线的新功能」还是「断链尸骸」？判断依据是意图（排期标注、TODO 注释、对应的能力声称），不是引用数
- 文档里描述的功能，代码里只剩尸骸——入口删了实现还在，或实现删了文档还在
- 配置项 / 环境变量定义了但没人读——是预留还是遗忘？注释里写「下版接线」的，下版真的接了吗？
- 「临时方案」活了多个版本还没转正——临时方案不死，往往不是没人发现，是没人负责
- 同一功能两处实现，其中一份已经漂移——旧的那份是历史隔离还是该合没合的死副本？

---

### 🔭 视角二十一 [21]：未来架构师

前面所有视角都在挖问题——你反过来，**挖未来**。你不报 bug：你基于对全仓的理解，指出这个项目最值得投的下一步。发现问题的审计员同时是最有资格提建议的人；只向后看（打分）不向前看（建议）的审查丢了一半价值。

**两条硬约束**：
- **必须最后执行**——建议的质量取决于你对仓库理解的深度。前面视角没跑完，你没有资格给建议。
- **每条建议标注「由哪些前置发现支撑」**——没读完仓库就写的建议是空中楼阁；锚不住具体发现的建议，自己砍掉。

**✏️ 必答**（先写下再开工）：站在「这个项目一年后会因为什么而衰退」的角度，最大的那个威胁是什么？（方法举例：不是外部竞品，而是从仓库内部长出来的——某类债、某个被忽视的面、某种流程惯性。）

你可能会输出的方向（举例，不是清单）：
- 三个最有价值的下一步架构投资——为什么是它不是别的、预期收益、风险
- 三个应该战略性放弃的方向——当前在做但性价比存疑；敢说不要比敢说要更难
- 如果项目要在半年内服务量级翻倍，最先崩的三个点是什么、现在该铺垫什么
- 对照 ROADMAP：它漏了什么、多了什么、什么排序错了
- 一个 wild idea——如果不受现有架构约束，最想实验的一件事

---

### 🔍 视角二十二 [22]：GitHub 发现优化审查员

你做开源项目的被发现性（discoverability）审计。你不是运营——你是从「一个陌生开发者怎么找到这个项目、找到后 60 秒内发生了什么」出发倒推仓库门面的漏洞。

**✏️ 必答**（先写下再开工）：如果这个项目的主要发现渠道不是 README 首页（而是 topics 搜索 / GitHub 搜索 / 外部 awesome 清单跳入 / 搜索引擎），哪个入口最可能先失守？为什么？（方法举例：README 通常是被照顾最好的入口——失守的往往是 secondary 入口：README.en、topics、description、仓库首目录视觉。）

**你的心态**：门面审计员。每个入口都是一次 60 秒转化——访客带着疑问进来，五个信号（一句话定位/视觉演示/徽章/Quickstart/社交证明）内没被回答就走人。你不是打分——你在找「访客流失的具体原因」。

你可能会注意到的事（举例，不是清单）：
**GitHub 搜索与 topics**：
- topics 覆盖了目标用户的真实搜索词吗——20 个名额里有多少被低流量词/自造词占着（例：内部缩写、产品代称）；核心品类词（llm / ai-agents / mcp 等）与差异化长尾词（agent-governance / harness-engineering 等）的比例
- description 是否把「项目是什么 + 关键差异化词」塞进前几个词——GitHub 搜索与 Google 站外索引都读它
- repo 名 / README 一级标题 / description 三处关键词是否一致（搜索引擎对一致性敏感）
**README 落地页转化（首屏 60 秒）**：
- 一句话定位：不看任何上下文，首屏第一段能否让「不知道这个项目的人」明白它是干什么的——术语自洽（「FDE」这种自造概念有没有在首屏就地解释）
- 视觉演示：有没有 GIF/图示/截图让访客「眼见为实」——纯文字首屏的流失率
- Quickstart：复制粘贴 ≤5 分钟能跑通吗——第一次运行成功的访客才会回来 star
- 社交证明：星数徽章 / star-history / 被收录标志（awesome 清单、marketplace）是否露出
- 徽章行：构建/版本/协议齐全吗，有没有失效徽章（比没有更糟）
**双语与外部入口**：
- README.en 与主 README 是否同等转化力——英文是站外搜索引擎的主要索引面，英文首屏弱 = 国际流量全丢
- 外部跳入点（awesome 清单条目、PR 描述、社交分享卡片）的措辞与仓库门面是否一致——访客被一个承诺吸引进来，落地页却讲另一个故事
- 仓库首目录视觉：访客点开根目录第一眼（文件命名、目录结构、CONTRIBUTING 是否友好）——GitHub 仓库页本身就是落地页
**反模式**（看到即报）：
- 关键词堆砌（keyword stuffing）——GitHub 搜索是导航式搜索，相关性与准确 > 密度；堆砌反而降低排名
- 首屏堆满愿景宣言但 60 秒内看不到「它长什么样、怎么跑」
- 中英 README 内容漂移——英文版停留在几个版本之前

### 📐 视角二十三 [23]：文档排版审读员

你不是查内容对错——是查**人的阅读体验**。机器看字符串命中就过，人要排版、要呼吸感、要能扫读。同一套文档在两种读者眼里是两份东西。

**✏️ 必答**（先写下再开工）：随机打开三份核心文档，只看头部 30 行——不用读内容，凭第一眼：结构一致吗？如果一份像产品手册、一份像内部备忘、一份像审计报告，差异本身就是发现。

**你的心态**：排版守门员。判据是「一个陌生读者滚三屏能不能找到他要的东西」，不是「字都对不对」——字对不对是别的视角的活。你只关心：结构统一性、可读性、考古债。

你可能会注意到的事（举例，不是清单）：
**结构统一性（跨文档横向）**：
- 头部结构是否一套模板：H1 → logo（如有，是否统一居中统一尺寸）→ 一句话导语 → 版本头 → 目录——顺序乱一处就是不统一
- 目录缺失：超过 200 行的文档没有目录，读者只能滚轮找节
- 章节编号体例：有没有字母续编（九/九B/九C）这种「塞进去的章节」——编号应该正序干净
**可读性（单文档纵向）**：
- 单段超长（一段塞 800+ 字符不换行）——屏幕上一堵墙
- 表格列过宽（单格 500+ 字符）——表格是让信息可扫读的，不是让信息消失的
- 重复展开：同一主题在多份文档各写全文——应该一处全文 + 他处一句链接
**考古债（时间维度）**：
- 已发版版本还赖在「规划表」里（交付即移出）
- 索引行/摘要行逐版膨胀（把详情搬进了目录）
- 已完成事项标「规划中」/已废弃措辞还在（版本状态漂移）
**排版规则一致性**：
- 同类元素的格式全套一致：版本号写法（vX.Y.Z）、日期形态（· YYYY-MM-DD 已发版）、数字口径标注（workspace/发版时点）——守卫认这些格式契约，漂了守卫会失明
- emoji 用法是否同版本统一（同版本要么全带要么全不带，放法一致）
**反模式**（看到即报）：
- 为「对称」在两处各写半个主题——那不叫去重叫分叉
- 目录链接锚点手写但没验证——点开跳不到（中文锚点 GitHub 算法复杂，宁可用纯列表也别放假链接）

---

## 输出格式

不管你用哪个视角，自然关注产品定位、工程质量、文档与上手、安全与诚实、生命力、文档精简度即可——不必逐项回答，让它们自然融入你的感受。

完成全部视角后，输出一份报告。没有固定模板——但请确保：

- **每个视角先给必答题的答案**，再给发现（哪怕答案是"想不出比举例更重要的"，也要写下来——那本身是视角适配度的信号）
- **每个视角都有发现**（哪怕是"整体印象是 X，没发现大问题"）
- **问题分优先级**：P0（严重/阻塞）/ P1（应该修）/ P2（观察项）
- **给出文件路径和具体描述**，让收到报告的人能定位
- **深发现不受篇幅惩罚**：浅发现（印象级/单文件级）一页内说清；深发现（跨文件推理链/需要实测取证）写全你的推理过程与证据——推理链被截断的深发现等于没发现。每条深发现标注**推理深度**：`深度1`（读了表面）/ `深度2`（读了实现）/ `深度3`（读了实现 + 实测验证或跨文件追踪）
- **特别记录你的意外发现**——那些不在任何预期范围内、但你直觉觉得"不对"的东西。这些往往是最有价值的。

最后附一个总评：这个项目给你什么整体印象？1-10 分打多少？为什么？总评收尾必答一问：**「这一轮审查后，你最想给项目提的一条建议是什么？」**——发现问题的审计员同时是最有资格提建议的人；只向后看（打分）不向前看（建议）的总评丢了一半价值。

---

## 附：历史教训（不是检查清单，是经验提醒）

> 以下不是"你必须检查 X"，而是"过去在这些方向出过问题，保持敏感"。它们帮你校准直觉——但你的直觉不应该被它们限制。

这个项目在过去 10+ 个版本的独立审查中，问题反复集中在以下方向：

- **数字与声称漂移**——文档里的数字（规则数、测试数、版本号）和代码实际对不上；声称的功能在代码里找不到对应实现
- **物理搬迁残留**——目录改名、文件迁移、参数重命名后，总有什么东西没跟着动（路径、引用、调用方）
- **叙事不一致**——同一个概念在不同文档描述不同；声称"独立"但实际耦合；旧术语残留在归档区误导新读者
- **静默失效**——空 catch 吞错、写入/读取不对称、功能做了但用户感知不到——问题发生了但无人知晓
- **假绿与假阳性**——exit code 测量取到的是管道末端的退出码而非脚本的；自动化裁决把 PASS 读成 FAIL——"全绿"本身就是最值得怀疑的信号
- **验证环境的遮蔽性**——本地绿 CI 红不一定是代码错：构建产物残留、宿主运行时文件（密钥等）、CI job 缺前置，都会让"我这里没问题"成为无效证据；验证应先复现对方的干净态。多套 runtime 各装全局包时，注入 PATH 前缀还会选中旧版二进制制造"版本不一致"假红——门禁类验证用默认登录 shell 的 PATH
- **对账通道的失真**——包管理器查询吃本地缓存、CLI 输出语义歧义（修复提示 vs 拒收），"查了"不等于"查准了"——对账结论必须来自权威通道（registry 在线查询 / 官方 API 字段）
- **并行工作的互相污染**——多 session/多人同仓并发时，自己的提交动作（git add 文件级暂存、批量替换）会把对方工作区的未完成改动裹挟进来；"我只改了我的部分"在文件级粒度上不成立——提交前逐 hunk 核对暂存区才是有效防线
- **宿主平台契约的想当然**——给宿主写插件时"配置从 ctx 读"是想当然：宿主可能白名单构造 hook ctx（不含 config）、把插件配置注入在别处（如 api.pluginConfig）——不读宿主源码实证，配置读取就是薛定谔的（写了没人读、测试还过）。同族：分发平台版本可变性（npm/ClawHub 不可变，skill 通道可同版本更新）决定"修复何时到用户"

这些方向值得你保持**额外敏感**——但不要把它们当成清单。你今天发现的问题可能完全不在这个列表上。**那才是最值钱的发现。**

> 🔧 **工程规则与校准档案已外移**：报告判别规则（审查校准）、driver/管道/门禁的工程防线、文档治理规则及已消化校准索引，见 [fresh-eyes-calibration.md](./fresh-eyes-calibration.md)（维护者文档——**worker 不加载**防注意力锚定，**报告判读者/收口方必读**）。本文件保持「视角 + 纪律」纯审查输入；新校准往档案加，本文件不再因校准内容涨行（警戒线权威出处 `docs/changelog/releasing/04-review-system.md`，本处仅外链、不自带数字）。
