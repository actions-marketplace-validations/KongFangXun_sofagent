# 孤儿守卫豁免清单（tools/check/check-gate-inventory.sh 用）
#
# 用途：登记「按设计在开源仓内不存在合法自动调用点」的 tools/check/ 守卫，
#   使其免于被判 C1 孤儿。与 playbook/.coverage-exempt 同构（`#` 起头为注释）。
#
# 格式：每行 `文件名:理由`——**理由不可为空**（C2 陈旧豁免会拦）。
#   与 .coverage-exempt 只写关键词相比，本文件强制「名 + 理由」同行：
#   理由是豁免的**前提**，不是注释里的可选装饰——写不出理由就该接线，而不是豁免。
#
# 纪律：豁免是**有论证的例外**，不是「处理不掉就勾一笔」。新增豁免必须能说清
#   「为什么开源仓里**不存在**合法的自动调用点」。可接受的豁免依据是**设计性约束**
#   （需仓外输入 / 需真宿主在场 / 默认非阻断的巡检），不是「暂时没接」「以后再补」。
#   后者属于欠账——欠账的正确处置是接线，不是登记豁免。
#
check-interface-roadmap.mjs:须带 `--spec <商业机制文档>` 才实跑，spec 属商业侧文档、不进开源仓（CI 与本地均无处传参）。无参时打印 SKIP 并 exit 0，而该脚本自陈「SKIP 不算通过」——接进自动链路等于制造一条**免费绿灯**（看着跑了、实则什么都没判）。商业侧对账时人工带 spec 实跑。
check-seam-drift.mjs:宿主 seam 词表漂移巡检（登记本 vs 宿主实测差集），cadence = 宿主升级后 / 定期，非推前链路；且**默认只提示不阻断**——漂移是**宿主侧**信息，不是本仓的错。接进 pre-push 只会在每次推送时产出恒定提示噪声而无任何阻断价值。`--strict` 供巡检时收紧。
