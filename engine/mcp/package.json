{
  "name": "@sofagent/mcp",
  "version": "1.5.2",
  "description": "sofagent MCP Server — 暴露审计能力给 MCP Client（JSON-RPC 2.0 over stdio）",
  "main": "dist/mcp-server.js",
  "types": "dist/mcp-server.d.ts",
  "files": [
    "dist/",
    "!dist/**/*.js.map",
    "!dist/**/*.d.ts.map",
    "!dist/**/*.test.*",
    "README.md"
  ],
  "bin": {
    "sofagent-mcp": "dist/mcp-server.js"
  },
  "scripts": {
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "build": "npm run clean && tsc",
    "postbuild": "node -e \"const fs=require('fs'),path=require('path');if(process.platform==='win32'){console.log('[postbuild] Windows 平台跳过 bin 可执行位检查（Windows 无 POSIX 执行位，改用 .cmd shim）');process.exit(0)}for(const b of ['dist/mcp-server.js']){const p=path.join(__dirname,b);fs.chmodSync(p,0o755);if(!(fs.statSync(p).mode & 0o111)){console.error('❌ bin 入口不可执行: '+b);process.exit(1)}}\"",
    "prepublishOnly": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\" && npm run build && find dist -name '*.js.map' -delete && find dist -name '*.d.ts.map' -delete",
    "check": "tsc --noEmit",
    "test": "vitest run"
  },
  "keywords": [
    "sofagent",
    "mcp",
    "model-context-protocol",
    "audit"
  ],
  "license": "MIT",
  "author": "KongFangXun",
  "repository": {
    "type": "git",
    "url": "https://github.com/KongFangXun/sofagent"
  },
  "bugs": {
    "url": "https://github.com/KongFangXun/sofagent/issues"
  },
  "homepage": "https://github.com/KongFangXun/sofagent#readme",
  "dependencies": {
    "@sofagent/ab-test": "1.5.2",
    "@sofagent/audit": "1.5.2",
    "@sofagent/core": "1.5.2",
    "@sofagent/eval": "1.5.2",
    "@sofagent/ontology": "1.5.2",
    "@sofagent/orchestrator": "1.5.2",
    "@sofagent/train": "1.5.2",
    "@sofagent/rules": "1.5.2",
    "@sofagent/evolve": "1.5.2",
    "@sofagent/think": "1.5.2",
    "js-yaml": "^5.4.2"
  },
  "//optionalDependencies": "playwright 是 browser-tools.ts 惰性 require 的可选运行时（浏览器工具需要时才加载，缺失时降级返回提示不阻断安装）——放 dependencies 会经 umbrella 总包（npm i -g sofagent 硬依赖 mcp）强制全装数百 MB 浏览器二进制。照抄 root package.json @langchain/anthropic 的 optionalDependencies 先例。",
  "optionalDependencies": {
    "playwright": "^1.62.1",
    "@sofagent/daemon": "1.5.2"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "exports": {
    ".": {
      "types": "./dist/mcp-server.d.ts",
      "require": "./dist/mcp-server.js",
      "default": "./dist/mcp-server.js"
    },
    "./tool-registry": {
      "types": "./dist/tool-registry.d.ts",
      "require": "./dist/tool-registry.js",
      "default": "./dist/tool-registry.js"
    },
    "./package.json": "./package.json"
  }
}
