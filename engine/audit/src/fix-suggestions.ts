// ============================================================
// fix-suggestions.ts · 审计违规修复建议
// v1.3 新增：每条违规/警告带「怎么修」建议
// ============================================================
// 每条规则的修复建议字符串（覆盖随 defaultRules 动态扩展）
// printResults 输出时在 detail 行后追加「怎么修」行
// ============================================================

/**
 * 规则修复建议映射表
 * key = 规则名称（与 rules/index.ts 的 name 字段一致）
 * value = 修复建议字符串
 */
const FIX_SUGGESTIONS: Record<string, string> = {
  'A1 不碰敏感': '.env 不应提交。将敏感文件加入 .gitignore，或在 config.yml 的 lowRiskPatterns 中添加文件路径',
  // 顺序即优先级：密钥一旦进过暂存区，改 .gitignore 无效（文件已被 git 跟踪），
  // 第一步必须是吊销——否则「按建议修完」的人反而以为已经安全。
  'A2 不泄密钥': '立即到服务商控制台吊销/轮换该密钥 -> 从提交中移除（git rm --cached 或改提交内容）-> 写入 .env 并加入 .gitignore -> 重新提交。注意：已被 git 跟踪的文件加 .gitignore 无效，必须先吊销',
  'A3 不改越界': '拆分提交，只提交与当前任务相关的文件，无关改动单独提交',
  'A4 不删配置': '确认是否为有意删除。如是，在 commit message 中说明原因',
  'A5 不瞒真相': '用 Conventional Commits 格式描述具体改动（如 fix: 修复登录页 token 过期）',
  'A6 不坏构建': '确认构建配置变更不会破坏构建，本地跑一次 npm run build 验证',
  'A7 不存盲改': '修改文件前先 Read 目标文件。Agent 日志记录在 data/task/logs/',
  'A8 不逃验证': '变更后跑 npm test 或 npm run build，日志记录在 data/task/logs/',
  // 旧文案把检测签名明文印给被拦者（等于给出绕过配方）——只描述意图，不复述特征串
  'A9 不纳注入': '移除 diff 中形似「要求模型忽略既有指令/改变行为边界」的指令性文本——这类内容即便出现在注释、文档或字符串里也会被拦截',
  'A10 不引毒源': '改用官方 npm registry（npm config set registry https://registry.npmjs.org）',
  'A11 不滥资源': '拆分为多个小提交，每次只做一件事',
  // 扩展规则
  // run-07 verdict P1-5：E1 语义 = src/ 源码变更但缺测试（rule-e1-no-test-files.ts
  // 判定 hasTestFiles=false 才 WARN）——旧文案「分离到 dev 分支或加 .gitignore」
  // 与规则意图相反（按文案操作会加剧测试缺失）。改为补测试导向。
  'E1 不落测试': 'src/ 源码变更应伴随测试：为变更的源码文件补充 *.test.ts（同目录或 tests/），随本次提交一并纳入',
  'E2 TODO 未声明': 'TODO 必须关联 issue 编号（如 TODO(#123)）或直接解决',
  'E3 不滥删除': '确认删除范围，使用保守修剪原则：只删纯 UI，保留结构性模块',
  'E4 不低注释': '补充函数注释，目标注释率 >= 10%',
  'A14 知识库越权': '检查 workflow.yml 的 knowledge-domain 配置，确认节点只访问授权范围内的知识页面',
  // v1.4.5 审查补齐：扩展规则此前 9 条无「怎么修」——恰好是不植后门/不越权限/
  // 不泄外联/不逃路径等最高危面，被拦下时用户零下一步，只能去翻 HANDBOOK。
  'A15 不盲动': '先 Read 目标文件确认现状再改——未读先改的变更会被判为盲动；如确为自动生成，在 commit message 中说明来源',
  'A16 非授权文件变更': '本次任务未授权改动此文件——回退该文件（git checkout -- <file>），或先向任务负责人取得授权再改',
  'A17 异常批量变更': '拆分为多个语义完整的提交；确需批量时，在 commit message 中说明批量范围与原因，并逐文件自审',
  'A18 垃圾文件': '删除临时/调试残留文件；如是构建产物，加入 .gitignore 而不是提交',
  'A19 msg 质量': '改写 commit message：用 Conventional Commits 格式（type: 具体做了什么），说清改了什么与为什么，去掉「修改」「更新」等无信息词',
  'A20 不泄外联': '移除指向外部服务的数据外发（webhook/上传/第三方 API 调用）；确需外发时改走用户显式配置且默认关闭的通道，并在文档中声明',
  'A21 不植后门': '移除隐藏的持久化/远控/权限绕过代码——删除可疑的开机自启、外连回连、鉴权跳过逻辑，并在 commit message 中说明删除原因',
  // 措辞避开自身规则的特征串（A22 会命中任何出现的全权限 chmod 字面量，含本表文案）
  'A22 不越权限': '降低到完成任务所需的最小权限：移除提权执行、放开全权限、读写授权目录之外文件等操作，改为受限目录内操作',
  'A23 不逃路径': '改用受管目录内的相对路径或配置声明的路径，不要用 ../ 逃逸出授权工作区；确需跨目录时在任务声明中登记',
};

/**
 * 获取规则修复建议
 * @param ruleName 规则名称
 * @returns 修复建议字符串，无匹配时返回 null
 */
export function getFixSuggestion(ruleName: string): string | null {
  return FIX_SUGGESTIONS[ruleName] ?? null;
}
