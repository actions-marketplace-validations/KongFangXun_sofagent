# v1.2.7 开发日志 — 编排引擎增强（DeerFlow 启发）+ 🔗 激活链 Phase 2 后半（StateGraph 构建）

> 状态：✅ 已开发（代码完成，开发日志阶段八定稿） · 孔放勋 · 2026-08-06
> 开发完成快照：npm test 1527/1527 通过（audit 587 / orchestrator 374 / core 205 / daemon 171 / mcp 69 / skillopt 33 / eval 29 / rules 28 / ab-test 12 / harness 13 / ontology 5 / think 1 共 12 包）+ acceptance-test 141 场景（218 通过 / 0 失败）+ release-gate-loop run-06 verdict=PASS · check-version.sh 日期一致（package-lock 阶段十一同步） · check-test-count.sh 13 处校验一致 · 版本号已 bump 至 1.2.7
> 激活链设计文档：[docs/guides/fde-activation-chain.md](../../guides/fde-activation-chain.md)

## 🔗 激活链 Phase 2 后半：StateGraph 构建

### 定位

v1.2.6 让 workflow-parser 认识了企业 Agent。v1.2.7 构建企业专属 LangGraph StateGraph——不拆任务，直接用 FDE 交付的 workflow.yml 编排。

### 交付

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/enterprise-graph.ts` | 新建 | 企业编排图构建 |
| `engine/orchestrator/src/composer.ts` | 修改 | 新增 `composeEnterpriseWorkflow()` |
| `engine/orchestrator/src/__tests__/enterprise-graph.test.ts` | 新建 | 单测 |

### 核心函数

`composeEnterpriseWorkflow()`：从 FDE 交付物构建企业专属编排方案。与现有 `compose()`（通用拆解）的区别：不调 LLM 拆任务，直接用 workflow.yml。

### 数据流设计

三层架构：实时业务数据走 LangGraph State（内存）+ 知识数据走 ontology entity（磁盘持久化）+ 状态标记双写。

> 详细开发 Prompt：`~/Desktop/v1.2.7-dev-prompt.md`（权威 SSOT，2026-08-04 审查定稿）

## 功能清单（9 项功能全交付）

> 编号体系以 dev prompt ①-⑨ 为权威，与 ROADMAP v1.2.7 行一致。

① **Session Goals**（`/goal` 给线程附完成条件 + 非思考模型评估 + N 次续接上限）— 改进 FORGE fresh-eyes-loop 停止条件
② **手动上下文压缩**（`/compact` 用户侧减压阀）
③ **Skill 渐进式加载**（核心铁律层 core-rules.md ~30 行始终注入 + 岗位规范按 task type 按需追加）— 直击加载链步进脆弱性
④ **`--doctor` 可操作修复提示 + `--repair` 模式**（缺依赖给安装命令，权限错给 chown 命令；仓库无 Makefile，不采用 `make doctor`）
⑤ **FORGE driver 三方抽象**：提取公共 driver-base.mjs——原计划第 3 个 loop 出现时提取，2026-08 用户拍板提前至本版（接受 2 样本抽象风险；base 层只抽公共工具函数不抽 main()）
⑥ **🔗 激活链 Phase 2 后半**——见上方独立章节
⑦ **`sofagent-audit --support-bundle`**：一键生成 issue 摘要 + 证据 zip（version/log/doctor 输出打包，自动脱敏 API key/路径）
⑧ **One-Line Agent Setup**：独立轻量 bootstrap.sh（<50 行）一行 curl|bash 入口，install.sh（940 行）不动（零回归面，2026-08-04 定稿）
⑨ **Agent Mailbox**：见下方独立章节

## SubAgent Agent Mailbox（从 v1.2.5 P6b 拆入）

> ⚠️ **勘误（v1.4.8 深模块审查）**：Agent Mailbox 自引入起既无生产者也无消费者（节点侧从未接线 `.injectMessages`），已于 v1.4.8 整块退场。本章节为历史快照，正文保留原貌不作改写。

> 来源：v1.2.5 规划 P6b（SubAgent 间异步消息）。v1.2.5 瘦身时拆至本版，与 Session Goals / HITL 中断处理协同更自然。

**问题**：当前 SubAgent 间通信只有 `artifacts` 字段（单向、同步、全量替换）。reviewer 发现 engineer 正在写的文件有冲突无法实时通知；engineer 需要问 auditor「这条规则的具体含义」无法直接提问。

**设计**：每个 SubAgent 一个邮箱目录（`data/agent-mailbox/<agent>/inbox/`），消息以 JSON 文件投递。在 LOOP StateGraph 每个节点开始前扫描邮箱，高优先级消息注入 system prompt（非中断执行，只补充上下文）。

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/mailbox/mailbox.ts` | 新建 | 邮箱读写（send / read / markRead） |
| `engine/orchestrator/src/mailbox/message-injector.ts` | 新建 | 节点开始前扫描邮箱 → 注入 system prompt |
| `engine/orchestrator/src/mailbox/index.ts` | 新建 | 导出 + 类型 |
| `engine/orchestrator/src/loop/nodes.ts` | 修改 | 每个节点 wrapper 加邮箱检查 |
| `engine/orchestrator/src/__tests__/mailbox.test.ts` | 新建 | 发送/接收/注入全流程单测 |

> 与 L2 协作协议（v1.3.3）的关系：L2 处理「多设备/多 Agent 编排级」协作（共享态/意图广播），Agent Mailbox 处理「单个 LOOP 内 SubAgent 之间」的点对点通信。两者互补不冲突。

---

## Release Notes

### 新增功能

| # | 功能 | 交付文件 | 测试数 |
|---|------|---------|--------|
| ① | Session Goals（`/goal`） | slash-commands/goal.ts · loop/state.ts · loop/nodes.ts · loop/graph.ts | graph.test.ts 42 tests 含 goal_eval 路由 |
| ② | 手动上下文压缩（`/compact`） | slash-commands/compact.ts · slash-registry.ts | slash-registry.test.ts 20 tests |
| ③ | Skill 渐进式加载 | SKILL/core-rules.md · role-audit.md · role-fde.md · role-orchestrate.md · handler.ts · install.sh | 现有 load-chain 测试 |
| ④ | --doctor 修复提示 + --repair | doctor.ts · cli.ts | doctor 现有 4 tests |
| ⑤ | FORGE driver 三方抽象 | FORGE/src/driver-base.mjs | driver-base.test.mjs 22 tests |
| ⑥ | 🔗 enterprise-graph（StateGraph 构建） | enterprise-graph.ts · entity-store.ts · composer.ts | enterprise-graph.test.ts 11 tests |
| ⑦ | --support-bundle | support-bundle.ts · index.ts · package.json(+archiver) | support-bundle.test.ts 10 tests |
| ⑧ | One-Line Agent Setup | bootstrap.sh · README.md | macOS/Linux 兼容 |
| ⑨ | Agent Mailbox | mailbox/mailbox.ts · message-injector.ts · index.ts · nodes.ts · graph.ts | mailbox.test.ts 14 tests |

### 质量验证

- **测试套件**：全部测试套件通过（slash-registry 20 · doctor 4 · graph 42 · enterprise-graph 11 · mailbox 14 · driver-base 22 · support-bundle 10 = 共 123 个新增测试）
- **构建**：`npm run build` 成功
- **依赖**：新增唯一外部依赖 `archiver@^7.0.0`（support-bundle ZIP 打包），落点 `engine/audit/package.json`
- **回归**：现有 compose() 签名不变；install.sh（940 行）不动；现有 driver 测试全绿

### 约束遵守

- composeEnterpriseWorkflow 与现有 compose() 并行，无运行时调用方（v1.2.8 dag-runner 接线）
- v1.2.7 不碰 dag-runner.ts / workflow-parser.ts / registry.ts
- bootstrap.sh 纯新增，install.sh 零改动
- Agent Mailbox 补录 ROADMAP 统一收归 T05（唯一执行点）

---

## 阶段四·fresh-eyes-loop 质量循环修复（4 commit）

> run-06~run-10 五轮实测迭代，将 fresh-eyes-loop 从「理论修复」推到「能跑通全 5 步、能发现真问题、能修真 bug、能验证真修好了」。以下 4 个 fix commit 是质量循环中的修复产物。

### fix-1 `5fb5a34` — fresh-eyes-loop 三个根因问题修复

| 问题 | 修复 |
|------|------|
| 审查步骤工具预算过宽（模型无收敛压力） | TOOL_SOFT_LIMIT / TOOL_HARD_LIMIT 收紧 + prompt 加「工具预算」铁律段落（35 次上限 + 5 条策略） |
| generateReportWithoutTools 常态调用（绕过正常审查流程） | 加触发条件：仅 hardBreak=true 时才走无工具裸 LLM 报告生成 |

### fix-2 `b099990` — build 脚本 `|| true` 吞编译错误

| 问题 | 修复 |
|------|------|
| `tsc && [...] \|\| true` 末尾 `\|\| true` 吞了编译失败的 exit code | 3 个 package.json（core/daemon/orchestrator）改为 `(...; true)` 子 shell 分组 |
| tsconfig.json audit 路径错误 + 漏 exclude test/cli.ts | 路径修正 + exclude 补充 |

### fix-3 `bf6f050` — 实跑验证修复（A→GLM + 零窗口 + 报告门控）

| 问题 | 修复 |
|------|------|
| Qwen3.8-max thinking-only 无法约束（59 次工具调用后 OOM exit 137） | A 角色改回 GLM-5.2；双盲审查通过 prompt 视角差异保证（a-check.md ≠ b-check.md） |
| extractAgentText 缺报告质量门控——GLM 172 字符思考碎片被当报告写入 check-a.md | 加 isReportText 检查（≥500 字符 或 含 `##` 标题行），不达标返回 null 走降级 |
| 写报告窗口完全无效——GLM 和 Qwen 都在 15 步窗口内继续调工具（tools 数组仍在 = 物理可调） | REVIEW_GRACE_STEPS / DEFAULT_GRACE_STEPS 改为 0（零窗口模式，撞硬上限立即 break） |
| generateReportWithoutTools 摘要太小（200 字符×40 条） | 摘要加大到 500 字符，条数收到 20 条（减少全标 P2） |

### fix-4 `71ce5cf` — isReportText 作用域 bug + worker 内存

| 问题 | 修复 |
|------|------|
| isReportText 定义在 runWorker 局部作用域，但 extractAgentText（模块级函数）也调用它 → `ReferenceError` → a-consolidate 步骤直接崩溃 | isReportText + REPORT_MIN_CHARS 提到模块级（L798-805） |
| run-09 两轮 4 个 worker 消息累积导致 worker OOM | `--max-old-space-size` 1536→2048 |

### run-10 验证结果（完整 5 步跑通）

| 步骤 | 工具调用数 | 产出 |
|------|-----------|------|
| a-check | 49 | 裸 LLM 1353 字符报告（2 P1 + 2 P2） |
| b-check | 54 | 裸 LLM 3342 字符报告（1 P1 + 3 P2） |
| a-consolidate | 38 | findings.md + result.md（驳回 1 误报 + 3 条 P2） |
| b-fix | 50 | 实际修改 3 个文件（sf_edit×9 + sf_write×2） |
| a-verify | 32 | 验证回填 |

> 停止判定：P0=0 / P1=0 / P2=0 → CLEAN（干净轮），exit 0。b-fix 修改的 3 个文件（runner.ts / engine.ts / check-test-count.sh）改动合理，已还原不污染工作树。

---

## 发布检查清单（汇总）

### 代码质量

- [x] npm test 1527/1527 全绿（audit 587 / orchestrator 374 / core 205 / daemon 171 / mcp 69 / skillopt 33 / eval 29 / rules 28 / ab-test 12 / harness 13 / ontology 5 / think 1 共 12 包）
- [x] npm run build exit 0（12 包全部编译成功）
- [x] acceptance-test 141 场景全通过（含 v1.2.7 新增 scenario 198-207，S197 归并至 S164）
- [x] bash -n acceptance-test.sh 语法通过
- [x] check-version.sh 日期一致（package-lock 阶段十一同步，非阻塞）
- [x] check-test-count.sh 13 处校验一致（v1.2.6 改为历史冻结，当前 SSOT 1527 / 141 场景）
- [x] 版本号已 bump 至 1.2.7（package.json SSOT + 13 类位置同步）
- [x] 文档头日期同步 2026-08-06（EXPECTED_DOC_DATE + 文档头）

### 审查体系

- [x] regression-checklist 51 维度（声称=实际），1046 行 ≤1050 警戒线
- [x] acceptance-test 141 场景（声称=实际），1649 行 ≤1650 警戒线
- [x] fresh-eyes-review 历史教训补入 v1.2.7 两条（镜像 driver 漂移 + 碎片文本伪装）
- [x] 维度 71（package.json build 禁 || true）↔ S201/S202 ↔ fresh-eyes 验证链闭环
- [x] 维度 72（函数作用域引用）↔ driver 修复验证链闭环

### 阶段六 release-gate-loop

- [x] release-gate-driver 4 项熔断修复（零窗口 + generateReportWithoutTools + 报告质量门控 + model 实例修正）
- [x] release-gate-loop run-06 verdict=PASS（acceptance 218/0 · regression 51 维 0 FAIL · coverage 9 功能全覆盖）

### 功能交付（9 项功能全交付）

- [x] ① Session Goals（/goal 命令 + goal_eval 路由）
- [x] ② 手动上下文压缩（/compact 命令）
- [x] ③ Skill 渐进式加载（core-rules.md + role-*.md 分层）
- [x] ④ --doctor 修复提示 + --repair 模式
- [x] ⑤ FORGE driver 三方抽象（driver-base.mjs）
- [x] ⑥ enterprise-graph（composeEnterpriseWorkflow + StateGraph 构建）
- [x] ⑦ --support-bundle（诊断打包 + 脱敏）
- [x] ⑧ One-Line Agent Setup（bootstrap.sh 32 行）
- [x] ⑨ Agent Mailbox（邮箱模块 + 节点注入）

### 阶段四 fresh-eyes-loop 修复

- [x] fix-1（5fb5a34）工具预算收紧 + generateReportWithoutTools 触发条件
- [x] fix-2（b099990）package.json build 脚本 || true 吞错误修复
- [x] fix-3（bf6f050）A→GLM + 零窗口 + 报告门控 + 摘要加大
- [x] fix-4（71ce5cf）isReportText 作用域 bug + worker 内存 2048
