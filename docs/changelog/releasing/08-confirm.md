<!-- SOP-ANCHOR: S8 | file: 08-confirm.md | prev: S7 | next: S9 -->
<!-- 机器锚（模型检索用，人不影响阅读）：grep "SOP-ANCHOR:" 可一跳取全部阶段元信息；本阶段完成后 → S9 -->
# 阶段八：发布放行关口

> **定位**：阶段八不是「再做一轮检查」——检查在阶段一~七已全部完成且各有关口。阶段八是把前七阶段的验证**汇总成一份放行清单**，作者**一次性放行**（说「发布」即进入阶段九），唯一产出物是发布 prompt（交接给执行方）。不逐项让作者过目——内容重复且无决策意义，改为放行决策 + 就绪汇总。
>
> **放行原则**：作者一句话放行（「发布吧」「确认」）即可；发布就绪汇总只展示不逐项求确认。三拍板项（push 积压/npm 发布/Skill 分发）在放行时一次性确认。

---

## 步骤

| # | 完成 | 步骤 | 验证方式 |
|:--:|:--:|------|------|
| 一 | | **发布就绪汇总**：门禁基线表（各阶段实测值汇总——测试数/acceptance/check-version/CRS/CTH/anchors/release-gate/fresh-eyes/**仓外门面 check-storefront**）+ 改动清单一句话（commit 数/文件数/行数） | 汇总表展示 |
| 二 | | **冻结窗口重验（门禁时点纪律）**：门禁基线表必须是**冻结窗口内的终态实测**——跑之前先过独占窗口三查（同 01-review 步骤 0：git status 文件数 / 近 5 分钟 mtime / 当日 memory 日志他人痕迹），任一命中说明仍有 session 在写仓库，基线表标注「时点快照」并等收尾后重跑。多 session 并行期各阶段的绿都是时点值，只有冻结窗口内跑出的绿才算放行依据；测试数有疑漂先跑 `bash tools/check/sync-test-count.sh --dry-run` 预览归属（谁的测试谁同步），勿抢改在途数值 | 三查通过 + 门禁 EXIT=0 |
| 三 | | **审查遗留拍板清单（v1.4.6 拍板固化）**：收集本轮审查/开发中所有「需作者拍板」项（公开仓内容取舍 / 渠道决策 / 策略选择），逐项列现状 + 建议方向，作者当场拍板——拍板即执行，或显式登记「不处理 + 理由」。**不允许悬而未决项进发布**（v1.4.6 实锤：playbook 星数博弈 / VALIDATION 备忘录 / npm 裸名 / 官网 404 四项漂了两轮审查仍无归口） | 拍板清单零遗留 |
| 四 | | **作者放行决策**：一次性确认三拍板项（push 积压 / npm 发布 / Skill 分发）+ 说「发布」即放行——**放行 = 阶段九发布链一揽子授权**（push→CI→tag→release→npm publish 一次走完，只在报错或硬前置不满足时停，见 09-publish 授权边界） | 作者确认 |
| 五 | | 复核开发日志「发布检查清单」全 `[x]`（打勾动作在阶段六，此处只复核，未勾项须为「发布时补做」类）。**「归发布关口终验」类勾（各章验收标准中声明归终验的 `npm test` 等）在本步骤统一处置**：CI/基线终验实测通过后当场打勾并注明取证实测值（曾留 11 处未勾靠本步骤复核统一收口） | 无遗留 |
| 六 | | AI 生成完整发布 prompt，交接给项目负责人（可授权 AI 代执行） | prompt 就绪 |

> 🔴 **步骤五防漂移铁律**：生成发布 prompt 时，**gh release 段必须逐字引用 09-publish.md 步骤七「Release Note 生成 → 自检 → 上一版结构对照（三道工序）」的命令模板**（`--title "vX.Y.Z — {emoji 主题短语}"` + 生成后自检 + 上一版结构对照），**禁止凭记忆手写简化**——凭记忆手写会把标题简化成裸 `--title "vX.Y.Z"`，丢掉「vX.Y.Z — emoji 短语」的既有惯例。生成完对照 09-publish.md 逐段核对一次再交接。
>
> 🔴 **Release Note body 与发布 prompt 同批产出（本步骤）**：三道工序的 body 生成**在本步骤内完成**并落 `~/Desktop/release-note-vX.Y.Z-body.md`——**放行 = 授权对外发布，作者理应过目发布物本体**；body 若留到阶段九才现场生成，放行决策就缺了「到底要发什么」这一半依据（曾出现放行时只见到 prompt 里的生成指引、见不到实际 body）。body 数字取**阶段八冻结基线终值**（与 09-publish「数字取值锚」同源，故此时生成不存在数字未定问题）。阶段九步骤七仍跑三道工序，性质是**复核 + 落终版**。
>
> 🔴 **发布物落盘铁律**：发布 prompt + release note body 等发布物，统一落盘 `~/Desktop/`（命名 `vX.Y.Z-publish-prompt.md` / `release-note-vX.Y.Z-body.md`，与阶段一审查草稿/阶段十一 dev-prompt 的桌面惯例一致）。**禁止自建新目录**（如需留存走既有标准位置）——发版产物一律桌面，SOP 引用的路径只有两种：仓库内既有文件 或 `~/Desktop/`。

> **AI 代执行边界**：如果项目负责人说「交给你了」明确授权，AI 可以在项目负责人已登录的环境代执行发布命令（npm publish / git tag / gh release / Skill 分发）。但：npm 凭证必须在项目负责人已登录的终端中使用，**AI 不得自行 `npm login` 或操作凭证**；每一步执行后立即报告结果，遇到报错暂停并请示。授权 AI 跑命令 ≠ 授权 AI 管凭证——凭证始终在人的控制下。
