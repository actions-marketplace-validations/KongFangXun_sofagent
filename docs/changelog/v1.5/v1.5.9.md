# v1.5.9 开发日志 — 进化模块 · 写面审计、能力基线与配置面契约、元验证

> 📋 **已排期。** 前置依赖：v1.5.8（晋级判据——写面变更的触发源之一；出题考核器——写面变更的触发源之一）+ v1.5.2（第二章独立验签语义——写面记录可导出具证）+ v1.5.7（第五章可拔契约与主干能力清单——配置面契约的同族前置：那章管能力开关，本章管配置字段）
> ⚠️ **尚未实现。** 以下内容为版本目标，不代表当前可用能力（开发排期见 ROADMAP）。
> 状态：已排期 · **2026-09-20 地基期拆版（用户拍板颗粒度细化）：本版承接原 v1.5.5 第六章（自进化写面的审计覆盖）与第七章（能力基线版本线）——两章是同一件事的「当下留痕」与「跨时间可追溯」两面，内容零改写** · 原依据链：2026-09-19 用户拍板 RSI 缺口裁定收编（第三轮写面补盲 + 能力版本线）
> 状态（续）：已排期 · **2026-09-20 地基期拆版（用户拍板颗粒度细化）：本版承接原 v1.5.5 第五章（加载链配置面契约）与第八章（验证器自身老化检测）——两章同属「把门本身管起来」（配置面无死角 / 门本身不老化），内容零改写** · 原依据链：2026-09-19 用户拍板 RSI 缺口裁定收编（RSI-Harness Genome 架构对位 + 元验证缺口） · **本版由旧 v1.5.16、旧 v1.5.17 撤并而来**
> 状态（再续）：已排期 · **2026-09-22 用户拍板（版本边界纪律）：V2.0 起版本全部是 S1M 的迭代域（FDEing × S1A，S1A = S1M + Harness），V2.0 前的能力不放 V2.0 后的版本去迭代——网络出口治理面（原 v2.0.1，Guardian 启发 2026-09-16 排期）与 AI 节点治理接入（原探索方向）当日先归位本版，同日二次拍板（归位再分配）转出：网络出口治理面 → [v1.5.2 第五章](./v1.5.2.md)，AI 节点治理接入 → [v1.5.4 第五章](./v1.5.4.md)——按主题亲和归位（出口治理属审计举证面，AI 节点属路由决策链面），两章全文零丢失迁出，本版回归进化模块四主题**
> 状态（三续）：已排期 · **2026-09-22 探索方向清仓（用户拍板「逐项裁定、排进 V2.0 前」）：「透明仓」与「敌手自测」两项自探索方向迁入本版并**合流为第六章**——两项触发条件同为「第三方首次独立审查」（外部事件不排期，等价于永久孤儿），且同一动机的两面（自证 / 他证）证据链同源，分版会断链；本版第五章元验证「把门本身管起来」与本章「自己打自己的门」同族，时机亲和**

## 定位


> ⚡ **进化模块 · 写面审计与能力基线**——补上 RSI 治理最大的盲区：**prompt 面与 memory 面**的变更此前完全在审计视野外（权重面 manifest 验签、skill 面 eval-gate 已覆盖），本版为两类非代码写面定义 diff 契约并落可举证记录（与 git diff 并列为第三通道，规则判定逻辑不变）；同时把「单次执行可复现」升级为「**跨时间可追溯**」——能力基线版本线，出事能回答「当时它是怎么做事的」。

> ⚡ **进化模块 · 配置面契约与元验证**——把「门本身」管起来：**配置面**（加载链四层字段 SSOT + 所有权唯一 + 越界检测——判据不是「能配置」而是「配置面无死角」）与**验证器自身**（进化越久门越松——验证集老化度、收益归因对照组、验证漂移三态、评估器与执行模型解耦三级精判、判定件作验证器的自评禁则，老化只提示不阻断）。

> 🥊 **审计可信度的攻防两面（自探索方向迁入 · 两项合流）**——审计系统的可信度靠「**被打过而没被打穿**」证明：**敌手自测**定期以攻击者视角尝试绕过审计（篡改 `history.jsonl` / 伪造 HMAC 链 / 绕过 ToolGate），结果进 anti-cases；**透明仓**把治理原始记录（含误报与翻案）实时公开，「审查自己也在被审计」。两项触发条件同一（第三方首次独立审查）故合流成章；透明仓开仓另受**误报率红线**（<5%）闸门与人工确认双控。

---

## 一、自进化写面的审计覆盖（prompt / memory 两面补盲 · RSI 治理的地基章）


> **形态归属**：`[主干]`（判定·留痕——自进化变更面的审计取证）｜ **不可拔**（审计地基：拔掉它，Agent 改自己的 prompt 与 memory 就彻底逃逸审计视野，RSI 治理失去立足点）
>
> 📋 **缺口实测（2026-09-19）**：审计地基是「不信任自我报告，只看 git diff 硬证据」（[ARCHITECTURE](../../ARCHITECTURE.md) 核心叙事），v1.5.1 规划第二通道（调用意图 + 结果文本）。但 RSI 的四个可写面里——**权重面**（v1.4.4 manifest + sha256 验签）与 **skill 面**（eval-gate 收编门 + v1.4.9 执行时 skill 快照）已有覆盖，**prompt 面与 memory 面完全在审计视野外**：Agent 改了自己的 system prompt、往记忆里写了一条「经验」，不留任何可举证的变更记录——**自我改进最大的两个写面恰恰是我们唯一没有硬证据的地方**。行业对位：Meta-RSI 把可写面明确为 data/harness/model 三类并要求保护面独立于所有写面——本章是「保护面如何真的看得见写面」的工程答案。

### 交付

| 组件 | 能力 |
|------|------|
| **写面变更留痕** | prompt 面（SKILL.md / fde.md / overrides 配置变更）与 memory 面（think.md / knowledge / 记忆事实写入）的每次变更落一条可举证记录：变更前后值摘要 + 发起人（Agent 实例 id / 人）+ 触发来源（自我改进 / 人工 / 系统）+ 时间戳 |
| **写面 diff 语义** | 为两类非代码写面定义 diff 契约（配置面按字段级 before/after；记忆面按条目级新增/修订/失效），与既有 git diff 规则并列为第三通道——**规则判定逻辑不变，只扩输入面**（对齐 v1.5.1 双通道的同款纪律） |
| **与改进链绑定** | [v1.5.8 第二章](./v1.5.8.md) 与 [v1.5.8 第四章](./v1.5.8.md) 的晋级 / 考核动作自动产生写面变更记录（改了什么 → 留痕自动发生，不靠调用方自觉）；无留痕的写面变更 ⇒ 回退并告警（fail-closed） |
| **举证面** | 写面变更记录可导出具证（对齐 [v1.5.2 第二章](./v1.5.2.md) 约束导出与独立验签语义——第三方无需安装 sofagent 即可举证） |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/audit/src/write-surface-log.ts` | 新建 | 写面变更记录（prompt/memory 两类契约 + HMAC 挂链） |
| `engine/core/src/loadchain-write-hook.ts` | 新建 | 加载链配置写入钩子（before/after 捕获） |
| `engine/core/src/memory-store.ts` | 修改 | 记忆写入接留痕钩子 |
| `engine/audit/src/__tests__/write-surface.test.ts` | 新建 | 两类契约 / 无留痕回退 / 举证导出单测 |

### 验收标准

- [ ] prompt 与 memory 两类写面变更可留痕（前后值摘要 + 发起人 + 来源 + 时间）
- [ ] 写面 diff 作为第三通道接入既有规则（判定逻辑零改动）
- [ ] 晋级/考核动作自动产生写面记录（不靠调用方自觉）
- [ ] 无留痕的写面变更 ⇒ 回退 + 告警（fail-closed）
- [ ] 写面记录可导出具证（对齐独立验签语义）
- [ ] `npm test` 全绿

---

## 二、能力基线版本线（四层状态快照 · 出事能回答「当时它是怎么做事的」）


> **形态归属**：`[主干]`（留痕·能力状态可复现基线）｜ L1 关档
>
> 📋 **缺口实测（2026-09-19）**：v1.4.9 已交付**执行时** skill 快照（单次执行级可复现——执行前打包、执行后清理），但缺**跨时间**的能力基线：Agent 进化三个月后与当初判若两人，出事故时无人能回答「当时的它是哪个状态」。审计的可复现要求（举证要能还原当时条件）在 RSI 时代需要一条**能力版本线**，而不是只靠单次执行快照。

### 交付

| 组件 | 能力 |
|------|------|
| **基线快照** | 每次能力变更（晋级/回退/写面变更）前打包四层状态：prompt 配置版本 + skill 集合版本 + memory 事实锚点 + 权重 manifest 引用——形成可寻址快照 id |
| **版本线** | 快照按时间成链（前驱指针），任一时点可回答「当时 Agent 的完整能力状态」；与 v1.4.9 执行级快照分工：执行级管单次可复现，本版管跨时间可追溯 |
| **基线回滚** | 按快照 id 回滚四层状态（复用既有 snapshot/restore 语义，不新建回退机制）；回滚动作本身落 decision-log |
| **举证面** | 快照 id 可被引证记录引用（「事故发生在基线 X 下」——举证不止于代码版本，而是能力版本） |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/evolve/src/baseline-snapshot.ts` | 新建 | 四层状态打包 + 版本链 + 回滚 |
| `engine/evolve/src/__tests__/baseline-snapshot.test.ts` | 新建 | 快照完整性 / 版本链前驱 / 回滚可举证单测 |

### 验收标准

- [ ] 能力变更自动生成四层基线快照（可寻址 id）
- [ ] 版本线可回溯任一时点能力状态
- [ ] 按快照 id 可回滚四层状态（复用既有机制）且回滚留痕
- [ ] 举证记录可引用快照 id（能力版本可举）
- [ ] `npm test` 全绿

---

## 三、加载链配置面契约（四层配置无死角 · 与 v1.5.7 可拔契约同族）


> **形态归属**：`[主干]`（判定·配置面所有权与越界检测）｜ L1 关档
>
> 📋 **研究收编**（RSI-Harness Genome 架构）：12 组件互斥字段所有权 + 契约文件 + **全覆盖测试**（从宿主 `.d.ts` 提取全部配置 key，任何未路由 key 测试变红——宿主升级加开关，测试先知道）。对位 sofagent：加载链四层（SKILL.md/fde.md/think.md/knowledge/）无配置面 schema，「哪些字段可配、归哪层管」没有机器审计。与 [v1.5.7 第五章](./v1.5.7.md) 可拔契约同族（那章管能力开关，本章管配置字段）。

### 交付

| 组件 | 能力 |
|------|------|
| **加载链配置 schema** | 四层各自的配置字段显式声明（归属层 + 类型 + 默认值），形成配置面 SSOT |
| **字段所有权断言** | 生成期校验：字段归属唯一（同字段两层声明 ⇒ 拒绝）；加载链实现引用的字段未登记 ⇒ 拒绝（配置面无死角） |
| **越界检测** | 配置值写入非本层字段 ⇒ 拒绝并具名（对齐 [v1.5.7 第五章](./v1.5.7.md) 装配面陷阱机器化同款思路） |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/core/src/loadchain-schema.ts` | 新建 | 四层配置字段 SSOT + 所有权断言 |
| `tools/check/check-loadchain-config.mjs` | 新建 | 实现引用 vs schema 登记 双向对账（未路由 key ⇒ 红） |
| `engine/core/src/__tests__/loadchain-schema.test.ts` | 新建 | 归属唯一 / 未登记拒绝 / 越界具名单测 |

### 验收标准

- [ ] 四层配置字段全部登记（schema SSOT 在位）
- [ ] 实现引用未登记字段 ⇒ 校验红（配置面无死角）
- [ ] 同字段两层声明 / 越界写入 ⇒ 拒绝并具名
- [ ] `npm test` 全绿

---

## 四、验证器自身老化检测（元验证 · 进化越久门越松的反向治理）


> **形态归属**：`[主干]`（判定·验证集有效性）｜ L1 关档
>
> 📋 **缺口实测（2026-09-19）**：进化循环靠 eval 验证集当门，但**验证集自己会老化**——项目既有叙事已点破同一现象（[guides/loop-development](../../guides/loop-development.md)「测量衰退：测试数据老化 95% 通过率是假象」）。RSI 时代这个风险被放大：进化循环持续跑，样本被反复消费，通过率越来越好看而能力未必提升——**越进化，门越松**。行业对位：验证器必须独立于写面，但「独立」不等于「永远有效」——验证器自身也要被检验。**形式化锚（RQGM）**：自改进 agent 的搜索普遍假设**评价标准静止**（固定 verifier / 基准 / 标注集）——「The Red Queen Gödel Machine」（[arXiv:2606.26294](https://arxiv.org/abs/2606.26294)）给出受控效用演化的形式化：搜索分 epoch、**epoch 内判据固定**、只允许在 epoch 边界更新效用，使「自改进保证按 epoch 成立」；其对位读数＝最强基线评审员**过度接受 AI 生成论文达人类率的 1.91×**（对抗目标可纠正）。本章的老化指标 + 复检入口正是该命题在治理面的落法：判据集的「epoch 边界」＝人工复检留痕，自动消费只发生在 epoch 内。

### 交付

| 组件 | 能力 |
|------|------|
| **验证集老化指标** | 每组验证集记「样本消费次数 / 通过率趋势 / 上次人工复检时间」，形成老化度（对齐 [v1.5.7 第四章](./v1.5.7.md) 能力面五维健康度同款思路：低价值技能能退役，老化验证集也要能退役） |
| **收益归因对照组** | 进化收益的**反事实基线**：进化开启前先冻结一份对照配置（不改 skill / 不注入 / 不训练），收益曲线四指标同时跑「进化组 vs 对照组」——差值才是进化的净贡献（防「环境变好了」冒充「进化有效」：任务变简单、模型升级、季节波动都会让通过率上涨，无对照的收益声明不可信）。对照组按周期轮换重置（防止对照组本身过时），轮换留痕 |
| **验证漂移三态** | 考核失败时区分「真失败 vs 环境漂移」（对齐 checkHistoryChainDetailed 三态先例——`tampered`/`unverifiable`/`insufficient-history`）：考核环境本身变了（依赖版本/路径/权限）导致复现失败 ≠ 经验失效。漂移态不降经验置信度、不触发回退，但必须告警并标记待环境复核——**宁可挂起，不误杀好经验** |
| **老化告警** | 通过率连续上升但真实任务指标未改善（或消费次数超阈值且长期无复检）⇒ 告警并建议冻结该验证集的自动晋升资格——**只提示不阻断**（对齐「只提示不阻断」铁律） |
| **验证集复检入口** | 提供人工复检通道：标记样本失效/新增样本，复检动作留痕（与 [v1.5.2 第四章](./v1.5.2.md) 审计结论失效语义同族——失效的不当新证据用） |
| **评估器解耦与三级精判** | ① **自评禁令**：评估 / 反思角色**不得复用被考核的执行模型**（同一模型既做题又判题＝自评失效——行业实证：自评判与环境真值一致率 0.88，而价值估计误差同样 0.88，评判准确度与下一步改进几乎零相关，[S³Gym](https://arxiv.org/abs/2608.31100)）；② **三级精判**：规则/格式预筛（廉价、覆盖全量）→ 独立评估模型精判（有成本、覆盖疑难）→ 关键样本人工抽检（**定标权在 `human` 档**，对齐 [v1.6.0 §七](../v1.6/v1.6.0.md) 教师蒸馏的定标纪律）；③ **评估模型选型**：稳定、可复现、便宜优先——反思/评估不需强创造力，须与执行模型解耦（对齐 [v1.8.0 §三](../v1.8/v1.8.0.md) 实现路线不绑定） |
| **判定件作为验证器（优先级与自评禁则）** | 判定件（类型化概率 · 单次前向 · 可校准）是 `model-judge` 档的**首选形态**——它是 [v1.5.8 第一章](./v1.5.8.md)「验证比生成便宜」这条准入判据的最优解，也是**判定面与 RSI 循环的结构耦合点**：有判定件的域，进化循环才真跑得起来（无廉价验证器的域，循环空转）。三条边界：① **能判不能出题**——判定件只出结论与概率、不生成文本，故它作**判题器**不作**出题器**（出题仍归 [v1.5.8 第四章](./v1.5.8.md) 的出题器；两者混用会让「自己出题」从机制上复活）② **自评禁则**——判定件不得为**自身所属判定面**的进化当裁判（含自身微调、含同判据集上的判据变更），是本行上一项自评禁令在判定面上的展开 ③ **跨面互审**——判定件可验证其他判定面与其他可写面（prompt / memory / skill / 配置）的进化产物，跨面关系须在域验证器登记里**显式声明**；无声明 ⇒ 按自评处理（fail-closed） |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/evolve/src/validator-health.ts` | 新建 | 老化度计算 + 告警 + 复检标记 |
| `engine/evolve/src/__tests__/validator-health.test.ts` | 新建 | 老化度 / 告警阈值 / 复检留痕单测 |

### 验收标准

- [ ] 验证集老化度可计算（消费次数 / 通过率趋势 / 复检时间）
- [ ] 收益归因对照组在位（进化组 vs 对照组差值 = 净贡献；轮换留痕）
- [ ] 考核失败三态可分（真失败 / 环境漂移 / 证据不足——漂移不误杀、挂起告警）
- [ ] 老化告警只提示不阻断（建议冻结自动晋升资格，交人决策）
- [ ] 验证集可人工复检且复检留痕
- [ ] 评估角色与执行模型解耦（自评分数不作改进信号）
- [ ] 三级精判结构在位（规则预筛 → 独立评估模型精判 → 关键样本人审抽检）
- [ ] 判定件登记为 `model-judge` 档的首选验证器，且「能判不能出题」边界在位（出题仍归出题器）
- [ ] 判定件不为自身所属判定面的进化当裁判（跨面关系显式声明，未声明按自评 fail-closed）
- [ ] `npm test` 全绿

---

## 五、FDE Handbook 升格——GUIDE 六段生命周期展开（万物皆可 FDEing 文档重构 · R8 · 2026-09-22 用户拍板）

> **形态归属**：`[非功能]`（文档治理·重构——[v1.5.1 第十二章](./v1.5.1.md#十二断言承载面普查万物皆可-fdeing-文档重构--r1-前置--2026-09-22-用户拍板)升级清单消费；B 步注入的重头章）｜ —
>
> 📋 FDE/GUIDE.md 升格为 **FDE Handbook**（2026-09-22 用户拍板命名——不叫「全书」，对齐 docs/HANDBOOK.md 命名族）：标题升格（`FDE 完全指南` → `FDE Handbook`），**文件名 GUIDE.md 不动**（全仓 20+ 文件引用面零波及）；「万物皆可 FDEing」的六段生命周期（进场前 → 进场时 → 交付时 → 离场时 → 驻留期 → 进化期）展开成方法论主体，[S1M-DECISION-POINTS.md](../../../FDE/S1M-DECISION-POINTS.md) 决策点清单挂接为判定面索引。

### 交付

| 组件 | 能力 |
|------|------|
| **标题与定位升格** | GUIDE.md 一级标题改「FDE Handbook · 万物皆可 FDEing 的打法手册」；头部目录补六段生命周期总览（现有六章结构保留——章内容是方法论主体，六段是生命周期外骨架，两层用映射表衔接不推翻重写） |
| **六段生命周期展开** | 每段补生命周期导引节：该段的判定面（决策点清单引用）、交付物、进出段条件。进场前（§1.10–1.11）/ 进场时（§2–4）/ 交付时（§5.1–5.14）/ 离场时（§5.8/5.10）/ 驻留期（相位关系叙事）/ 进化期（§5.13/5.15）——与 S1M-DECISION-POINTS.md 六段分段完全同构 |
| **决策点清单挂接** | 各章决策密集节（三问判定 §4.1 / 授权递进 §4.1.3 / 离场五能力 §5.10）补「S1M 判定归属」引用框——指向对应决策点表行 |
| **FDE/README.md 同步** | README「深入了解」区 Handbook 措辞同步（完整方法论手册 → FDE Handbook），ClawHub 分发叙事同批评估（skill 描述不自动跟随，独立评估） |

### 验收标准

- [ ] GUIDE.md 标题升格 + 六段生命周期总览在位（映射表衔接既有六章，零内容丢失）
- [ ] 决策点引用框在位（≥3 处，指向 S1M-DECISION-POINTS.md 对应表行）
- [ ] FDE/README.md Handbook 措辞同步
- [ ] 全仓 GUIDE.md 引用零改动（文件名不动，回归断言）
- [ ] 门禁全绿：`check-docs`（FDE/ 不耗 A 预算）/ `check-anchors`

---

## 六、透明仓与敌手自测（合流 · 自探索方向迁入 · 2026-09-22 用户拍板）

> **形态归属**：`[主干]`（判定·审计面红队验证 + 记录公开闸门）｜ L1 关档
>
> 📋 **为什么现在排 + 为什么合流**（2026-09-22 用户拍板 · 版本边界纪律）：两项原为探索方向里的「wild idea」，触发条件都是「第三方首次独立审查」——**外部事件不排期，等它等于把两项一起挂成永久孤儿**（V2.0 起版本域让给 S1M 迭代，无承接窗口）。合流不是合并同类项而是**同一动机的两面**：敌手自测是**自证**（自己打自己），透明仓是**他证**（把过程交出去让别人打）；前者产出的 anti-cases 正是后者要公开的素材——分两版做会让同一条证据链跨版本断掉。
>
> 📋 **两条硬边界**：① **透明仓开仓受闸门**（误报率 <5% 红线未达则**只攒不开**——红线当前未测，本条是门槛不是承诺）；② **公开面不含租户数据与凭证**（只公开治理原始记录：判定、误报、翻案过程），且开仓单向（公开不回撤）故须人工确认双控。

### 交付

| 组件 | 能力 |
|------|------|
| **红队用例集** | 三类攻击面成对用例：篡改 `history.jsonl` / 伪造 HMAC 链 / 绕过 ToolGate——每条用例含「预期被拦」与「拦不住时的检出证据」两态。**hub 时代第四类攻击面（[v1.5.4 第五章](./v1.5.4.md) cordis 兼容层引入后新增）**：**恶意插件**——① 偷取 services 桥暴露的审计/判定能力（未授权调用 @public 面）② 经事件桥给判定输入投毒（构造带诱导性的 tools/result 事件污染下游判据）③ **经 instinct 池灌训练毒数据**（污染在训练时发生比推理时操纵更隐蔽——结晶的"假经验"被 S1M 学走后才发作，检测须回溯到入池审核链）④ 插件 apply 阶段的供应链注入（挂载即写文件/改配置）。每条同样配「预期被拦 / 检出证据」两态 |
| **红队 run 与结论** | 定期跑红队用例并落结论：被打穿 ⇒ 修（进回归清单）；未被穿透 ⇒ 留证（进 anti-cases）——「没被打穿」也要有记录，否则等于没测 |
| **误报率红线判定** | 治理面误报率可测（当前未测 ⇒ 先建读数）——红线 <5% 为**开仓前置**，未达则透明仓保持只攒不开 |
| **透明仓开仓** | `evidence/` 实时公开治理原始记录（含误报与翻案全过程），受红线闸门 + 人工确认双控；不含租户数据与凭证 |
| **hub 插件安全维度并预检** | [v1.5.4](./v1.5.4.md) 四段预检补第五段**安全段**：挂载探针跑第四类攻击面的四个最小用例（能否越权调 services / 投毒事件能否被输入契约层拦 / 假经验能否过入池审核 / apply 是否有挂载外写动作）——**预检结论含安全评级**（通过 / 受限通过〔某能力降级挂载〕/ 拒载），恶意插件在预检段暴露而非运行时 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `docs/evidence/red-team/` | 新建 | 红队用例集与 run 结论 |
| `docs/evidence/anti-cases/README.md` | 修改 | 红队结论接入（已规划目录，接成正式入口） |
| `tools/check/red-team-runner.mjs` | 新建 | 红队用例执行与两态结果判定 |
| `docs/evidence/evidence.md` | 修改 | 误报率读数与 <5% 红线闸门登记 |

### 验收标准

- [ ] 四类攻击面用例齐备（含 hub 时代恶意插件第四类：越权 services / 事件投毒 / 训练毒数据 / 供应链注入），每条带「预期被拦 / 检出证据」两态
- [ ] hub 插件预检第五段（安全段）落地且结论含三态安全评级（通过 / 受限通过 / 拒载）
- [ ] 红队 run 可执行且结论落盘（被打穿进回归清单，未被穿透进 anti-cases）
- [ ] 误报率读数建立且 <5% 红线作为开仓前置在位（未达则不开仓）
- [ ] 透明仓公开面零租户数据 / 零凭证（核对实证）
- [ ] `npm test` 全绿

---

## 七、进化面插件化——evolve 升格 RSI 独立模块（形态收口）

> **形态归属**：`[插件]`（产品形态改挂——判定与审计逻辑零改动，纯挂载位迁移）｜ L1 关档
>
> 📋 **为什么在 v1.5.9 做**：进化面的全部主干能力到本版收齐（一至六章），V2.0 宣告版紧随其后要做三因子归属——**形态收口放在能力收齐之后、身份宣告之前**，一次迁移一次对账；放早了（v1.5.8 前）后续章还要往旧挂载位写，等于迁两次。

### 交付

| 组件 | 能力 |
|------|------|
| **evolve 插件位升格** | 进化面（v1.5.8 准入门/晋级判据/经验池/出题考核 + 本版一至六章）的产品形态从 harness 主干功能改挂 `cordis-plugin-sofagent-evolve`（DSH 七款之一，插件位已存在，**零迁移成本——改的是归属标注与 manifest 声明，不是代码搬移**） |
| **沉淀/进化边界定型** | **沉淀留 harness（数据管道）、进化独立成 RSI 插件（决策管道）**——沉淀（knowledge/ 蒸馏 + think.md 回写 + instinct 池）消费 HMAC 链数据、喂 TrainChannel，是审计→进化飞轮的中间环节，留在主干；进化插件消费沉淀面产出的 verified 数据做准入/晋级/自验证。五能力对外口径（注入/审计/回溯/沉淀/进化）不变——进化仍是五能力之一，只是**运行载体**从主干改插件 |
| **RSI 定位的产品形态落点** | 「RSI 治理与审计层、不做 RSI 引擎」自此有了物理形态：治理件（门禁/台账/验证器）全在插件内可独立拔插——**换掉进化插件 ≠ 失去审计与沉淀**（主干完整性不受插件存废影响）；插件不加载时进化面不生效但审计链不断 |
| **manifest 与计数同步** | 插件 manifest 升格声明 + 计数口径同步（DSH 插件面计数不变——evolve 本就在七款内；变的是插件的能力声明范围） |
| **写面权限窄化：模型只写沉淀面** | 进化插件运行时，模型（被治理的执行 Agent）**只能写 memory 与 skill 两类产物**——策略、注入、路由、状态、审计历史全是**控制面数据，模型永不可写**。这条把「治理面在可写面之外」从架构原则压成插件运行时的路径级硬边界（生态同构先例：自进化插件家族明写「模型只能写 memory 与 skill，其余全是 control-plane」） |
| **策展快照与软删除** | curator（合并近重复 / 归档停用项）每次运行前先对技能树打**快照**；归档移入 `.archive/` 而非物理删除——**技能销毁永远不是硬删**。与 v1.5.7 退役纪律「归档优先于物理删」同族，补的是进化面自己的实例 |

### 验收标准

- [ ] evolve 插件 manifest 声明含 RSI 模块定位（准入门/晋级/经验池/考核/写面审计/能力基线/透明仓）
- [ ] 插件不加载时：审计链、沉淀管道（instinct 池/think.md 回写）行为与今日一致（主干完整性验证）
- [ ] 插件加载时：进化面全部能力可用（准入/晋级/考核/写面审计/敌手自测）
- [ ] ARCHITECTURE 五模块图 evolve 注记更新（运行载体=插件，能力归属不变）
- [ ] `npm test` 全绿

---

## 与后续版本的依赖

| 后续版本 | 依赖 v1.5.9 的什么 |
|------|------|
| v2.0.0 | 配置面契约——加载链字段冻结后方可安全做 DSH 节点交付封装（USB 两档，见 [v1.8.0 §五](../v1.8/v1.8.0.md)） |
| v3.x | 出站治理面随分层模型架构沿用（本地档出站白名单与云端档审批联动） |

> 📋 本版「写面审计与能力版本线」部分（旧 v1.5.16）为审计写面补盲，后续版本不依赖其产物。
