# v1.5.7 开发日志 — 审计模块 · 覆盖面扩展与能力面治理

> 📋 **已排期。** 前置依赖：v1.5.0（治理 KPI 面板——SMB 场景 KPI 复用；技能健康度与工具使用率同源消费）+ v1.3.9（Agentic Browser 多模态链路——仅 UI 层可行性评估引用）+ v1.4.8（plugin featureGates 分档已交付且带验收硬线——可拔契约的插件侧现状）（全外部）
> ⚠️ **尚未实现。** 以下内容为版本目标，不代表当前可用能力（开发排期见 ROADMAP）。
> 状态：已排期 · **2026-09-20 地基期拆版（用户拍板颗粒度细化）：本版承接原 v1.5.4 第一章（SMB 场景审计扩展）、第二章（UI 层审计前置评估）、第三章（OWASP Agentic Top 10 补条）——三章同属审计覆盖面主题，内容零改写** · 原依据链：2026-08-31 用户拍板 OWASP 补条（探索方向表收编，Microsoft AGT 启发）· 2026-09-16 三章自原 v1.5.2 后移本组（按排期准入判据章数上限收敛，用户拍板）
> 状态（续）：已排期 · **2026-09-20 地基期拆版（用户拍板颗粒度细化）：本版承接原 v1.5.4 第四章（能力面使用率治理）与第六章（可拔契约与主干能力清单）——两章是同一件事的两半（哪些能力值得退役 / 退役怎么拔），内容零改写** · 原依据链：2026-09-15 研究收编 Google《Towards a Systems Foundation for Agentic Skills》SkillOps 五维（用户拍板）+ 工具使用率遥测并入 · 2026-09-16 能力面使用率治理自原 v1.5.2 后移 · 2026-09-18 用户拍板落盘可拔契约与主干能力清单 · **本版由旧 v1.5.11、旧 v1.5.12 撤并而来**
> 状态（三续）：已排期 · **2026-09-22 探索方向清仓（用户拍板「逐项裁定、排进 V2.0 前」）：国标 Agent 审计对位（第七章）、决策日志作为审计输入源（第八章）、ACS YAML 策略引擎兼容性评估（第九章）自探索方向迁入本版——改判理由是版本边界纪律（均为 V2.0 前治理面/接口面工作，V2.0 后无承接窗口；ACS 评估另有硬截止：须在 [v1.6.0 第二章](./v1.6.0.md) 判据 schema 定稿前给出结论）**

## 定位


> 🔍 **审计模块 · 覆盖面扩展**——审计从代码开发往外扩：向**场景**扩（SMB 数据处理 / 报表生成——没有代码仓库的企业也要被审计：数值勾稽 / 来源可溯 / 口径一致）与向**证据形态**扩（UI 行为多模态截图，本版只出可行性报告、不实做），顺手按每版 1-2 条节奏补 OWASP ASI 规则走向 10/10 全覆盖。**同版补三处对外/对内接口面**：国家 AI 智能体互联标准的对位评估（标准跟踪与差距表，不预设对齐动作）、**决策日志升为审计输入面的正式一路**（已记 15 类可问责决策、规则判定面此前零消费）、策略表达格式（ACS YAML）的兼容性评估。

> 🔍 **审计模块 · 能力面治理与可拔契约**——先回答「**哪些能力值得退役**」（技能 SkillOps 五维健康度 + 工具使用率遥测；退役候选人工确认制、归档不物理删），再回答「**退役怎么拔**」（主干能力清单 SSOT + `SOFAGENT_*` 功能门控 / 配置参数分流 + 级联校验 `exit 2` + 关档硬线扩主干侧）。本版同时立**形态标注守卫**（`tools/check/check-forms.mjs`）——声明面与守卫同批落，否则「形态改动同批改标注」只是手改荣誉制。

---

## 一、SMB 场景审计扩展（数据处理/报表生成）


> **形态归属**：`[主干]`（判定·数据产物三规则）**+ `[外部]` 成分**（SMB onboarding 模板——数据类：模板 / 配方 / 规则集）｜ L1 关档
>
> 📋 **探索方向表收编**：审计从代码开发扩展到数据处理/报表生成——SMB（中小企业）没有代码仓库，但报表生成/数据清洗同样需要审计（数字对不对/来源可溯/口径一致）。

### 交付

| 组件 | 能力 |
|------|------|
| **数据产物审计规则** | 新审计维度：AI 生成报表/数据产物时审计——数值一致性（合计/明细勾稽）、来源可溯（每个数字可回溯源数据）、口径一致（同名字段同口径） |
| **DecisionKind 扩展** | 新增 DATA_PRODUCT 决策类型（data 产物审计的决策分类）⚠️ `DecisionKind` 属 **@public 契约**——新增成员须同步 `tools/check/public-api-baseline.json`，否则 API 面校验红（对照定义 `engine/audit/src/decision-schema.ts`） |
| **SMB onboarding 模板** | FDE 模板库新增 SMB 场景模板（无代码仓库的纯数据节点部署路径） |
| **治理 KPI 复用** | v1.5.0 治理 KPI 面板对 SMB 场景同样可用（审计覆盖率/问题率照常统计） |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/audit/src/rules/data-product-rules.ts` | 新建 | 数据产物审计规则（勾稽/溯源/口径） |
| `engine/audit/src/decision-log.ts` | 修改 | DecisionKind 加 DATA_PRODUCT（既有 `decision-log.ts` 与 `decision-schema.ts` 均在 audit 包，**不在 core**；单测 `engine/audit/src/decision-log.test.ts` 同步扩） |
| `FDE/templates/smb-onboarding.md` | 新建 | SMB 场景 onboarding 模板 |

### 验收标准

- [ ] AI 生成数据产物触发数据审计（勾稽/溯源/口径三类规则）
- [ ] DATA_PRODUCT 决策类型落 decision-log
- [ ] SMB onboarding 模板可用（无代码仓库部署路径）
- [ ] 治理 KPI 面板覆盖 SMB 场景
- [ ] `npm test` 全绿

---

## 二、UI 层审计前置评估（不实做 · 可行性报告）


> **形态归属**：`[非功能]`（可行性报告·本版不实做）｜ —
>
> 📋 **探索方向表 2026-08-20 新增**：审计从代码 diff 扩展到 UI 行为——Agentic Browser（v1.3.9）截图经多模态分析产出 UI 层审计证据。**实做窗口按版本边界纪律判定**：多模态证据模型扩展的 `v2.x` 承接窗口已关闭（[v2.0.0 第七章](../v2.0/v2.0.0.md)），报告结论即为「不实做」——浏览器四件套随之退役删除（见 [v2.0.0 §七 B 表](../v2.0/v2.0.0.md)）。**本版只做可行性评估报告，不实做。**

### 交付

| 组件 | 能力 |
|------|------|
| **可行性评估报告** | 多模态链路现状（v1.3.9 Agentic Browser + dsh-vision 视觉降级）+ UI 证据入 history.jsonl 的 schema 扩展草案 + 成本测算（截图审计的 token 成本）——落盘 `docs/eval/ui-audit-feasibility.md` |
| **浏览器四件套退役执行（本版收口）** | 可行性报告落盘即触发退役（[v2.0.0 §七 B 表](../v2.0/v2.0.0.md) 预定裁定：退役删除）——四工具移出主线走发版 SOP breaking 收尾：registry 注销 + API.md/五域图/全仓计数落点（105→101）同批 + 断言与门禁锚同批改 + README 能力段去浏览器叙事。**执行清单五件**：① `tool-registry` 四条目注销 ② `docs/API.md` 工具清单与变动史补行 ③ `ARCHITECTURE` 五域图重算（27+19+28+16+15 → 按新分桶）④ check-docs §20 分桶断言与 check-version §26b 逐处对账基线更新 ⑤ CHANGELOG breaking 公告块 |

### 验收标准

- [ ] 可行性评估报告落盘（含 schema 草案 + 成本测算）
- [ ] 浏览器四件套退役执行五件清单逐项勾（registry 注销 / 计数落点 / 五域图重算 / 门禁锚更新 / breaking 公告），全仓 `playwright_` 零残留（`git grep` 实证）
- [ ] 退役后四门禁 + 双语 README 对称全绿（计数 105→101 落点同批）
- [ ] `npm test` 全绿（本版无代码改动，此条为门禁惯例）

---

## 三、OWASP Agentic Top 10 补条（自原 v1.5.2 后移 · 2026-08-31 用户拍板）


> **形态归属**：`[主干]`（判定·ASI 规则补条）｜ L1 关档
>
> 📋 **探索方向表收编**（Microsoft AGT 启发）：当前 4/10（v1.3.7 补 ASI08+ASI10，v1.3.9 补 ASI01+ASI04）——审计场景扩展版顺手补 1-2 条 ASI 规则，按每版补条节奏走向 10/10 全覆盖。具体补哪几条以本版开发时 OWASP Agentic Top 10 与规则引擎现状差距为准（候选：ASI03 提示注入防护 / ASI06 记忆投毒检测——发版前评估定案）。**规则语料带双分类映射**：每条规则（含既有 24+3 条回填）附 OWASP ASI 与 MITRE ATLAS 双编号映射——审计发现须能同时按行业标准与攻击方知识库归档，企业 SIEM 管道按 ATLAS 编号聚合是常态；映射是元数据不是新判定逻辑，回填随规则集修订标识同批。

### 验收标准

- [ ] 补 1-2 条 ASI 规则（24+3 条基线上增量，含测试）
- [ ] 新旧规则全部带 OWASP ASI + MITRE ATLAS 双编号映射（元数据回填，随判据集修订标识同批）
- [ ] ROADMAP 探索方向行更新覆盖率进度
- [ ] `npm test` 全绿

---

## 四、能力面使用率治理（技能 SkillOps 五维 + 工具使用率遥测 · 自原 v1.5.2 后移 · 2026-09-15 / 09-16 用户拍板）


> **形态归属**：`[主干]`（治理·统计与退役结论）｜ L1 关档
>
> 📋 **研究收编**（Google《Towards a Systems Foundation for Agentic Skills》arXiv:2608.29596）：技能库存储借鉴 OS 虚拟内存思想，SkillOps 提出五维技能库健康度治理——**剪除低价值技能、偿还「技能技术债」**。本仓现状：SKILL 库只进不出（instinct→skill 单向晋升），无退役机制——技能熵只增不减。综述同时实证「技能效用相当脆弱」（SkillsInWild 3.4 万技能测量）+「弱模型进化技能对强模型负迁移」（WikiSkill 4B 技能把 Gemini-3.5-Flash 从 50.5% 拉到 18.1%——v1.4.8 modelScope 门控已防）。与 [v1.5.2 第二章](./v1.5.2.md) ruleset_export / [v1.5.3 第二章](./v1.5.3.md) 自测 schema 同属「规则与技能的治理面」主题。
>
> 📋 **并入工具使用率遥测**（用户拍板）：同一治理面从技能扩到工具——MCP 工具面目前**无调用遥测**，静态引用密度只能证明文档完备、不能证明被调用（全量盘点实测：`orchestrator/workflows`、`task/plans` 等子系统零运行时痕迹，但无法据此判断其余工具的冷热）。退役与加码决策需要真实调用数据，故补工具面使用率；**后置本版**的理由是遥测需一段数据积累期才能产出可用结论。

### 交付

| 组件 | 能力 |
|------|------|
| **五维健康度指标** | 按综述 SkillOps 框架落五维（候选：使用频率 / eval 分数趋势 / modelScope 覆盖面 / 依赖健康（引用的工具/接口是否还在）/ 年龄与版本漂移）——`skill-health report` 输出，Dashboard 可见 |
| **低价值技能退役** | 五维连续 N 期低于阈值 → 退役候选清单（进 FDE 人工确认，不自动删——对齐「只提示不阻断」）；退役技能归档不物理删（可回滚） |
| **技能技术债台账** | 每技能记「上次 eval 验证时间 / 当前分数 vs 历史最优 / 待还债清单」——与 v1.4.5 skill-impact 台账同源扩展 |
| **工具使用率遥测** | 每次 MCP tool 调用追加一行到 `~/.sofagent/data/tool-usage.jsonl`（**tool 名 + 时间戳 + 会话标识**，不含调用参数与内容——隐私边界）；**纯本地存储、无任何上报路径**（对齐数据主权铁律「记忆/日志/决策记录永不离开本地」）；遥测默认开、配置可关 |
| **工具使用率盘点** | `sofagent stats --tools` 输出三视图：被调用 / 零调用 / 频次分布——为工具退役与加码提供数据依据（与五维使用频率同源消费） |
| **与 evolve 门控联动** | dream-cycle 晋升面照常，本项补**出口**：晋升进来就有健康度追踪，降级/退役有据 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/evolve/src/skill-health.ts` | 新建 | 五维指标计算 + 退役候选生成 |
| `engine/evolve/src/cli.ts` | 修改 | `skill-health report` 子命令 |
| `engine/evolve/src/__tests__/skill-health.test.ts` | 新建 | 单测（五维计算 / 阈值判定 / 退役候选 / 归档可回滚） |
| `engine/mcp/src/usage-tracker.ts` | 新建 | 调用埋点与 jsonl 追加（本地路径 + 无上报；含关闭开关） |
| `engine/mcp/src/server.ts` | 修改 | tool 调用分派点接 usage-tracker |
| `engine/mcp/src/__tests__/usage-tracker.test.ts` | 新建 | 单测（落盘字段完整性 / 不含内容字段 / 开关生效 / 无网络路径断言） |
| `engine/core/src/cli/stats.ts` | 修改 | `--tools` 三视图输出 |

### 验收标准

- [ ] 五维健康度指标可计算（`skill-health report` 输出）
- [ ] 退役候选清单生成（人工确认制，非自动删）
- [ ] 退役技能归档可回滚
- [ ] 技能技术债台账落盘（上次验证/分数/待还债）
- [ ] tool 调用落 `tool-usage.jsonl`（tool 名 + 时间戳 + 会话标识；**断言无参数/内容字段**）
- [ ] `sofagent stats --tools` 三视图可用（被调用 / 零调用 / 频次分布）
- [ ] 遥测开关生效（关闭后零写入）；代码面无网络上报路径（对齐数据主权铁律）
- [ ] `npm test` 全绿

---

## 五、可拔契约与主干能力清单（能力面可裁剪性声明 · 2026-09-18 用户拍板落盘）


> **形态归属**：`[主干]`（**契约本身即可拔性的定义面**）｜ **不可拔**（关掉它整套可拔声明失去依据）
>
> 📋 **来源**：第四章「能力面使用率治理」只回答「**哪些能力值得退役**」，未回答「**退役怎么拔**」——两者是同一件事的两半，本节补齐后者：把四级可拔机制（L1 关档 / L2 收窄 / L3 拆件 / L4 卸包）从「插件侧已交付」扩到「主干侧有统一声明面」。判据原文见 [ARCHITECTURE · 缰绳面 / 执行面判据](../../ARCHITECTURE.md#缰绳面--执行面判据新功能过筛表)。

### 缺口（2026-09-18 实测）

| 面 | L1 关档 | 现状 |
|------|:--:|------|
| 插件侧 | ✅ | `featureGates` settings 分档已交付且带**验收硬线**——关档后该域工具确实不注册（`plugin-kit` 注册前过滤 + 剔除优先 + 空集响亮告警） |
| 工具面 | ✅ | `SOFAGENT_MCP_ROLES` 收窄专职工具面 |
| **主干内部功能点** | ❌ | **无统一声明面**——83 个 `SOFAGENT_*` 环境变量（含重复别名口径，去重后 82——与 v2.0.0 A 表「82 能力位」同源）中「功能门控」与「配置参数」混装同一命名空间，无从一眼看出哪些是能拔的功能；无级联校验（[ARCHITECTURE](../../ARCHITECTURE.md) 已记两个装配面语义陷阱：写错插件 id 静默、禁用服务提供者必须级联）；无关档硬线 |

### 交付

| 组件 | 能力 |
|------|------|
| **主干能力清单 SSOT** | 新建 `engine/capabilities.json`（复用 `engine/dsh-plugins/plugins.json` 的范式与生成器形态）：每个能力单元声明 `id / 域（注入·审计·回溯·沉淀·进化·执行）/ 载体包 / 开关档位名 + 默认值 / 依赖（拔它须级联拔谁）/ 关档后失效面` |
| **`SOFAGENT_*` 分流** | 82 个环境变量（同上传口径）区分「功能门控」与「配置参数」——门控进清单、参数不进；分流结论落 DEVELOPMENT 说明，与清单双向一致 |
| **级联校验** | 生成期 `exit 2` 断言：① 档位名 / 依赖 id 幽灵名（对齐 `gen-plugin-manifests.mjs` 既有范式）；② 声明某能力可拔但依赖项未标级联 ⇒ 拒绝生成并具名报错 |
| **关档硬线扩主干** | 现有硬线只测插件侧工具注册面；扩一条到主干侧——主干能力关档后「**其判定确实不发生** + 其余能力不受影响 + 响亮告警」 |
| **可拔能力地图** | `list_capabilities` 接上清单——从「工具清单」升级为「**可拔能力地图**」（含档位名与依赖关系） |
| **形态标注守卫（同批）** | 本章既立声明面，须同时立**守卫**——否则 [ROADMAP「形态改动同批改标注，不留悬空」](../../ROADMAP.md#形态归属判据新功能落位过筛)只是手改荣誉制（本轮审计实测：38 章形态归属行中 7 章曾标错或标缺，ROADMAP 另有 2 处计数与实测不符——全部靠人工自查发现，无一条被守卫拦住）。`tools/check/check-forms.mjs`（新建）断言：changelog 每个 `## ` 章下必须紧跟 `> **形态归属**：` 行 · 形态词 ⊆ {`[主干]` `[通道]` `[插件]` `[外部]` `[非功能]` 部署形态} · 可拔级别 ⊆ {L1–L4 · 不可拔 · —}，**结构竖线按括号深度 0 定位**（括号只数全角圆括号——半角是散文标点不参与深度；散文里的全角竖线不算结构）· 标注含「主干」（主形态或成分）者不得配 L3/L4——主干不可拆不可裁（判据只用结构字段，散文提及不算） · 成分须合法且 ≠ 本章主形态 · 「已前移 / 已后移」指针章豁免标注的判据是**章体为纯指针存根**（判定面与豁免面解耦：含标记的标题一律进 A8 校验，标题形态不决定豁免——真章伪装指针会被 A2 直接拦下）且**豁免牌须能兑现**（版本号与章号按**槽位**取——紧跟方向词 / 紧跟版本号，两段号也占槽，槽位为章号时可回退尾注三段号；前移目标文件与章号必须真实存在、后移目标须见于 ROADMAP 或 changelog、禁指针自指本版、尾注散版本号须可兑现）· ROADMAP 版本行唯一（重复会掩盖冲突声明，与首列加粗与否无关）· **双向对账**：ROADMAP 声明的形态计数（主干 / 通道 / 插件 / 非功能 章数 · 通道成分 · 外部成分 · 部署形态）与 changelog 实测逐版相符——声明按**子句头部**取数（行内散文口径不再误判二义；声明链子句内多处计数直接报错，多个声明须用 ＋ 或 ； 分隔为独立子句），**声明计数本身被逐版 pin**（删章后同批改小声明数字 ⇒ pin 拦截），成分提取 A6/A7 **同源单提取器且按结构判据**（DECL 须粗体整体包裹 `**+ [X] 成分**`、SUFFIX 须括注内 `[X] 成分=`——散文提及 token 不算声明），且 changelog 实测出现的形态 / 成分不得超出该版 ROADMAP 声明。违规 / 缺失 / 越界 / 自相矛盾 / 对账不符 ⇒ `exit 1`；**失明 ⇒ `exit 2` 拒绝假绿**（扫描面 <6 文件或需标注章 <30 · 标注行括号不平衡 · 括号深度 0 处全角竖线非唯一）且报文披露未执行的断言。已知边界①–⑩见脚本头部注释（历史档案不扫 · 不维护章标题金表 · 尾注唯一版本号口径已评估否决 · 半角括号不做结构 · 存根伪造机器不可判——内容语义超出语法守卫能力，唯一防线是人工评审看显眼 diff · 槽位章号回退是有意放宽 · A8 绿行前缀是输出契约） |
| **装配面两条陷阱机器化（同批）** | [ARCHITECTURE · 插件裁剪纪律](../../ARCHITECTURE.md#缰绳面--执行面判据新功能过筛表)记的两条语义陷阱目前**只是纪律文字、无机器校验**：① 写错插件 id 静默（你要禁的照常运行）② 禁用服务提供者未级联。级联校验器接管②；① 靠干跑实测断言（裁剪后最终工具集以**实测**为准，不以声明为准）——两条并入同一校验器的断言族 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/capabilities.json` | 新建 | 主干能力清单唯一手写源（手改生成物即被下次生成覆盖） |
| `tools/gen/gen-capability-manifest.mjs` | 新建 | 幽灵名 / 遗漏级联 / 档位归属二义三类断言（对齐 `gen-plugin-manifests.mjs`） |
| `engine/mcp/src/tools/list-capabilities.ts` | 修改 | 返回可拔能力地图（档位名 + 依赖） |
| `engine/*/src/__tests__/capability-gate.test.ts` | 新建 | 关档硬线：关档 → 判定不发生 + 其余能力不受影响 + 告警可见 |
| `tools/check/check-forms.mjs` | 新建 | 形态标注 + 可拔级别封闭枚举断言（存量章标注即首个用例，v1.4.9 及更早不纳入） |

### 验收标准

- [ ] `engine/capabilities.json` 覆盖主干全部能力单元，逐项声明档位名与依赖级联
- [ ] 幽灵名 / 遗漏级联项 ⇒ 生成器 `exit 2`，错误信息具名到条目与档位
- [ ] 主干能力关档后：判定确实不发生 + 其余能力不受影响 + 响亮告警（不得静默）
- [ ] `SOFAGENT_*` 分流结论落文档，且与清单逐项一致
- [ ] `list_capabilities` 返回可拔能力地图（档位名 + 依赖关系）
- [ ] 形态标注守卫：全版本缺标注 / 形态词越界 / 可拔级别缺失 / 主干内容配 L3-L4 / 成分不合法 / 指针目标不可兑现（按槽位判定）/ ROADMAP 版本行重复 ⇒ `exit 1`；失明（扫描面异常 · 括号不平衡 · 结构竖线二义）⇒ `exit 2` 且报文披露未执行断言；ROADMAP 各版本形态计数声明值与 changelog 实测逐版双向对账一致——声明按子句头部取数、成分提取 A6/A7 同源单提取器（空白个数不改变判定）；指针章豁免判据＝章体纯存根（判定面与豁免面解耦，真章伪装指针被 A2 拦截）
- [ ] 装配面两条陷阱有机器校验：插件 id 写错不再静默（干跑实测断言）+ 禁用未级联被拒（生成器断言）
- [ ] `npm test` 全绿

---

## 六、VALIDATION 与 WIKI 生态叙事注入（万物皆可 FDEing 文档重构 · R7 · 2026-09-22 用户拍板）

> **形态归属**：`[非功能]`（文档治理·重构——[v1.5.1 第十二章](./v1.5.1.md#十二断言承载面普查万物皆可-fdeing-文档重构--r1-前置--2026-09-22-用户拍板)升级清单消费）｜ —
>
> 📋 VALIDATION / WIKI 两文档注入三因子口径，补齐文档族群注入的收尾面。**FDEing 注入点（已勘察）**：VALIDATION 既有「从 FDE 到 FDEing」行业印证段（P4 谱系）扩一句动词化口径（行业验证的是「岗位→能力」，动词化 FDEing 是本仓在此之上的概念首创——首创面无第三方印证可引，如实标注）；WIKI 术语表补 FDEing 词条（动词 / 语法纪律 / 首创声明三行）。**时机亲和**：本版第五章（可拔契约与主干能力清单）正建立「能力单元 → 档位 → 依赖级联」的声明面——VALIDATION 的行业对标叙事与 WIKI 的能力地图叙事同批按三因子归位。

### 交付

| 组件 | 能力 |
|------|------|
| **VALIDATION 注入** | docs/VALIDATION.md 行业对标叙事按三因子归位：激活链对标（FDEing 场景层）、判定底座对标（S1M——v1.6.0+ 施工的行业印证章节）、治理对标（harness）；既有锚定纪律零改动（不做代码 grep 验证的行业理念纪律不变） |
| **WIKI 注入** | docs/WIKI.md 能力地图按三域分组重排（对齐 [v1.5.3 第五章](./v1.5.3.md) ARCHITECTURE 三域口径）；导航与索引同批更新 |
| **SKILL 注入** | SKILL/SKILL.md 加载链头注补三因子一句话口径（FDE 打法 / S1M 判定 / harness 治理——Agent 侧自我定位声明） |
| **升级清单销账** | R1 普查清单 VALIDATION / WIKI / SKILL 条目逐条销账——**至此 R1 清单全部销账，B 步渐进注入收尾** |

### 验收标准

- [ ] VALIDATION 行业对标按三因子归位（既有锚定纪律与来源标注零改动）
- [ ] WIKI 能力地图三域分组与 v1.5.3 ARCHITECTURE 三域口径一致（无第二口径）
- [ ] SKILL 头注三因子口径在位
- [ ] R1 升级清单全部销账（C→B 链条闭合的验收锚）
- [ ] 门禁全绿：`check-docs` / `check-anchors` / `check-forms`

---

## 七、国标 Agent 审计对位（标准跟踪与差距表 · 自探索方向迁入 · 2026-09-22 用户拍板）


> **形态归属**：`[非功能]`（标准对位评估·本版不实做对齐动作）｜ —
>
> 📋 **为什么现在排**（2026-09-22 用户拍板 · 版本边界纪律）：本项原为探索方向，触发条件写的是「标准正式发布后评估对齐」。改判理由是**版本边界纪律**——标准发布的节奏不在本仓控制内，若一直等，本项会随 V2.0 版本域收口永久出局。改为**把「跟踪与差距表」这件不依赖标准发布的事先做完**，标准落地后再评估对齐动作（届时是评估结论的消费，不是新机制）。
>
> 📋 **边界**：只做**对位评估与差距登记**——不转述、不分发标准文本，不在标准正式发布前预设对齐动作；结论落档供后续评估消费。

### 交付

| 组件 | 能力 |
|------|------|
| **标准跟踪登记面** | 登记与本仓审计面相关的标准（国家 AI 智能体互联标准草案等）进展状态：草案 / 征求意见 / 正式发布 / 生效日期——状态变更进 decision-log |
| **对位差距表** | 逐条对照本仓已交付审计能力与标准条款：已覆盖 / 部分覆盖 / 未覆盖（差距项须指认落点版本或登记为差距，不给空结论） |
| **阶段结论** | 每条差距给「对齐 / 暂不对齐 + 理由」两态结论（暂不对齐须写明理由，不做静默省略） |
| **国际治理基准对照（生态侧参照系）** | 除国标外同步登记国际开源治理基准作参照：**AgentGovBench**（NIST AI RMF 映射，48 场景八类——身份传播 / 逐用户策略 / 委托溯源 / 范围继承 / 限流级联 / 审计完整性 / fail-closed 纪律 / 跨租户隔离；宣称测"模型周围的治理层"而非模型本身，与本仓定位同题；自带 vanilla 基线 13/48 vs 治理完备 46/48 的落差读数）、**ST-WebAgentBench**（ICLR 2026，**CuP 指标=任务完成×政策合规双轴**，3057 政策实例，三层政策层级 组织>用户>任务——"完成度与合规分列"与判定底座分布内外分列纪律同族）。**边界**：只登记为对照参照系与差距表的补充维度（治理基准场景可反查本仓覆盖面），**不承诺跑分、不预设对齐动作**——是否实跑留待差距表结论后裁定（口径：第三方基准的落差读数是参照不是背书） |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `docs/standards/agent-audit-gap.md` | 新建 | 标准跟踪登记 + 对位差距表（非审计承诺，是差距登记） |
| `SECURITY.md`（仓库根） | 修改 | 引一条指针到差距表（不做内容复制，单 SSOT） |

### 验收标准

- [ ] 标准跟踪登记面与对位差距表落盘（每条差距有「已覆盖 / 部分覆盖 / 未覆盖」结论）
- [ ] 未覆盖项逐条给「对齐 / 暂不对齐 + 理由」（无静默省略）
- [ ] 不转述分发标准文本（门禁或人工核对：差距表只作对位引用）
- [ ] 门禁全绿：`check-docs` / `check-anchors` / `check-forms`

---

## 八、决策日志作为审计输入源（规则面消费 decision-log · 自探索方向迁入 · 2026-09-22 用户拍板）


> **形态归属**：`[主干]`（判定·审计输入面新增一路）｜ L1 关档
>
> 📋 **为什么现在排**（2026-09-22 用户拍板 · 版本边界纪律）：本项原为探索方向，触发条件写的是「审计输入面扩容章实施后评估」。改判理由是**版本边界纪律**——本项属 V2.0 前治理面能力，V2.0 后无承接窗口；而 [v1.5.1 第七章](./v1.5.1.md) 扩容已完成，触发条件早已兑现。
>
> 📋 **为什么不新造采集器**（本仓已有但只被报表消费）：`decision-log.jsonl` 已记 15 类可问责决策（`TOOL_GATE` / `ARTIFACT_EDIT` / `RULE_TOGGLE` / `COVERAGE` / `ESCALATE_REPORT` …），但**只有治理报表在消费、规则判定面零消费**。判定所需的大量事实（放过没放 / 改了什么 / 说与干的覆盖差）本就在里面——把它升为审计输入面的正式一路，比新造采集器便宜一个量级。与 [v1.5.1 第七章](./v1.5.1.md) 同族：那章拓的是**输入面宽度**（调用意图 + 结果文本 + 门禁留痕），本章是把**已有台账**正式接进判定面。

### 交付

| 组件 | 能力 |
|------|------|
| **输入源注册** | decision-log 登记为审计输入面的一路（与 history / 意图流并列），输入源清单落盘可查 |
| **类型映射** | 15 类决策逐类声明「供哪条/哪组规则消费」——无用例的类型显式登记「不消费 + 理由」，不做静默挂空 |
| **只读取用** | decision-log schema 与本版零改动（只读取用）——规则侧消费不改台账落盘形态 |
| **可判命题落地** | 至少一条实测可判命题（如「有动作记录但无放行也无批准 ⇒ 未授权执行」）配正负样例（对齐 [v1.5.3 第二章](./v1.5.3.md) 自测 schema——新规则出生即带样例） |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/audit/src/inputs/decision-log-source.ts` | 新建 | decision-log 输入源适配（只读） |
| `engine/audit/src/rules/` | 修改 | 消费该输入源的规则（含正负样例） |
| `engine/audit/src/__tests__/decision-log-source.test.ts` | 新建 | 单测（类型映射完备 / 只读不改 schema / 可判命题正负样例） |

### 验收标准

- [ ] decision-log 登记为审计输入面一路，输入源清单落盘
- [ ] 15 类决策逐类有「消费 / 不消费 + 理由」结论（无静默挂空）
- [ ] 至少一条可判命题落地且带正负样例
- [ ] decision-log schema 零改动（只读实证）
- [ ] `npm test` 全绿

---

## 九、ACS YAML 策略引擎（策略表达面兼容性评估 · 自探索方向迁入 · 2026-09-22 用户拍板）


> **形态归属**：`[非功能]`（可行性评估·本版不实做引擎）｜ —
>
> 📋 **为什么现在排**（2026-09-22 用户拍板 · 版本边界纪律）：本项原为探索方向，触发条件写的是「需评估兼容性」。改判理由是**评估窗口有硬截止**——[v1.6.0 第二章](./v1.6.0.md) 要把 24 条规则重述为「判据 + 证据」并明确**零格式分叉**，那之后再动表达格式就是分叉。评估结论必须在判据 schema 定稿**之前**给出，v1.5.7 是最后一个不前不后落得住的位置。
>
> 📋 **评估对象**（外部对照，不预设结论）：Microsoft AGT 的 ACS 用 YAML + OPA Rego + Cedar 三引擎承载策略，本仓 ruleset 现为 JSON。本章只回答「**换不换、什么时候换**」，不实做引擎。**边界**：评估结论若为「采纳」，只是把「在判据 schema 定稿前采用 YAML 表达」这一条写进 v1.6.0 的输入，不在本版引入第三套格式。

### 交付

| 组件 | 能力 |
|------|------|
| **表达面对照** | 逐维对照 JSON ruleset 与 YAML + 三引擎方案：人类可读性 / 表达力上限 / 校验与 lint 生态 / 与 [v1.5.3 第二章](./v1.5.3.md) 正负样例 schema 的贴合度 |
| **格式分叉风险** | 显式算出引入成本：是否会与 v1.6.0 判据 schema 形成两套表达（分叉风险量化，不做「都留着」的和稀泥结论） |
| **结论与窗口** | 给两态结论（采纳 / 不采纳 + 理由）与采纳窗口（若采纳，须在 [v1.6.0 第二章](./v1.6.0.md) 定稿前落定） |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `docs/guides/policy-expression-evaluation.md` | 新建 | 表达面兼容性评估报告（对照 + 分叉风险 + 结论） |
| `docs/changelog/v1.6/v1.6.0.md` | 修改 | 仅当结论为「采纳」时：在第二章输入清单登记该结论（不预设） |

### 验收标准

- [ ] 表达面逐维对照落盘（含与正负样例 schema 的贴合度）
- [ ] 格式分叉风险量化并给两态结论（采纳 / 不采纳 + 理由，无和稀泥结论）
- [ ] 若结论为「采纳」，须写明须在 [v1.6.0 第二章](./v1.6.0.md) 定稿前落定
- [ ] 本版未引入第三套策略表达格式（不做引擎实装）
- [ ] 门禁全绿：`check-docs` / `check-anchors` / `check-forms`

---

## 与后续版本的依赖

| 后续版本 | 依赖 v1.5.7 的什么 |
|------|------|
| v2.0.0 | UI 层审计可行性结论——多模态证据模型扩展的 `v2.x` 承接窗口已关闭（[v2.0.0 第七章](../v2.0/v2.0.0.md)），报告结论为「不实做」，浏览器四件套随之退役删除；能力面使用率治理的退役结论——工具面按 [v2.0.0 §七](../v2.0/v2.0.0.md) B 表执行减法（USB 已改判重定位，不在减法面） |
