# v1.5.3 开发日志 — 执行模块 · 路由与验证（模型路由层 + 凭证隔离 Vault + 多实例自验证 + 全节点执行状态机 + tool search 按需加载 · 自 v2.0.0 前移）

> 📋 **已排期。** 前置依赖：v1.3.6（model_register / model_switch / 分级降级梯队 + routeReason 可解释）+ v1.3.5（run_ab_test 多实例基础设施）+ v1.3.7（虚拟 key 凭证边界——Vault 的部分覆盖基础）
> ⚠️ **尚未实现。** 以下内容为版本目标，不代表当前可用能力（开发排期见 ROADMAP）。
> 状态：已排期 · 2026-08-29（自 v2.0.0 拆分前移，用户拍板——三项均**不依赖企业专属精调模型**，无需等 v2.0.0 数据主权窗口）· **2026-08-31 版本号顺延：原 v1.5.4 本版改号 v1.5.3（前序版本号撤销引起顺延，见 [v1.4.5 第七章](../v1.4/v1.4.5.md#七进化模块实证收口自-v151-整版前移--2026-08-31-用户拍板)）** · **2026-09-05 收编：router 网关化拆件两件收进第一章**（敏感度实时分类器——敏感度路由的输入前置件；权重灰度发布 AB——本地权重部署链路的灰度面），用户拍板落盘 · **2026-09-08 三件前移 v1.4.9（用户拍板——一体机数据飞轮整链在 v1.5.0 前成型）：敏感度实时分类器 / 权重灰度发布 AB / session 承接（改写为 router 伴生 exporter 协议——router 基于开源自建，引擎不自研 router 本体）整体迁出，见 [v1.4.9 第七/八/九章](../v1.4/v1.4.9.md)；本版主题收窄为路由与验证（调用侧分流），消费 v1.4.9 承接面产出**

> 🔗 **拆分依据（2026-08-29 拍板）**：v2.0.0 原含四件，其中模型路由层（ROADMAP 明示「路由层可提前到 v2.x 做（不依赖精调模型）」）、Vault（v1.3.7 虚拟 key 已部分覆盖）、多实例自验证（run_ab_test 基础设施现成）三件的依赖全部就绪——前移当时的 v1.5.4（现 v1.5.3），v2.0.0 收窄为数据主权核心（离线 USB 节点合体）。

## 定位

> ⚡ **执行模块 · 路由与验证**——把 v2.0.0 里「不依赖精调模型」的三件提前交付：模型路由层（云端规划/本地执行/管道分层 + 敏感度 fail-closed——敏感度分类输入前置件已前移 v1.4.9 第八章，本版只做调用侧分流消费）+ 凭证隔离 Vault（Agent 代码碰不到 token）+ 多实例自验证（并发小模型交叉表决）+ **全节点执行状态机（SKILL.state 收编，2026-09-03 拍板新增并同日拍板全面推广不试点——全部长任务节点切换执行协议，全局降级开关兜底）**。v2.0.0 届时只剩离线 USB 节点合体一件核心，交付面收窄、发版确定性提高。

---

## 一、模型路由层（Harness 路由 · 云端/本地分流）

> **形态归属**：`[主干]`（判定·分流 + fail-closed + routeReason 留痕）｜ L1 关档
>
> 📋 **ROADMAP 分层模型架构**：云端 32B+ 负责规划推理 → 本地 7B 执行多步 workflow → 本地 0.5B 跑管道层。**引擎层只做模型路由（model-router.ts 已有四档插槽），精调 pipeline 属模型层非开源范围。路由层不依赖精调模型——对接现有云端 API + 本地推理服务（Ollama 等）即可交付。**

### 交付

| 组件 | 能力 |
|------|------|
  | **路由策略引擎** | 任务分级路由：规划类→云端大模型 / 执行类→本地 7B / 管道类（模板/格式/字段提取）→本地 0.5B——按任务类型+数据敏感度双维决策 |
| **敏感度路由** | 数据分级（公开/内部/敏感）自动决定可否走云端——敏感数据强制本地（fail-closed）；**分类输入前置件已前移 [v1.4.9 第八章](../v1.4/v1.4.9.md)（2026-09-08 拍板）**，本版消费分类结果做调用侧分流 |
| **路由可解释** | 每次路由决策附 routeReason（复用 v1.3.6 EndpointProfile + route-policy 模式；v1.4.9 session 承接的路由决策回流经本层全链路可查） |
| **降级梯队** | 云端不可用→本地承接（复用 v1.3.6 分级降级梯队） |
| **本地端点注册** | model-registry 注册本地推理服务端点（Ollama 兼容协议），云端/本地统一注册面 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
  | `engine/orchestrator/src/router/policy-engine.ts` | 新建 | 任务×敏感度双维路由决策 |
  | `engine/orchestrator/src/model-registry.ts` | 修改 | 本地模型端点注册（7B/0.5B 档位） |

### 验收标准

- [ ] 三级任务路由可用（规划→云/执行→本地7B/管道→本地0.5B）
- [ ] 敏感数据强制本地路由（fail-closed，配置错误时拒绝执行）
- [ ] 每次路由附 routeReason
- [ ] 云端不可用自动降级本地
- [ ] `npm test` 全绿

---

## 二、凭证隔离 Vault（OMA 启发）

> **形态归属**：`[通道]`（**执行层安全基础设施**——凭证托管/轮换/吊销是通用轮子，换 HashiCorp Vault 价值不受损；**「Agent 代码碰不到 token」的拦截约束留主干**）｜ L1 关档
>
> 📋 **探索方向表收编**：沙箱发 HTTP 请求时动态注入凭证、Agent 代码碰不到 token——执行层安全基础设施，v1.3.7 虚拟 key 凭证边界已部分覆盖，完整 Vault 前移本版。

### 交付

| 组件 | 能力 |
|------|------|
| **Vault 服务** | 凭证托管：Agent 运行时凭证不出 Vault（动态注入请求头，Agent 代码/日志/审计面均见不到 token 明文） |
| **轮换与吊销** | 凭证可轮换（定时/事件触发）+ 单凭证吊销（泄露响应） |

### 验收标准

- [ ] Agent 沙箱 HTTP 请求经 Vault 动态注入凭证，代码层拿不到 token
- [ ] 凭证轮换/吊销可用
- [ ] `npm test` 全绿

---

## 三、多实例自验证（并发小模型交叉表决）

> **形态归属**：`[主干]`（判定·多数表决 + 分歧路由 HITL）｜ L1 关档
>
> 📋 **探索方向表 2026-08-24 新增**（方向参考不引数字）：同一任务并发跑多个小模型实例 + 交叉验证多数表决，替代单次大模型调用。方向上与 AB 测试基础设施（v1.3.5 run_ab_test）可复用。

### 交付

| 组件 | 能力 |
|------|------|
| **表决编排** | 同任务 N 实例并发（复用 run_ab_test 多实例基础设施）+ 结果交叉比对 + 多数表决产出 |
| **分歧路由** | 实例分歧超阈值 → 路由人工介入队列（HITL 复用）或升级大模型重跑 |

### 验收标准

- [ ] N 实例并发 + 多数表决可用
- [ ] 分歧超阈值路由 HITL
- [ ] `npm test` 全绿

---

## 四、全节点执行状态机（SKILL.state 机制收编 · arXiv:2608.26263 · EMNLP · 2026-09-03 用户拍板：全面推广不试点）

> SKILL.state 把技能执行从聊天记录改成状态机：每步模型只收 P（不可变技能说明）+ Σt（结构化状态 JSON）+ ot（最新观察），输出推理轨迹 Rt / 状态补丁 ΔΣt / 动作 at；运行时确定性校验合并 Σt+1 = Σt ⊕ ΔΣt 后**推理轨迹永久丢弃**——单步 prompt O(1)、累计 token O(T)。论文实测：16.2 倍 token 压缩（1,062,387→65,408）、抗噪 ≥0.97（ReAct 崩到 0.53）、状态恢复零步数、InterCode CTF 54.2%（+7.8 且省 60.4% token）；**预算对齐控制实验实锤赢在结构化不在压缩**（同预算下：滑窗截断 0.18 / LLMLingua 0.22 / SKILL.state 0.94）。
>
> **sofagent 的增量空间在节点内部**：编排层（LoopGraphState + artifacts 浅合并 reducer）已是 sofagent 版的 Σt⊕ΔΣt，checkpoint 已支持状态恢复；但 `node-executor.ts` 用 createReactAgent，节点内部是消息历史式滚雪球（无 trimMessages），长任务 token O(T²) 与上下文中毒风险与论文基线相同。
>
> **sofagent 天生的落地底气**：wrapToolCall 运行时审计在每步动作发生时实时落记录——推理轨迹可弃而行为仍可溯，**轨迹可弃、行为可溯**，这是论文没有、sofagent 独有的审计前提。
>
> **全面推广判据（2026-09-03 用户拍板，不做单点试点）**：① 风险已被 sofagent 自己消解——wrapToolCall 审计通道在位，轨迹丢弃的审计风险天然兜底，无需靠试点验证安全性；② 论文已在三个基准族（仓库管理 SkillExecBench/InterCode CTF/τ-Bench）跨域验证协议通用性，「先试一个域再推广」的验证价值已被论文做完；③ 兜底不靠试点靠**全局降级开关**——所有节点默认带降级路径，任何节点出问题单独回退消息历史式，不影响其他节点，等效于每节点自带「试点失败保险」，且比先试一点的方案更快拿到全量收益。

>
> **形态归属**：`[主干]`（账本·确定性合并）｜ **不可拔**（状态不可复现则审计无锚，属命脉件）
### 交付

**执行协议规范（SKILL.state 采纳形态——先定协议，再写代码）**

| 协议要素 | sofagent 落地形态 |
|------|------|
| **P（技能说明，不可变）** | 现有 system prompt / SKILL.md 注入内容，执行期只读恒定 |
| **Σt（执行状态 JSON）** | 每个节点类型定义结构化状态（见下方基础 schema + 领域扩展），随执行逐步演化，是唯一跨步携带的记忆 |
| **ot（最新观察）** | 上一步工具返回（截断至 token 预算内），只看最新一条，历史观察不进 prompt |
| **ΔΣt（状态补丁）** | 模型每步随动作输出 JSON Patch 子集（`{op: add/remove/update, path, value}`），**由运行时代码执行合并** Σt+1 = Σt ⊕ ΔΣt，schema 校验不过即拒绝该补丁——状态合并不靠模型自觉，靠代码保证 |
| **Rt（推理轨迹，即弃）** | 步内 CoT 完整保留供本步推理，状态转移验证通过后**不进下一步 prompt**；丢弃前先落审计摘要（动作 + ΔΣt + 因果边）进 wrapToolCall 通道 |
| **降级路径（全局开关）** | 每节点带 `executionMode: stateful | legacy` 配置，补丁连续校验失败 N 次自动降级 `legacy` 并审计告警；环境变量 `SOFAGENT_STATEFUL_EXEC=off` 一键全回退——单节点故障不扩散，协议层 bug 不卡死任何任务 |

**基础状态 schema（全节点通用底座，六字段）**：

```json
{
  "goal": "本节点任务目标",
  "done": ["已完成步骤（带结果摘要）"],
  "todo": ["待办步骤"],
  "facts": ["工具观察到的关键事实（不含原始输出）"],
  "files": ["已改动文件路径"],
  "blockers": ["阻塞项与绕行决策"]
}
```

**节点类型 schema 扩展表**（基础六字段 + 各节点领域字段）：

| 节点 | 领域扩展字段 | 说明 |
|------|------|------|
| engineer | `tests: {pass, fail, skipped}`、`reviewComments: [{file, line, status}]` | 代码修复循环——测试状态与审查意见回链 |
| checker | `checkRuns: [{rule, verdict, evidence}]`、`falsePositives: []` | 审查循环——规则判定留状态不留原始日志 |
| reviewer | `issuesBySeverity: {p0, p1, p2}`、`coveredFiles: []` | 审查循环——覆盖度与问题分级实时账 |
| refine-agent | `qualityScores: [{round, score}]`、`triedStrategies: []` | 优化循环——已试策略防重复（对齐 failure-ledger 精神） |
| 通用长任务 | 基础六字段即可 | 文档批处理/数据处理等默认形态 |

**推广范围**：`node-executor.ts` 全部 createReactAgent 节点（engineer/checker/reviewer/parallel_wave 单元）+ refine-agent 优化循环 + loop-agent driver——**所有长任务节点一次性切换到执行协议**，短平快节点（单次工具调用）不改造（改造收益低于成本）。

| 组件 | 能力 |
|------|------|
| **执行状态机执行器** | `node-executor.ts` 改造为协议执行器：每步产出 ΔΣt，代码确定性合并，下一步 prompt 只带 P+Σt+ot，推理轨迹合并后即弃；`executionMode` 双模式在位（stateful 默认 / legacy 保底） |
| **审计闸门前置** | Rt 丢弃前落一条审计摘要（动作 + 状态补丁 + 因果边）——wrapToolCall 通道接线，原则「轨迹可弃、行为可溯」写进 DEVELOPMENT.md 执行层章节 |
| **节点 schema 注册表** | 节点类型 → schema 映射注册表（基础六字段 + 领域扩展），新节点类型注册即用——schema 是领域资产沉淀进 SKILL/ 设计文档，非任务级配置 |
| **全局度量** | 每节点 token 曲线（对齐论文 O(1) 平线）/ 轮次稳定性 / 结果质量对比（vs legacy 模式）落 `data/evolution/stateful-metrics.jsonl`，Dashboard 可见 |

### 涉及文件（预估）

`engine/orchestrator/src/node-executor.ts`（协议执行器改造）/ `engine/orchestrator/src/refine-agent/`（优化循环接协议）/ `engine/orchestrator/src/loop-agent/driver.ts`（driver 接协议）/ `engine/orchestrator/src/middleware/`（审计摘要接线）/ `engine/orchestrator/src/execution-state/`（schema 注册表新建）/ `SKILL/`（schema 设计文档）/ `docs/DEVELOPMENT.md`（执行层章节——「轨迹可弃、行为可溯」原则 + SKILL.state 蒸馏小节）。

### 验收标准

- [ ] 执行协议六要素落地（P 恒定/Σt 唯一记忆/ot 仅最新/ΔΣt 代码合并校验/Rt 即弃/降级开关）
- [ ] 全部长任务节点运行在执行协议上（engineer/checker/reviewer/parallel_wave/refine-agent/loop-driver），单次工具调用节点不改造
- [ ] 节点 schema 注册表在位（基础六字段 + 四类领域扩展）
- [ ] 审计摘要先于轨迹丢弃落盘（动作+状态补丁可溯）
- [ ] `executionMode` 降级路径可用：单节点自动降级 + `SOFAGENT_STATEFUL_EXEC=off` 全局一键回退
- [ ] 全局度量落盘：每节点 token 曲线 + 稳定性 + 质量对比（stateful vs legacy）
- [ ] 五文档挂点同步：DEVELOPMENT.md 补 SKILL.state 蒸馏小节（与九B WikiSkill 对称）+ PHILOSOPHY §五自进化谱系挂「执行协议层」+ ARCHITECTURE 数据流图补「节点内部状态机」
- [ ] `npm test` 全绿

---

## 五、Tool search 按需加载（OpenAI Agents API 启发 · 2026-09-15 用户拍板排期本版）

> **形态归属**：`[主干]`（注入面·工具选择即信任边界）｜ L1 关档（回落全量注入）
>
> 📋 **研究收编**（[OpenAI Agents API](https://openai.com/index/introducing-the-agents-api/) 公测，2026-09-10）：工具定义不全量注入——Agent 按任务需要检索并加载相关工具 schema，官方收益「reduce token usage and cost while preserving the model's cache」。本仓 TOOLS=104 已进适用区间（loop-development 已有判断：「工具数上百时瓶颈不在推理而在工具描述歧义」）；v1.4.8 plugin-gate/ToolGate 管的是「能不能用」（权限面），本章管「要不要装进上下文」（供给面）——两者正交。与第一章模型路由层同属**调用侧上下文供给**：路由决定「用哪个模型」，tool search 决定「带哪些工具」。

### 交付

| 组件 | 能力 |
|------|------|
| **工具检索索引** | 工具注册表（tool-registry）之上建语义检索索引（名称/描述/标签），按当前任务描述召回 top-K 候选工具 |
| **节点域注入** | 按 workflow 节点类型注入工具子集（复用业务图谱节点级权限模型——节点绑定工具域，域外工具不进上下文）；手动指定工具可显式覆盖检索结果 |
| **分步按需加载** | 单节点执行中途可追加加载工具（模型第一步发现缺工具 → 检索加载 → 继续执行，对齐 Agents API tool search 的任务中检索语义） |
| **预算与命中率度量** | 检索命中被实际调用的比率 + 工具注入 token 曲线（对比全量注入）落 metrics——为「按需加载是否值得」提供实证数据 |
| **静态重叠检测联动** | v1.1.0 规划的静态重叠检测（防工具描述歧义）与本章共用索引底座——先去重后检索 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/tools/tool-search.ts` | 新建 | 检索索引 + top-K 召回 + 节点域过滤 |
| `engine/orchestrator/src/loop/`（graph/executor 相关） | 修改 | 工具注入点改造：全量注入 → 按需注入（节点执行前按域过滤，运行中支持追加） |
| `engine/orchestrator/src/__tests__/tool-search.test.ts` | 新建 | 单测（召回相关性/域过滤正确性/追加加载/预算度量） |

### 验收标准

- [ ] 任务描述可检索出相关工具子集（top-K 召回，相关性有测试样例）
- [ ] 节点域过滤生效：域外工具不进上下文
- [ ] 执行中可追加加载工具（分步按需）
- [ ] 工具注入 token 对比度量落盘（按需 vs 全量）
- [ ] v1.4.8 ToolGate 权限面不受影响（权限判定先于检索）
- [ ] `npm test` 全绿

> 📋 **关联评估项（不排期，随本版评估）**：mid-turn steering（运行中插话纠偏）——Agents API Session 级能力（任务运行中插入新指令转向）；本仓 FileCheckpointer + resumeLoopGraph 已覆盖断点恢复，缺「运行中」实时纠偏。fresh-eyes 单轮 20-40 分钟场景价值明确，依赖事件总线（v1.5.1）派发通道，本版只出评估结论不实做。

---

## 六、CLI 单入口收敛（13 个 bin → `sofagent <域> <动作>` · 2026-09-16 用户拍板排期本版）

> **形态归属**：`[非功能]`（重构·13 bin → 1 + 旧命令 shim 化；shim 退役部分属可拔契约执行面）｜ —
>
> 📋 **来源**：探索方向表「CLI 单入口收敛（叙事收敛批）」——实测当前 **13 个 bin 条目**（12 个 `sofagent-*` + 1 个裸名 `sofagent` 主入口，实测口径 = 根与各 `engine/*/package.json` 的 `bin` 字段并集），对用户是 13 个记忆点，新用户第一步摩擦有一半来自「我该用哪个命令」。收口为 `sofagent <域> <动作>` 单入口（`sofagent audit` / `sofagent train` / `sofagent ontology` …）。
>
> **现状与工作量**：裸名 `sofagent` 主入口**已在位**——本章补的是**子命令路由**（域分发到既有实现）与**旧命令 shim 化**，不是从零造入口。
>
> **边界**：旧命令保留**兼容期**（shim 转发 + 弃用提示），不 breaking；bin 面收口与文档面收敛是同一件事。与 v1.5.0 第九章命名债务清扫同属「对外认知面收敛」。

### 交付

| 组件 | 能力 |
|------|------|
| **单入口子命令路由** | `sofagent <域> <动作>` 域分发（audit / train / ontology / evolve / daemon / device / workflow …）路由到既有实现，行为与旧命令一致 |
| **旧命令 shim** | 12 个 `sofagent-*` 改为转发 shim（打印弃用提示 + 指向新命令），兼容期一个 minor 版本 |
| **文档面收敛** | 全部文档的命令示例改新入口；旧命令仅在「兼容」小节出现 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/core/src/cli/sofagent.ts` | 修改 | 补域路由（主入口已在位） |
| 各包 `bin/sofagent-*.js` | 修改 | 改为转发 shim + 弃用提示 |
| `docs/**` + `SKILL/**` | 修改 | 命令示例全量收敛 |

### 验收标准

- [ ] `sofagent <域> <动作>` 全域可用（与旧命令行为逐条一致）
- [ ] 旧命令 shim 转发正确 + 弃用提示（兼容期不 breaking）
- [ ] 文档命令示例全量收敛（旧命令仅在兼容小节出现）
- [ ] 门禁断言同步：CLI 签名 grep（`playbook/acceptance-test.sh`）与相关断言同批改
- [ ] `npm test` 全绿

## 七、本地槽位排队与判定分层（一体机在线服务面 · 2026-09-18 用户拍板独立成章）

> **形态归属**：`[主干]`（判定·槽位/分层/决策下达）｜ L1 关档
>
> **形态归属**：`[主干]`（判定·槽位/分层/决策下达）｜ L1 关档
>
> 📋 **来源**：排期准入判据 **W1 具名消费方**（企业内网私有化部署的一体机形态——模型全部跑在客户自有硬件上，本地推理槽位是稀缺资源，真实客户诉求）+ **W2 链路缺口**（一体机**在线服务链路**缺这一环；数据飞轮链路 session→分拣→脱敏→数据集→训练→灰度已在 v1.4.9 整链成型）。
>
> **与第一章的关系**：第一章「模型路由层」解决**往哪走**（云端/本地/管道分流决策）；本章解决**去不了怎么办**（本地并发槽位满了如何排队与升级）与**怎么判**（判定链如何不占本地推理槽）。两章同属一条决策链的相邻段，分章为保交付面清晰。
>
> **与 v1.4.9 第七章的关系（双向闭环）**：v1.4.9 已交付 router 伴生 exporter——**数据承接面**（router → 引擎：过站 session / usage / 路由决策推送 + HMAC 验签 + 幂等）。本章补**决策下达面**（引擎 → router）——引擎判定出的去向决策下达给 router 执行，两向合起才是完整回路。
>
> **纪律承继（不自研 router 本体）**：v1.4.9 已拍板「router 基于开源自建、引擎不自研 router 本体、key 发放/吊销/管理归 router」——本章**不碰转发实现、不碰 key 生命周期**，只交付决策产出、槽位管理与决策下达协议。

### 交付

| 组件 | 能力 |
|------|------|
| **本地并发槽位信号量** | 本地推理槽位上限可配（一体机默认 3–5）；超限请求排队（FIFO + 优先级），前台可见排队位置与预估等待；**排队是治理策略不是模型能力**——vLLM 双实例无全局视图，router 无 GPU 槽位概念，引擎是唯一看得到全局的层 |
| **排队超时升级** | 排队等待达阈值 → 按合规路由决策**自动提议转云端**（合规允许出门时）或继续等待（信创全封场景）；升级动作留痕 routeReason，可查可举证 |
| **判定分层编排（L0/L1/L2）** | **默认不用本地主模型做判定**（占槽 / 循环依赖 / 判定延迟放大 / 判错路由错下游放大四问题）：**L0 声明式映射**——请求来自已定义 workflow 的 Agent 节点，直接查节点级 modelPreference 映射（复用 v1.4.8 已交付 schema，零模型成本）→ **L1 语义分类**——自由文本走 embedding 相似度分类（复用本地 RAG 的 embedding 模型，零新增组件，不占本地推理槽）→ **L2 难例兜底**——置信度低于阈值时按配置分流（判定特征走云端强 API / 信创场景本地低峰复核） |
| **L1 判定通道接口（DecisionChannel）** | L1 语义分类的**可插拔判定源协议**（研究收编：System One 决策模型 Jev 启发，2026-09-19 拍板）：`judge(state, questions) → typed answers + 概率` 标准接口，Noul/Choice/Score 三问题原语语义对齐。**通道而非依赖**——引擎定义接口与编排，实现由用户按 TrainChannel 同款纪律自带（云端 provider 走用户 key opt-in，默认关闭；本地部署走开源并行约束解码方案——KV cache 广播 + 单次前向出全部字段，开源 RLCD 小权重可跑一体机，数据主权不破）。判定结果带概率落 decision-log 留痕（类型化输出天然可审计）。**边界**：决策模型不进信任地基（其可被 state 内容操纵的失灵面已知）——只坐确定性规则之后的语义兜底层，24 条审计规则判定面零改动 |
| **决策下达面** | 引擎产出去向决策（目标模型 / 降级路径 / 槽位归属）按标准 schema 下达 router 执行（与 v1.4.9 exporter 同族 schema，反向）；schema 校验 fail-closed，坏格式拒绝下达而非静默放行 |
| **槽位与排队可观测** | MCP tool 查询槽位占用 / 排队深度 / 各请求等待时长（运维可见，数据本地不出门） |
| **三层队列边界** | 明确不重叠的分工：router 层管 per-key 限速与云侧 cooldown（防单 key 打爆配额）· **引擎层管本地 GPU 槽位**（本章核心）· vLLM 实例层管实例内 continuous batching 微调度 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/router/slot-manager.ts` | 新建 | 本地槽位信号量 + 排队 + 优先级 + 超时升级判定 |
| `engine/orchestrator/src/router/intent-triage.ts` | 新建 | 判定分层编排（L0 映射 → L1 embedding → L2 兜底） |
| `engine/orchestrator/src/router/policy-engine.ts` | 修改 | 第一章新建件——决策产出对接槽位管理与下达面 |
| `engine/orchestrator/src/router/decision-dispatch.ts` | 新建 | 决策下达协议（schema 校验 fail-closed + HMAC 挂链） |
| `engine/train/src/router-exporter.ts` | 修改 | 与承接面 schema 同族对齐（双向 schema 一致性校验） |
| `engine/mcp/src/tools/router-slots.ts` | 新建 | 槽位/排队状态查询 tool |
| `engine/orchestrator/src/__tests__/router-slot.test.ts` | 新建 | 单测（槽位上限/排队顺序/优先级/超时升级/判定链不调主模型） |

### 验收标准

- [ ] 本地槽位上限可配（一体机默认 3–5），超限排队，前台可见排队位置与预估等待
- [ ] 排队超时达阈值按合规决策升级云端（或在全封场景继续等待），升级留痕 routeReason
- [ ] 判定三层链可用：L0 命中零模型调用 / L1 embedding 分类不占本地推理槽 / L2 难例按配置兜底
- [ ] DecisionChannel 接口在位：`judge(state, questions)` 三原语语义可插拔替换实现，云端 provider 默认关闭 opt-in，判定结果带概率落 decision-log
- [ ] L1 语义分类器的置信度阈值有校准依据（声称 X% 准确的阈值须有实测验证支撑，不接受拍脑袋数字——RLCD 校准判据）
- [ ] **判定链不调用本地主模型**（行为锁：判定期间无本地主模型推理请求）
- [ ] 决策下达 schema 校验 fail-closed（坏格式拒绝下达，不静默放行）
- [ ] 与 v1.4.9 承接面构成双向闭环（下达的决策在后续承接过站数据中可对账）
- [ ] 槽位与排队状态可观测（MCP tool 查询）
- [ ] 门禁断言同步：`playbook/acceptance-test.sh` 新增槽位/判定链行为锁场景
- [ ] `npm test` 全绿

## 与后续版本的依赖

| 后续版本 | 依赖 v1.5.3 的什么 |
|------|------|
| v2.0.0 | 模型路由层（离线 USB 节点直接复用路由底座——本地权重接入路由插槽）；Vault（USB 离线场景的凭证托管）；多实例表决（离线节点的轻量验证通道）；执行状态机（离线节点的长任务执行层——token 效率在离线算力受限场景更重要） |

> 📋 **前移收编说明（2026-09-08）**：原第一章「router 网关化拆件」三件（敏感度实时分类器 / 权重灰度发布 AB / session 全量记录与蒸馏对接）已整体前移 [v1.4.9 第七/八/九章](../v1.4/v1.4.9.md)——一体机数据飞轮（session 承接→分拣→脱敏→数据集→训练→灰度上线）在 v1.5.0 前成型，用户拍板。本版保留模型路由层（调用侧分流消费 v1.4.9 分类结果）/ Vault / 多实例表决 / 执行状态机四件。
