# v1.5.6 开发日志 — 存量收敛（单入口 + 数据面）

> 📋 **已排期。** 前置依赖：v1.5.2（第二章独立验签器——归档段独立锚点指纹与主链校验同族语义）
> ⚠️ **尚未实现。** 以下内容为版本目标，不代表当前可用能力（开发排期见 ROADMAP）。
> 状态：已排期 · **2026-09-20 地基期拆版（用户拍板颗粒度细化）：本版承接原 v1.5.3 第六章（CLI 单入口收敛），内容零改写；非功能重构独立成版（影响面覆盖全部文档命令示例与门禁断言）** · 原依据链：2026-09-16 用户拍板排期本版（探索方向表「CLI 单入口收敛（叙事收敛批）」）
> 状态（续）：已排期 · **2026-09-20 地基期拆版（用户拍板颗粒度细化）：本版承接原 v1.5.4 第五章（数据生命周期治理），内容零改写；独立成版（增长面收敛是运维面单件，不与能力治理混版）** · 原依据链：2026-09-16 用户拍板排期（运行时数据实测发现 `~/.sofagent/data` 332M 只增不减，三处结构性问题） · **本版由旧 v1.5.10、旧 v1.5.13 撤并而来**

## 定位


> 🧭 **执行模块 · CLI 单入口收敛**——13 个 bin 条目对用户是 13 个记忆点，新用户第一步摩擦有一半来自「我该用哪个命令」。收口为 `sofagent <域> <动作>` 单入口 + 旧命令 shim 化（兼容期一个 minor 版本，不 breaking），文档面同批收敛。与 [v1.5.0 命名债务清扫](./v1.5.0.md)同属「对外认知面收敛」。

> 🔍 **审计模块 · 数据生命周期治理**——运行时数据增长面收敛：memory 事实二级分层 + 归档轮转、审计链历史段归档（锚点重算，**归档 ≠ 删除**——HMAC 链只追加不删改是铁律，归档 = 移走历史段 + 锚点重算）、遗留备份按策略接管、doctor 报 data 目录健康项。

---

## 一、CLI 单入口收敛（13 个 bin → `sofagent <域> <动作>` · 2026-09-16 用户拍板排期本版）


> **形态归属**：`[非功能]`（重构·13 bin → 1 + 旧命令 shim 化；shim 退役部分属可拔契约执行面）｜ —
>
> 📋 **来源**：探索方向表「CLI 单入口收敛（叙事收敛批）」——实测当前 **13 个 bin 条目**（12 个 `sofagent-*` + 1 个裸名 `sofagent` 主入口，实测口径 = 根与各 `engine/*/package.json` 的 `bin` 字段并集），对用户是 13 个记忆点，新用户第一步摩擦有一半来自「我该用哪个命令」。收口为 `sofagent <域> <动作>` 单入口（`sofagent audit` / `sofagent train` / `sofagent ontology` …）。
>
> **现状与工作量**：裸名 `sofagent` 主入口**已在位**——本章补的是**子命令路由**（域分发到既有实现）与**旧命令 shim 化**，不是从零造入口。
>
> **边界**：旧命令保留**兼容期**（shim 转发 + 弃用提示），不 breaking；bin 面收口与文档面收敛是同一件事。与 v1.5.0 第九章命名债务清扫同属「对外认知面收敛」。

### 交付

| 组件 | 能力 |
|------|------|
| **单入口子命令路由** | `sofagent <域> <动作>` 域分发（audit / train / ontology / evolve / daemon / device / workflow …）路由到既有实现，行为与旧命令一致 |
| **旧命令 shim** | 12 个 `sofagent-*` 改为转发 shim（打印弃用提示 + 指向新命令），兼容期一个 minor 版本 |
| **文档面收敛** | 全部文档的命令示例改新入口；旧命令仅在「兼容」小节出现 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/core/src/cli/sofagent.ts` | 修改 | 补域路由（主入口已在位） |
| 各包 `bin/sofagent-*.js` | 修改 | 改为转发 shim + 弃用提示 |
| `docs/**` + `SKILL/**` | 修改 | 命令示例全量收敛 |

### 验收标准

- [ ] `sofagent <域> <动作>` 全域可用（与旧命令行为逐条一致）
- [ ] 旧命令 shim 转发正确 + 弃用提示（兼容期不 breaking）
- [ ] 文档命令示例全量收敛（旧命令仅在兼容小节出现）
- [ ] 门禁断言同步：CLI 签名 grep（`playbook/acceptance-test.sh`）与相关断言同批改
- [ ] `npm test` 全绿

---

## 二、数据生命周期治理（~/.sofagent/data 增长面收敛 · 2026-09-16 用户拍板排期本版）


> **形态归属**：`[主干]`（归档锚点重算 + decision-log 留痕 + doctor 判定）**+ `[通道]` 成分**（归档轮转与遗留文件清理的**执行动作**）｜ L1 关档
>
> 📋 **运行时数据实测发现**（2026-09-16 深挖）：`~/.sofagent/data` 332M 只增不减，三处结构性问题——① `memory/audit` 17178 个事实文件 67M 平铺单目录（dream-cycle `extract-facts` 按 key 前缀分桶后 bucket 内无二级分层、无归档、无轮转，已达文件系统性能退化区间）；② `audit/` 目录三个遗留备份文件 49M（`history.jsonl.bak×2` + `broken-*`）无生命周期策略接管；③ 审计链文件（history 52M / decision-log 56M）永久追加无归档机制。**边界**：只治理数据文件生命周期，不动链结构本身（HMAC 链只追加不删改是铁律——归档=移走历史段+锚点重算，非删除）。

### 交付

| 组件 | 能力 |
|------|------|
| **memory 事实二级分层** | bucket 内按日期（或哈希前缀）二级分目录，索引文件统一寻址——兼容现有 key 读取（读侧按索引查，不依赖目录遍历） |
| **memory 归档与轮转** | 事实级记忆按 updatedAt 冷热分层：N 天未更新移归档目录；归档不进常规检索（显式 `--archive` 才查）——对齐「记忆遗忘机制」 |
| **审计链归档机制** | history.jsonl / decision-log.jsonl 支持历史段归档：超阈值（如 50MB）把头部段移入 `archive/` 子目录 + 重算链锚点（锚点链头指向新末尾，归档段带独立锚点指纹可独立校验——与 [v1.5.2 第二章](./v1.5.2.md) 独立验签器语义衔接） |
| **遗留备份接管** | `*.bak-*` / `*.broken-*` 文件按时间上限清理（默认保留 30 天），清理动作进 decision-log 留痕 |
| **doctor 检查项** | 新增 data 目录健康检查：单目录文件数超阈值（如 1 万）黄色告警 + 总量趋势提示 |
| **沉淀记忆的项目作用域（跨项目不自动）** | 审批入池的沉淀记忆（skill / 经验）**作用域默认钉到项目**（按仓库解析），跨项目与用户全局沉淀**不自动共享**——一个项目里验证过的经验直接污染另一个项目是记忆面的越权。确需跨项目复用走显式导出/导入（带血缘与审批留痕，对齐 G1 模板五件套通道）。与租户隔离互补：租户隔离管「企业间不串」，项目作用域管「项目间不串」——两层都默认关，显式才开 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/core/src/memory-store.ts` | 修改 | 二级分层路径 + 归档轮转（读侧索引寻址不变） |
| `engine/core/src/audit-history.ts` | 修改 | 历史段归档 + 锚点重算（decision-log 同构逻辑） |
| `engine/core/src/doctor.ts` | 修改 | data 目录健康检查项 |
| `engine/core/src/__tests__/memory-archive.test.ts` | 新建 | 归档/轮转/读兼容单测 |

### 验收标准

- [ ] memory/audit 单目录文件数降到阈值内（二级分层生效），既有 key 读取零回归
- [ ] 归档事实不进常规检索、显式可查
- [ ] 审计链归档后：主链可校验 + 归档段可独立校验（双锚点指纹）
- [ ] 遗留 bak/broken 文件按策略清理且留痕
- [ ] doctor 报 data 目录健康项
- [ ] `npm test` 全绿

---

## 三、运维与安全文档注入（万物皆可 FDEing 文档重构 · R6 · 2026-09-22 用户拍板）

> **形态归属**：`[非功能]`（文档治理·重构——[v1.5.1 第十二章](./v1.5.1.md#十二断言承载面普查万物皆可-fdeing-文档重构--r1-前置--2026-09-22-用户拍板)升级清单消费）｜ —
>
> 📋 HANDBOOK / LIMITATIONS / SECURITY 三文档注入三因子口径。**时机亲和**：本版第二章（数据生命周期治理）正是 HANDBOOK「周 / 月 / 季度报告当前状态」与 LIMITATIONS 数据面叙事的同批治理——文档注入与数据面收敛同版完成。**FDEing 注入点（已勘察）**：HANDBOOK「FDE Harness 能替你干什么」节首段后补一句口径句（「把这份清单读成：你的每个业务对象都能被 FDEing 一遍」）——价值叙事原位升级，不另立新章。DEVELOPMENT.md 注入点：「一、工作原理」章首补一句开发者口径（「开发者视角的 FDEing：Skill 五件 / 加载链 / 闸门就是 FDEing 在工程面的实体——本仓自身的开发流程也是被 FDEing 的对象」）。

### 交付

| 组件 | 能力 |
|------|------|
| **HANDBOOK 注入** | docs/HANDBOOK.md 叙事口径升级：审计/巡检/养护描述按「S1M 判定 + harness 治理」归位（审计留痕 = S1M 判定的证据面；巡检 = harness 驻留相位动作）；运维面操作流程零改动 |
| **LIMITATIONS 注入** | docs/LIMITATIONS.md 补「S1M 判定失灵面」条目占位（域外泛化未验证 / 校准依赖数据分布——完整三条与 v1.8.0/v1.9.0 同口径的部分提前声明，v2.0.0 §三补全时销账） |
| **SECURITY 注入** | SECURITY.md 身份段按新身份口径更新（审计引擎自述 → 自带治理环境的 S1M 自述）；规则集叙事与 [v1.6.0 §二](../v1.6/v1.6.0.md) 判据重述衔接（条数以 SECURITY SSOT 现值为准，A24 落地后 25） |
| **升级清单销账** | R1 普查清单 HANDBOOK / LIMITATIONS / SECURITY 条目逐条销账 |

### 验收标准

- [ ] 三文档注入完成，旧身份断言清零（对照 R1 清单核对）
- [ ] LIMITATIONS 失灵面条目与 v1.8.0 §六 / v1.9.0 §五口径一致（无第二口径）
- [ ] 门禁全绿：`check-docs` / `check-anchors` / `check-forms`

---

## 与后续版本的依赖

无。本版为存量收敛（单入口 + 数据面），后续版本不依赖本版产物。
