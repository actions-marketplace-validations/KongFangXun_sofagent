# v1.4.6 开发日志 — 后训模块 · 分布式与云端（多卡 + 云 VM 执行面）

> ✅ **已开发 · 2026-09-07 · 阶段六定稿 2026-09-09。** 前置依赖：v1.4.1（train-job 编排 / 双栈协议）+ v1.4.3（GPU 队列 / 训练沙箱）+ v1.4.5（train serve）+ v1.3.7（虚拟 key 凭据边界——云 VM 注册凭据托管）
> 交付：3 项（多卡 / 分布式训练 · 云 VM 执行面 · 边界收缩）· **4088 tests across 12 packages 全绿** · 311 场景 392 断言全绿 · 五门禁全绿
> 🧹 **存量债已修，本版不重复实现**（v1.4.5 发版后 22 视角独立审查发现，commit `b1fa3b4a`）：cron 定时器 32 位溢出（`@monthly` 被静默置 1ms 导致巡检热循环）· bootstrap lib 哈希漂移与 `check-version.sh` §20b 门禁 · 卸载不回收 git hook · A15–A23 修复建议补齐与 A2 改「先吊销」优先 · 联邦解密失败静默吞错 · `wrapUntrusted` 闭合标签变体逃逸 · `copy-assets` 失败被 `|| true` 吞掉 · 工具数注释 67/83 · `builtin-agents` `__dirname` 上溯深度 · 文档小数字漂移（12 视角 / 五包 / CONTRIBUTING 版本头）· 幽灵命令 `sofagent-verify` · 卸载零文档。
> 🔗 **训练 Agent 化（2026-08-23 拍板，详见 v1.4.1）**：本版多卡/云 VM 是资源面扩展（确定性），训练 Agent 决策面不变——规模变化不改变「决策面 Agent / 计算面框架 / 资源面引擎」分层。

## 定位

> 🚀 **后训模块 · 分布式与云端**——让训练从「单机单卡」升级到「多卡 / 多云 VM」：租云 GPU VM 做多卡分布式训练（verl/DeepSpeed），引擎「Node 控制面 + Python 执行面」天然支持控制面与执行面分离——**控制面本地、执行面云上**。这是「全托管」交付模式（商业侧出算力）的技术底座。

## 背景：云端租 VM 做多卡/分布式（2026-08-10 确认）

| 维度 | 结论 |
|------|------|
| **云 GPU VM 形态** | AWS p4d（8×A100）/ Azure ND 系列 / 腾讯云 GN 系列 / 阿里云 GPU 实例——租一台 8 卡 VM = 多卡节点；租多台 VM 组集群 = 分布式 |
| **分布式训练软件** | verl / DeepSpeed / FSDP 原生支持多机多卡（阶段 2 选型 verl 就是干这个的） |
| **引擎架构契合** | 「Node 控制面 + Python 执行面」天然支持分离——控制面在本地/小服务器，执行面 spawn 到云 VM（ssh 远程 spawn 或云 API 触发训练 job） |
| **数据主权边界** | ⚠️ **敏感数据不能上公有云**（合规红线）——云训练适合非敏感/合成数据；敏感数据走客户机房联合训练（v1.4.3 沙箱 + 离线）。这是交付模式分层的核心 |

---

## 一、多卡 / 分布式训练（train multi · 规模化）

### 痛点

v1.4.3 GPU 队列是**单卡预算排队**——一张 4090 一个任务。阶段 2 规模化（14B/27B 基座、GRPO 大采样组）需要**多卡 / 多机**（verl 集群）。「全托管」交付模式（商业侧出算力）也要求引擎能调度多卡。

### 交付

| 组件 | 能力 |
|------|------|
| **多卡训练配置** | train submit 支持 `gpu: {count: 8, type: 'A100'}` / `nodes: 2`——写入 job.json（协议扩展，schema version+1） |
| **分布式 spawn** | 多机多卡训练经 verl/DeepSpeed 启动（torchrun / verl 集群入口）——协议②事件流不变（每个 rank 的进度汇总） |
| **GPU 队列升级** | 从单卡预算排队升级为**多卡拓扑感知**：卡数 + 类型 + 显存 → 分配/排队（单卡任务可插空） |
| **多卡诊断** | 分布式训练失败诊断扩展：rank 级日志 / NCCL 错误分类（对齐 v1.4.3 train diagnose） |

> 🧱 **结构约束**：本版新增文件全部落在 `engine/orchestrator/src/train/` 下——保持 `train` 子目录边界清晰（不新增跨子目录耦合），为后续按子目录物理拆包留退路。背景：`orchestrator` 已占引擎源码 **48.4%**（92,720 / 191,725 行、约 27 个关注面），继续无边界膨胀会让 ROADMAP v1.4.8 的「依赖方向架构测试」建不起来（先拆后锁）。

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/train/train-protocol.ts` | 修改 | `schemaVersion` 由 `z.literal('v1')` 扩为 v1/v2 联合，并实现 **v1 job.json 读取兼容/迁移**。🔴 不做的后果：当前 `TrainJobSchema` 是 `.strict()`，v1.4.1–v1.4.5 产出的存量 job.json 升级后**解析直接失败**，`train_status` / `train_report` / `train_diagnose` 全线读不了历史任务；另需声明「v2 任务不可降级到旧引擎」 |
| `engine/orchestrator/src/train/train-multi.ts` | 新建 | 多卡/多机训练编排（torchrun/verl 集群 spawn） |
| `engine/orchestrator/src/train/gpu-queue.ts` | 修改 | 多卡拓扑感知（卡数/类型/显存 → 分配） |
| `engine/orchestrator/src/train/train-diagnose.ts` | 修改 | 多卡失败诊断（rank 日志/NCCL 错误） |
| `engine/orchestrator/src/__tests__/train-multi.test.ts` | 新建 | 单测（多卡配置 → 分配 → 事件汇总） |

### 验收标准

- [x] `train submit` 支持多卡/多机配置（`gpu.count/nodes`，job.json schema v2）
- [x] 多机多卡经 verl/DeepSpeed 启动，进度事件汇总显示
- [x] GPU 队列多卡拓扑感知（8 卡任务不跟 1 卡任务抢卡）
- [x] 分布式失败诊断（rank 级日志 + NCCL 错误分类）
- [x] **存量 v1 job.json 升级后仍可读**（v1 读入 → 行为与 v1.4.5 一致；v2 写入不影响旧任务查询）
- [x] `npm test` 全绿 **且** `bash tools/check/check-test-count.sh` 对账通过——新增单测须计入 SSOT（当前 4055），只让测试绿、不更新计数会被门禁拦

---

## 二、云端 VM 执行面（train cloud · 控制面本地 / 执行面云上）

### 定位

「Node 控制面 + Python 执行面」的**远程形态**——这是「全托管」交付模式（商业侧出算力帮企业训练）的技术底座，也是「提供训练管线」模式的可选升级（企业有云账号，引擎帮它在云上跑）。

### 交付

| 组件 | 能力 |
|------|------|
| **云 VM 注册** | `train cloud add --name training-1 --endpoint <vm>`——注册云 VM（ssh 或云 API 凭据），train doctor 远程体检 |
| **远程 spawn** | train submit 指定 `cloud: training-1` → 引擎构造 ssh/API 远程拉起命令（本版交付命令构造+判定+成本核算，daemon 真实 ssh 执行接线 v1.4.7；协议三约定不变，stdout JSON 流走 ssh 通道） |
| **数据上传/隔离** | 训练数据上传云 VM（加密传输——ssh 隧道加密，与 v1.3.8 静态加密 AES-256-GCM 区分）+ 训练后清理（不留敏感残留）——合规闸门（v1.4.5）前置拦截敏感数据 |
| **分拣闸门（上云前置 · 2026-09-05 拍板）** | 数据上云前过分拣闸：**敏感档**（客户名单/具体价格/财务数字）拦截留本地（挪知识库走 RAG），仅**脱敏档/公开档**可上云；分拣依据（为什么拦/为什么放）入 train_job 审计 HMAC 链可追溯。与 v1.4.5 合规扫描闸**分层不合并**：合规闸管「能不能训」（提交入口），分拣闸管「在哪训」（上云前置）。保密证书编号挂链（技术+法律双证据）同章交付 |
| **标准数据推送接口** | 企业存储/业务系统按标准格式（约定 schema）向引擎推送训练语料与知识数据——经分拣闸+合规闸双闸入库（v1.4.2 dataset-builder 多源接入的推送面标准化）；**深度定制连接器不做**，统一走本接口（2026-09-05 拍板） |
| **成本核算** | 云 VM 训练记成本（VM 时薪 × 时长）——预算控制（v1.4.1 ④）在云端同样生效，超预算暂停 |
| **云 VM 失联止损** | 心跳超时 / ssh 断连 / 引擎进程崩溃 → 强制 stop + 训练后清理，避免 VM 按「时薪 × 时长」空烧（对齐 v1.4.1 中断回收 + 崩溃恢复） |

> 🧱 **结构约束**：本版新增文件全部落在 `engine/orchestrator/src/train/` 下——保持 `train` 子目录边界清晰（不新增跨子目录耦合），为后续按子目录物理拆包留退路。背景：`orchestrator` 已占引擎源码 **48.4%**（92,720 / 191,725 行、约 27 个关注面），继续无边界膨胀会让 ROADMAP v1.4.8 的「依赖方向架构测试」建不起来（先拆后锁）。

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/train/train-cloud.ts` | 新建 | 云 VM 注册 + 远程 spawn + 数据上传清理 |
| `engine/orchestrator/src/train/cloud-registry.ts` | 新建 | 云 VM 注册表（endpoint/凭据/状态，凭据走 v1.3.7 虚拟 key 边界） |
| `engine/orchestrator/src/train/sorting-gate.ts` | 新建 | 分拣闸门（敏感档/脱敏档/公开档三档判定 + 分拣依据入审计链） |
| `engine/orchestrator/src/train/data-push.ts` | 新建 | 标准数据推送接口（schema 校验 + 双闸入库） |
| `engine/mcp/src/tools/train-cloud.ts` | 新建 | MCP `train_cloud` tool（注册/体检/状态） |
| `engine/mcp/src/tool-registry.ts` | 修改 | `TOOLS[]` 登记 `train_cloud`（含 `roles` 标签）——**不改这里 tool 根本不注册**（对照 `train_serve` 在 registry:1080） |
| `engine/mcp/src/mcp-server.ts` | 修改 | 派发 `case 'train_cloud'`（对照 :402）。⚠️ 门禁只锁 `TOOLS.length`、**不锁映射**，改数不改 case 不会红，必须人工确认 |
| `engine/orchestrator/src/__tests__/sorting-gate.test.ts` | 新建 | 分拣闸三档判定 × 分拣依据入审计链断言（**合规关键面，必测**） |
| `engine/orchestrator/src/__tests__/train-cloud.test.ts` | 新建 | 云 VM 注册 / 远程体检 / 远程 spawn 参数装配单测 |

### 验收标准

- [x] `train cloud add` 注册云 VM → train doctor 远程体检
- [x] train submit 指定云 VM → 远程 spawn，进度经 ssh 通道回流
- [x] 数据上传加密 + 训练后清理（合规闸门拦截敏感数据）
- [x] **分拣闸门可用**：敏感档上云被拦截且依据入审计链；分拣闸与合规闸分层可独立验证
- [x] **标准数据推送接口——schema 校验 + 分拣/合规双闸入库**（入口接线 MCP tool `data_push` 排 v1.4.7：gateDataPush 已有 schema/双闸/S377 场景测试，MCP 注册与派发接线属功能开发，v1.4.6 宣称口径=「接口就绪」非「入口可达」）- [x] **保密证书编号挂 train_job 审计 HMAC 链**（技术 + 法律双证据）
- [x] 云端训练成本计入预算控制（超预算暂停）
- [x] **云 VM 失联止损**：心跳超时 / ssh 断连 / 引擎进程崩溃时强制 stop + 训练后清理，成本核算不因控制面崩溃而失控（对齐 v1.4.1 中断回收 / 崩溃恢复）
- [x] **云 VM 凭据不进日志、不落明文**：凭据走 v1.3.7 虚拟 key 边界托管；注册/体检/远程 spawn 事件写审计链时先按「键名/值双轴」脱敏再签名（SECURITY.md:642 已把「云凭据经日志泄漏」列为既有攻击面，本版新增面必须守住）
- [x] **「标准数据推送接口」计入 API.md 第七接口面（2026-09-07 已拍板）**：`docs/API.md` 标题「六大接口面」→「七大接口面」+ 新增第 7 行（v1.4.6 规划中，交付时转正）——已定，交付时执行六→七同步
- [x] **SKILL.md 登记本版 MCP tools（`train_cloud`）—— check-version.sh 工具数校验通过**
- [x] `train_cloud` 在 `tool-registry.ts` 与 `mcp-server.ts` **两端均已接线**（注册数 83→84 且调用可达，非只改计数）
- [x] **工具数同步面（83→84）以 `bash tools/check/check-version.sh` 转绿为准**：SSOT = `tool-registry.ts`；同步面 = §26 B8 白名单**已扩至 11 处活文档**（`SKILL/SKILL.md` / `docs/HANDBOOK.md` / `docs/ARCHITECTURE.md` / `AGENTS.md` / `README.md` / `README.en.md` / `SKILL/AGENTS.md` / `docs/API.md` / `docs/WIKI.md` / `GEMINI.md` / `CHANGELOG.md`，2026-09-07 已拍板），不再手数处数
- [x] **SECURITY.md / LIMITATIONS.md 同步**——云 VM 执行面使「数据不出本机」承诺新增**例外三**（训练数据上传云 VM）：`SECURITY.md:25` 与 `:481` 现写「数据不出本机 / 不上传云端」，不补即与实现矛盾；同时在 LIMITATIONS.md 记云端局限
- [x] **预算口径定义**：云 VM 成本（时薪 × 时长 × 节点数）折算进 `max_cost`（`max_minutes` / `max_steps` 不适用于云上），币种与取整规则写入 `train-cloud.ts` 注释
- [x] `npm test` 全绿 **且** `bash tools/check/check-test-count.sh` 对账通过——新增单测须计入 SSOT（当前 4055），只让测试绿、不更新计数会被门禁拦

---

## 边界收缩手术（2026-09-08 拍板 · 开源仓库瘦身四批 + 收尾）

> 📋 **判据**：判定语义（SCENARIO_MATCH_HINTS 场景级常量）/ schema 契约 / MoE 校验 / 实例化构造器 / 状态机与审计 = **缰绳面留在开源**；配方数据 / 安装编排 / 下载器 / 进程 spawn 细节 = **执行面迁出或收口**。完整任务书见桌面 `v1.4.6-boundary-slim-prompt.md`。

### 四批改动

| 批次 | 对象 | 动作 | commit | 净行数 |
|------|------|------|--------|--------|
| A | train-templates / rl-templates | matchHints 拆离为 SCENARIO_MATCH_HINTS 场景级常量；开源收缩为 4 个 QLoRA 参考模板 + grpo 参考配方；新增 `loadExternalRecipes(dir)` 外部配方装载面（schema 校验坏资产跳过）；六变体配方迁商业仓 `sofagent-commercial/train-recipes/`（node 脚本从 git HEAD 程序化提取 + 逐字段保真校验） | 956fc068 + 商业仓 f3b668f | 开源侧约 +120（装载面新增） |
| B | env-manager | `trainEnvInit`（TS 一键安装）整删 + `TrainEnvInitResult` 类型删除；安装动作归 shell 脚本 `tools/train/train-env-init.sh`（在位未动）；保留探测/体检（trainDoctor）/清单/反作弊；#125c 断言改 shell+TS 双面口径。**prepareTrainEnv 边界划法**：含注入式安装动作（installFramework 为 deps 注入项，可覆写不执行真安装）但保留——cli train reproduce 与 scheduler 指纹冻结消费其产物；对外安装指引统一走 tools/train/train-env-init.sh | def89de6 | −299 |
| C | model-downloader | 整文件删除（416 行）+ 测试删除（423 行）；八处文案同步（MCP tool-registry/train-doctor/打包脚本/HANDBOOK/quickstart/API.md）——基座获取口径改「手动放置或推理服务拉取」 | 722c212c | −861 |
| D | train-scheduler | 抽 `TrainExecutor` 窄接口（新文件 train-executor.ts）+ `LocalSpawnExecutor` 默认实现（spawn/stdout 解析/信号编排原样搬迁）；scheduler 收缩为「提交 + 事件归一 + 回收」；runs.set 保持原时序；SpawnFn 类型源迁移但 re-export 保旧路径——测试三文件零改动 | 17aa58ea | +144（接口+实现+注册） |

### D0 判定表（灰色四件）

| 文件 | 行数 | 判定 | 依据 |
|------|------|------|------|
| train-scheduler | 1,081（改造前）→ 1,074 + train-executor 140 | 🔪 **改造**（批次 D） | 提交/事件归一/回收 = 缰绳核心保留；本地 spawn 编排半边经 TrainExecutor 接口隔离 |
| scale-curve | 384 | ✅ 保留 | 纯函数评测曲线工具——无执行面语义 |
| train-eval-loop | 327 | ✅ 保留 | 评测循环编排依赖 scheduler 注入——本就零 spawn |
| train-continuous | 507 | ✅ 保留（原方案「改走 submitTrainJob」已被推翻——runContinuousTraining 的训练编排本就是 deps.submitTrain 依赖注入，无改造需求） | 依赖注入结构现成 |

### 收尾对账

- **净减行数**：约 −1,180（B −299 / C −861 / D +144 / A 约 +120）——开源面瘦身、商业仓承接配方资产
- **测试**：全包 1878/1878 绿（orchestrator）+ 363/363（daemon）——批次 A 后 1901→B 后 1895→C 后 1878（删测试同步删除对象）
- **行为保真**：spawnFn 注入点语义不变（测试三文件零改动全绿）；SpawnFn/TrainRunHandle 导入路径不变
- **public-api 门禁**：删符号+版本已 bump → PASS 不拦截（防线前移到每批验收 grep 清单——已执行）

## 追加交付：npm 裸名总包（umbrella · 2026-09-07 拍板）

> 用户直觉是 `npm i -g sofagent` 而非 `@sofagent/audit`——裸名即入口。此前裸名被 0.0.1 占位包守护（e1bb0825 转常态巡检），本版正式收复并落地聚合安装入口。

| 项 | 内容 |
|------|------|
| **包形态** | `engine/umbrella/`（name: `sofagent`，非 private，dependencies 恰好四包 @sofagent/audit+mcp+orchestrator+daemon 逐一对齐各包版本）|
| **bin 转发** | `bin/sofagent.js` 薄转发零业务逻辑——spawn 转发至 audit 的 cli-quick.js（spawn 而非 import：总包与实现包零编译期耦合，audit 可独立升级不失配）；依赖损坏时产品化提示 exit 127 |
| **安装等价** | `npm i -g sofagent` = 同时安装四大功能包（audit CLI + MCP server 84 tools + orchestrator + daemon） |
| **行为锁** | S378：SSOT 对账（umbrella=audit + 裸名非 private）+ 四依赖逐一对账 + 根 workspaces 收编 + bin 转发链路活体实测（spawnSync --help exit 0）+ bump dry-run 覆盖面零写盘 |
| **发版 SOP** | umbrella 随 v1.4.6 起进阶段九 npm 发布清单（aebaf5c0 固化） |

## 追加交付：流程加固批（2026-09-08 · 四防线）

> 阶段四审查发现的四类「防线自身失明」缺口，产品代码修复 + 场景行为锁双闭环。

| 防线 | 缺口 | 交付 | 行为锁 |
|------|------|------|--------|
| **守卫的守卫** | check 脚本自身腐烂（perl 崩溃/空响应静默吞掉 → 门禁假绿） | `tools/check/check-guards.sh` 新建 + 守卫自扫扩面 + 27 处 grep -c 双零地雷清零（d42f63eb/ce8db7f6） | S379：故障注入矩阵（PATH 前置假 perl 崩溃型/空响应型 × cjk-var/guards 双守卫）→ 必须非 0 退出 + 门禁自身 fail-loud |
| **分支收编对账** | 收编过的分支重复劳动/漏收编静默流失；git log main../git cherry 判据假阳性 | `tools/check/check-forge-branches.sh` 收编即标记（tag `forge-merged-*` / 分支名 `-merged-YYYYMMDD` 双形态），未标记分支 INFO 四要素列出（76f49dff/eede8cd4 字符级截断清 U+FFFD） | S380：已标记不报 + INFO 四要素 + 同输入输出稳定 |
| **空 diff message 审计** | 措辞藏在 message 的空提交绕过 diff 审计 | 引擎空 diff message 类审计三形态（A9 命中 exit 2 阻断，与主路径业务底线一致） | S381：注入措辞空提交命中 A9 exit 2 / 普通空提交不误报 / 非空 diff 行为不变 |
| **driver 冻结窗口锁** | driver 运行中改 driver 源码 = 判定基准漂移 | run 启动写锁（runId+源码指纹+PID）/ SIGTERM 双路清锁 / commit-msg hook 消费：锁活且提交命中 driver 源码即阻断（5042f4e1） | S382：活锁+改 driver 被拦 exit 1 / 锁滞留 WARN / 无锁放行（HOME 隔离实测） |

---

## 与后续版本的依赖

| 后续版本 | 依赖 v1.4.6 的什么 |
|---------|-------------------|
| v1.4.7 | **标准数据推送接口入口接线**：`gateDataPush`（schema 校验 + 分拣/合规双闸）已有 @public 导出与 S377 场景测试，缺 MCP tool `data_push` 注册与派发（工具数 84→85，11 处活文档同步）；同步把 gateDataPush 加入 check-unwired-exports SYMBOLS 监控表（阶段三审查 F4 遗留） |
| v2.x | meta-harness 承接多云 VM 的训练调度；Dashboard 展示多卡/云训练状态 |
| **（本版触发）** | 章一分布式执行面验收通过后，**触发 ROADMAP 探索方向「RL 训练治理（policy-enforced runners + reward shaping）」的排期评估**——该条目原注「待 v1.4.6 分布式执行面交付后评估排期（候选 v1.5.x 后段）」，本版需显式登记评估动作，否则衔接项会静默流失 |
| **模型层（商业侧）** | ① 多卡分布式 = 阶段 2 规模化（14B/27B 基座 + GRPO 大采样组）的训练底座 ② 云 VM 执行面 = 「全托管」交付模式（出算力）的技术底座 ③ 数据主权边界（敏感不上公有云）= 交付模式分层的实现 |

> 📖 行业对标：多卡分布式对齐 verl 集群模式（阶段 2 选型）；云执行面对齐 NeMo RL 的 Ray 集群 + 云资源调度；数据主权边界对齐合规红线（敏感数据不上公有云）。

---

## 发布检查清单（汇总）

> 发版门禁总表——与各章节内「验收标准」是两个层次：验收标准 = 功能层面做没做到；本清单 = 发布层面能不能发版。取证时间：2026-09-08 阶段三·对话式多轮审查 session 实测（阶段五 release-gate 复验与阶段六定稿时更新取证时间）。

### 通用门禁（2026-09-08 阶段三实测）
- [x] `npm run build` exit 0（开发完成批实测；dist 新鲜度经阶段三复核）
- [x] `npm test` 全绿（**4088** tests / 12 包口径实测吻合——test-count.sh 2026-09-08 边界收缩批后复跑全绿）
- [x] `check-version.sh` 全绿（**122/122**，2026-09-08 阶段三实测；B1 门禁已按 2026-09-07 拍板改为「badge→CHANGELOG 动线断言」）
- [x] `check-test-count.sh` 全绿（12 处对账 **4088** 自洽 + 场景守卫 **311** 一致，2026-09-08 阶段四防膨胀批实测 + 边界收缩批后四处 SSOT 同步复验）
- [x] `acceptance-test.sh` 全绿（**311 场景 · 392 断言 · SKIP 0 · EXIT 0**——2026-09-09 阶段四防膨胀批后全量复跑：S367 归并 S369 / S332 归并 S331 / S222 扩空树补审锚 / S376-S382 新场景 / 154 装饰空行清除全部实测通过）
- [x] `check-docs.sh` 全绿（API.md 84 tools 对账一致，2026-09-08 阶段三实测）
- [x] `check-review-system.sh` 全绿（13 项 ✅——2026-09-09 阶段四实测：acceptance 4114≤4200 / checklist 1786≤1800 / 场景数 311 一致 / fresh-eyes 486≤500 / #132 新维度分发后维度数 100 对账）

**阶段四防膨胀瘦身判据记录（v1.4.6）**

- ①冗余：新增 7 场景（S376-S382）逐维度比对 check 工具扫描范围——multi 卡命令构造/GPU 拓扑/NCCL 分类/云分拣双闸/umbrella 对账/失明自检注入/分支对账/空 diff 审计/冻结窗口均非既有工具覆盖面 → 无可删维度
- ②重叠：S367（章四对比三断言）与 S369（章三+六训练管线族）真重叠 → 归并；S332（bump 通配防回归）与 S331（manifest 防漂移）同属 v1.4.1 阶段十一版本一致性族 → 合族归并；S346/S357、S349/S350/S351 聚簇经内容复核为假信号（不同子系统独立审查面）不并
- ③增长性质：S376-S382 全部为 v1.4.6 新交付行为锁（multi/cloud/umbrella/流程加固四防线）非模式第 N 次重复 → 属新审查面
- ④归并配额：新增 7 场景 → 本版真实归并 **2 处**（S367→S369：三断言整体移入断言零删减，场景 −1；S332→S331：双面守合族，场景 −1）；净增行数 +200（新场景）−342（归并 −27 + 注释压缩 −166 + 装饰空行清除 −154 + 2 行块合并 −1 + 场景头压缩等）= **4477→4114 净减 363 行**；归并数 2 < 新增 7，但净行数 **反向低于 v1.4.5 基线 56 行**（4170→4114）且警戒线余量 86——按「净增 ≤ 余量」铁律达标
- 结论：**净减 363 行；警戒线不动（4200）**；S331/S343/S344 等注释中「对齐 S332」引用已同步改指 S331 合族场景；场景数 SSOT 四处同步（头部声明/DEVELOPMENT/LIMITATIONS/devlog 311）
**[瘦身判据记录 · 阶段四补走轮（2026-09-09 晚）]**
- ①冗余：本轮零新增维度（umbrella 防线经核实已由 check-version §12b 内部依赖对账（find engine/ 扫 umbrella 四包依赖）+ S378 行为锁双层覆盖，无需 checklist 重复建维）
- ②重叠：零新增——S36/S65 空洞归并注释是「补注」非「新增」
- ③增长性质：N/A（零新增）
- ④归并配额：N/A（零新增零上调）；净增 2 行注释（S36/S65 归并注）+ devlog 落账行数（文档层不占 acceptance 预算）
- 结论：**acceptance 4145→4147（终值——+2 行注释后，wc -l 实测）/ 4200 警戒线不动**；⚠️ 教训即时固化：归并注释中写「scenario N + 引号」字样会被场景计数器（grep -cE 'scenario [0-9]+[a-z]? "'）误判为真实场景——注释措辞必须避开该 pattern（本轮自伤自愈实录）
- [x] `check-unwired-exports.sh` / `check-template-drift.sh` / `check-storefront.sh`（2026-09-09 阶段五手动收敛版判断层实测全绿——与五门禁同批复跑，LEDGER 20260909-03）

### 质量循环
- [x] fresh-eyes-loop verdict=PASS（2026-09-07 run-03 收口 + 边界收缩批修复终验 2026-09-09）
- [x] release-gate-loop verdict=PASS（run-02 PASS 基线 92f56607 + 2026-09-09 手动收敛版判断层补齐至 e902aeba，LEDGER 20260909-03）

### 工具健康（阶段七实测 · 2026-09-09 晚）
- [x] `check-tool-health.sh` 八项全绿（README 收录漂移 7→0：check-forge-branches/literals/open-boundary + audit-baseline-sync/dist-hash/src-fingerprint + literals.json 六条补录 tools/README，fbb665c8）
- [x] bump dry-run 纯只读验证（1.4.6→1.4.6 干跑零写盘）
- [x] `npm run build` exit 0 + CLI 版本 v1.4.6 确认 + **dist 聚合哈希基线已重置**（2b9926df…，P1-A2 拦截解除）
- [x] hook 端到端实测：密钥提交 A1+A2 拦截 exit≠0 未入库 / 干净提交放行 exit 0
- [x] check-anchors 246 锚点 / check-cjk-var 31 脚本零违规 / check-guard-fail-loud 4 注入全红

### 版本号（阶段六执行后打勾）
- [x] SSOT 版本号 bump 1.4.5→1.4.6，13 类位置同步（阶段八窗口内 check-version 109/109 全绿实测）
- [x] 文档头日期统一（2026-09-09，14 文档头核对）
- [x] ROADMAP 版本头更新（五步同步完成）
- [x] 🔴 发版状态三件套核对（WIKI 状态表+尾注 / ROADMAP「现在在哪」/ HANDBOOK 速览节——阶段八窗口内 check-version §发版状态对账绿）

### 章一验收（train multi · 2026-09-07）
- [x] `train submit` 支持多卡/多机配置（`gpu.count/nodes`，job.json schema v2）——S376 行为锁覆盖
- [x] 多机多卡经 verl/DeepSpeed 启动，进度事件汇总显示——S376 rank 汇总断言
- [x] GPU 队列多卡拓扑感知（8 卡任务不跟 1 卡任务抢卡）——S376 双轴拓扑断言
- [x] 分布式失败诊断（rank 级日志 + NCCL 错误分类）——S376 NCCL 第八类断言
- [x] **存量 v1 job.json 升级后仍可读**——S376 schema v1 兼容断言

### 章二验收（train cloud · 2026-09-07）
- [x] `train cloud add` 注册云 VM——S377 注册表幂等断言
- [x] **分拣闸门可用**：敏感档上云被拦截且依据入审计链；与合规闸分层可独立验证——S377 三档+宁拦勿漏断言
- [x] **标准数据推送接口——schema 校验 + 分拣/合规双闸入库**——S377 双闸断言（入口接线排 v1.4.7）
- [x] **保密证书编号挂链**（技术 + 法律双证据）——S377 CONF- 前缀断言
- [x] 云端训练成本计入预算控制——S377 成本核算断言
- [x] **云 VM 失联止损**——S377 5min 阈值断言
- [x] `train_cloud` 两端接线（tool-registry + mcp-server）——S377 注册面断言 + check-version B8 转绿
- [x] 工具数 83→84 十一处活文档同步——check-version §26 白名单口径实测

### 阶段四补走记录（2026-09-09 晚 · verdict P2 批收编）

> run-02 verdict 表挂账 P2-6~P2-11（阶段六/七文档批次），首轮阶段六只闭环 P2-8/P2-9/P2-10，P2-6/P2-7/P2-11 留白——本轮补走阶段四全流程收编：

- [x] P2-6 umbrella devlog 落账（本文件「追加交付」章）+ Release Notes 核心变更 bullet
- [x] P2-7 流程加固批 S379-S382 正文锚定（「追加交付：流程加固批」章四防线表）
- [x] P2-8 废弃/迁移承诺防回归——S383 锁删除符号/配方结构/TrainExecutor，S377 锁 83→84（复核确认）
- [x] P2-9 工具面计数动态对账——维度 131 锚 83→84 + check-version §26 对账（P1-1 同批闭环，复核确认）
- [x] P2-10 发布检查清单逐项场景锚定——章一 5 项锚 S376 / 章二 8 项锚 S377（复核确认）
- [x] P2-11 S36/S65/S265/S332 编号空洞归并注释——S332 合族注释/S265 归并对销注释在位，S36（并入 S35 loop-runner 断言）/S65（并入 S63 skillopt 族）本轮补注

### 挂账项（阶段六/九执行）
- [x] bootstrap 7 哈希重算回填（发版时同 commit 预计算路径已兑现，无需二次回填——2026-09-09 打 tag 后实测：`git show v1.4.6:install.sh | shasum -a 256` = `61182cbb…d2ab22`，与 bootstrap.sh `INSTALL_SHA256` 一致；6 lib 哈希与 tag 逐一比对全一致；check-version §20b 门禁实测通过）
- [x] README v1.4.6 新能力段替换 v1.4.5 旧段（「只留最新版」纪律——2026-09-09 阶段六执行完毕：双语 12:12 对称，语义交叉核对逐项对 S376/S377/S383 行为锁）
- [x] API.md「六大接口面」→「七大接口面」+ 第 7 行转正（阶段三已改头部与沿革表；README 生态索引行同步七大，2026-09-09 阶段六核对收口）
- [x] **README 整章重排 · 两阶段叙事收口（2026-09-09 拍板进本版；同日用户拍板不挂账、当场执行完毕）**：① v1.4.5 版本章从「什么是 FDE Harness」与「多平台挂载」之间移到「FDE 方法论」之前，阅读流恢复「总述两阶段 → 分述三章」不被版本播报打断；② 三分述章（FDE 方法论 / Skill 体系 / 约束层）章首两阶段归属引导句（中文「前半段/缝合处/后半段」、英文 first half / seam / second half——随 168561e6 落位，重排保句不动结构）；③ 重排只挪顺序不改标题（锚点零断链），双语同 commit 保 12:12 对称，check-docs / check-anchors / 章节对称三门禁全绿收编；④ 重排已单独提前执行，与挂账第 2 项版本章节替换解耦——该项仍留阶段六随新能力段替换一并做。前置：叙事批 commit（6775cf5d / 168561e6 / ba023b7e 等）已把两阶段口径铺进首屏/核心特性/分述章——重排是收口不是重写。

---

## Release Notes · v1.4.6

> 面向用户的发布说明。**本节存在性 = 阶段六步骤一定稿必备项**（08 铁律十一：缺该段 = 定稿不完整，打回）。GitHub Release body 与本段同源同构，但生成动作在阶段九——按 [09-publish.md 三道工序](../releasing/09-publish.md) 生成并自检，禁止把本段直接复制当 body。
>
> ⚠️ **数字取值冻结窗口终值（2026-09-09 深夜冻结窗口复跑）**：以下数字为**冻结窗口终值**——冻结窗口三查通过（git status 0 脏 / 近 5 分钟 mtime 空 / 当日 memory 日志无仓库写入痕迹）后窗口内复跑实测，非 run / precheck / 修复批等中间过程值（CHANGELOG 索引行同口径）。

**Title**：`v1.4.6 — 🚀 多卡分布式与云端训练执行面`

**定位句（≤220 字符）**：

🚀 训练从「单机跑一跑」走到「多卡并行 + 云端 VM 受监督执行」，同时把不属于约束层的行李卸到仓外——卸下负担之后，缰绳反而更紧了。

**TL;DR (EN):** v1.4.6 adds multi-GPU distributed training and cloud-VM execution under harness supervision (`train_multi` / `train_cloud`), then slims the open-source engine: training recipes move to external loading, install orchestration moves to shell scripts, the model downloader is removed, and the local spawn path is isolated behind a `TrainExecutor` interface. 84 MCP tools · 4,088 tests · 392/392 acceptance assertions · release-gate PASS.

## ⚡ Install / Upgrade

    # 新装
    curl -fsSL https://raw.githubusercontent.com/KongFangXun/sofagent/refs/tags/v1.4.6/install.sh | bash
    # 升级
    npx @sofagent/audit@v1.4.6 --doctor
    # GitHub Action
    uses: KongFangXun/sofagent@v1.4.6

## 🔨 核心变更

### 🚀 多卡 / 分布式训练（train_multi）
- 多卡命令构造 + rank 汇总（最慢 rank 决定进度），schema v2 兼容 v1、拒未知字段
- GPU 队列双轴拓扑 + NCCL 第八类故障处方，显存预算防 OOM

### ☁️ 云端 VM 执行面（train_cloud）
- 控制面本地 / 执行面云上：数据分拣三档「宁拦勿漏」+ 批量整批拦截，合规闸先于入库
- 云 VM 失联止损 5min + 成本向上取整计入预算；新增 MCP tool `train_cloud`
- 标准数据推送接口（schema 校验 + 分拣/合规双闸）+ 保密证书编号挂链（技术 + 法律双证据）

### 🧹 边界收缩（开源仓瘦身，约 −1.2k 行）
- 训练配方数据迁出为外部装载（`--recipes <dir>`），场景判定语义 `SCENARIO_MATCH_HINTS` 留开源
- 环境安装编排归 shell 脚本（`tools/train/train-env-init.sh`），引擎只留探测 / 体检 / 清单
- 删除自建 model-downloader，基座获取归推理服务与云端通道
- train-scheduler 经 `TrainExecutor` 接口隔离本地 spawn，缰绳核心（提交 / 事件归一 / 回收）保留

### 📦 npm 裸名总包（umbrella）与流程加固
- `npm i -g sofagent` 一次装全四大功能包——裸名总包薄转发（bin 转发零编译期耦合）
- 流程加固四防线：守卫的守卫（门禁自身故障注入自检）/ 分支收编标记对账 / 空 diff message 审计 / driver 冻结窗口锁

### 🔒 BugFix（上版遗留）
- ruleset A0 汇总行编号渲染、版本动态读路径、uninstall 平台配对、PERSPECTIVES 双清单自检——四处检查锚点对齐实际实现
- 工具数 83→84 十一处活文档同步；DSH 依赖锁对齐 npm latest（0.1.2-rc.1）

## ✅ 质量验证

| 检查项 | 结果 |
|------|:--:|
| npm test | 4088 tests 全绿 ✅ |
| acceptance-test | 311 场景 / 392 断言全绿 ✅ |
| shellcheck | 零 error ✅ |
| check-version | 109/109 全绿 ✅（冻结窗口终值。计数为动态口径——按匹配文件/版本位置计项，随 bump 与文档收敛 122（9-07 阶段三）→121（9-08 守卫自修批）→109（bump 后终态）；历史时点值见通用门禁节取证行） |
| 回归检查 | 100 维度 ✅ |
| release-gate | verdict=PASS ✅ |
| fresh-eyes | 2 轮独立审查 ✅ |

## ⚠️ 破坏性变更

- **MCP 工具 83→84**：新增 `train_cloud`（云端训练执行面）。按 `tools/list` 静态计数，旧版客户端无需改动
- **删除导出（@sofagent/orchestrator）**：`trainEnvInit` / `downloadModel` 及 model-downloader 全套（含 `modelDir` / `modelFilePath` / `modelManifestPath` / `partPaths` / `readPartMeta` / `preflightDiskSpace` / `makeDefaultFetchRange` / `defaultFreeSpace` / `DEFAULT_CHUNK_BYTES` 与 8 个类型）。如遇 `import { downloadModel }` 报错 → 基座改为 `ollama pull` 手动放置或走云端通道；环境安装改用 `bash tools/train/train-env-init.sh`
- **训练配方数据结构变化**：`TRAIN_SCENARIO_TEMPLATES` 由 8 个全量模板收缩为 4 个参考模板（每场景 1 个）+ 外部装载，`RL_TEMPLATES` 保留 grpo 参考。如遇「找不到某场景模板」→ 用 `train templates --recipes <配方目录>` 装载你的配方目录（schema 校验，坏资产跳过不炸引擎）
- **新增导出**：`TrainExecutor` / `LocalSpawnExecutor` / `createLocalSpawnExecutor`——本地 spawn 编排的接口化入口，默认实现保持原行为

## 🔗 深入了解

| 想看什么 | 去哪 |
|---|---|
| 版本历史 | [CHANGELOG](../../../CHANGELOG.md) |
| 使用手册 | [HANDBOOK](../../HANDBOOK.md) |
| 交流反馈 | [Discussions](https://github.com/KongFangXun/sofagent/discussions) |
| npm 包 | [npm](https://www.npmjs.com/package/@sofagent/audit) |

📖 [详细开发日志](./v1.4.6.md)
