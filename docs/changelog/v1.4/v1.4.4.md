# v1.4.4 开发日志 — 后训模块 · 信号与部署闭环（语料 + 权重 + 注册 + 对比 + 决策因果链 + CI 加固 + 存量升级 + spec-first 硬禁令 + 审查收编批）

> ✅ **已发版（v1.4.4 · 2026-09-03）。** 前置依赖：v1.4.1（checkpoint 管理 / train done 事件）+ v1.4.2（训练集版本 / eval 报告）+ v1.4.3（沙箱）+ v1.3.6（model_register / model_switch / 公开格式约定）+ v1.3.2（Trace 任务级轨迹 / FDE 梳理辅助）
> 十章新功能 + 审查收编批已全部交付（验收勾下方逐条打勾；阶段五 run-07 终裁 PASS · 2026-09-03）。
> 状态：已排期 · 2026-08-10（v1.4.1-1.4.4 拆分第四版）· 2026-08-26 语料导出补「规则 → reward/verifier 映射」（第一章）——语料（样本）到 RL 训练（reward 配方）的最后一公里，River/ScaleRL 调研收编，用户拍板落盘 · **2026-08-31 收编「六轮审查 17 项修复批」（第九章，用户拍板——fresh-eyes 29 视角报告桶一全量 + 桶三裁决 4 项落位，零上下文复验 5 处证据漂移修正）**

> 🔗 **训练 Agent 化（2026-08-23 拍板，详见 v1.4.1）**：本版评估/晋升是训练 Agent 的决策点（训练质量评估 → 决定晋升/回滚），权重注册走 model_register（治理层）；语料导出五源 = 训练 Agent 的决策原料。

## 定位

> 🚀 **后训模块 · 信号与部署闭环**——后训模块的**两端出口**：往训练管线出**训练信号**（语料导出三件套），往生产环境出**可部署产物**（企业专属模型本地权重部署 + 产物→注册自动衔接），外加**多基座对比**（选型数据支撑）。后训模块到这一版闭环。

---

## 一、训练语料导出三件套（规则 + 方法论 + 样本 · 训练信号）

> 🔗 **2026-08-10 归集**：原 v1.3.2「训练语料导出 MVE」+ 原 v1.3.6「训练语料导出三件套」合并为本章（MVE 与完整版同版交付，不再拆分）。依赖 v1.3.0 decision-log（已交付）+ v1.3.1 llm-calls/evaluation-log + v1.3.2 Trace 任务级轨迹（样本富矿）。

开源侧已有丰富的现成语料——GUIDE 方法论、SKILL 加载链、24 条审计规则、25+ 案例——本版把它们全部机器可读化，成为可喂给训练管线的训练信号：

| 语料 | 来源 | 交付 |
|------|------|------|
| **规则定义** | 24+3 条审计规则（TS 代码内聚 · 口径定义见 [表下注 A](#表下注-a243口径定义)） | 序列化导出（JSON/YAML 双格式）+ 版本化 + HMAC 签名 |
| **FDE 方法论** | `FDE/GUIDE.md`（五要素/三问判定/量化公式）+ SKILL 加载链 | 结构化导出（markdown → JSON 摘要 + 原文）+ **结构化锚点维护机制**（`<!-- METHODOLOGY: five-elements -->`，GUIDE 更新后重导即同步） |
| **带标签审计样本** | 五源（落点实测见 [表下注 B](#表下注-b五源落点实测2026-09-01)）+ Trace 轨迹 | 聚合导出（脱敏 + 按任务分组 + 标签）——**runtime-audit 含工具调用 + FAIL/PASS/HITL 判定，是 RL「行为 → 奖励信号」对应数据（⚠️ 现仅 FORGE 侧实现）；fde-session 含 FDE 进场梳理的关键决策+理由（自举：训练自己的 FDE 模型的最佳语料，见 v1.4.1 训练 Agent 化）；Trace 轨迹提供任务级完整序列** |

> #### 表下注 A：「24+3」口径定义
>
> **2026-09-01 核对 `engine/audit/src/rules/index.ts`（规则注册权威处）后补。** 该「24+3」自 2026-08-10 沿用至今从未定义，而仓库并存多种计数，导致本章验收第 1 条「规则零遗漏」不可判定。源码真相：
>
> | 口径 | 数值 | 构成 | 出处 |
> |------|:--:|------|------|
> | `defaultRules` | **17** | A1-A11 + A18-A23（quick 默认启用） | `rules/index.ts:43-64` |
> | `extendedRules` | **7** | E1/E2/E4 + A14-A17（需 `extendedRulesEnabled`） | `rules/index.ts:72-81` |
> | `rules`（全量） | **24** | 17 + 7（README「完整 24 条 = 17 默认 + 7 扩展」同此） | `rules/index.ts:84` |
> | 永久跳号编号位 | **3** | **A12 / A13 / E3**——A12 供应链安全、A13 文件权限于 v0.99.4 并入 A11；E3 于 v1.2.5 并入 A11（行数维度）。三者已无独立实现 | `rules/index.ts:32`、`:55`、`:75` |
>
> **本版取「24+3」= 24 条已实现规则 + 3 条跳号编号位（A12/A13/E3）**——这是源码证据下唯一自洽的读法：跳号数恰为 3，且三处跳号在同一文件内被显式写明、「+3」这个加数才成立。导出须覆盖 **27 个编号位**（跳号 3 条以 `status: 'merged-into-A11'` 占位登记），使「零遗漏」可对**编号空间**对数而非仅对数组对数，避免 3 个已知合并位被静默丢掉。
>
> ⚠️ **待用户拍板**（不代为决定）：若拍板为「只导 24 条实现、跳号不占位」，则本章验收第 1 条同步改为「24 条全部可导出」，且导出的编号空间出现 A12/A13/E3 三个空洞需在格式里显式声明为 `skipped`（而非静默缺省）。
>
> #### 表下注 B：五源落点实测（2026-09-01）
>
> | 源 | 状态 | 实际落点 |
> |------|:--:|------|
> | decision-log | ✅ 引擎侧在位 | `engine/audit/src/decision-log.ts` → `data/audit/decision-log.jsonl` |
> | llm-calls | ✅ 在位（**异名**） | 实现文件为 `engine/core/src/llm-call-trace.ts`（落盘 `data/audit/runtime/llm-calls.jsonl`，见 `:160`）——规划文档习称 `llm-calls`，全仓无同名实现文件，施工勿按名找 |
> | evaluation-log | ✅ 引擎侧在位 | `engine/orchestrator/src/benchmark/evaluation-log.ts` → `data/<project>/benchmarks/<benchmark_id>/evaluation-log.jsonl`（注意在 **orchestrator** 包，不在 audit 包） |
> | runtime-audit | ⚠️ **仅 FORGE 侧实现** | `FORGE/src/audit-middleware.mjs:111` → `data/audit/runtime/<repo-hash>/runtime-audit.jsonl`（实测本机 `a325d4ba64c8/runtime-audit.jsonl` 有数据）。**`engine/` 侧无实现**——`docs/ARCHITECTURE.md:481` 明载「引擎侧 data-sovereignty 审计日志仍全局，**已移排 v1.4.7** 复用 FORGE 方案补齐」 |
> | fde-session | ✅ 引擎侧在位（尚无数据） | `engine/orchestrator/src/fde-session/index.ts`（v1.3.7 交付 5 #2，286 行）→ `data/fde/sessions/<sessionId>/context.md` + `meta.json`；本机实测 sessions 目录为空，即模块在位但尚未产生样本 |
>
> **对施工的直接影响（不代为拍板，列选项由用户定）**：
> - **runtime-audit 一源有两个选项**：① **读 FORGE 产物**——sample-aggregator 直接解析 `data/audit/runtime/<repo-hash>/runtime-audit.jsonl`（零新增代码，但语料面只覆盖跑过 FORGE 自托管 SubAgent 的仓库）；② **等 v1.4.7 引擎侧补齐**后再纳入，本版五源降级为四源。
> - **llm-calls / fde-session 两源无阻塞**，仅需按上表真实路径接线（前者异名、后者尚需触发产生数据）。

**样本是训练信号里最值钱的部分**——真实的 FAIL/通过案例 = 带标签的「什么是对的」，对应 self-recording improvement（模型协作产生 trace → 用 trace 训练）。

### 规则 → reward/verifier 映射（样本到 reward 配方的最后一公里 · 2026-08-26 补）

> 📋 **2026-08-26 补充**：三件套导出了**样本**（什么行为被判 FAIL/PASS），但 RL 训练还需要 **reward 定义**（用什么函数给行为打分）。sofagent 的天然优势：24+3 条审计规则的判定逻辑本身就是机器可执行的 verifier——**规则导出（第一件）从「给人看的规则定义」升级为「可挂进训练循环的 reward 骨架」**。

| 组件 | 能力 |
|------|------|
| **规则 → reward 骨架导出** | 每条审计规则导出时附带 `reward_hint` 段：判定函数签名（输入=工具调用序列/产物文本，输出=PASS/FAIL 三态）+ 严重度权重 + 可判定性标注（机器可判 / 需人审 / 启发式） |
| **verifier 清单** | 规则导出附 `verifiers.json`——哪些规则可直接当 reward 函数（机器可判类）、哪些只能当训练后验收（需人审类）——训练管线按清单接线，不混用 |
| **与 RL 配方模板衔接** | reward 骨架 + v1.4.3 RL 配方模板（grpo/dapo/cispo）= 完整训练配置——商业侧「reward 配方」的开源对应物（开源导骨架，商业侧调权重） |

**边界（不做）**：本版只导「规则 → reward 骨架」的映射格式与清单，**不实现训练循环本身**（那是计算面，复用开源框架——v1.4.1 拍板的「轮子全复用」边界）；也不做自然语言指令直转权重（self-distillation 属商业模型层）。

### 通用脱敏管线（业务语义脱敏）

现役脱敏只有 A2 规则的**格式匹配**（AKIA / sk- / ghp_ / PEM 块）+ decision-log `sanitizeWhy`——企业业务语义脱敏（客户名/内部代号/API 响应体业务字段）无通用管线。本版新增 `engine/core/src/export/redactor.ts`：

- **规则配置**：`redact-rules.json`——正则 + 实体名 + 占位符映射（`{CUSTOMER_NAME}`, `{INTERNAL_CODE}`）
- **三类红名单**：格式类（复用 A2 检测）/ 语义类（实体名库，FDE 梳理辅助可录入）/ 结构类（API 响应体字段）
- **验收**：样本导出后 grep 原始企业专有名词 0 命中

### 合规红线（仅脱敏聚合，不落个体级数据）

| 红线 | 规则 |
|------|------|
| **不落个体级数据** | 样本导出仅脱敏聚合，可反推具体企业/具体请求的原始字段一律剥离 |
| **字段白名单** | 可导出：ruleId / ruleType / severity / token 数 / 耗时 / 失败码 / 脱敏后的文本模式 |
| **字段黑名单** | 禁止导出：原始 API 响应体 / 密钥类值 / 企业专有名词 / 用户可识别信息 / 未脱敏代码片段 |
| **导出审计** | 每次导出记 `corpus_export` 事件（谁导的、范围、签名）——导出行为本身受审计 |

### FDE 梳理 → 语料导出衔接（人工基准 = ground truth）

v1.3.2 FDE 梳理辅助产出的 workflow.yml 草稿 + 五要素 + 量化四字段，是企业 eval「访谈替代率」的人工基准（ground truth）——随语料导出（标记 `source: 'human-fde'`，人工基准样本）。

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/audit/src/export/rule-schema.ts` | 新建 | 规则导出格式定义（含 `reward_hint` 段——判定签名/严重度权重/可判定性标注） |
| `engine/audit/src/export/exporter.ts` | 新建 | 规则 + 样本序列化 |
| `engine/audit/src/export/reward-mapping.ts` | 新建 | reward 骨架 + `verifiers.json` 清单生成（机器可判/需人审/启发式三类分桶，2026-08-26 补） |
| `engine/core/src/export/methodology.ts` | 新建 | FDE 方法论结构化导出（锚点解析） |
| `engine/core/src/export/redactor.ts` | 新建 | 通用脱敏管线（业务语义） |
| `engine/core/src/export/sample-aggregator.ts` | 新建 | 五源 + 轨迹聚合 + 脱敏 + 标签（fde-session 第五源 · 2026-08-23 新增） |
| `FDE/GUIDE.md` | 修改 | 关键章节加结构化锚点标记 |
| `engine/mcp/src/tools/corpus-export.ts` | 新建 | MCP `corpus_export` tool |
| `engine/audit/src/cli/index.ts` | 修改 | CLI `sofagent-audit corpus export` 子命令注册（双入口的 CLI 面——audit 包 bin 面，`sofagent corpus export` 经 CLI 桥接可达，2026-09-01 审查补） |
| `engine/audit/src/__tests__/export.test.ts` | 新建 | 单测（规则零遗漏 + 锚点解析 + 脱敏 0 命中） |

### 验收标准

- [x] 24+3 条规则全部可导出（零遗漏）——**口径以「表下注 A」为准：24 条已实现 + 3 条跳号编号位（A12/A13/E3）以 `status: 'merged-into-A11'` 占位，共 27 个编号位**——已落地：CLI 实跑 `counts: { implemented: 24, mergedPlaceholders: 3, totalSlots: 27 }`，占位三件 A12/A13/E3 均带 `mergedInto: 'A11'`；单测 27 编号位零遗漏 + 连续性双锁定
- [x] GUIDE 方法论可结构化导出（五要素/三问判定/量化公式齐全）；锚点标记生效（GUIDE 更新重导即同步）——已落地：`FDE/GUIDE.md` 三锚点（`<!-- METHODOLOGY: five-elements / three-questions / quantification -->`）+ `parseMethodologySections` 解析（段边界=锚点→下锚点/章节头，表格计数）；单测含真实 GUIDE 三段提取 + 合成锚点同步用例
- [x] 样本源 + 轨迹聚合导出（脱敏 + 分组 + 标签），含 `source: 'human-fde'` 人工基准——**源数与路径以「表下注 B」为准**——已落地：五源提取器全接线（llm-calls 按异名 `llm-call-trace.ts` 真实路径、runtime-audit 读 FORGE 产物 `data/audit/runtime/<repo-hash>/`、fde-session 标 `label='human-fde'`），缺席源显式登记 `absentSources`；CLI 实跑五源缺席登记正确（本机 data/ 暂无数据非实现缺失）
- [x] **规则导出含 `reward_hint` 段 + `verifiers.json` 清单**（机器可判/需人审/启发式分桶正确——机器可判类可直接当 reward 函数接线）——已落地：CLI 实跑三桶 `machine: 22 / human: 4 / heuristic: 1`（合计 27），`reward_hint` 三件套（signature/severityWeight/verifiability）逐规则齐全；`buildVerifiersWithOverrides` 支持人工覆写
- [x] 导出结果带版本号 + HMAC 签名；脱敏 grep 企业专有名词 0 命中——已落地：`schemaVersion: v1` + `engineVersion: 1.4.3`（audit 包 package.json 真实读取，readEngineVersion 路径 bug 已修+回归测试锁定）+ HMAC（stableStringify + sha256 截 32）；`verifyNoLeak` 断言 0 命中入单测
- [x] CLI `sofagent corpus export` + MCP `corpus_export` 双入口可用——已落地：`sofagent-audit corpus export [--scope all] [--json]` CLI 子命令 + MCP `corpus_export` 工具（roles: ops）均实跑通过；CLI 端到端实测产物三件（rules-all.json/yaml + verifiers.json）结构完整；MCP 协议面测试 3 用例（initialize → tools/call → 27 编号位/三桶/HMAC/五源断言，`engine/mcp/src/__tests__/corpus-export.test.ts`）；施工中修出两处接线 bug：① `readEngineVersion` 路径三层上错解到 engine/（回退 0.0.0）→ 改两层 + 回归测试锁定；② corpus 导出面只注册在 audit 包 `index.ts`（CLI 入口）未进 `public-api.ts`（包 main 入口）——跨包消费者（MCP createRequire）解析不到、工具实际不可用 → 补 public-api 导出块（含 5 函数 + 5 类型）
- [x] SKILL.md 登记本版 MCP tools（`corpus_export`）—— check-version.sh 工具数校验通过（79→80）——已落地：SKILL.md 两处 + SKILL/AGENTS.md 全量表登记（模型训练 11→12 类），B8 六文档口径全绿（check-version 第 17/22/26 项 ✓，registry 实数 80）
- [x] `npm test` 全绿——已落地：全仓 vitest 3990/3990（core 388 · audit 969 含 export.test.ts 33/33 · mcp 157 含 corpus-export 协议面 3/3 · 其余包全绿）

---

## 二、企业专属模型本地权重部署链路

> 🔗 **2026-08-10 归集**：原 v1.3.6「本地模型部署链路」并入本版——模型注册/灰度的 **endpoint 型**能力仍在 v1.3.6 ④（云端/可寻址服务），**权重文件型**（企业专属模型交付）随训练主题归本版。

| 组件 | 能力 |
|------|------|
| **权重文件分发** | 训练产物（LoRA adapter / 量化权重）目录规范 + 版本清单——`model_register` 支持 `source: 'local-path'`（权重文件路径，非 URL） |
| **本地加载** | 从权重目录加载模型（vLLM / Ollama / OpenAI 兼容本地端点）——复用 v1.3.2 `client_type: 'openai-compatible'` 插槽 |
| **版本回滚** | 权重版本切换可回滚（git snapshot 兜底，对齐 model_switch 异常回滚语义） |

> ⚠️ **施工顺序（2026-09-01 审查定）**：本章在第三章之前——第三章 artifact-register 调 `model_register source: 'local-path'`，该 source 类型是本章交付物；先做第三章则端到端验收（自动注册权重产物）不可写。依赖链：第七章·一 → 第一章 → **第二章 → 第三章 → 第四章**。

> 🧭 **现状基线（2026-09-01 实测 · 施工前必读）**：`local-path` 的**类型位与 schema 已全面预留，实现未落地**——`engine/orchestrator/src/model-registry.ts:29` 已 `export type ModelSource = 'endpoint' \| 'local-path'`，`model-register.ts:27` 与 `tool-registry.ts:821` 已声明该取值，但 `model-registry.ts:10-11` 自陈「字段可存，但 local-path 条目**不可切换为活动模型（无可加载权重）**」；权重目录解析 / 版本清单 / 权重加载三者全仓零命中。故本章是「填实现」而非「从零设计」，勿重复造类型槽。

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/model-registry.ts` | 修改 | 权重条目落盘与解析（`:29` 类型已有、`:56` endpoint 字段对 local-path 型为「权重目录占位」，补权重目录规范 + 版本清单 + 完整性校验；`:10-11` 的「不可切换为活动模型」限制随之解除） |
| `engine/mcp/src/tools/model-register.ts` | 修改 | `source: 'local-path'` 分支实装（`:9` 注释「本版只处理 endpoint 型」待改、`:58` 已解构 source、`:118` why 串已留 local-path 分支） |
| `engine/orchestrator/src/model-router-config.ts` | 修改 | 本地加载接线——复用 `:42` `provider: z.enum(['ollama', 'openai-compatible'])` 既有插槽，不新增协议 |
| `engine/mcp/src/tools/model-switch.ts` | 修改 | 权重版本回滚（`:8`/`:21` `action='rollback'` 已有，补权重版本级回滚语义，对齐异常回滚不要求人审） |
| `engine/orchestrator/src/__tests__/`（权重部署用例） | 新建 | 注册 / 加载 / 回滚三路径回归用例 |

### 验收标准

- [x] 权重目录规范 + 版本清单落盘；`model_register` 以 `source: 'local-path'` 可注册本地权重路径（解锁 `model-registry.ts:10-11` 的「不可切换为活动模型」限制）——已落地：`weights-manifest.ts` 新建（manifest.json 目录规范：versions[] + current 指针 + 逐文件 sha256；appendVersion 幂等登记 / checkWeightsDir 四项校验 / hashDir 确定性哈希）；registerModel local-path 分支强制校验（无 manifest / 哈希不匹配 / 当前版本缺失一律拒绝注册——供应链红线），注册成功 localWeights{dir,currentVersion,versionCount} 落档；「不可切换」限制解除（switchModel 对 local-path 改为切换前重校验，通过即挂载）
- [x] 本地加载可用：权重目录 → vLLM / Ollama / OpenAI 兼容本地端点（复用 `client_type: 'openai-compatible'`，不新引入协议）——已落地：注册条目 clientType=openai-compatible + endpoint 指向本地推理服务（vLLM/Ollama）；applyRegistryOverrides 既有覆盖链路原样承接 local-path 活动条目（toLocalModel 映射 provider/client_type）；端到端用例断言注册→灰度→晋升后 readActiveEndpoints 可解析
- [x] 版本回滚可用：权重版本切换可回滚（git snapshot 兜底），与 `model_switch` 异常回滚语义一致（止损不要求人审）——已落地：`rollbackWeightsVersion`（模型级 rollbackModel 之外新增版本级——manifest.current 指针回拨，缺省回上一版 / targetVersion 显式跳版；止损语义对齐：直接生效不要求人审）；MCP `model_switch action='rollback-weights' + target_version` 双参数透传
- [x] 端到端全链审计留痕：**电池厂场景**——一张 4090 + 电芯数据训出的 LoRA → 权重目录 → `model_register` 本地注册 → `model_switch` 挂载到产线校准节点，每一步进 `model_registry` 事件链——已落地：weights-deploy.test.ts 端到端用例（v1/v2 双版本训练产物 → openai-compatible 注册 → pipeline 灰度 20% → 人审晋升 100% → v2 翻车版本回滚 v2→v1；事件链断言 ops=[register,switch,promote,rollback] + actor/lane/percent/comment 逐项 + 终态 active+current=v1）
- [x] `npm test` 全绿——已落地：orchestrator 1673/1673（含 weights-deploy 17/17 新增 + model-registry 新语义 21/21）；mcp tsc 通过（dispatch/schema 同步）；全仓口径见全局验收引擎 · 信号与部署闭环（语料 + 权重 + 注册 + 对比 + 决策因果链 + CI 加固 + 存量升级 + spec-first 硬禁令 + 审查收编批）

> 📌 **待拍板（不代为决定）**：权重文件入册是否强制校验哈希/签名（可复用 v1.4.1 `artifact-signing.ts` 的权重 HMAC 链路）。本章设计只写了「目录规范 + 版本清单」，**未含供应链完整性校验**；若要求「无清单或哈希不匹配即拒绝注册」，需在此处补一条验收项。
> ✅ **施工落地**：`checkWeightsDir` 默认 `verifyHash: true`（manifest sha256 与实际权重逐文件比对，不匹配即拒绝注册）+ 无 manifest 拒绝注册——「无清单或哈希不匹配即拒绝注册」已是既成事实，本待拍板项转为追认性质；如拍板放宽再降级。

---

## 三、训练产物 → 模型注册自动衔接（训练闭环的最后一步）

> 📋 **2026-08-10 补充（架构审查新发现）**：后训模块的「训练完成」和 v1.3.6 的「模型注册」是**断的**——训练完的权重产物不会自动进 `model_register`，差一个自动衔接动作。缺了它，后训模块闭环不完整（train → eval → 部署 三环节断裂）。

### 交付

```
train job 完成（v1.4.1）
  → eval 通过（v1.4.2 阈值判定）
  → 权重产物打包（第二章目录规范）
  → 自动调 model_register（v1.3.6 ④，source: 'local-path'）
  → model_switch 挂载到 workflow 节点（灰度比例）
  → 全程 train_job + model_registry 审计留痕
```

| 组件 | 能力 |
|------|------|
| **训练产物注册器** | train done + eval pass → 自动调 `model_register`（幂等：同权重版本不重复注册） |
| **挂载建议** | 注册后给出 `model_switch` 建议（哪个 workflow 节点可挂载）——人工确认后执行（晋升强制人审，对齐 v1.3.5） |

> 🔗 **商业平台接口预留 G1（2026-08-17 补充）**：本版「训练产物 → 模型注册自动衔接」与商业产品层的 workflow 模板注册同类机制（商业平台接口规划 §9.1 G1）——商业平台跨企业 fork 需要 `workflow_export`/`workflow_import`（workflow 模板导出/导入 + 血缘追踪），挂靠本版的注册器模式扩展：**模型可注册，workflow 模板同样可注册**（模板元数据加血缘字段，记录「此模板来自哪个企业/哪个 fork」）。**本版先打通模型注册，workflow 模板注册随商业平台 SaaS 需要再补**。

> 📖 **G1 导出口径（2026-09-01 明确 · 缺口 B）**：`workflow_export`/`workflow_import` 的「模板」= **五件套完整导出**——① workflow 定义（DAG）② 各节点 Agent 配置 ③ 节点挂载的工具清单（MCP/CLI）④ 模型绑定（modelPreference，v1.4.8 ⑧）⑤ 验收标准（merge_criteria/approver）。商业平台 §2.1「workflow 包是综合交付物」——fork 交易的就是完整结构，不是裸 DAG。血缘字段随导出物携带（来源企业/fork 链）。
>
> ⚠️ **G1 去向版本待拍板（2026-09-01 审查发现 · 不代为决定）**：上述口径已记，但 `docs/ROADMAP.md` 规划表（v1.4.4~v2.0.0 全部 11 行）**无任何版本单元格承接 G1**——它是 G1~G13 中唯一悬空的缺口（G7/G12/G13 均已落 v1.4.7 / v1.5.1）。违反 ROADMAP 自身「🔴 阻塞项占位纪律：任何阻塞项必须占据一个明确的版本单元格，不得仅写在散文备注里」。
>
> **两个候选（附推荐）**：
>
> | 候选 | 理由 | 反理由 |
> |------|------|--------|
> | **v1.4.7（推荐）** | ① 与 G13 PR 生命周期接口同批——G1 的血缘字段与 G13 的 `merge_criteria`/approver 落在同一套「workflow + 验收元数据」结构上，同批可共用一次 schema 变更；② v1.4.7 已是「商业平台接口版」，G 系列缺口本就归此；③ 与 v1.4.9 设备侧的 G9/G10/G11 同属 G 系列收口节奏 | v1.4.7 已有 G2/G4/G6/G7/G8/G13 六项 + workflow 烧进 USB，负载偏重 |
> | v2.0.0 离线 USB 节点合体 | ① workflow 烧录 USB 已在 v1.4.7，导出/导入是烧录的自然前序；② v2.0.0 收窄后只余「离线 USB 节点合体」一件核心，容量充足 | 太远（v1.5.x 尚有 4 版），商业平台 fork 需求等不及；且 G1 是**接口**（写侧），不该等到交付形态版本 |
>
> **推荐 v1.4.7**：G1 是接口面而非交付形态面，归「商业平台接口版」比归「离线 USB 节点」更合语义；且 G13 同批可摊薄一次元数据 schema 变更成本。拍板后由 ROADMAP v1.4.7 行落单元格，本条注记即作废。

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/train/artifact-register.ts` | 新建 | 训练产物 → model_register 自动衔接 |
| `engine/orchestrator/src/__tests__/artifact-register.test.ts` | 新建 | 单测（eval pass → 自动注册 + 幂等） |

### 验收标准

- [x] train done + eval pass → 自动调 `model_register`（权重产物）——已落地：`train/artifact-register.ts` 新建（registerTrainArtifact：前置双闸——train done（getJobGuarded 企业隔离 + status=completed）+ eval pass（decision=stop + 报告归属一致防串档）→ 产物拷贝（排除 .log/.jsonl）→ appendVersion 登记（meta 带 trainJobId/evalScore/baseModel 溯源）→ registerModel（source: local-path + verifyHash 强制））
- [x] 幂等：同权重版本不重复注册——已落地：双层幂等（manifest 层：versions 按 meta.trainJobId 命中即不重复拷贝登记；registry 层：同名 local-path 条目 + 同权重目录 + 版本在册即不重复调 registerModel，事件链不增长）；幂等命中前仍校验既有版本哈希——篡改即拒绝（红线优先于便利）
- [x] 挂载建议给出（人工确认后 model_switch）——已落地：MountSuggestion{model, lane, suggestedPercent, requiresHuman: true, hint}（注册器只建议不代执行——晋升强制人审对齐 v1.3.5；幂等命中时建议仍返回，人审点不因重放消失）
- [x] `npm test` 全绿（2026-09-02 实测：npm test --workspaces EXIT=0，12 包 3744/3744）

---

## 四、多基座对比训练（train compare · 选型数据支撑）

> 📋 **2026-08-10 补充（后训模块完善 · 阶段 2 选型前置）**：后训练阶段 2 有两个待测对比——**Qwen3 vs R1-Distill**、**7B vs 14B**（eval 分数 ÷ 训练/部署成本 = ROI）。本版把「同一数据多基座并行训练 → 对比报告」做成引擎能力，选型不用手跑实验。

### 交付

| 组件 | 能力 |
|------|------|
| **对比训练** | `train compare --bases qwen3-8b,r1-distill-7b` → 同一数据并行提交多个 train job（同数据 hash / 同超参模板） |
| **对比报告** | 各基座 eval 分数 + 训练/部署成本 → ROI 排序（复用 v1.4.2 train report） |
| **GPU 调度** | 对比任务走 v1.4.3 GPU 队列（串行 or 预算并发） |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/train/train-compare.ts` | 新建 | 多基座对比（并行提交 + 汇总） |
| `engine/orchestrator/src/__tests__/train-compare.test.ts` | 新建 | 单测（同数据多基座 → ROI 排序） |

### 验收标准

- [x] `train compare` 同数据多基座并行提交（同 hash / 同模板）——已落地：`train/train-compare.ts` 新建（submitCompareJobs：computeDatasetHash 同 hash 锚点 + jobId=compare-<hash8>-<slug> + 共享超参模板 + per-base override 合并；train-job 幂等语义承接对比重放）
- [x] 对比报告含 eval 分数 + 成本 + ROI 排序——已落地：buildCompareReport（ROI=evalScore/trainCost，零成本拿分→Infinity 排最前，同 ROI 按 evalScore 决胜；未完成基座不进排序不扭曲结论；提交与汇总解耦——训练完成后调用，不阻塞提交方）
- [x] 对比任务走 GPU 队列（不 OOM）——已落地：createGpuQueue（serial 串行 / budget 显存预算并发两模式）+ estimateTrainVramMiB 基座显存估算（参数量从 baseModel 名提取，qlora/grpo/全参三档）→ acquire/release 控并发，snapshot 留档；队列生命周期与对比绑定不残留
- [x] `npm test` 全绿（2026-09-02 实测：npm test --workspaces EXIT=0，12 包 3744/3744）

---

## 五、决策因果链与先例检索（Semantica 启发 · 2026-08-29 排入）

> 🔗 **参照而非依赖**（2026-08-29 拍板）：Semantica（semantica-agi/semantica，Python/MIT/5.5k+ stars）的核心设计「决策不是日志行，是一等公民图节点」值得吸收，但其 Python 栈（gensim/FAISS/Oxigraph）与 sofagent 纯 TS 零重依赖定位冲突——**只参照设计，不引入依赖**。本版把 decision-log 从「完整性很强但无因果边」升级为可查因果链的结构。

### 定位

> decision-log.jsonl 的 HMAC 链保证**完整性**（防篡改），但决策之间没有因果关联——审查者看到的是一串孤立判定。Semantica 的 `CAUSED / INFLUENCED / PRECEDENT_FOR` 三种因果边映射到 sofagent：拦截决策引用触发它的路由决策、HITL 决策引用待审的上游决策。**审计叙事从「每条自证清白」升级为「决策链可追溯」**。

### 交付

| 组件 | 能力 |
|------|------|
| **决策因果边** | `DecisionLogEntry` 加可选字段 `causedBy?: string[]`（引用前序 decisionId）+ `causalType?: 'caused' \| 'influenced' \| 'precedent_for'`——写入时由触发链路带上（拦截→路由、HITL→待审项），HMAC 计算含新字段 |
| **因果链查询** | decision-query 扩展 `traceDecisionChain(id)`——沿 causedBy 回溯至根因，输出链式叙事（A 导致 B，B 触发 C 拦截） |
| **先例检索** | decision-query 扩展 `findSimilarDecisions(query)`——按 `DecisionWhy.tags + triggeredRule` 结构化匹配（不用向量，字段已有，零新依赖）；HITL 审批界面展示「历史类似决策 + 结果」 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/audit/src/decision-schema.ts` | 修改 | 加 `causedBy` / `causalType` 可选字段（旧条目无此字段照常校验——向后兼容） |
| `engine/audit/src/decision-query.ts` | 修改 | 加 `traceDecisionChain` / `findSimilarDecisions` 查询 |
| `engine/audit/src/decision-query.test.ts` | 修改 | 因果链回溯 + 先例匹配用例（实测位于 `src/` 根下，非 `__tests__/` 子目录——audit 包测试文件两种位置并存，施工勿找错） |

### 验收标准

- [x] 旧 decision-log（无 causedBy 字段）加载与链校验不破坏（向后兼容）——已落地：causedBy/causalType 均可选字段（不传不落盘，老语义不变）；新条目接老条目链连续（测试断言 prevHash 非 genesis）；traceDecisionChain 对无 causedBy 条目返回单节点链不报错
- [x] traceDecisionChain 能回溯多级因果链并输出人类可读叙事——已落地：沿 causedBy 逐级上溯至根因（支持多父展开，depth 递增）；叙事从根因到起点按因果序（「XX 导致了 YY」连接，influenced 边用「影响了」）；防环（visited 集合拦截，环时 brokenAt 如实标注）；测试覆盖三级链（路由→拦截→上报）
- [x] findSimilarDecisions 按 tags + triggeredRule 匹配，HITL 场景可展示先例——已落地：结构化打分（tags 交集 ×2 分/命中 + triggeredRule 精确匹配 ×3 + kind 相同 ×1），只返回 score>0 条目降序，零新依赖（字段已有，不用向量）；matchedOn 记命中原因（人读先例展示）
- [x] HMAC 计算涵盖新字段（链完整性不因 schema 扩展而弱化）——已落地：causedBy/causalType 进 baseSanitized 后 stableStringify 全字段签名（机制自动涵盖，无白名单遗漏面）；实测篡改验证——改 causedBy 指向目标后 checkDecisionChainDetailed 判 tampered（红）

## 六、CI 供应链加固三件套（Semantica 工程治理启发 · 2026-08-29 排入）

> 🔗 参照 Semantica 仓库治理实践（GHSA-j4mq 修复 + LiteLLM/Trivy 供应链攻击防御），原排 v1.4.3 候选，2026-08-29 拍板统一落本版。

### 交付

| 组件 | 能力 |
|------|------|
| **CI Action SHA 固定** | `.github/workflows/` 8 个文件的 `uses:` 从浮动 tag（`@v5`）改为完整 commit SHA（tag 留尾注释）——防第三方 Action 被入侵后窃取 release token（LiteLLM/Trivy 同款攻击面） |
| **pin 验证脚本** | `tools/check/check-action-pins.sh`——核对每个 SHA pin 与注释 tag 指向同一 commit（GitHub API 对账），纳入 CI + 发版门禁 |
| **本地面 fail-closed 自查** | dashboard serve（localhost:3780）与 Explorer 类本地服务面核查——无密钥配置时拒绝服务（503）而非静默匿名，绑定默认 127.0.0.1，`ALLOW_ANONYMOUS` 须显式 opt-in。**已发现实锤**：`tools/dashboard/serve-dashboard.mjs:687` `server.listen(port)` 未传 host——Node 默认绑 `::`（IPv6 全网卡），局域网可达，须改 `server.listen(port, '127.0.0.1')`（2026-09-01 已先行落地为 `DASHBOARD_HOST || '127.0.0.1'`，本章验收第 3 条回归确认即可） |

### 验收标准

- [x] 8 个 workflow 全部 SHA 固定，浮动 tag 引用清零——已核验：20 处 `uses:` 全部 40 位 commit SHA + 行内注释 tag（checkout@fbc6f399 / setup-node@a0853c24 / action-shellcheck@00cae500），check-action-pins 静态检查通过
- [x] check-action-pins.sh 能抓到「SHA 与 tag 不一致」的坏样本——已实测：合成注入 `actions/checkout@v4` 浮动 tag 到 pr-check.yml → 静态检查 4 处不合规 EXIT=1（阻断）；在线对账 20 处 SHA 与 tag 全部同 commit（离线降级不阻断门禁）；实测后还原干净
- [x] dashboard/本地服务面自查结论落 LIMITATIONS——已落盘：§三「Dashboard 本地服务面自查结论」节（有网络面已修：默认绑定 127.0.0.1 须显式 DASHBOARD_HOST=0.0.0.0 才上局域网 / 零外链 CDN / 无密钥凭据面）
- [x] **dashboard 完全离线可用**——已核验：dashboard.html `jsdelivr|cdn` 零命中（36 图标全内嵌 SVG，microscope→eyeglasses 替代在位），断网打开图标零丢失


## 七、存量升级审查落本版（十六项 · 2026-08-29 用户拍板收编）

> 📋 **2026-08-29 用户拍板**：「老的功能开发好了不能一直放着，看看有什么可以更新或优化的。」升级清单（与 v1.4.3 第十一章清扫相对——清扫是删除型，本章是翻新型）自 v1.4.3 第十二章迁入本版为正式章节。同日全仓发散扫描（文档/代码/工程链/DX 多视角）追加三项消化项（四/五/六）——dashboard 去 CDN 已当场直接修复（36 图标内嵌 + microscope→eyeglasses 上游缺失替代），v1.4.3 im-bridge 死链已顺手修，不占本版条目。三轮复查再追加消化项第七项（install.sh 部署模式）。复核轮 F-15 拍板追加消化项第八项（过期承诺限时检查，机制层——承诺从注释升级为门禁盯防）。

### 本版消化项（一~十 · 发版前完成 · 2026-09-01 编号校正：原「一~九」标题与十项表体不符）

| # | 对象 | 现状证据 | 升级动作 |
|---|------|---------|---------|
| 一 | **eval 三套体系统一** | `engine/eval`（评分引擎独立包）+ `engine/orchestrator/src/benchmark/`（评测设计/执行/MLflow 导出）+ `engine/ab-test`（A/B 对照）——三套各建 runner/scorer | **本版语料导出前置统一**：eval 包为 SSOT scorer，benchmark 与 ab-test 复用其 scoring 层——语料导出五源含 evaluation-log，训练 eval 闭环要求三源同口径（本版第一章依赖） |
| 二 | **79 MCP tools 瘦描述复核** | v1.4.0「瘦描述」交付时 66 tools，现 79（v1.4.1 +1 train_submit / v1.4.2 +9 fde 六件+train 三件 / v1.4.3 +3 train_status+train_list+train_diagnose——累计新增 13 个）覆盖度未复核 | 发版前复核新增 13 个 tools 描述长度与去版本号纪律（对齐 v1.4.0 瘦描述规范；2026-09-01 审查修正：原「76/新增 10」漏 v1.4.3 三件） |
| 三 | **trimMessagesSafe 作用域审计**（自探索方向迁入 · 2026-08-29） | v1.3.4 release-gate 遗留：FORGE 消息裁剪的作用域边界未确认（跨闭包引用同款风险家族——run-12 effectiveHardLimit 同族） | 本版发版前完成作用域审计：确认裁剪函数无跨闭包引用，必要时加固 + 回归用例 |
| 四 | **13 包 engines 字段统一**（全仓扫描发现 · 2026-08-29） | `audit`/`mcp` 已声明 `engines: {node: '>=18'}`；`core`/`orchestrator`/`daemon` 等 9 包未声明——Node 16 用户装包得到运行时玄学错误而非清晰版本提示 | 13 包 package.json 统一补 `engines`（CLI 入口包优先），与根 README Node 版本要求对齐 |
| 五 | **npm 包 README 门面补齐**（全仓扫描发现 · 2026-08-29） | `audit`（366 行）/`eval`（92 行）达标；`orchestrator` 仅 10 行、`core`/`daemon`/`skillopt`/`think`/`rules` 各 9 行——npm 页面 13 包中 8 个门面近乎空白，核心包 `@sofagent/orchestrator` 尤甚 | 模板化补齐：每包「职责一句话 + 主入口 + 安装 + 文档链接」30 行内（不写长文），发版前一次完成 |
| 六 | **check-anchors 文件存在性断言**（全仓扫描发现 · 2026-08-29） | 锚点门禁只查「章节锚点」不查「链接目标文件存在」——本轮实测 664 链接中 6 处文件级死链/假阳性（v1.4.3 im-bridge 相对路径错一级为真死链，已顺手修；release SOP 模板占位符 `v{major}.{minor}` 4 处为假阳性需白名单） | `check-anchors.mjs` 加文件存在性断言（约 20 行）+ 模板占位符白名单，纳入发版门禁 |
| 七 | **install.sh 静态资源部署模式升级**（三轮复查发现 · 2026-08-29） | install.sh:713-719 只拷 `dashboard.html` + `serve-dashboard.mjs` 两文件到 `~/.sofagent/web/`——任何未来对 `docs/assets/*` 或新增静态资源的引用都会在安装态断链（v1.4.4 header logo 断链事件的根因之一，已内嵌 data-URI 当场修复但模式性风险仍在） | 部署段从「白名单两文件」升级为「tools/dashboard/ 目录整体同步（rsync 或 cp -R）」+ serve-dashboard 静态根指向 web/——新增资源零改动自动部署；同步更新 install.sh 提示文案 |
| 八 | **「将在 vX.Y.Z 移除」过期承诺限时检查**（复核轮 F-15 拍板 · 2026-08-29） | engine/ 内存在 `将在 v1.5.0 移除` 三处弃用 shim 承诺（audit CLI `engine/audit/src/index.ts:332/:693/:734`）——承诺只活在注释里，到期无人盯防即静默过期（v1.4.4 第六章 TODO(v1.4.0) 拖两版本才收口是同款事故） | check-docs（或独立定期脚本）加过期承诺检查：`grep -rn "将在 v[0-9.]* 移除" engine/` 逐条解析承诺版本，承诺版本 ≤ 当前版本且 shim 仍在即告警——与 v1.5.0 六）shim 兑现移除联动，承诺从「注释自觉」升级为「门禁盯防」 |
| 九 | **FDE 三层交付模板外置**（2026-08-30 用户拍板直接排入） | 引擎五 `distillDeliverables` 的 docLayer/skillLayer 渲染骨架硬编码在 `engine/orchestrator/src/fde/fde-quantify.ts` 字符串数组里——FDE 每个企业要定制交付物样式，现在改个模板骨架就得走包发版 | 模板骨架外置到 `FDE/templates/deliverables/`（engine 运行时读取渲染，含占位符约定）+ 模板缺失/占位符不全时 fail-closed 回退内置默认 + 模板结构断言测试——模板优化从「发版」降级为「改文档」，FDE 现场即可定制（hugozhu《FDE 的 20 个问题》「问变成查」方向：能力沉淀从代码层移到文档层，Plugin/Skill 不频繁升级也能进化） |
| 十 | **DSH 内嵌执行路径 cordis 断裂修复**（v1.4.3 发版闸门 2026-08-30 拍板记待办） | v1.4.0 拍板「CLI 桥接 = 当前正式形态」——但内嵌路径 execution-backend 走 cordis 的分支在并行 session 升级中断裂（v1.4.3 闸门 precheck 发现；本版不阻塞发版：验收口径「全 step 走 dsh」不分内嵌/CLI） | 修复内嵌路径 cordis 分支接线 + 内嵌/CLI 双路径回归用例（同一 workflow 两种执行面同结果），修复后与「CLI 桥接正式形态」口径一并复核——若双路径长期并行成本过高，评估收敛为单一路径 + 另一路径显式弃用声明 |

### 登记分发项（去向版本已定，本版只登记）

| # | 对象 | 去向 |
|---|------|------|
| 十一 | 加载链 token 预算（当前仅 persona 500 字符/knowledge 2000 字符截断——harness/src/index.ts:147，非 token 级） | v1.4.8 ④ 自动上下文压缩（触发条件依赖 token 计量） |
> 注：原编号「十」与消化项「十（DSH cordis 修复）」撞号，2026-09-01 校正为十一起顺延（十二~十六 同步 +1，正文引用同步） |
| 十二 | webhook 事件源扩展（三态推送目前仅审计场景→训练完成/治理周报/死信告警统一事件源） | v1.5.1 事件总线（webhook 入站+出站统一；v1.5.x 顺延后编号，2026-08-31 校正） |
| 十三 | daemon 巡检 28 项归组（inspectors/ 粒度参差——commons-health 与 commons-catalog-daily 合并候选） | v1.5.0 治理 KPI 面板（面板化前先归组，避免面板读 28 个源） |
| 十四 | IM 桥一等公民评估（install.sh 默认不装——FDE Harness 执行通道叙事下应重估） | v1.5.0 FDE 陪跑期补全联动（陪跑日报经 IM 推送是自然场景） |
| 十五 | 证据强度分级（VALIDATION/THANKS 有来源纪律无强度分级——按「公开可查/用户自报/自测自报」三级标注） | v1.4.5 措辞收紧联动（进化样本报告先落地三级标注——随进化模块前移，2026-08-31 拍板） |
| 十六 | 本体混合检索评估（向量召回+图谱扩权——Semantica 实测同任务 token 38k→12k 省 60%、准确率 82.1%→89.2%） | v1.5.0 ②双时态联动评估（同一 schema 层变更） |

### 验收标准

- [x] eval 统一：三套评分同口径（benchmark/ab-test 引 eval 包 scorer，无重复实现）——**d8d03616 落地**：evalBridgeScoringFn 桥接（动态 import + 非 JSON 回退）+ ab-test v1.4.2 起直引 evalCase；零信任核查三目录零重复评分实现（grep 三维度关键词仅命中桥接注释与 SSOT 本体）
- [x] 79 tools 瘦描述复核完成（新增 13 个对齐规范——v1.4.1×1 + v1.4.2×9 + v1.4.3×3）——**已施工并验收**：6 处违规全修（train_doctor/train_status/train_diagnose/fde_derive/fde_deploy/train_dryrun，去版本号/批次/对标出处/实现细节），tsc 冒烟通过；2026-09-02 全局验收 npm test --workspaces EXIT=0
- [x] trimMessagesSafe 作用域审计结论落盘（无跨闭包引用风险或已加固）——**已落盘并验收**：`FORGE/calibration/trimessagessafe-audit.md`（纯函数零自由变量、调用点同层闭包、run-12 修复模式保持；两份同构实现属 FORGE 内部设计债维持现状）
- [x] 13 包 engines 字段全声明且与根 Node 版本要求一致——**前批已落地并验收**（12 包 `{"node":">=18"}` 复核 + 本轮回归确认）
- [x] 13 包 npm README 门面达标（职责+入口+安装+文档链接）——**前批已落地并验收**（c013c9a6 补齐 core/orchestrator/daemon/skillopt/think/rules）
- [x] check-anchors 文件存在性断言上线（真死链清零 + 模板占位符白名单不误报）——**前批已落地并验收**（check-anchors.mjs L388 文件存在性断言在位）
- [x] install.sh 部署段升级为目录同步（tools/dashboard/ 整体部署，新增静态资源零改动自动可达）——**前批已落地并验收**（bc603aa1 目录同步模式）
- [x] 过期承诺限时检查上线（注入「将在当前版本移除」合成坏样本能告警，v1.5.0 shim 三处到期前门禁先红）——**已上线并验收**：check-docs.sh §16（正则锚定「将在 v…移除」紧贴版本防误抓；实测 5 处承诺全为 v1.5.0 未到期；合成坏样本注入 v1.4.3 承诺正确告警后还原）
- [x] FDE 三层交付模板外置（FDE/templates/deliverables/ 可定制生效：改模板不改代码，渲染输出与内置默认逐字节等价；模板坏样本 fail-closed 回退 + 结构断言测试在位）——**已落地并验收**：4 模板文件 + resolveTemplatesDir 三级查找链 + renderTemplate split/join 渲染（残留 `{{` 即回退）+ fde-quantify.ts 接线 + 4 新用例（25/25 绿）
- [x] DSH 内嵌路径 cordis 断裂修复（内嵌/CLI 双路径回归用例通过：同一 workflow 两种执行面同结果；或已收敛单一路径 + 弃用声明）——**已收敛双路径并行可用**：诊断结论 = src 契约本正确（setup 块体无 return，`?.commit()` 安全；probe1 报 TypeError 系诊断脚本简写体箭头隐式 return disposer 的自伤）；真缺口在 usage 提取——alpha.1 事件流 usage 在 `assistant/chunk`（type:"usage" camelCase）而 `assistant/message` 顶层无 usage 面，老 `extractSessionUsage` 恒返 null（v1.4.3 第六章步三 token 自动计量在内嵌路径实际失效）。修复 = extractSessionUsage 三通道（chunk usage / message.usage / usage_metadata）+ normalizeUsageCandidate 归一 + 9 单测（dsh-backend.test.ts 33/33 绿）；双路径端到端实测同结果（内嵌 createDshBackend 与 CLI createDshCliBackend 同任务均 output=`"say pong"` exit 0；内嵌 usage=8595/147/8742，修复前恒 null）
- [x] 分发项六项全部有明确去向版本（上表右列；2026-09-01 编号校正后为十一~十六）——2026-09-02 核验：六项去向行均在第七章分发表在位
- [x] `npm test` 全绿（2026-09-02 实测：npm test --workspaces EXIT=0，12 包 3744/3744）

### 十六项 → 场景/证据映射表（run-04 P1-1 补齐 · 2026-09-03）

> 本表为发版闸门 run-04 P1-1 闭环产物：十六项逐项给出验收证据锚点（acceptance 场景号 / 门禁脚本 / commit），无锚点项显式声明证据形态。消化项一~十的场景锚点集中在 S369/S370/S371（章七十收口）+ 各专项门禁；分发项十一~十六为「登记不施工」，证据形态 = 本版分发表去向列 + 对应版本规划文件。

| 项 | 验收证据锚点 | 证据形态 |
|---|---|---|
| 一 eval 三套统一 | `FORGE/calibration/trimessagessafe-audit.md` 同目录校准档案 + commit d8d03616 + 零信任核查记录（本章验收标准一） | commit + 校准档案（评分逻辑属引擎内部，无 CLI 观察面，不设 acceptance 场景） |
| 二 79 tools 瘦描述复核 | S371（registry 实数对账含新增 tools）+ tools/check/check-tool-health.sh 门禁 | 场景 + 门禁 |
| 三 trimMessagesSafe 审计 | `FORGE/calibration/trimessagessafe-audit.md`（结论落盘） | 校准档案（纯函数审计无运行时行为，静态档案即验收物） |
| 四 13 包 engines 统一 | S371 锚点 12（`CNT_ENGINES ≥ 12` 断言） | 场景断言 |
| 五 npm README 门面补齐 | commit c013c9a6 + S371 锚点 11（js-yaml/SECURITY 等包门面抽查同族） | commit（README 属 npm 门面非运行时行为，git 历史即证据） |
| 六 check-anchors 文件存在性 | check-anchors.mjs L388 断言 + 每版发版门禁实跑（check-anchors 237 锚点全绿） | 门禁 |
| 七 install.sh 目录同步 | commit bc603aa1 + 维度 49（install.sh 检查） | commit + 门禁 |
| 八 过期承诺限时检查 | S370 锚点（`将在 v` 检查在位断言）+ check-docs.sh §16 坏样本实测 | 场景 + 门禁 |
| 九 FDE 模板外置 | S370 锚点 3/4（templates 目录 + resolveTemplatesDir 接线） | 场景断言 |
| 十 DSH cordis 修复 | S370 锚点 5（normalizeUsageCandidate 三通道）+ dsh-backend.test.ts 33 用例 | 场景 + 单测 |
| 十一 加载链 token 预算 | 分发表去向 v1.4.8 ④ | 登记项（本版不施工，去向在位即验收） |
| 十二 webhook 事件源扩展 | 分发表去向 v1.5.1 | 登记项（同上） |
| 十三 daemon 巡检归组 | 分发表去向 v1.5.0 | 登记项（同上） |
| 十四 IM 桥一等公民评估 | 分发表去向 v1.5.0 | 登记项（同上） |
| 十五 证据强度分级 | 分发表去向 v1.4.5（措辞收紧联动） | 登记项（同上） |
| 十六 本体混合检索评估 | 分发表去向 v1.5.0 ②（双时态联动） | 登记项（同上） |

> 口径说明：消化项一/三/五属「档案型」验收（审计结论/校准档案/commit 历史即最终产物，无稳定 CLI 观察面可锁场景）；其余消化项均有场景或门禁锚点；分发项六项按拍板「本版只登记」，分发表去向列即验收物。S371 由本表同批新增（章九 17 项 + 章一样本聚合 + 章七门面三合一场景）。

## 八、spec-first 硬禁令（OpenFDE 启发 · transcript 永不直驱代码 · 2026-08-30 排入）

> 📋 **2026-08-30 用户拍板**：探索方向表挂了多时的「spec-first 硬禁令（OpenFDE 启发 · 设计约束）——单一事实源：transcript 永不直驱代码，spec 才是唯一驱动」一直停在方向格。本版把断言基建已就绪（第六章 check-anchors 文件存在性断言同批交付）的条件用上，落成可执行的门禁 + 纪律双层交付。

### 定位

单一事实源纪律的工程化：**代码变更必须可追溯到 spec**（工作流声明 / 任务书 / 变更说明），conversation transcript（对话记录、访谈原文）本身永远不是改代码的依据——transcript 先沉淀为 spec（workflow.yml / fde.md / task 书），spec 才驱动实现。这与 SKILL 既有「先读再用」纪律同源，补的是「留痕可对账」这层机制。

### 交付

| 层 | 组件 | 能力 |
|------|------|------|
| **门禁层** | check-spec-first.mjs（tools/check/） | 扫 commit message / PR 描述与变更文件：涉及 `engine/*/src` 的代码提交，message 须含 spec 关联标记（`spec:` 前缀引用 workflow/fde/task 文件路径，或 `no-spec: <理由>` 显式豁免声明）；无标记无豁免即 WARN——**只提示不阻断**（新机制渐进纪律，跑一个版本观察误报率再议升级 FAIL） |
| **纪律层** | SKILL/skills/ 规范升级 | 五步循环的「实现」步前插入 spec 引用要求：Agent 改代码前先声明「本变更对应哪个 spec」——transcript 直出的口头需求须先落 fde.md/task 书再动代码；SKILL.md 加载链 L1 铁律区同步一行 |
| **对账层** | doctor 联动 | `--doctor` 输出近期 N 条 commit 的 spec 关联覆盖率（纯展示，不判 FAIL）——让「多少代码变更是 spec 驱动的」从不可见变为可运营的数字 |

### 涉及文件（预估）

| 文件 | 改动 |
|------|------|
| `tools/check/check-spec-first.mjs` | 新建（约 80 行：git log 解析 + 标记/豁免识别 + WARN 输出） |
| `SKILL/SKILL.md` | 加载链 L1 铁律区 +1 行（spec-first 一句话纪律） |
| `SKILL/skills/04-deliver.md` | 实现步前 spec 引用要求 |
| `engine/core/src/doctor.ts` | spec 关联覆盖率展示段（读 git log 纯展示） |

### 验收标准

- [x] check-spec-first.mjs 三态实测通过——已落地：tools/check/check-spec-first.mjs（git log 解析 + engine/*/src 变更过滤 + merge commit 跳过 + spec:/no-spec: 标记识别，WARN only 恒 exit 0）；三态逻辑单测（标记识别/豁免识别/无标记 WARN/空值 spec: 不算标记）全过
- [x] SKILL 双处落盘——SKILL.md 铁律区第 7 则（spec-first：改代码前先声明对应 spec，transcript 永不是改代码依据）+ skills/04-deliver.md §7 黄金顺序后插 spec 引用要求（commit message 携带 spec: 标记或 no-spec: 豁免）
- [x] doctor 输出覆盖率——已落地：engine/core/src/doctor.ts 尾部新增「spec-first 覆盖率（纯展示）」段（execFileSync 读 git log 近 30 条，代码提交分类计数，不判 FAIL）；实跑输出「spec 关联覆盖率 0%（近 30 条：代码提交 2，spec: 标记 0，no-spec: 豁免 0，无标记 2）」
- [x] 自查完成——近 30 条 commit 实跑：代码提交（含 engine/*/src）仅 2 条（12ec0171/de86d2ff，均为审查修复批，属「无 spec 直接修复」的真实形态非误报）；纯文档提交 28 条正确跳过零误报；merge commit 跳过正确。观察结论：观察期 WARN only 合理，本仓库代码提交主要走 changelog 驱动（spec 载体为 docs/changelog/），误报率 0
- [x] `npm test` 全绿（2026-09-02 实测：npm test --workspaces EXIT=0，12 包 3744/3744）

### 与 OpenFDE 的对齐边界

只借「单一事实源」纪律形态，不引入其工具链——sofagent 的 spec 载体是自有体系（workflow.yml / fde.md / task 书），检查器是自有门禁（tools/check/ 家族），与 v1.4.3 第九章 onboarding 走查的「不断头路」原则一致。

---

## 九、六轮审查 17 项收编批（fresh-eyes 29 视角 · 2026-08-31 拍板）

> 📋 **2026-08-31 用户拍板**：六轮独立审查（含团队子代理轮 + 对账轮，29 视角报告三桶视图——报告留档作者机不入库）的桶一 17 项立即修复项全部收编进本版。分桶判据：改动小、风险低、独立可验、不依赖产品决策。原台账编号保留以便溯源。**裁决阶段零上下文复验全部锚点**，5 处证据漂移已修正（#2 半拉子修复维持 P0、#32+47「daemon init 即修」不成立、#49 定性改「已实现未接线」、#73 占位实为 4 个、#16 已消解撤销）。

### 修复清单（17 项 · 按报告台账编号）

| # | 级别 | 问题 | 修复动作 | 工作量 |
|---|---|---|---|---|
| 2 | 🔴 P0 | `audit.cost.budget` 配置被静默丢弃（89e8d856 已加告警白名单，merge 透传仍缺——半拉子） | mergeWithDefaults 补 cost 透传行（config-loader.ts:600-614）+ topLevelAuditKeys（:422-426）+ 契约测试 | 一行+测试 |
| 3 | 🔴 P0 | SOFAGENT_KEY_PATH / HOME_ALLOWED_PREFIXES 未披露 | SECURITY.md 补两行威胁模型（对齐 SOFAGENT_DATA 格式） | 纯文档 |
| 58 | 🟡 P1 | webhook SSRF 对 IPv6 字面量整体失效 | isPrivateWebhookUrl（webhook.ts:32）剥方括号一行 + IPv6 测试用例 | 一行+测试 |
| 59 | 🟡 P1 | pre-commit hook 未知退出码静默放行 | 退出码白名单（0/1/2 放行、其余 fail-closed） | 小改 |
| 60 | 🟡 P1 | `--reset-chain` 幽灵指引（4 处文档推荐不存在的参数） | 二选一：实装 flag 或删 4 处指引（doctor.ts:435/:444、verify.ts:49/:65） | 小改 |
| 61 | 🟡 P1 | `--history` 幽灵旗标 + SKILL.md 悬空引用 | HANDBOOK:338 改 --timeline；SKILL:175 修工具表引用 | 纯文档 |
| 48 | 🟡 P1 | @sofagent/mcp 幽灵依赖 js-yaml（独立安装即 MODULE_NOT_FOUND） | mcp package.json dependencies 加 `"js-yaml": "^5.3.0"` | 一行 |
| 24 | 🟡 P1 | SECURITY/LIMITATIONS 引用已退役 API checkHistoryChainIntegrity | 两处（SECURITY.md:259、LIMITATIONS.md:204）改 checkHistoryChainDetailed | 纯文档 |
| 36 | 🟡 P1 | SECURITY automerge 安全声明整段失真 + 4 处活文档漂移 | 重写 §五段（@automerge/automerge@^3.4.1 实态）+ 同步 ARCHITECTURE:420、team-collab:22/:49、releasing/02-dev:65 | 文档批 |
| 12+26+33 | ⚠️ P2 | 文档数字漂移三处（281→284、76→79×2、README:59 混排） | FDE/GUIDE:949、WIKI:207、README:59 三处数字更新 | 纯文档 |
| 38+78 | ⚠️ P2 | 「工作流」术语漏网 4 处 | SECURITY:305、audit/README:289/:291、WIKI 统一改「工作流」 | 纯文档 |
| 13+44 | ⚠️ P2 | audit-history.ts 头部 v1.3.2 时代注释（两份） | 注释更新为 atomicAppendSync 实态；统一 v1.3.2/v1.3.1 编号 | 注释 |
| 14 | ⚠️ P2 | install.sh 自述冻结（941 vs 1372 行）+ @latest 浮动 | 头部行数更新；@latest 改固定版本 | 注释+一行 |
| 15 | ⚠️ P2 | SECURITY 目录图缺 keys/、automerge 版本（并入 #36 批） | 目录图补 keys/ | 纯文档 |
| 42 | ⚠️ P2 | audit.md 落盘零脱敏（history.jsonl 有双层脱敏） | audit-log.ts 接 sanitizeFreeText（REDACTION_PATTERNS SSOT） | 小改 |
| 32+47 产品半 | 🟡 P1 | daemon 守护死亡无告警（exit 78 无检测 + writeHealthFile 诞生即死 #62 同族） | 接线心跳写 + 退出码检测 + doctor 读同一路径（环境半=plist 手工修，不占代码项） | 小改 |

> 📖 **计数口径**：「17 项」= 六轮审查报告桶一台账条目数（报告留档作者机不入库）；上表按落盘批次合并呈现（#12+26+33 三合一、#38+78 二合一、#13+44 二合一、#32+47 二合一），**表行数与项数不一一对应**，溯源以台账编号为准。下文 A 批「13 项」= 唯一台账编号数（#3/#12/#13/#14/#15/#24/#26/#33/#38/#61/#70/#71/#78，合并编号展开去重）——commit `dd72c20d` message 的「9 项」系按改动文件组计，口径以本注为准。

> 附带收编 3 项顺手修（不占 17 项口径）：#20 daemon 两处 IM 推送复用 isPrivateWebhookUrl（两行接入）；#63 commit-msg hook 追加与 eval 写路径收口（与 #42 同批）；#46 accumulateWarnings 下沉 core（一行测试改 import）。**落盘状态核实（2026-09-01 实测）**：#20 已落盘（`engine/daemon/src/webhook/index.ts` + `push-target.ts` isPrivateWebhookUrl 引用在位✓）；#46 全仓 grep `accumulateWarnings` 零命中——函数名不符或未落盘，施工时先核实实际函数名再定夺；#63 待施工核验（tools/hooks/ 仅见 sofagent-precommit.sh）。另 **#37（根 `package.json` `overrides` 段 6 条本机绝对路径）是发版收编必做**——不是代码修复，但若直接提交此配置，干净机器 `npm ci` 即炸。**实证对象校正（2026-09-01 实测）**：污染不在 `package-lock.json`（`dsh-deployed` 零命中；含 dsh 的 232 个 `packages` 条目 `link` 字段全为 null），而在根 `package.json:50-55` 的 `overrides` 段——6 条 `/Users/kongfangxun/.local/share/dsh-deployed/node_modules/@deepseek-ai/cordis*`，已随 v1.4.3 公开且至今在远端 main；防复发场景 S361 原只查 lock、未覆盖 `package.json`，故漏检——**此项已由 `f5d55b26` 补面**：S361 现双文件（`package-lock.json` + `package.json`）双关键字（`dsh-deployed` + `/Users/`）同查，负例实测「注入 `dsh-deployed` 路径判 FAIL」「仅改目录名判 FAIL」非空转，本条防复发缺口已闭合。**本版收口（删除 `overrides` 整段 · 2026-09-01 已落地 `f5d55b26`）**：修法是**删除整个 `overrides` 段**（该键现已不复存在），**不是**改为 registry 版本号——实测删除前后 `npm install --package-lock-only` 重解析，`package-lock.json` diff 为 0 行、六个 cordis 包 `version` 与 `resolved` 完全一致（均 `registry.npmjs.org`、`link: false`），证明这 6 条是惰性死配置、从未生效，删除为零行为变更；反之若写死六个 registry 版本号（`@deepseek-ai/cordis` 4.0.2 / `cordis-plugin-timer` 1.1.4 / `cordis-plugin-group` 1.0.2 / `cordis-plugin-hmr` 1.0.17 / `cordis-plugin-include` 1.0.7 / `cordis-plugin-loader` 1.0.3）则等于新增恒强约束，将来 dsh 父包若要求其他 range 将触发 ERESOLVE，属引入新风险。**本条口径与代码侧均已闭环，本版无需再施工。**

### 桶三裁决归位（8 项存疑的落点）

- **落本版**：#70-core（core/package.json:7「不含审计逻辑」描述改一行）、#71-①（think-generator.ts:16 注释改 getDataDir 实态——实现站对了边，注释漂移）、#71-②（drillDshStableGuardRelease JSDoc 补「检查一~三为代码事实断言非运行时探测」定性说明）、#73-占位（4 个纯占位测试重写：cron/constraints/think-generator/inspectors——SSOT 数字走 check-test-count 既有基线流程）
- **维持桶二不动作**：#34（#57+74 锁死 v1.4.7 硬前置，接线不得先行）、#25（ROADMAP v1.4.3 状态列维持三态，发版自然消除）、#49（推荐 B：标注预留 + 接线排 v1.5.x 与 #8b 网关同批，最终归属随 v1.5.0 定稿）

### 执行进度（2026-08-31 · 三批推进）

**A 批 · 纯文档/注释类 13 项已修**（commit `dd72c20d`，2026-08-31 02:50）：#3 / #24 / #61 / #12+26+33 / #38+78 / #13（core 半复核不成立，实修 audit 版） / #14 / #15 / #70-core / #71-① / #71-②。修复中两处复核修正：WIKI「工作流」命中为黄仁勋引文（行业观点转述，术语口径注豁免不改）；README:59 版本段数字为 v1.4.2 历史快照（加历史锚标注，不改数为历史事实）。门禁：check-anchors 236/236 · check-docs 全绿 · check-version 102/102 · tsc 冒烟 · 13 文件 U+FFFD 零。

**B 批 · TDD 代码修复 4 项已随 v1.4.3 落盘**（commit `d6907083`，2026-08-31——v1.4.3 发版流程走完后当场执行，原文「排程本版开发批」已过时，2026-09-01 校正）：#2 cost 透传（config-loader mergeWithDefaults + topLevelAuditKeys + 契约测试）、#58 IPv6 SSRF（剥方括号 + 测试）、#59 退出码白名单（fail-closed + 测试）、#42 audit.md 脱敏接线（sanitizeFreeText + 测试）——**本版仅回归验证，勿重复实现**。

**C 批 · 顺手修 4 项当场执行**（2026-08-31 拍板）：#60 删 4 处 `--reset-chain` 幽灵指引（`--init --reset-chain` 从未实装；实装属新功能应走规划而非顺手加）、#36 SECURITY automerge 段重写 + 4 处活文档同步、#73 占位测试重写（4 个纯占位文件）+ #48 幽灵依赖一行。

### 验收标准

- [x] #2 契约测试（回归✓——config-loader.test.ts:122「audit.cost.budget 被透传到返回配置」实测 1 passed，d6907083 已落盘勿重复实现）
- [x] #3/#24/#36/#15 SECURITY.md（回归✓——checkHistoryChainIntegrity 双文件 0 命中、SOFAGENT_KEY_PATH/HOME_ALLOWED_PREFIXES 披露 3 处、目录图含 keys/ 两处、automerge ^3.4.1 实态声明在位）
- [x] #58 IPv6（回归✓——webhook-push.test.ts 18 passed 含 ssrfIpv6LoopbackEndpoint 用例，方括号剥离判定实测）
- [x] #59 未知退出码（回归✓——sofagent-precommit.sh:178-198 白名单 0/1/2 放行、137/139/126/127 fail-closed，bash -n 语法过）
- [x] #60/#61 幽灵旗标（回归✓——全仓 grep --reset-chain/--history 残留指引清零，dd72c20d/85a1b550 已落盘）
- [x] #48 mcp 独立安装冒烟（2026-09-02 实测——干净目录 npm install 本地包 → js-yaml 可解析 → sofagent-mcp bin 在位 → JSON-RPC initialize 握手返回 serverInfo v1.4.3，不炸）
- [x] #42 audit.md 落盘（回归✓——audit-log.ts:13 import sanitizeFreeText + :70 REDACTION_PATTERNS SSOT 注释，与 history.jsonl 同口径）
- [x] #32+47 产品半：daemon exit 78 可被 doctor 感知（心跳文件 + 检测路径在位）——已落地：①daemon-health.ts 加 `lastExitCode`/`stoppedReason` 字段 + `recordDaemonExit()`（exit 事件落盘 status='stopped'）+ checkDaemonHealth 新增 dead 态判定（非零退出码 + 心跳陈旧 >10min = 守护已死亡，心跳新鲜不误报）；②cli.ts start 路径接线「诞生即死」修复——启动即 writeHealthFile('start')、5min 心跳定时器（unref 不阻退出）、SIGINT/SIGTERM 落盘 exit 0、uncaughtException 落盘 exit 78（EX_CONFIG 约定）、main().catch 启动失败落盘 exit 78；③cli.ts doctor 子命令新增 💀 dead 态展示（exit 1）+ 修复指引；④core/doctor.ts 加「daemon 守护状态」段（总结段前）——读 resolveDataDir()/daemon-health.json（与 daemon 侧同源口径），dead → FAIL、stopped → WARN、running → OK、never-started → info 不告警；⑤daemon-health.test.ts +6 用例（退出码落盘/正常停止不误报/exit 78+陈旧心跳判 dead/重启后不误报/fatigue 保留/never-started）+ doctor.test.ts +5 用例（FAIL 死亡+修复提示/运行中零误报/正常停止 WARN/不存在 info/损坏 WARN 不崩）——daemon 302 绿 + core 393 绿；端到端冒烟实测：真实启动→SIGINT→exit 0 落盘✓、构造 exit 78+陈旧心跳→doctor 报「💀 守护已死亡（exit 78）」EXIT=1✓
- [x] 桶三 4 个纯占位测试重写 + check-test-count 基线同步（重写已在位：cron 5/constraints 4/think-generator 3/inspectors 4 用例全绿——头部「#73 占位重写」注释在位；基线 2026-09-02 实测同步：README/README.en/WIKI 3744 由 sync-test-count.sh 自动更新，LIMITATIONS 两行+ARCHITECTURE 四行手工对齐 audit 976/core 393/orchestrator 1697/daemon 302，check-test-count 11/11 绿）
- [x] 文档批全量（回归✓——#12+26+33 三处数字已更新（284/79 旧值零命中）；#38+78 工作流→工作流 SECURITY/audit-README 零残留；#13+44 双副本注释对齐实态（core 原语层/audit 持久化层双向互指）；#14 install.sh 行数历史锚+「勿以此行为当前行数口径」注解、@latest 两处均为纪律说明非使用）
- [x] `npm test` 全绿（2026-09-02 实测——npm test --workspaces EXIT=0；12 包 3744/3744（test-count.sh TOTAL=3744）；#32+47 端到端冒烟：SIGINT→exit 0 落盘✓、exit 78+陈旧心跳→doctor「💀 守护已死亡」EXIT=1✓）

---

## 十、五能力升级（叙事升级 · 2026-09-02 用户拍板）

> 用户拍板原话：「直接在这个版本里面把它改掉，就叫"注入审计回溯沉淀进化"」。
- 五词定稿：注入·审计·回溯·沉淀·进化；第五能力英文 Distill（📚）
- 语义分工：沉淀=蒸馏知识资产 / 进化=消费资产自动变强（停机测试判据：互不包含）
- 喻体调整：PHILOSOPHY 组织喻体——沉淀=组织记忆（新）；进化=越用越强（原「经验沉淀」让位）
- 拍板批收编：五条悬空项全定谳（导出 27 编号位 / runtime-audit 走 FORGE 产物 / G1 落 v1.4.7 / 权重哈希强制校验 / OpenClaw 4 插件标 private）——决策记录见五能力升级 prompt §〇
- 施工面：23 个施工点（入口 3 + 文档 10 + 分发 3 + 装载 7；⑮ 覆盖 4 个 SVG、②③⑦⑧⑯ 含二轮复核补点），豁免区 11 项登记
- 门禁机械化：五能力断言 + U+FFFD 断言 + GrapHub/FlowHub 断言落 check-docs（坏样本必红必绿自验）
- 门禁：check-docs / check-anchors / check-version 全绿 + 残留扫描零命中（十形态 pattern）
- commit：048173c7（单次提交收编，未 push）

---

## 十一、阶段四 B 类行为锁补测批（37cab2b9 修复批行为锁欠账 · 2026-09-03）

> 阶段四（审查体系合并更新）对 v1.4.4 全量交付做四文档分发核查：十章主体已被 S362-S371 全覆盖（防膨胀「已覆盖不重收」），真缺口 = 37cab2b9 修复批的 9 个行为锁未落 acceptance 场景——修复只改了产品代码没有场景锁，回退不可见。本批补齐。

### B1-B8 补测明细（+9 用例 · 三包全绿）

| # | 锚点 | 用例 | 包 |
|---|---|---|---|
| B1 | 回滚哈希直验 | rollbackWeightsVersion 篡改 v1 必拒 + manifest.current 无副作用 | orchestrator +3 |
| B2 | nodeId 清洗 | 中文 nodeId「电池质检」清洗空回退 'node'（skill name/文件名安全） | orchestrator +1 |
| B3 | YAML 转义 | relations from/to 含冒号 round-trip 完整（yamlScalar 防护面） | orchestrator +1 |
| B4 | corpus CLI scope | 非法 scope exit 1 + 非法值报错（parseCorpusArgs） | audit +1 |
| B5 | switchModel 空判空 | localWeights.dir 清空后提示重新注册不炸 | orchestrator +1 |
| B6 | 源码契约锁 | corpus-export 降级体 auditEvent:null 形态（createRequire 绕过 vi.mock 不可行为测） | mcp +1 |
| B7 | appendVersion model | model 参数写入清单不被覆盖 | orchestrator +1 |
| B8 | 五源样本字段 | fde.enterpriseId 透传 + ruleId 不混入 | audit +2 |

> B2 暴露真实产品 bug：模板路径 `{{nodeId}}` 拿原始值绕过 sanitizeNodeId（37cab2b9 修复只盖了内置默认分支）——修 `fde-quantify.ts` vars 构造处统一清洗（模板与内置默认同一条规则），测试即回归锁。

### 覆盖证据映射（coverage 闭环 · 双层证据形态）

本模块承诺的覆盖证据分两层，均可追溯：

**层一 · 单元/行为锁层（+9 用例 · 四测试文件 · commit 2e8659eb）**——B1-B8 属函数级行为锁，归单元测试层非验收场景层：

| 测试文件 | 用例锚 |
|---|---|
| `engine/orchestrator/src/__tests__/weights-deploy.test.ts` | B1 回滚哈希直验（rollbackWeightsVersion describe）· B5 switchModel 判空 · B7 appendVersion model |
| `engine/orchestrator/src/__tests__/fde-workbench.test.ts` | B2 中文 nodeId 清洗（test_distillDeliverables_中文nodeId清洗）· B3 YAML 冒号 round-trip（test_deriveOntology_特殊字符nodeId的YAML安全转义） |
| `engine/audit/src/__tests__/export.test.ts` | B4 corpus CLI scope（describe parseCorpusArgs：非法显式报错 + 合法三值全过）· B8 五源样本字段（describe 五源样本聚合） |
| `engine/mcp/src/__tests__/corpus-export.test.ts` | B6 源码契约锁（audit 包缺席降级分支 auditEvent 置 null） |

**层二 · 验收场景层**——S365 扩（回滚哈希直验断言入场景）/ S366 压 / S367 归并（见上方「四文档分发」节）；回归面收口为 checklist #129 新维度（19 行四组八锚）。

### 场景号 S364 去向（结构化注记 · coverage 闭环）

**S364（corpus_export 双入口对账）已真实归并入 S348**（v1.4.3 阶段三「训练监控三 MCP tools 注册面——registry 79 + SKILL 对账」已含 MCP 注册锚 + SKILL 锚，双入口对账语义被其完整覆盖）——归并对销 1 处，断言零删减。落痕三处：acceptance-test.sh 场景索引 SSOT 行内注记、S348 场景行归并注记（L3514/L3527）、本节。S364 非起草后误删，章一/章二无未知覆盖缺口。

### 四文档分发与防膨胀处置

- **acceptance**（S365 扩 + S366 压 + S367 归并）：S365 补回滚哈希直验断言；S366 三 grep 并一；S367 CLI 接线锚归并 checklist #129a（check-docs §13 submitCompareJobs 断言更强不重复）——最终 4130 行恰在警戒线内（同版二次上调冻结，压缩而非调线）
- **checklist**（+19 行 #129）：新维度「fresh-eyes 19 项修复防复发——CLI 接线断链 + 供应链回滚路径 + nodeId/YAML 清洗 + 结构性收口」四组八锚，99 维
- **fresh-eyes**（+3 条视角）：视角七「止损路径社工」+ 视角十一「文档声称命令三面 grep 接线验证 + 注释理由须有运行时兜底」
- **数字对账**：测试数 3744→3753 全站同步（README/WIKI/README.en/CHANGELOG/LIMITATIONS×2/ARCHITECTURE×2，check-test-count 11 处全绿）

### 验收

- [x] 三包测试全绿：orchestrator 1703 / audit 978 / mcp 158（+9 用例）
- [x] check-review-system 13 ✅（acceptance 4130 ≤ 4130 · checklist 99 维一致 · 场景 304 一致 · 覆盖零遗漏）
- [x] check-test-count 11 处全绿（TOTAL_TESTS=3753 SSOT 对账）
- [x] check-docs / check-version / check-deps 全绿；U+FFFD 零命中

## 与后续版本的依赖

| 后续版本 | 依赖 v1.4.4 的什么 |
|---------|-------------------|
| v1.4.5 | 语料导出是持续后训练管线的底座；多基座对比是阶段 2 选型数据来源（v1.4.5 已排期——进化模块实证收口前移件，2026-09-01 校正原文「若排期」） |
| v2.x | meta-harness 承接训练任务的多 harness 编排；Dashboard 展示训练报告 |
| **模型层（商业侧）** | ① 训练语料导出 = 训练信号前置 ② 企业专属模型本地权重部署 = 专属模型落地上线 ③ 产物注册衔接 = 训练→部署闭环（train done → eval pass → model_register）④ 多基座对比 = 阶段 2 选型（Qwen3 vs R1-Distill / 8B vs 14B ROI）的引擎化 |

> 📖 行业对标：语料导出对齐合规红线（仅脱敏聚合，不落个体级数据）；多基座对比对齐 Qwen3.8-27B 三方横向对比思路。

---

## 实现纪要（2026-09-03 · 阶段五闸门三轮收敛）

v1.4.4 的阶段五发版闸门经三轮 driver 迭代收敛至 PASS：run-05 揪出六文件未收编（维度 124）+ install.sh 术语漂移（维度 127g「训练引擎」漏改口）+ 维度 127 块级退出码假绿（`|| echo` 吞失败码）——收编批 3b710a10 + 修复批 e58e2638 闭环；run-06 regression 97/97 转绿但 coverage 新 P0（本模块在 300 场景索引零映射）——补 S372 场景批 ea85759d（四测试文件在位锚 + 五代表断言锚）+ 场景数 303→304 全站五处同步 + acceptance 行数守线 4126≤4130（同版二次冻结下压缩不调线：装饰横线对摘除 + 汇总口径三注并一）；run-07 终裁 Conditional PASS——唯一 P0（npm registry 停 1.4.3）经主 session 零信任复验为**发版时序正常态误报**（维度 17 `npm=` 取 `npm view` 在线值，闸门在阶段六 publish 在阶段十「允许滞后一步」为脚本注释明文口径；本地 12 包实测全 1.4.4，bump 006db13c 在祖先链），按裁决书 §六升级条款 checklist 五项全满足改判 PASS（§九收口附注）。P2×12 转校准池不阻塞（含 P2-2 doctor.ts:681 疑硬编码路径——复验 resolveHomeDir 走 sanitizeSofagentHome 白名单 SSOT 入口，非问题关闭）。acceptance 终态 384/384 零跳过（S372 新增后刷新，此前 383/384+环境跳过 1）。

### 测试数演进（v1.4.4 批 +134）

| 批次 | 增量 | 落点 |
|------|------|------|
| 章一~十功能批 | +121 | audit +12（export.test.ts 33 · B8 五源 +2 并入）/ mcp +2（corpus-export 协议面 3 + B6 源码契约锁 1）/ orchestrator +107（weights-deploy 17 · artifact-register · train-compare · fde 模板外置 · S372 场景锚） |
| 章十一行为锁补测批（2e8659eb） | +9 | B1-B8 四测试文件（weights-deploy +3 / fde-workbench +2 / export +1 / corpus-export +1 / 五源样本 +2） |
| 章九 B 批 TDD 修复（d6907083，随 v1.4.3 落盘） | 已含上版 | #2 cost 透传 / #58 IPv6 SSRF / #59 退出码白名单 / #42 audit.md 脱敏——本版仅回归 |
| **终态** | **3619→3753** | 12 包 workspace 口径（test-count.sh TOTAL=3753） |

acceptance 场景 294→304（S362-S371 新增十场景 + 章十一 S372；S364 归并入 S348）；MCP 79→80（corpus_export）。

---

## 发布检查清单（汇总）

> 发版门禁总表——与各章节内「验收标准」是两个层次：验收标准 = 功能层面做没做到；本清单 = 发布层面能不能发版。

### 通用
- [x] `npm run build` exit 0（2026-09-03 实测）
- [x] `npm test` 全绿（3753 tests / 12 包口径，test-count.sh 实测）
- [x] `check-version.sh` 全绿（bump 006db13c 后终验 101/101）
- [x] `check-test-count.sh` 全绿（SSOT 304 场景对账，11 处全绿）
- [x] `acceptance-test.sh` 全绿（**384/384 断言 · EXIT 0**，run-07 driver 快照 + 主 session 独立核验）
- [x] `check-docs.sh` 全绿

### 质量循环（阶段三~五 · 2026-09-03）
- [x] fresh-eyes-loop：四轮审查收尾，findings 全量处置（修复项 → 场景锚点/豁免映射表见第十章）
- [x] release-gate-loop：**run-07 verdict=PASS**——acceptance 384/384 / regression 97 维全过 / coverage 12/13 模块锚点一致；唯一 P0 为发版时序正常态误报（零信任复验改判，裁决书 §六）；P2×12 转校准池不阻塞
- [x] 阶段四审查体系：S365/S366/S367 等闸门批场景并入 acceptance，四份审查文档同步

### 版本号
- [x] SSOT 版本号 bump 1.4.3→1.4.4（commit 006db13c + check-version 101/101）
- [x] 文档头日期统一（2026-09-03 发版日）
- [x] ROADMAP v1.4.4 行「⏳ 待发版」状态保持（tag/npm 发版在阶段十，届时自然消除）
- [x] 发版状态三件套核对（WIKI 状态表 / ROADMAP 现在在哪 / HANDBOOK 速览——三处 v1.4.4 一致）

## Release Notes

> 面向用户的发布说明——GitHub Release body 由此段同源生成（发布动作在阶段十，按 [09-publish.md 5.0 三道工序](../../changelog/releasing/09-publish.md)）。定位句承接 v1.4.3「训练跑得动、找得到问题、出得了交付」：本版让训练信号出得来、部署落得下，后训模块到此闭环。

v1.4.3 让训练跑得起来、坏了知道为什么，本版把训练的**信号**和**部署**接上——语料能导出（训练信号喂给训练管线）、权重能落地（企业专属模型本地部署）、产物能注册（训练完成自动衔接 model_register）、选型有依据（多基座对比出 ROI 排序），后训模块的两端出口打通。

## 🔨 核心变更
### 📡 训练语料导出三件套
- `corpus_export` MCP tool + `sofagent-audit corpus export` CLI（规则 / FDE 方法论 / 带标签样本三件序列化导出，JSON/YAML 双格式 + HMAC 签名 + 27 编号位零遗漏）
- 规则导出附 `reward_hint` 段 + `verifiers.json` 清单（机器可判 22 / 需人审 4 / 启发式 1 三桶）——规则定义升级为可挂进训练循环的 reward 骨架
- 通用脱敏管线（格式/语义/结构三类红名单）+ 合规红线（仅脱敏聚合，不落个体级数据）
### 🏗️ 企业专属模型本地权重部署
- `model_register source: 'local-path'`——权重目录 manifest.json 规范（版本清单 + 逐文件 sha256，无清单/哈希不匹配拒绝注册）
- `model_switch action: 'rollback-weights'` 权重版本级回滚（current 指针回拨，止损不要求人审）
- 端到端链路审计留痕（训练产物 → 注册 → 灰度 → 晋升 → 回滚全程 model_registry 事件链）
### 🔗 训练产物 → 模型注册自动衔接
- train done + eval pass → 自动调 `model_register`（双层幂等 + 哈希强校验）——train → eval → 部署三环节不再断
- 挂载建议（MountSuggestion）——注册器只建议不代执行，晋升强制人审（对齐 v1.3.5）
### 📊 多基座对比训练
- `train compare` 同数据多基座并行提交（同 hash 锚点 + 共享超参模板）
- ROI 排序报告（evalScore÷trainCost，未完成基座不进排序不扭曲结论）+ GPU 显存预算队列（串行/预算并发）
### 🧭 决策因果链与先例检索
- `DecisionLogEntry` 加 `causedBy/causalType` 因果边（拦截→路由、HITL→待审项），HMAC 全字段签名
- `traceDecisionChain` 多级回溯人类可读叙事 + `findSimilarDecisions` 结构化先例匹配（tags×2 + triggeredRule×3，零新依赖）
### 🔒 CI 供应链加固
- 8 个 workflow 全部 SHA 固定（20 处 uses:，防 Action 入侵窃 release token）+ `check-action-pins.sh` 门禁（SHA 与 tag 对账）
- dashboard/本地服务面 fail-closed 自查（默认 127.0.0.1，无密钥 503 拒服务而非静默匿名）
- 完全离线可用（36 图标全内嵌 SVG，CDN 零外链）
### 📚 五能力叙事升级
- 五词定稿：**注入 · 审计 · 回溯 · 沉淀 · 进化**（第五能力英文 Distill；23 施工点 + check-docs 门禁机械化三断言在位）
- 术语「本体结构」全站改「本体数据」（八处活文档，命名一致性一并收口）
### 🔒 BugFix（上版遗留）
- 六轮审查 17 项收编批全量处置：daemon 守护死亡可感知（心跳 + dead 态判定）/ IPv6 SSRF 剥方括号 / 未知退出码 fail-closed / 幽灵旗标幽灵依赖清扫（--reset-chain/--history/js-yaml）/ FDE nodeId 模板路径清洗（B2 暴露真实产品 bug 一并修）
- DSH 内嵌路径 usage 提取修复（三通道归一，内嵌/CLI 双路径端到端同结果）

## ⚠️ 破坏性变更
- MCP 工具数 79→**80**（新增 corpus_export，全量见 `AGENTS.md`「MCP 全量工具表」）
- 决策日志 schema 扩展 `causedBy`/`causalType` 可选字段（旧条目无此字段照常校验，向后兼容）
- install.sh 静态资源部署升级目录同步模式（tools/dashboard/ 整体部署——新增静态资源零改动自动可达）

## ✅ 质量验证
| 检查项 | 结果 |
|------|:--:|
| npm test | 3753 tests 全绿 ✅ |
| acceptance-test | 384/384 断言全绿 ✅ |
| shellcheck | 零 error ✅ |
| check-version | 101/101 全绿 ✅ |
| 回归检查 | 97 维度 ✅ |
| release-gate | verdict=PASS ✅ |
| fresh-eyes | 29 视角审查闭环 ✅ |

📖 [详细开发日志](./v1.4.4.md)


