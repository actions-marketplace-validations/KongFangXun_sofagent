# v1.4.2 开发日志 — 训练引擎 · 数据与评估（管道 + 版本 + eval + 环境 + 报告）+ FDE Harness 层（六引擎 + IM 桥 + DSH 执行深化）

> 📋 **✅ 已交付（已发版 v1.4.2 · 2026-08-28）。** 前置依赖：v1.3.6（训练协议三约定 · B2 决策前移）+ v1.4.1（train-job 编排 + train_job 审计）+ v1.3.1（Benchmark 评测 / LLM Trace）
> 状态：已交付 · 2026-08-27（九大交付块全部落地：训练数据管道/版本/eval 闭环/环境/dry-run/报告六章 + FDE 六引擎 + IM 桥 + FORGE 步零数据流地基与重复率熔断；MCP 工具 67→76，测试 3202→3349 +147）· 此前排期：2026-08-10（v1.4.1-1.4.4 拆分第二版）· 2026-08-25 FDE Harness 层收编 · 2026-08-26 DSH 执行深化收编 + dry-run 算力外推预检（ScaleRL 方法论收编）

> 🔗 **训练 Agent 化（2026-08-23 拍板，详见 v1.4.1）**：本版数据管道/质检是训练 Agent 的决策输入——数据质检由受约束的训练 Agent 把关（质量闸门 = 决策点），多源接入管道走代码。

## 定位

> 🚀 **训练引擎 · 数据与评估**——让训练**吃得到数据、评得出好坏**：企业异构数据进得了训练集（管道 + 版本）、训练连着评估（eval 闭环 + 训练报告）、环境可交付（train env/doctor）。v1.4.1 打了地基（协议+编排+审计+预算），本版让地基上长出"数据与评估"两条腿。

---

## 实现纪要（D1 · 2026-08-28 补建 · 五波次）

| 波次 | 内容 | 落点（commit） | 测试数 |
|------|------|---------------|--------|
| 〇 审查修复批（阶段一） | 四轮 16 视角对话式审查 30 项全修：H-01 commit-msg 三层防线 / G-01 基线显性报错 / G-05 data-paths SSOT / G-07 webhook 脱敏 / H-02 密钥四类 / H-03 空白折叠 / 品牌清零与口径校准 +24 回归用例（详见下方 BugFix 章） | deeeee7b · 2c47cb52 · 9b7adb30 · 6df6cbef · 94add363 · 4c7345c0 · 92b8107e 等 | +24（audit 878→902） |
| 一 FORGE 章九步零 | 数据流地基三债修复 + findings 重复率熔断 repeat-convergence + A 侧收敛指令 | 0b110f8a | 计入 orchestrator |
| 二 IM 桥 + 训练九章 | 章七 IM 桥（dsh-im 指南 + install.sh 分支）；章一~六数据管道/版本/eval 闭环/环境/dry-run/报告（92c2bd9c 章一二 → e821e3df 章三四 → 5836344c 章五六）；章八 FDE 六引擎工作台 | d4a123e1 · 92c2bd9c · e821e3df · 5836344c · a4ffc866 | +147（orchestrator 1295→1442） |
| 三 收尾对账 | devlog 状态转已交付 + 43 验收框全勾 + 测试数七处同步 + SKILL/AGENTS 工具表 67→76 + CHANGELOG/README/ROADMAP 登记 | 9fe47cfb | 门禁四绿 |
| 四 阶段三~五质量循环 | fresh-eyes 16 视角草稿（LLM 双失败走人肉降级路径）→ N-1 修复（11 工具 dataDir SSOT 分叉 b72048b6 + S339）→ 阶段四 A/B/C 收编（#125/#126 + S328-S340 十场景 271→272）→ 阶段五脚本层全绿 + 判断层六轮（run-02 FAIL 三项修复 → run-08 四假 FAIL 改判 → run-14/17 零覆盖补测 S341/S342/S343 → **run-20 verdict=PASS**） | b72048b6 · 9febb5bc · a7d9fb6a · 75f068ba · 1c21ac9f | acceptance 272→276 场景（S341-S343 补测） |
| 五 阶段六文档收尾 | 阶段五移交 4 项收口（AGENTS.md 67→76 / 维度 103 DSH rc 校准 / 维度 23 探测串 / P2-1 定谳）+ devlog 定稿 + bump 1.4.2 + CHANGELOG/ROADMAP/WIKI/HANDBOOK 同步 + README 双语新能力段 | 6871ec5b 起 | 门禁全绿 |

**测试数汇总**：3178（前版基线）+ 24（审查批）+ 147（开发批）= **3349**（workspace 12 包口径）· MCP 67→**76** tools（fde 六件 + 训练三件）· acceptance 场景 271→**276**（阶段三 S328-S340 十场景 + 阶段五 S341-S343 三补测）· 版本窗口 v1.4.1..HEAD 共 55+ commit（含品牌升级/开源脱敏/banner/发版收尾文档批）

---

## 一、企业数据 → 训练集管道

### 痛点

需要专属模型的企业客户常常是「有数据没能力」——电芯充放电曲线 CSV、产线校准参数、工艺配方等**异构数据进不了训练框架**。数据管道是训练引擎的入口。

### 交付

| 组件 | 能力 |
|------|------|
| **异构数据接入** | CSV / Excel / JSON / 文本 → 统一中间格式（列映射 + 类型推断 + 空值处理） |
| **DB / API 数据源** | 企业 ERP / 数据库 / API 数据接入（PostgreSQL / MySQL / REST API）——文件接入之外的第二入口（企业客户数据常在企业系统里，不只文件） |
| **训练集构建** | 中间格式 → instruction / 偏好对（DPO）/ RL 提示集——按算法选型（衔接 v1.4.4 语料导出格式） |
| **数据校验** | 训练集质量闸门：字段完整性 / 样本量 / 标签分布（不达标拒绝提交） |
| **训练入口脱敏** | 客户数据进训练前的语义脱敏——本版先落地最小可用版（见下方注记） |

> ⚠️ **脱敏前向依赖注记**：v1.4.4 通用脱敏管线（redactor）尚未交付——本版先实现训练入口脱敏最小可用版（复用现有审计脱敏能力），v1.4.4 通用管线落地后升级对齐（复用同一套语义脱敏规则，接口预留）。

> 🔗 **商业平台接口预留 G5（2026-08-17 补充）**：本版「DB / API 数据源」是商业产品层存量系统对接（OA/CRM/ERP）的技术基础（商业平台接口规划 §9.1 G5）——商业平台的 MCP 连接器注册/发现（`connector_list` + 第三方连接器注册）挂靠本版扩展：企业系统经 MCP 暴露数据 → 既供训练（本版）也供 workflow 编排（商业平台 §3.2）。**本版先打通「接入」，连接器注册/发现机制随商业平台 SaaS 需要再补**。

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/train/data-ingest.ts` | 新建 | 异构数据 → 中间格式（文件 + DB/API） |
| `engine/orchestrator/src/train/db-source.ts` | 新建 | DB/API 数据源适配（连接串 → 拉取 → 中间格式，只读连接不落库） |
| `engine/orchestrator/src/train/dataset-builder.ts` | 新建 | 中间格式 → 训练集（按算法） |
| `engine/orchestrator/src/train/dataset-validator.ts` | 新建 | 质量闸门 |
| `engine/orchestrator/src/__tests__/dataset-builder.test.ts` | 新建 | 单测（CSV→instruction + DB 拉取 + 质量闸门拦截） |

### 验收标准

- [x] CSV / Excel / **DB / API** 数据 → instruction 训练集（列映射 + 类型推断）
- [x] DPO 偏好对 / RL 提示集两种构建路径可用
- [x] 质量闸门：样本量不足 / 字段缺失 → 结构化错误拒绝
- [x] 训练入口脱敏生效（最小可用版——复用现有审计脱敏能力，v1.4.4 redactor 落地后升级对齐，见上方注记）
- [x] `npm test` 全绿

---

## 二、训练集版本管理（数据可复现）

> 📋 **2026-08-10 补充（架构审查新发现）**：同一份数据反复迭代训练，**数据变了训练结果不可比**——需要训练集 hash + 版本号供 eval 对比时引用。

### 交付

| 组件 | 能力 |
|------|------|
| **训练集版本记录** | 每次数据管道产出训练集 → 记 `dataset_version`（hash + 生成时间 + 样本数 + 配置） |
| **eval 引用版本** | eval 报告记录用的训练集版本（训练前后对比可复现） |
| **版本对比** | 两版训练集差异概览（样本数/分布变化）——判断"数据变了导致分数变了" |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/train/dataset-version.ts` | 新建 | 训练集版本记录 + 对比 |
| `engine/orchestrator/src/train/dataset-builder.ts` | 修改 | 产出时写 dataset_version |

### 验收标准

- [x] 每次训练集产出记 `dataset_version`（hash + 样本数 + 配置）
- [x] eval 报告引用训练集版本（可复现）
- [x] 两版差异概览可用
- [x] `npm test` 全绿

---

## 三、训练中 eval 闭环（不 eval 的训练是盲训）

### 定位

OpenAI `prove` 思想：训练必须连着评估。**机制开源、阈值外部化**（2026-08-10 拍板）：本版提供 eval 运行机制（复用 v1.3.1 Benchmark 评测体系），「什么指标算合格」的验收阈值由商业侧配置（机制是通用工程能力，阈值是部署侧配置项）。

### 交付

| 组件 | 能力 |
|------|------|
| **训练后自动 eval** | train job 完成后自动跑 Benchmark（读 statement/rubric，隔离执行 read-only） |
| **继续 / 停止判定** | eval 分数 vs 目标阈值 → 继续训练（调整超参）或停止（产出权重） |
| **eval 报告** | 训练前后对比（SFT 基线 vs RL 后）——写入 evaluation-log（v1.3.1 格式）+ 引用训练集版本（第二章） |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/train/train-eval-loop.ts` | 新建 | 训练-eval 循环（复用 v1.3.1 case-evaluator） |
| `engine/orchestrator/src/__tests__/train-eval-loop.test.ts` | 新建 | 单测（eval 触发 + 阈值判定） |

### 验收标准

- [x] train job 完成后自动触发 Benchmark eval（read-only 隔离执行）
- [x] 阈值判定 → continue / stop 分支正确
- [x] eval 报告写入 evaluation-log（v1.3.1 格式，HMAC）+ 引用训练集版本
- [x] 阈值配置外部化（商业侧可配，机制开源）
- [x] `npm test` 全绿

---

## 四、训练环境管理（train env / train doctor · 执行面前置）

> 📋 **2026-08-10 补充（架构审查新发现）**：双栈方案假设"服务器上有一套 Python 环境"——但这套环境**谁装、怎么验证、怎么换版本**没有落点。「提供训练管线」交付模式的前提是环境可交付、可检测。

### 交付

| 组件 | 能力 |
|------|------|
| **train env init** | 一键安装训练环境（Python venv + Oumi/verl + PyTorch + CUDA 校验）——脚本化，可打包进设备 |
| **train doctor** | 环境体检：CUDA 可用 / 显存 / 框架版本 / 基座模型缓存——`sofagent train doctor` 输出体检报告（对齐 v1.3.x doctor 模式） |
| **环境版本清单** | `train-env.json`：Python 版本 + 框架版本 + CUDA 版本——train job 记录用的环境版本（训练可复现） |
| **基座模型下载管理** | 基座模型（Qwen3-8B/14B 等，十数 GB）下载 / 版本清单 / 断点续传（对齐 v1.2.5 daemon 推送重试模式）——train doctor 只查缓存，本组件管"没有时怎么拿到" |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/train/env-manager.ts` | 新建 | 环境安装/检测/版本清单 |
| `engine/orchestrator/src/train/model-downloader.ts` | 新建 | 基座模型下载（断点续传 + 版本清单 + 磁盘空间校验） |
| `tools/train-env-init.sh` | 新建 | 一键环境安装脚本 |
| `engine/mcp/src/tools/train-doctor.ts` | 新建 | MCP `train_doctor` tool（环境体检） |

### 验收标准

- [x] `train env init` 一键装好训练环境（venv + 框架 + CUDA 校验）
- [x] `train doctor` 输出体检报告（CUDA/显存/框架/基座缓存）
- [x] **基座模型下载：断点续传 + 版本清单 + 磁盘空间预检**（十几 GB 下载不因断线重来）
- [x] train job 记录环境版本（训练可复现）
- [x] `npm test` 全绿

---

## 五、训练 dry-run 与配置预检（train dry-run · 失败前预防）

### 定位

> 📋 **2026-08-10 补充（首次使用体验）**：长任务训练最怕跑了一半发现配置错（显存不够、数据格式错、学习率发散）。v1.4.1 有 job.json schema 校验（格式对不对），v1.4.3 有 train diagnose（失败后诊断），但**缺失败前的预检**。企业第一次用训练引擎，配置没填对，跑 2 小时崩了才知道哪里错——这个体验直接劝退。diagnose 是「事后诊断」，dry-run 是「事前预防」，两者互补。
>
> 📋 **2026-08-26 补充（算力外推预检 · ScaleRL 论文方法论收编）**：dry-run 还差一个维度——**成本外推**。企业提交训练前最想知道「这次训练大概要花多少算力/钱、值不值」，但训练的真实成本只有跑完才知道。ScaleRL（arxiv 2510.13786，Meta/UT Austin 等，论文公开可复现）给出方法：算力-性能遵循 sigmoid 缩放律，**用几个小规模 pilot run 的数据点拟合曲线，即可外推大规模 run 的性能天花板与算力需求**。这把训练预算控制从「超了暂停」（v1.4.1 预算控制，事后）升级为「投之前先算」（事前）。

### 交付

| 组件 | 能力 |
|------|------|
| **train dry-run** | 用极小数据集（10 条）跑通整条管线（数据→训练→eval），不真的训练——验证管线连通性（数据能读、格式能解析、训练脚本能 spawn、eval 能跑） |
| **显存预检** | batch size × 模型大小 → 估算显存占用，超 GPU 上限提前告警（不是 OOM 崩了才知道） |
| **数据预检** | 训练集提交前快速抽样检查：字段完整性 / 标签分布是否离谱 / 样本量是否达标——复用 v1.4.2 ① 质量闸门的轻量版 |
| **算力外推预检** | `train dry-run --extrapolate`：跑 2-3 个递增规模的 pilot run → 采集（算力, 性能）数据点 → 拟合 sigmoid 缩放律 → 外推目标规模的性能天花板与算力/成本估算，附置信区间（数据点不足时明示「外推置信低」不硬报）。预算控制（v1.4.1）衔接：外推成本超预算 → 提交前告警。参考实现：ScaleRL 官方曲线拟合工具（devvrit.com/scalerl_curve_fitting） |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/train/train-dryrun.ts` | 新建 | dry-run 编排（极小数据集跑通管线 + 显存估算 + 数据抽样 + 算力外推编排） |
| `engine/orchestrator/src/train/scale-curve.ts` | 新建 | 缩放律拟合（sigmoid 参数拟合 + 外推 + 置信区间，数据点不足降级——2026-08-26 补） |
| `engine/mcp/src/tools/train-dryrun.ts` | 新建 | MCP `train_dryrun` tool |
| `engine/orchestrator/src/__tests__/train-dryrun.test.ts` | 新建 | 单测（管线连通 + 显存估算 + 数据预检拦截 + 外推降级） |

### 验收标准

- [x] `train dry-run` 用 10 条数据跑通整条管线（不真训练，验证连通性）
- [x] 显存预检：batch size × 模型大小 → 估算占用，超限告警
- [x] 数据预检：字段缺失 / 标签分布离谱 / 样本量不足 → 结构化告警
- [x] `train dry-run --extrapolate`：pilot run ≥2 个数据点时输出外推（性能天花板 + 算力/成本估算 + 置信区间）；数据点不足明示降级不硬报
- [x] 外推成本超预算 → 提交前告警（预算控制事前化衔接 v1.4.1）
- [x] `npm test` 全绿

---

## 六、训练报告（train report · 客户可读交付物）

> 📋 **2026-08-10 补充（训练引擎完善 · 绩效量化输入）**：训练完成 = 训练数据 + eval 结果 + 产物清单，**应产出一份客户可读的训练报告**——FDE 交付给企业看（对齐 GUIDE §4.3 量化四字段）。

### 交付

| 组件 | 能力 |
|------|------|
| **训练报告生成** | `train report <trainJobId>` → markdown/JSON 报告：数据概况（样本数/来源/脱敏说明）+ 超参 + 训练曲线（loss/reward）+ eval 对比（基线 vs 训后）+ 产物清单（checkpoint/权重路径） |
| **绩效衔接** | 报告含量化四字段输入（当前成本 / AI 后成本 / 年节省 / 回本周期，对齐 GUIDE §4.3）——供绩效量化引擎消费（年节省 = 岗位真实市场年薪 × AI 接管工时占比） |
| **报告归档** | 报告写入 `data/dashboard/train-reports/`——FDE 交付物 + 可追溯 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/orchestrator/src/train/train-report.ts` | 新建 | 训练报告生成（markdown/JSON） |
| `engine/mcp/src/tools/train-report.ts` | 新建 | MCP `train_report` tool |

### 验收标准

- [x] `train report` 生成可读报告（数据/超参/曲线/eval/产物）
- [x] 报告含量化四字段输入（绩效量化衔接）
- [x] 报告归档 `data/dashboard/train-reports/`
- [x] **SKILL.md 登记本版 MCP tools（`train_doctor` / `train_dryrun` / `train_report`）—— check-version.sh 工具数校验通过**
- [x] `npm test` 全绿

---

## 七、IM 桥远程指挥（FDE Harness 层的执行通道 · 2026-08-25 收编）

### 定位

> 无头设备（树莓派 / NAS / 旧电脑 / 服务器）上没有屏幕没有键盘，**远程指挥是唯一操作方式**——FDE Harness 层必须能被 IM 对话驱动（微信 / 钉钉 / 飞书 / 企微）。成熟底座已有现成通道，**不写一行 IM 协议代码**：DSH 侧 `@xmanrui/dsh-im`（9 渠道扫码接入 + AI Office Connector 无公网 IP）+ OpenClaw 侧 `@openclaw-china/channels`（国产 IM 聚合层）。本版做「接入 + 适配 + 安全审计」，把远程指挥变成 FDE Harness 层的标准执行通道。

### 交付

| 组件 | 能力 |
|------|------|
| **IM 桥接入** | 以 DSH `dsh-im` 为主通道（与训练 Agent 走 DSH 为主的架构主线同生态）：扫码接入微信 / 钉钉 / 飞书 / 企微 → 消息驱动 FDE Harness 层（派活 / 查进度 / 审批） |
| **AI Office Connector 适配** | dsh-im 的「本机 Harness 主动连公网 Office」能力（SSE 下行 + 90s 任务租约 + 工具审批进人工面板）——**无公网 IP / 无端口转发**，天然命中无头设备定位 |
| **安全审计** | dsh-im 是第三方社区插件（非 DSH 官方）——接入前代码审计（凭据只进本机存储声明需核实）+ 机器人命令白名单（/new /models /stop 等）+ 可信用户限制 |
| **FDE 指挥面** | 基于 IM 的 FDE 业务命令：派活（梳理/审计/巡检）、查状态、审批（HITL 远程化）——把「语言对话驱动训练/FDE」操作面定位落到 IM |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `docs/guides/im-bridge.md` | 新建 | IM 桥接入指南（dsh-im 安装 / 扫码 / 命令白名单 / 安全边界） |
| `install.sh` | 修改 | 补 IM 桥安装分支（可选，不装不影响核心） |
| 安全审计产物 | 新建 | dsh-im 代码审计报告（凭据处理 / 网络行为 / 权限模型） |

> ⚠️ **E 层预算预声明**：`docs/guides/im-bridge.md` 属 E 运维指南层，当前水位 3484/3600 仅余 116 行——本指南大概率超标，按铁律「超标上调预算不删内容」处理（v1.4.1 先例 3200→3300）：先压缩旧指南腾空间，不足再评估上调 LIMIT_E。

### 验收标准

- [x] DSH 上装 dsh-im 后，微信/钉钉/飞书任一渠道扫码接入成功，消息可驱动 FDE Harness 层（派活 + 查状态）
- [x] AI Office Connector 在本机无公网 IP 环境下连通（SSE 下行 + 租约续期 + 审批面板回调）
- [x] dsh-im 代码审计完成（凭据只进本机存储 + 命令白名单 + 可信用户限制）
- [x] 安全边界写入 `docs/guides/im-bridge.md`（机器人命令 / 权限 / 凭据）
- [x] `npm test` 全绿

---

## 八、FDE 引擎化（方法论 → 可执行引擎 · 2026-08-25 收编）

### 定位

> 🔴 **现状诊断**：FDE 方法论 1173 行在 `FDE/GUIDE.md`，但引擎层只有 `fde_compose` 一个工具真实现（`fde_interview/classify/quantify/derive/distill/deploy` 六 tool 全是「规划中形态」）——**方法论是文字，Agent 读了一本手册，但没有 FDE 工作台**：访谈不落库、判定不计算、量化靠心算、交付靠手搓。本版把 GUIDE 六章方法论变成 6 个可执行引擎，让 FDE 从「.md 驱动」升级为「引擎驱动」。

### 交付（六引擎 · 全部做成 MCP tool）

| # | 引擎 | 方法论来源 | 做什么 | 产物落盘 | 复用底座 |
|---|------|-----------|--------|---------|---------|
| 一 | **fde_interview 访谈引擎** | GUIDE 第二章 五要素深挖 | 访谈引导（追问话术）+ 记录结构化（岗位/环节 × 输入/输出/负责人/耗时/痛点）→ 企业画像自动生成 | `data/fde/<企业>/interview.json` + 企业画像 | 复用 fde_compose 骨架 |
| 二 | **fde_classify 判定引擎** | GUIDE §4.1 三问判定 + §3.2 适配性判断 | 五要素 → 三问规则化判定（🔄 自动 / ⚡ 强化 / 👤 暂不动）+ 六步分解法切最小工作单元 | 节点方案（AI 节点 / Human 节点）`nodes.json` | 复用 classifyAutomation 逻辑 |
| 三 | **fde_quantify 量化引擎** | GUIDE §4.3 量化四字段 | `年节省 = 岗位年薪 × 接管工时占比` 计算器 + ROI 排序 + 量化四字段（当前成本/AI 后成本/年节省/回本周期） | `quantification.json`（喂训练报告与 dashboard） | 复用 v1.4.2 第六章训练报告量化四字段 |
| 四 | **fde_derive 本体推导引擎** | GUIDE 第三章 本体构建 | 五要素 + 访谈 → entity/concept/relations 推导建议（机器初稿 + 人确认） | ontology YAML 草稿 | **复用 ontology_import 完整链路** |
| 五 | **fde_distill 沉淀引擎** | GUIDE 第五章 三层交付 + sustain | 跑通过程 → SKILL.md 模板 + 交付手册自动生成 | 文档层 + Skill 层交付物 | 复用 GUIDE 第五章模板 |
| 六 | **fde_deploy 部署引擎** | GUIDE 第五章 运行层 | 三层交付物 → workflow.yml + 激活部署 | 运行层交付物 | **复用 workflow_submit + activate_workflow** |

### 关键设计决策

1. **六引擎 = fde_* 六 tool 的真实现**——DSH 插件 description 从「桥接单函数」升级回「六 tool 闭环」，叙事与实现终于对齐（2026-08-25 拍板）
2. **FDE 工作台数据层**：`data/fde/` 专属目录（用户拍板新落点）——interview.json + nodes.json + quantification.json + ontology.yml + deliverables/，方法论从「对话里的文字」变成「可审计的结构化资产」（复用审计引擎留痕，train_job 同款模式）
3. **与 IM 桥联动**（本版第七章）：语言对话（IM）→ 驱动六引擎 → FDE 四阶段可远程完成——「语言对话驱动」操作面定位落地

### 涉及文件（预估）

| 文件 | 改动 |
|------|------|
| `engine/mcp/src/tools/fde-interview.ts` 等 6 文件 | 新建（六引擎 MCP tool） |
| `engine/orchestrator/src/fde/` | 新建（引擎核心逻辑 + 数据模型） |
| `engine/orchestrator/src/__tests__/fde-*.test.ts` | 新建（每引擎单测） |
| `FDE/GUIDE.md` | 补「引擎化映射表」（方法论章节 ↔ 引擎 ↔ 产物） |
| `SKILL/SKILL.md` + `AGENTS.md` | 六 tool 登记（fde_* 六件 +6，全站文档同步——本版工具数终态 67→76 = fde 六件 +6 / 第四/五/六章训练侧三件 +3，详见本章验收口径） |

### 验收标准

- [x] 五要素访谈 → 结构化落盘 `data/fde/`（interview.json 可回读）
- [x] 三问判定 → 节点方案（🔄/⚡/👤 三态 + 最小工作单元切分）
- [x] 量化计算 → ROI 排序（喂训练报告量化四字段）
- [x] 本体推导 → ontology 草稿可导入 ontology_import
- [x] 三层交付 → workflow 可 submit + activate（复用现有链路）
- [x] 全程审计留痕（六引擎调用记 train_job 同款事件）
- [x] **SKILL.md 登记六 tool——本版九件 tool 全部登记完成后，check-version.sh 工具数校验通过（终态 67→76：本章 fde 六件 +6，第四/五/六章训练侧三件 `train_doctor` / `train_dryrun` / `train_report` +3）**
- [x] `npm test` 全绿

---

## 九、DSH 执行深化（FORGE loop 全量切 DSH · 2026-08-26 排期）

> 前置已交付（v1.4.1，commit 2fb524c7）：**工具注入**——`registerSofagentTools()` 把 sofagent 自定义工具（gate-tools / FORGE worker 工具面）经 `ctx.get('tools').register()` 挂进 DSH 会话全局层，端到端实测 agent 在会话内真实调用注入工具。这是本块三步的接线地基。
>
> **双轴目标（run-01 性能评估拍板）**：① **性能**——run-01 实测墙钟耗时中相当比例耗在非模型环节（轮询等待 + 串行长尾）；步一/步二落地后墙钟压向 LLM 耗时等值。② **稳定一次走完**——run-01 人工停轮的根因是结构性不收敛（worktree 钉死基线 + b-fix 写不回），跑满 max-rounds 也是在陈旧快照上重复发现；步零修复数据流地基 + 重复率熔断让机器自判停轮，不再依赖人工盯。

| 步 | 内容 | 验收 |
|---|------|------|
| 零、数据流地基（稳定性前置） | 三债修复：① worktree 逐轮 re-sync 主仓库新 commit（消除「钉死基线」——修复收编后下一轮基于新基线审）② b-fix sandbox workspace 对准 worktree（消除「修复落不回基线」——run-01 round-03+ 全在陈旧快照上空转的根因）③ summary-batch 中间产物落 runDir（消除主仓库残留）。**findings 重复率熔断进 driver**：相邻轮同 finding（文件+问题指纹）重复率超阈值 → 自动停轮 + verdict 标注 `repeat-convergence`——结构性不收敛由机器自判。注：#13 加权收敛（detectWeightedConvergence）管「发现数降下来」，管不了「修复没生效」，两维度互补不替代 | 同 finding 跨轮重复出现时 run 自动停轮报警；修复收编后下一轮 worktree 基线同步；主仓库全程无 loop 中间产物残留 |
| 一、流式面重建 | 消费 `session.events`（tool/call、tool/result、assistant/message）重建 LangGraph streamHandler 三职责：报告捕获（gotReport 即收工）/ 软硬熔断判定 / 逐步 usage 记账；`dsh-events.mjs` 事件订阅从骨架转正（notify_session 联调，消除 120s 轮询盲区） | fresh-eyes 审查 worker 在 DSH 后端下产出与 langgraph 后端等深的报告（工具预算消耗、熔断行为对齐） |
| 二、审查类 step 分级切 | 按流式依赖从弱到强：a-verify（有限任务）→ a-consolidate（长输出）→ check worker（流式依赖最重）；check 切换前必须双后端镜像验证（同 prompt 两后端跑，findings 一致性比对——v1.3.9 backend-ab 方法论复用） | `resolveFreshEyesBackend` 全 step 走 dsh，langgraph 降级链保留且产物不丢 |
| 三、治理面收口 | usage 自动计量（DSH 运行时级 token 数据接 usage.jsonl——「driver 逐步手记」升级为运行时自动记；run-01 实测全部调用 usage 缺失，成本盲拍）；rc→正式版守卫解除演练（DSH 正式版发布后守卫自动放行零代码变更） | usage.jsonl 记账零手记；release-gate-driver 同步受益 |

> 📌 **2026-08-28 拍板**：步一~三正式移交 v1.4.3 承接（登记见 [v1.4.3 开发日志第六章](./v1.4.3.md#六forge-dsh-执行深化步一三自-v142-拍板移交--2026-08-28)），本版实交付止于步零三债 + 重复率熔断 + A 侧收敛指令。

**性能辅助项（随步一顺带，零成本）**：A 侧 prompt 追加「先写报告骨架、再探查验证」收敛指令——run-01 实测 A 侧碎片率持续高位（多数 worker 撞硬熔断，次轮才减半），根因是探查行为与预算错配（预算耗尽前来不及写报告），prompt 层把行为从「探查完预算尽」掰到「报告保底」。

**既有守护确认够用（不新增）**：daemon 模式（detached 自脱离）+ watch 主管（心跳停 → death-audit 审计死因 → 自动 resume）+ 断点续跑（completedWorkers 跳过已完成 worker）+ 连续降级守卫（连续 2 轮 isDegraded → 优雅退出）——「稳定走完」的缺口不在守护面在数据流（步零），守护把无意义的轮跑满恰是结构性不收敛的症状。

**后续版本观察项（本版不做，等数据）**：模型分层（check worker 切低成本档、consolidate/b-fix 留强模型——待步三 usage 计量上线后按真实 token 账拍板）；增量审查（round-02+ 只扫上轮基线→当前 HEAD diff，findings 收敛后全量 24 视角重扫属浪费）；并发 1→2（FORGE_MAX_CONCURRENCY 放宽前先实测 DSH 单 session 内存曲线——8GB 机器 OOM 前科）。

**红线（不动）**：编排层永远不切（LangGraph StateGraph DAG = sofagent 自有面，2026-08-23 拍板「LangGraph 做编排，DSH 做执行」）；每步带降级验证（DSH 路径异常 fallback LangGraph 且产物不丢）；并发内存先实测（24 check worker 若各自 boot ctx，须单 worker 内存曲线外推——8GB 机器 OOM 前科）。

**已知待修（端到端实测发现）**：`summarizeSession` 提取 assistant 文本为空（事件结构字段与预期不符）——步一「流式面重建」的输入，不是阻塞项。

## 十、BugFix 批（阶段一 · 四轮对话式审查 30 项 · 2026-08-27）

> **来源**：四轮 fresh-eyes 对话式独立审查（16 视角 × 4：主会话单跑 → 8 并行子代理 → 四组终审+零信任复核 → 对话式取证，依据 `FORGE/playbook/fresh-eyes-review.md`）+ 第五轮对 prompt 自身零信任复核。产出桌面 `v1.4.2-bugfix-prompt.md`（30 项 = F-01~07 + G-01~10 + H-01~09 + J-01~04 + P2 批），全数修复收编，零信任复验通过。

| 批次 | commit | 修复内容 | 回归用例 |
|------|--------|---------|---------|
| 文档口径与声称校准 | 92b8107e · 4c7345c0 | 对外声称校准（双语 Hook 差异/经验沉淀收窄/加密双态）+ 测试增量分解定案 2937→3178 + ROADMAP 去重/包数 + 安装写入面如实 + 内部批注清理 + 约束层术语统一 | — |
| commit 链完整性（H-01 P0） | deeeee7b · 2c47cb52 | commit-msg reset fail-loud + post-commit HEAD tree 对账兜底 + **pre-commit 主防线补装**（三层防线：staged 清理前置到 commit 对象生成前） | init.test +6 |
| 密钥与脱敏（H-02/H-03/G-07） | 9b7adb30 · 6df6cbef | A2 密钥盲区补 4 类（Google/Slack/JWT/AWS Secret + contextKeyword 二次判定）+ A9 normalizeLine 空白折叠 + webhook payload.task 过 redactDetail | a2 +8 · a9 +4 · webhook +3 |
| SSOT 与品牌（G-05/G-06/F-01/F-02） | 94add363 | data-paths 收编 getDataDir（五处硬编码回退改顶层 import）+ env.ts 头注释如实化 + FDE Agent 品牌清零 | — |
| dist 完整性（G-01） | 6df6cbef | doctor 基线缺失从「首跑自动记录」改显性 fail + `--baseline` 子命令 + SECURITY.md 基线建立指引 | doctor-reset 4 用例更新 |
| 空 catch 降级注释（J-P2h） | 94add363 · 9b7adb30 | 10 处空 catch 补降级理由注释 | — |
| 数字纪律收尾 | efcf48c5 · 9924a9f3 | +24 后全站声称值同步（3178→3202 / 878→902 九处）+ LIMITATIONS 归因勘误（+24 全部为 audit 包 878→902） | 门禁 15/15 |

**合计 +24 回归用例**（audit 包 878→902），已计入 3202→3349 增量链。误报/关闭项（G-10 战略项 / G-02 版本三态已由 9fb9fc0d 前置收编等）见 bugfix-prompt 第六节，不重开。

## 发布检查清单（汇总）

> 阶段三代码审核产物——九章验收标准（43 框）+ 通用门禁 + 质量循环的打勾汇总。逐项核对记录，PASS 才进阶段四。

### 通用
- [x] `npm run build` exit 0（12 包 + 根 tsc，orchestrator/mcp rebuild 复验）
- [x] `npm test` 全绿（3349 测试 / 12 包口径，test-count.sh 15/15 实测）
- [x] `check-version.sh` 全绿（bump 1.4.2 后终验，见步骤六）
- [x] `check-test-count.sh` 全绿（SSOT 276 场景三处对账）
- [x] `check-docs.sh` 全绿（E 层 3300→3600 铁律上调登记 + 76 tools registry 一致）
- [x] `acceptance-test.sh` 全绿（**353/353 EXIT=0**，含 S333-S343 十一个 v1.4.2 新场景；阶段五判断层 run-20 快照 351/351 + 主 session 独立 grep 实证）

### 质量循环（阶段三~五 · 2026-08-28）
- [x] fresh-eyes 草稿：`~/Desktop/fresh-eyes-draft-v1.4.2.md`（16 视角齐全）——LLM 生成两次失败走 SOP 降级路径（人肉执行 + 定点取证：node 实测 scale-curve/CSV/audit 链 + grep 实证 76 tools/3349/272）。发现 **N-1（P1）已修**：11 个 MCP 工具 dataDir 兜底与 G-05 SSOT 分叉（commit b72048b6，含 S339 行为锁）；P2×3 观察项（千分位/前导零类型推断边界 + fde-audit 双序列化器既有设计记录）
- [x] 阶段三 driver 判定：**跳过**——待取证项 0（≤3 判据满足），且九大交付块主体已经阶段一四轮对话式审查覆盖（30 项修复收编），阶段三增量仅修复批自身新代码，草稿 + 定点取证闭环
- [x] 阶段四审查体系收编：#125（九大交付一压维度）+ #126（hook 端到端）并入 checklist；S328-S340 十场景并入 acceptance（271→272，历史口径）+ 警戒线上调 3450→3490→3560（同版二次上调，真实补测解锁冻结）
- [x] 阶段五 release-gate：**run-20 verdict=PASS**（regression 94 维 0FAIL · coverage 14 模块 0 零覆盖 · acceptance 351/351 注入）——六轮收敛：run-02 FAIL 三项（bootstrap 注释行数/pre-commit hook 装回/worker 时段速死）→ run-05 GLM 3 倍价时段 stall 熔断 → run-08 四假 FAIL 改判（precheck 快照 vs 并行 commit 时间差）→ run-14 train report 零覆盖补 S341 → run-17 两模块零覆盖补 S342/S343 → run-20 PASS。主 session 零信任复验三件套全过（verdict 判定行/stepErrors 空/无 f-* 产物）
- [x] 修复分工：草稿发现（loop worker）→ 修复（独立新 session：S342/S343 补测 / 主 session：S341 增量场景属阶段四职责）→ 复验（主 session 零信任三件套）——三角色分离对齐 03/05 SOP session 分工拍板

### 版本号
- [x] SSOT 版本号 bump 1.4.1→1.4.2（13 类位置，check-version 终验）
- [x] 文档头日期统一（2026-08-28 发版日）
- [x] ROADMAP v1.4.2 行「已交付」+ v1.4.3 承接登记（步一~三拍板移交）
- [x] CHANGELOG.md 索引行算术自洽 3202+147=3349
- [x] 发版状态三件套（WIKI 状态表+尾注 / ROADMAP 现在在哪 / HANDBOOK 速览）版本号一致

## Release Notes

> 面向用户的发布说明——GitHub Release body 由此段同源生成（发布动作在阶段十，按 [09-publish.md 5.0 三道工序](../../changelog/releasing/09-publish.md)）。定位句承接 v1.4.1 记录在案的训练协议与编排地基：本版让训练**吃得到数据、评得出好坏**。

v1.4.1 给训练引擎打了地基（协议+编排+审计+预算），本版让地基上长出数据与评估两条腿——企业异构数据经管道进训练集，训练连着评估，报告直接可交付。

## 🔨 核心变更
### 📊 训练数据管道与版本
- 企业数据 → 训练集管道（`train_dataset_ingest` 等 MCP tool，CSV 类型推断端到端）
- 训练集版本管理（dataset_version 台账三件套：指纹冻结+不可变+续跑版本锁）
### 🎯 训练评估闭环
- 训练中 eval 闭环（`train_eval_submit`，阈值判定双态，不 eval 的训练是盲训）
- 训练 dry-run 显存估算（单调性外推 + ScaleRL sigmoid 拟合建议，失败前预防）
- 训练报告 `train_report`（客户可读 Markdown+JSON 双产物归档，量化四字段 ROI）
### 🛠️ 训练环境与运维
- train env / train doctor（执行面前置，doctor 基线显性 fail + `--baseline` 子命令）
- 训练异常退出资源回收四步链（S330 行为锁）
### 🧩 FDE 六引擎工作台
- 方法论 → 可执行引擎：`fde_interview` / `fde_classify` / `fde_quantify` / `fde_derive` / `fde_distill` / `fde_deploy`（全部 MCP tool 化）
### 📡 IM 桥远程指挥
- dsh-im 指南 + install.sh 可选分支（默认关/失败不阻断/不代装 DSH 三纪律）——无头设备 FDE 标准执行通道
### 🔒 BugFix（上版遗留）
- 四轮 16 视角审查 30 项全修：commit-msg 三层防线（P0）/ A2 密钥盲区补四类 / dataDir SSOT 收编 11 处 / webhook 脱敏 / 空白折叠

## ⚠️ 破坏性变更
- MCP 工具数 67→**76**（新增 fde 六件 + 训练三件，全量见 `AGENTS.md`「MCP 全量工具表」）
- doctor 基线缺失从「首跑自动记录」改为显性报错（须 `--baseline` 显式建立）

## ✅ 质量验证
| 检查项 | 结果 |
|------|:--:|
| npm test | 3349 tests 全绿 ✅ |
| acceptance-test | 353/353 场景全绿 ✅ |
| shellcheck | 零 error ✅ |
| check-version | 全绿 ✅ |
| 回归检查 | 94 维度 ✅ |
| release-gate | verdict=PASS ✅ |
| fresh-eyes | 16 视角审查闭环 ✅ |

📖 [详细开发日志](./v1.4.2.md)

## 与后续版本的依赖

| 后续版本 | 依赖 v1.4.2 的什么 |
|---------|-------------------|
| **v1.4.3 运行与需求** | 训练报告提供失败诊断上下文；GPU 队列消费训练集版本（算力调度） |
| **v1.4.4 信号与部署** | 语料导出复用训练集版本（样本溯源）；训练报告作为企业交付物 |
| **模型层（商业侧）** | ① 数据管道 = 「有数据没能力」客户的数据入口 ② eval 闭环（机制开源/阈值外部化）= 定制 eval 的对接点 ③ 训练报告 = 绩效量化输入 + FDE 交付物 ④ train env/doctor = 「提供训练管线」交付模式的环境前提 |
| **FDE Harness 层（产品侧）** | IM 桥远程指挥 = 无头设备 FDE 的标准执行通道——后续 FDE 引擎化（v1.4.2 第八章）以 IM 为操作面，语言对话驱动 FDE 四阶段 |

> 📖 行业对标：eval 闭环对齐 OpenAI `prove`（为每个客户建定制 eval）；训练报告对齐 FDE GUIDE §4.3 量化四字段；IM 桥对齐 dsh-im（DSH 社区）/ OpenClaw-China channels（国产 IM 聚合）。
