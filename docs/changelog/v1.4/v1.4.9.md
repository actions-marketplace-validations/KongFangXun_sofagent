# v1.4.9 开发日志 — 设备接入与数据承接版（多设备 Harness 中间层 + router 伴生数据面）

> 🗒️ **状态注记（2026-09-18 补记 · 同日更新）**：本版 T8 敏感识别三层插槽（第八章）与 T9 权重灰度 AB（第九章）当前交付形态为 SDK 面先行——导出符号与测试就绪，生产管线接线排期 v1.5.1（原承诺 v1.5.0，开发批收口未含，2026-09-18 用户拍板顺延）；正文叙述保留时点原貌，不回改。
> 📋 **已发版（2026-09-17）。** 前置依赖：v1.3.1（Agent 身份码 Ed25519）+ v1.3.8（daemon 健康心跳：`daemon-health.ts` 的 `writeHealthFile('heartbeat')` + `cli.ts` 心跳计时器）+ v1.4.4（通用脱敏管线）+ v1.4.6（分拣闸门 + 标准数据推送接口）+ v1.4.7（G6 可见性 / G7 租户路径 / G8 cron 模板 / 第十一章云执行回流事件通道）
> ✅ **已实现。** 十六章验收全勾；发版闸门 run-04 verdict = CONDITIONAL PASS（0 P0 / 4 P1 / 9 P2，RC-1/RC-2 已闭环，RC-3/RC-5 阶段九 tag 前收口）；验收证据见各章「验收标准」与「发版闸门场景锚定登记」段。
> 交付：设备接入与数据承接（G9/G10/G11 + G5b/G1 + T6/T7/T8/T9/T10 五批）· **4805 tests across 13 packages 全绿**（13 包 workspace 口径；插件用例走根 `npm test --workspaces` 执行不计入；账链见附录「测试账」）
> 状态：✅ 已发版 · 2026-09-17 定稿（2026-09-01 排期 · 2026-09-08 主题升格 + 三章排入 · 2026-09-12 拍板 G5b/G1 迁入收口 · 2026-09-14 开发启动 · 2026-09-16 发版闸门 CONDITIONAL PASS）；原排期叙事留档：已排期 · 2026-09-01（商业平台多设备数据协同的引擎侧载体 · G9/G10/G11 接口预留）· 2026-09-08 主题升格 + 三章排入（用户拍板——一体机数据飞轮整链在 v1.5.0 前成型）：**第七章 session 承接与 router 伴生**（自原 v1.5.3 前移改写——router 基于开源自建非第三方，引擎不自研 router 本体、交付伴生 exporter 协议；含多轮 chat 样本模型 / 蒸馏偏好对构造 / 工作产物采集六源）、**第八章 敏感识别升级**（自原 v1.5.3 前移 + NER 外挂协议新编排——检测器插槽架构，分类器与脱敏共用）、**第九章 权重灰度发布 AB**（自原 v1.5.3 前移）——原 v1.5.3 收窄为路由与验证版 · 2026-09-12 拍板：**第十一章 连接器注册/发现（G5b）+ 第十二章 workflow 模板导出/导入 + 血缘（G1）** 两条自商业平台接口表迁入本版——原标 v1.4.8 / v1.4.7 承载，逐版对账确认未随版落地且开发日志未收录，属掉出版本序列，整体迁移收口

## 定位

> 商业产品层的多设备数据协同诉求：企业数据散落在**个人电脑（BYOD）**、**AI 节点设备**（Mac Mini 常驻）、**AI 模型一体机**（内置数据）上，平台要从这些设备读取数据做协同。**平台不直连设备**——设备侧必须有约束层承接：身份、授权、脱敏、审计、计量。本版把引擎层定位为多设备 Harness 中间层（承上启下：设备数据经治理后上平台，平台任务经治理后下设备）。
>
> **与 v1.4.7 平台接口版的关系**：v1.4.7 管「平台要的数据」（缺口/绩效/可见性/租户/账单——面向平台消费），v1.4.9 管「设备给的数据」（注册/心跳/授权读取/主动上行——面向设备供给）。两版合起来构成完整的平台-引擎-设备三层协同。
>
> **数据承接扩展**（2026-09-08 拍板）：一体机场景的第三类数据源——**模型网关 router**（基于开源项目自建，挡在千问等基座模型前面，过站流量含全部 session 上下文）。router 本体不在引擎开发范围（引擎内部另有 model-router.ts 调用侧路由，两者不冲突——一个是设备外置网关，一个是引擎内部路由），引擎交付**伴生 exporter 协议**：router 侧部署轻量 exporter，把过站 session/usage/路由决策按标准 schema 推给引擎，引擎承接后分拣→脱敏→数据集→蒸馏回流。至此设备数据三源齐备：G10 拉数据（平台发起）+ G11 推数据（设备发起）+ session 承接（router 伴生）。

| 设备类型 | 形态 | 引擎侧角色 |
|---------|------|-----------|
| **个人电脑（BYOD）** | 装用户侧 Agent（DSH / WorkBuddy），经 CLI + MCP 协同 | 任务执行端 + 数据源（经 G10 授权白名单读取） |
| **AI 节点设备** | Mac Mini 常驻一起跑 | 常驻执行节点：daemon 巡检 + 首部署 cron（G8）持续产出 + 计量出账 |
| **AI 模型一体机** | 内置模型 + 数据的盒子 | 本地推理 + 本地数据面（数据不出设备，只有脱敏后的结果/结论上行） |

## 一、设备注册/发现与心跳（G9 · MCP `device_register` / `device_list`）

### 定位

> 设备接入的第一步：谁在线、能干什么、健康吗。设备身份复用 v1.3.1 Agent 身份码 Ed25519（设备 = 一个 Agent 实例，身份体系零新建），健康复用 v1.3.8 daemon 健康心跳机制（`engine/daemon/src/daemon-health.ts` 的 `writeHealthFile('heartbeat')` + `engine/daemon/src/cli.ts` 心跳计时器，落 `daemon-health.json`）。**注意**：FORGE 工具链的 `--check-alive` 是 release-gate / fresh-eyes driver 的 liveness 探针，属内部自迭代工具链（非产品能力），设备心跳不可复用它。

### 交付

| 组件 | 能力 |
|------|------|
| **`device_register` MCP tool** | 设备上线注册：身份码验签（fail-closed）+ 设备类型（pc / node / appliance）+ 能力声明（挂载的 MCP / skill / 数据源清单） |
| **`device_list` MCP tool** | 设备清单查询：按租户/类型/能力过滤，含最后心跳时间与在线状态 |
| **心跳上报** | daemon 心跳定期上报（在 v1.3.8 `daemon-health.ts` 心跳落盘基础上扩展为网络上报），超时判定离线 |
| **离线告警** | 离线判定触发 webhook 推送（复用 daemon 推送通道） |

### 涉及文件（预估 · 交付实况见下方「交付实况」节）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/mcp/src/tools/device-register.ts` | 新建 | `device_register` tool（✅ 已交付 `184c0404`） |
| `engine/mcp/src/tools/device-list.ts` | 新建 | `device_list` tool（✅ 已交付 `184c0404`） |
| `engine/daemon/src/device-registry.ts` | 新建 | 设备注册表（身份 + 能力 + 心跳状态，HMAC 留痕）（✅ 已交付 `b2782ae3`，含 T11/T12 扩展） |
| `engine/daemon/src/__tests__/device-registry.test.ts` | 新建 | 单测（验签 fail-closed / 心跳超时 / 能力声明边界）（✅ 已交付 `b2782ae3`，35 用例） |

### 验收标准

- [x] 设备注册后 `device_list` 可查（身份/类型/能力/最后心跳/在线状态）——`b2782ae3`：`device_list` 经 daemon `listDevices`（在线态 `isOnline` 实时判定；测试「心跳更新新鲜度」「tenant/kind/capability 过滤」）
- [x] 伪造身份码注册被拒（fail-closed）——`b2782ae3`：`registerDevice` 验签 `verifyAgentIdentity` 不通过一律拒绝 + **拒绝动作本身留痕进事件链**（测试「伪造签名 → 拒绝且留审计」「篡改 publicKey」）
- [x] 心跳超时判离线并触发 webhook 告警——`b2782ae3`：`scanOfflineDevices` 扫描 + `onAlarm` 回调（cli.ts 接线 `createWebhookPusher` 复用既有 push 通道；测试「心跳超时 → 转离线并触发 webhook 告警」注入回调断言）
- [x] 注册表 HMAC 留痕可追溯——`b2782ae3`：事件链 `<dataDir>/audit/device-events.jsonl` prevHash 链 + hmacSig（密钥 core `getHmacKey` 同源），`verifyDeviceEventsChain` 读侧验链；测试含**篡改 summary → content-hash-mismatch** 与**删中间行 → prev-hash-mismatch** 两组故障注入
- [x] 设备清单只含已验签设备——测试「清单只含已验签设备（被拒设备不出现在清单）」：伪造签名设备注册被拒后不出现在 `device_list`
- [x] `/health` 返回结构化快照且三态判定可注入测试（单模型不可达 → degraded 而非 down）+ loopback 默认绑定——`b2782ae3`：`buildHealthVerdict` 纯函数（5 用例：全 ok→healthy / 单模型 dead→degraded / 核心 engine:* dead→dead / 空 checks fail-closed→dead / 单项 degraded→degraded）+ `startHealthEndpoint` 默认绑定 127.0.0.1:8787（`SOFAGENT_HEALTH_PORT` 显式配置才启用，cli.ts 接线）

### 交付实况（2026-09-15）

| 文件 | 改动 | 实况 |
|------|------|------|
| `engine/daemon/src/device-registry.ts` | 新建 | 注册表（身份+能力+心跳+租户+吊销位）· `isOnline` 纯函数 · `registerDevice`/`gateDevice`/`listDevices` · `reportHeartbeat`（响应体带 `pendingTasks` 捎带——T11）· `scanOfflineDevices`（离线告警）· `enqueueDeviceTask`/`claimDeviceTask`（T11 入队/回执）· `reassignOrHold`（T12 改派/挂起）· `appendDeviceEvent`/`verifyDeviceEventsChain`（HMAC 链）· 落地 commit `b2782ae3` |
| `engine/daemon/src/health-endpoint.ts` | 新建 | `/health` 三态端点：`buildHealthVerdict` 判定纯函数 + `collectHealthChecks`（engine/audit-chain 真探测 + models 注入面）+ `startHealthEndpoint`（loopback 默认）· `b2782ae3` |
| `engine/mcp/src/tools/device-register.ts` | 新建 | `device_register` MCP tool（lazy import `@sofagent/daemon` optionalDependencies——daemon-status 同款）· `184c0404` |
| `engine/mcp/src/tools/device-list.ts` | 新建 | `device_list` MCP tool（只读清单+在线态）· `184c0404` |
| `engine/daemon/src/__tests__/device-registry.test.ts` | 新建 | **35 用例一文件全覆盖**：isOnline 边界 / 注册验签 fail-closed×4 / 闸门×4 / 心跳新鲜度 / 离线告警×3 / 清单过滤×2 / HMAC 链含 2 组故障注入 / T11 捎带+回执×2 / T12 三态 / 健康三态×5 · `b2782ae3` |
| `engine/mcp/src/__tests__/device-tools.test.ts` | 新建 | 6 用例：[sofagent] 前缀 / 真实注册链路 / 伪造拒绝透传+留痕 / 清单过滤 / TOOLS 接线存在性（铁律 9）/ 幂等边界 · `184c0404` |
| `engine/mcp/src/tool-registry.ts` | 修改 | TOOLS 数组 +2（`device_register`/`device_list`，roles `['ops']`——与 `daemon_status` 同口径）；95→97 · `184c0404` |
| `engine/daemon/src/index.ts` | 修改 | `@public` 导出面补注册表 14 函数 + 端点 3 函数（MCP tools 经此桥接）· `b2782ae3` |
| `engine/daemon/src/cli.ts` | 修改 | 生产调用点（铁律 9）：daemon start 接线设备离线巡检（5min 扫描 + webhook 推送）+ `/health` 端点（env 门控）· `b2782ae3` |

**测试数联动**：daemon 包 +35、mcp 包 +6（全仓基线 4494→4535，以 `tools/check/test-count.sh` 当轮实测为准）。**工具数联动**：95→97 全 14 处落点同批同步（`39cd5d65`，含五域图 D3 20→22 重算）。

### 扩展：设备侧健康巡检端点（商业平台机制对账 · 2026-09-08 排入）

> 📋 **背景**：商业平台机制文档 §9.2 安全表定义了「设备侧 AI 健康巡检协议」（对标 wemux health 端点），引擎侧零排期——随 G9（设备健康主题章）一并落地。心跳只回答「活着吗」，健康巡检回答「活得怎么样」。

- **`/health` 标准端点**：设备 daemon 暴露本地 HTTP 健康巡检端点（默认仅 loopback，绑定地址可配置）——返回结构化健康快照：引擎版本 / 模型可用性（注册表内逐模型可达探测）/ 磁盘水位 / 审计链尾部 HMAC 校验 / daemon 自身进程指标
- **巡检项分层**：`status: ok / degraded / down` 三态汇总 + 逐项明细（单项降级不整体误报 down）
- **与心跳的关系**：心跳（G9 主交付）携带健康摘要字段引用巡检结果——心跳管在线状态，巡检管健康状态，两者不合并
- **验收追加（✅ 已交付 `b2782ae3`，验收归本章——T10 的 /health 项重复列示，两处任一实现即可）**：`/health` 返回结构化快照且三态判定可注入测试（单模型不可达 → degraded 而非 down）；loopback 默认绑定（外部不可达默认安全）

## 二、设备侧数据面授权读取（G10 · 数据目录白名单 + `device_data_query`）

### 定位

> 设备上的数据怎么「被授权地」给平台——G6 visibility 管 workflow 节点级可见性，G10 管设备数据目录级授权。**默认空白名单 = 全不暴露**（opt-in 模型）；读取全程过审计模块，这是「平台直连设备裸奔 vs 过约束层治理」的分界线。

### 交付

| 组件 | 能力 |
|------|------|
| **数据目录白名单** | 设备配置声明可暴露的数据目录/数据源（config.yml 数据面清单，默认空 = 全不暴露，opt-in 授权） |
| **`device_data_query` MCP tool** | 按白名单 + 请求方授权范围读取，结构化返回（不落原始路径） |
| **脱敏联动** | 敏感字段出设备前经 v1.4.4 通用脱敏管线处理 |
| **审计留痕** | 每次读取进 audit history（谁/何时/读了什么/依据什么授权）——HMAC 链天然覆盖 |
| **计量进账** | 读取动作进 worklog + cost（商业平台计量计费数据源） |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/core/src/device-data-policy.ts` | 新建 | 数据面白名单解析 + 授权判定（默认空 = 全拒） |
| `engine/mcp/src/tools/device-data-query.ts` | 新建 | `device_data_query` tool（白名单校验 → 脱敏 → 返回） |
| `engine/core/src/__tests__/device-data-policy.test.ts` | 新建 | 单测（空白名单全拒 / 白名单边界 / 授权越界拒绝） |

### 验收标准（✅ 已交付，见下交付实况表）

- [x] 空白名单设备对任何读取请求返回拒绝（fail-closed）——core 策略无配置=empty-policy 全拒 + mcp 面透传（测试覆盖）
- [x] 白名单内路径可读、白名单外路径拒绝——边界字符匹配防 /data-secret 借 /data 前缀绕过
- [x] 返回内容经脱敏管线（敏感字段不出设备）——redact 管线 + verifyNoLeak 实体名 0 命中双保险
- [x] 每次读取在 audit history 有完整留痕——emitDecision（ARTIFACT_EDIT，HMAC 链）含授权依据 allowedDir
- [x] 读取计量进 worklog/cost——decision-log 进 worklog 聚合三源，evidence 含 bytes/redactHits

### 交付实况（2026-09-14 批 2）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/core/src/device-data-policy.ts` | 新建 | 白名单解析 + authorizeDeviceRead（fail-closed：损坏配置按空策略全拒不静默）· `de03cdb5` |
| `engine/mcp/src/tools/device-data-query.ts` | 新建 | `device_data_query`：门禁→白名单→读取→脱敏→verifyNoLeak 自检→emitDecision 审计计量（返回体不落原始路径）· `bc7b71eb` |
| `engine/core/src/__tests__/device-data-policy.test.ts` | 新建 | 19 用例：isPathAllowed 边界×8（含前缀攻击面）/ 空策略全拒 / 损坏故障注入×4 / 往返 / 0600 权限 · `bc7b71eb` |

**测试数联动**：core +19、mcp +13（集成 6 用例：空白名单拒绝 / 白名单内读取脱敏（sk- 密钥不出设备）/ 越界拒绝 / 未注册拒绝 / 传输回调续传 / TOOLS 接线存在性铁律 9）。

## 三、数据上行通道（G11 · 采集声明 opt-in → WAL 暂存 → 加密上行 → 审计计量）

### 定位

> G10 管「平台**拉**数据」（平台发起、白名单校验、拉取式读取），G11 管「设备**推**数据」（设备/节点侧发起、主动上报）。两类场景：① 一体机本地推理的**结果/结论上行**（原始数据不出设备，只有脱敏后的结论上平台）② AI 节点持续产出的**计量/心跳/审计摘要上行**（G8 首部署 cron 持续出账的云端回传）。地基已有八成——白名单授权（G10 同源）、WAL 断点续传（复用 `engine/orchestrator/src/durable/wal-writer.ts` / `wal-recovery.ts` 的 WAL 语义）、加密（AES-256-GCM v1.3.8）、审计（HMAC 链）、计量（worklog/cost）、设备身份（Ed25519 v1.3.1）——缺的是把这些散落能力补成**一等公民 tool 化**的上行通道。

### 交付

| 组件 | 能力 |
|------|------|
| **采集声明（opt-in）** | 设备配置声明「哪些数据可上行」（config.yml 采集清单：数据类别 + 频率 + 目的地，默认空 = 不上行，opt-in 模型与 G10 白名单同源） |
| **WAL 暂存** | 上行队列本地预写日志（复用 `engine/orchestrator/src/durable/wal-writer.ts` / `wal-recovery.ts` 的 WAL 语义，落 `wal.jsonl`）——断网/平台不可达时先落盘不丢，恢复后续传 |
| **断点续传** | 按 WAL 游标续传（offset + ack 确认，已确认段不重传） |
| **加密上行** | 出设备前 AES-256-GCM 加密（复用 v1.3.8 静态加密栈），平台侧验签解密 |
| **`device_data_push` MCP tool** | 上行通道 tool 化：手动/定时触发上行（daemon 定时器或 skill 侧显式调用），push 前过白名单校验 + 脱敏管线 |
| **审计留痕** | 每次上行进 audit history（谁/何时/推了什么/依据什么声明）——HMAC 链覆盖 |
| **计量进账** | 上行动作进 worklog + cost（上行数据量/次数进计量） |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/core/src/device-upload-policy.ts` | 新建 | 采集声明解析 + 上行白名单判定（与 G10 device-data-policy 同源，默认空 = 不上行） |
| `engine/mcp/src/tools/device-data-push.ts` | 新建 | `device_data_push` tool（声明校验 → 脱敏 → 加密 → push） |
| `engine/daemon/src/upload-wal.ts` | 新建 | 上行 WAL 暂存 + 断点续传（复用 orchestrator `durable/wal-*` 的 WAL 语义；daemon 侧按 `long-tasks.ts` 同款「读 wal.jsonl、不跨包 import durable」纪律） |
| `engine/daemon/src/__tests__/upload-wal.test.ts` | 新建 | 单测（断网暂存 / 恢复续传 / 游标 ack / 越界拒绝） |
| `engine/core/src/__tests__/device-upload-policy.test.ts` | 新建 | 单测（空声明全拒 / 声明边界 / 目的地越界拒绝） |

### 验收标准（✅ 已交付，见下交付实况表）

- [x] 空采集声明设备对任何 push 请求返回拒绝（fail-closed）——core 策略无配置=empty-policy 全拒 + mcp 面透传
- [x] 声明内数据可上行、声明外拒绝——类别精确匹配 + 目的地核对（destination-mismatch 拒绝）
- [x] 断网暂存不丢（落盘可查）、恢复后按游标续传（不重传已 ack 段）——WAL enqueue 即落盘 + ackedSeq 游标单调 + fail 不回退游标
- [x] 出设备内容经脱敏 + AES-256-GCM 加密（原始数据不出设备）——明文不落 WAL（只存密文三件套）+ 密钥从 HMAC 主密钥 sha256 派生（无密钥=拒绝入队 fail-closed）
- [x] 每次上行在 audit history 有完整留痕——emitDecision（含 seq/delivery/pending evidence）
- [x] 上行动作（上行数据量/次数）进 worklog/cost——decision-log 进 worklog 三源，evidence 含 payloadBytes/redactHits/seq

### 交付实况（2026-09-14 批 2）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/core/src/device-upload-policy.ts` | 新建 | 采集声明解析 + authorizeDeviceUpload（与 T2 同源纪律：默认空=不上行；类别+目的地双核对）· `de03cdb5` |
| `engine/daemon/src/upload-wal.ts` | 新建 | 上行 WAL（enqueue/ack/fail 三段协议）+ 断点续传游标 + AES-256-GCM 加密入队（WAL 语义复用 durable/wal-writer 协议形态但 daemon 侧自实现——long-tasks 同款不跨包纪律，文件独立 upload-wal.jsonl 防误恢复）· `de03cdb5` |
| `engine/mcp/src/tools/device-data-push.ts` | 新建 | `device_data_push`：门禁→声明→脱敏→加密入队→注入式传输回调（送达 ack 推进/失败留队列）→emitDecision 审计计量 · `bc7b71eb` |
| `engine/core/src/__tests__/device-upload-policy.test.ts` | 新建 | 11 用例：空声明全拒 / 类别边界×3 / 目的地核对×3 / 损坏故障注入×2 / 坏条目剔除 · `bc7b71eb` |
| `engine/daemon/src/__tests__/upload-wal.test.ts` | 新建 | 13 用例：明文不落盘 / 无密钥拒绝 / 游标续传×3 / 密文篡改 null / WAL 崩溃半行 / 0600 权限 · `bc7b71eb` |

**测试数联动**：全仓 4535→4591（+56：core +30 计 451→481 / daemon +13 计 463→476 / mcp +13 计 199→212），以 `tools/check/test-count.sh` 当轮实测为准。**工具数联动**：97→99 全 15 处落点同批同步（`74557f1c`，含五域图 D3 22→24 重算、API.md 清单 gen 重生成 + NAME_TO_MODULE 拍板两新 tool 归属）。

## 附：跨设备任务路由（评估项 · 依赖 v1.5.1 事件总线）

> **评估不实做**：任务下发到最优设备（能力匹配 + 数据 locality + 负载水位）。v1.5.1 事件驱动触发（`on:` 声明式订阅 + 事件总线）交付后评估排期——路由本质是事件消费侧的设备选择问题，事件总线是前置。
>
> 📖 **G12 说明（2026-09-01 拍板）**：设备 OTA 远程升级（运维通道）不排本版——已定 **v1.5.1 第四章**（升级指令走事件总线 `device.upgrade` + daemon 拉取验签 + 灰度回滚，前置依赖本版 G9 设备注册/心跳）。G9/G10/G11 管数据通道，G12 管运维通道——四件合起来是设备生命周期完整拼图。

### 评估结论（2026-09-15 收口 · 本版交付边界确认）

> **结论**：维持「评估不实做」，跨设备任务路由排 **v1.5.2**（事件总线交付后的首个可排版本），本版零 `engine/` 改动。

- **前置依赖确认（实测 2026-09-15）**：① 事件总线（`on:` 声明式订阅 + 事件驱动触发）尚不存在——engine/ 下零 EventBus/事件总线实现落点，ROADMAP v1.5.1 行明确「业务节点事件驱动触发 + 死信重放」属 v1.5.1 交付物，前置未就位；② 本版已就位的输入件——G9 能力标签（`DeviceRecord.capabilities`，`device_list` 可按 capability 过滤）、T12 改派语义（同能力在线设备改派判据 `device-registry.ts:726`）、T11 心跳捎带任务清单（`pendingTasks` + `claim` 回执 HMAC 留痕）——三者构成路由算法的「设备选择输入面」，但**触发机制仍是轮询式心跳捎带**，非事件驱动。
- **排期建议**：v1.5.1 交付事件总线 + 任务下发通道二期（推送/实时）后，v1.5.2 在事件消费侧实现三因子路由（能力匹配 → 数据 locality（G10 白名单目录与任务数据目录交集判定）→ 负载水位（心跳携带的待执行任务数）），复用本版改派语义作降级路径。**不建议提前实做**：无事件总线的路由只能长轮询，与「平台不直连设备」铁律冲突。

## 四、installer skill（裸机自动安装 · 2026-09-07 登记 · 2026-09-08 拍板转正）

> 📋 **状态**：2026-09-07 登记候选，2026-09-08 用户拍板通过转正。

### 定位

> prompt 驱动的裸机自动安装：设备侧 Agent（OpenClaw / WorkBuddy 等）通过 skill 形态引导裸机完成 sofagent 引擎安装（依赖检测 → install.sh 执行 → 首部署 cron 创建 → G9 设备注册），把「人工跑安装命令」变成「Agent 读 skill 自主装机」。与 G8 首部署 cron 包（v1.4.7）、G9 设备注册（本版第一章）天然衔接——装完即注册、注册即在线。上岗 prompt（v1.4.7 生成器产出）是 installer 的上游输入。

### 交付

| 组件 | 能力 |
|------|------|
| **installer skill** | 分步引导：环境依赖检测 → install.sh 执行 → 安装结果校验 → 触发 G9 注册 |
| **失败兜底** | 依赖缺失/安装失败时输出结构化诊断，不打哑炮 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `SKILL/` 下 installer skill 定义 | 新建 | 分步安装引导 skill（依赖检测/执行/校验/注册四步） |
| installer skill 单测 | 新建 | 失败诊断路径覆盖 |

### 验收标准

- [x] Agent 加载 installer skill 后可在裸机完成安装并触发 G9 注册
- [x] 依赖缺失/安装失败输出结构化诊断（不打哑炮）

> **交付实况（2026-09-14 批 4）**：① skill 落 `SKILL/harness/installer.md`（122 行 ≤200 预算；与 harness 既有 kebab-case 命名式样一致）；② 四步+前置共五段：第 0 步读上岗 prompt 四要素（平台/身份码/策略/源——缺一停诊断不开始安装），第 1 步依赖检测（node≥18/git/磁盘/存量判升级），第 2 步 install.sh 执行（`--policy` 校验失败是设计行为不是故障——fail-closed 转述防 Agent 错误自愈绕过），第 3 步安装结果校验（目录+doctor 退出码 0+MCP 连通三证据），第 4 步触发 G9 注册（`invalid-identity` 不重试回派单方换身份码；`duplicate-device` 转已注册；注册成功立即首跳心跳带 availableModels——与 T10 第三项联动）；③ **可回滚/可中断**：每步诊断块带 rollback 字段（4 处完整诊断块 + 第 0 步简式）；④ 单测 `engine/daemon/src/__tests__/installer-skill.test.ts` 10 用例锁定义结构（四步标题齐/诊断五字段模板/逐步 rollback/doctor 判据/验签 fail-closed 转述/policy 语义/上游闭环/行数预算）——skill 是 prompt 资产，引擎侧能锁的是**定义不被改坏**，执行者是设备侧 Agent。

## 五、任务下发通道一期——心跳捎带（2026-09-07 登记 · 2026-09-08 拍板转正）

> 📋 **状态**：2026-09-07 登记候选，2026-09-08 用户拍板通过转正。平台 → 设备的**任务下发**通道一期复用本版 G9 新建的心跳上报链路（**注意**：daemon 现有心跳只是本地写 `daemon-health.json` 健康文件，尚无「设备→平台」网络上报通道——该网络链路本身是本版 G9 的新建工作，本章复用的是 G9 刚建的那条），下发指令搭心跳返回值捎带——设备心跳上来，响应里带回待执行任务清单（零新增连接）。二期推送/实时排 v1.5.1（事件总线）。

### 交付

| 组件 | 能力 |
|------|------|
| **心跳捎带下发** | 心跳响应体带任务清单（派单方写入，设备侧拉取式消费） |
| **任务确认回执** | 设备领任务后回执进审计链（谁/何时/领了什么） |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/daemon/src/device-registry.ts` | 修改 | 心跳响应体扩展任务清单字段 |
| `engine/daemon/src/__tests__/device-registry.test.ts` | 修改 | 补捎带下发 + 回执留痕用例 |

### 验收标准

- [x] 心跳响应可携带任务清单，设备侧可拉取消费——`b2782ae3`：`reportHeartbeat` 响应体 `pendingTasks` 字段（测试「入队 → 心跳响应带 pendingTasks → 领取 → 回执留痕」：入队后下一跳心跳捎带 1 任务，**领取后不再捎带**）
- [x] 领任务回执进审计链（HMAC 留痕）——`b2782ae3`：`claimDeviceTask` pending→claimed + 回执事件（谁/何时/领了什么）进 device-events.jsonl，返回 `receiptHash` 可验签回溯（测试断言回执内容三要素 + 链完整含回执记录）

**施工注记**：预估表把 `device-registry.ts` 标「修改」的前提是 T1 已建——实际 T1/T11/T12 同批合一交付（`b2782ae3`），心跳响应体从诞生即带 `pendingTasks` 字段，无单独的「修改」commit。

## 六、派单语义：在线才派单 + 掉线改派（2026-09-07 登记 · 2026-09-08 拍板转正）

> 📋 **状态**：2026-09-07 登记候选，2026-09-08 用户拍板通过转正——对「跨设备任务路由评估（评估不实做）」的语义收窄具体化，**写进 G9 验收标准**（不单独占版本）。派单前置条件 = 设备在线（G9 心跳新鲜度判定）；设备掉线时自动改派到同能力在线设备或挂起告警。**路由算法仍不在本版实做**（依赖 v1.5.1 事件总线），本版只做「派不派 + 掉线怎么办」的最小语义。

### 交付

| 组件 | 能力 |
|------|------|
| **在线才派单** | 派单前查 G9 心跳状态，离线设备不接新单 |
| **掉线改派/挂起** | 已派设备掉线 → 改派同能力在线设备（或配置为挂起告警） |

### 验收标准（并入 G9 第一章验收清单）

- [x] 离线设备不接新单（心跳新鲜度判定）——`b2782ae3`：`enqueueDeviceTask` 入队前置 `isOnline` 判定（从未心跳 / 心跳过期均拒，reason=device-offline 且拒单留痕；测试两条：从未心跳拒单 + 心跳过期拒单）
- [x] 已派设备掉线可改派同能力在线设备（或配置为挂起告警）——`b2782ae3`：`reassignOrHold` 三路径全覆盖（测试三条：改派同能力在线设备 + 改派目标心跳可见任务 / 无同能力候选 → 挂起告警 fail-closed / hold 模式直接挂起告警——即便有在线候选也不改派）

## 七、session 承接与 router 伴生（数据承接 · 2026-09-08 排入 · 自原 v1.5.3 前移改写）

> 📋 **背景与拍板**：原 v1.5.3 排「session 全量记录与蒸馏对接」以引擎自研 router 网关为前提；2026-09-08 用户拍板改向——**router 基于开源项目自建（非第三方托管），引擎不自研 router 本体**。引擎的职责是承接面：router 挡在基座模型前面收集的过站 session 是一体机最优质的训练数据源（真实业务对话 + 路由决策 + token 用量），引擎提供伴生 exporter 协议接住它。承接的数据**本地落盘**（数据主权铁律——记录不出企业边界），与原 v1.5.3 原文「本地优先存储」语义一致。

### 交付

| 组件 | 能力 |
|------|------|
| **router 伴生 exporter 协议** | 引擎交付轻量 exporter 组件（开源进仓，router 侧部署）：过站流量按标准 schema 推送——session 内容（多轮 messages）+ usage（token 数/模型/单价→成本）+ 路由决策（目标模型/降级路径）。推送走 v1.4.6 data-push 接口同源的扩展 schema |
| **多轮 chat 样本模型** | dataset-builder 样本模型扩展：`messages: [{role, content}]` 多轮形态（Qwen chat / sharegpt 兼容）——session 天然是多轮对话，现有单轮 Alpaca 形态装不下。含切窗（长 session 滚动窗口切分）+ 角色映射（user/assistant/system→样本角色） |
| **蒸馏偏好对构造** | 云端教师（强模型 API）好答案 vs 本地模型同题响应 → 天然 DPO 偏好对（chosen=教师 / rejected=本地）→ 进 dpo 数据集。同 prompt 双响应配对构造，作为持续后训练（v1.4.5 三触发）的蒸馏素材源 |
| **工作产物采集（五源→六源）** | corpus 样本聚合补第六源 `workflow-artifact`：workflow 执行的产物文件（生成的文档/报告/交付物）进语料管道，进管道前过分拣闸（产物敏感比例高——分拣闸主战场）。现有五源全是过程数据（审计轨迹/LLM 调用），产物数据首次入源 |
| **usage 入 cost 台账** | exporter 推送的 token 用量/单价入既有 cost 台账（v1.4.0 cost_query 可查）——router 过站流量的成本核算，引擎侧无需访问 router 内部 |
| **key 维度 harness 四件**（2026-09-08 升格拍板） | 对每个 key 施加引擎侧 harness——key 的发放/吊销/管理归 router，引擎基于 exporter 推送数据做四件：① **计量**：cost 台账按 key 维度聚合（谁用了多少 token/费用，cost_query 可查）② **审计**：key 维度过站行为入审计链 HMAC 挂链（该 key 何时调了什么模型、走了什么路由——逐条可溯）③ **异常检测**：单 key 用量突增/调用模式异常告警（复用 v1.4.0 超支告警机制扩展 key 维度；阈值外部化对齐 dataset-validator 模式）④ **处置建议**：异常 key 生成处置建议（暂停/降配额）推送告警通道——判定与建议在引擎，执行动作回 router 侧（引擎不直接碰 key 生命周期） | |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/train/src/session-ingest.ts` | 新建 | exporter schema 校验 + session→IngestRecord 转换（多轮展开） |
| `engine/train/src/router-exporter.ts` | 新建 | 伴生 exporter 参考实现（router 侧部署的推送组件） |
| `engine/train/src/dataset-builder.ts` | 修改 | 多轮 messages 样本模型 + 切窗 + 角色映射 |
| `engine/train/src/distill-pairs.ts` | 新建 | 同 prompt 双响应 → DPO 偏好对构造 |
| `engine/core/src/export/sample-aggregator.ts` | 修改 | 五源→六源（workflow-artifact 采集器） |
| `engine/mcp/src/tools/router-session-push.ts` | 新建 | `router_session_push` MCP tool（exporter 推送入口） |
| `engine/mcp/src/__tests__/router-session-push.test.ts` | 新建 | 单测（schema 校验/多轮转换/DPO 构造/usage 入账） |

### 验收标准（✅ 已交付，见下交付实况）

- [x] exporter 按标准 schema 推送 session，引擎侧校验落盘（本地优先，全程 HMAC 挂链）——批 5：`RouterSessionSchema` zod `.strict()` 八字段校验（sessionId/enterpriseId/source/messages[]/usage/route/apiKeyId/scope），坏格式 fail-closed 拒绝（测试：缺 messages 拒 / 空 messages 拒 / unknown 字段拒）；`RouterExporter.signPayload` 伴生参考实现 HMAC 签名 + `verifyPayload` 验签 + `exportSession` 走 transport 注入（零真实网络）；MCP tool `router_session_push` 落盘 `data/<ent>/router-sessions/<id>.jsonl` + `audit.jsonl` HMAC 挂链（测试：落盘路径 / 签名字段 / 幂等——同 sessionId 二次推送拒绝）
- [x] 多轮 session 可构建为 messages 形态训练样本（切窗/角色映射正确，validator 闸门兼容多轮）——`windowMessages` 滚动切窗（windowTurns/stride 双参，默认 8/8）+ `mapRoles` 角色映射（user/assistant/system/tool 透传，其余归 user）；`buildDataset` 增 `chat` 算法分支（messages 列 JSON parse→ChatSample，非法 JSON 跳过并汇总原因）；`defaultSampleSanitize` 递归 messages 数组逐 content 过 REDACTION_PATTERNS（sanitizeFn 注入点 :163/:192 保持原位）；`dataset-validator` 增 `chat` 必填字段（messages）+ 空数组判缺失（测试 6 用例：chat 构建 / 非法 JSON 跳过 / 脱敏贯通 / defaultSanitize 递归 / validator 兼容 / sft 回归锁——单轮路径零变化）
- [x] 同 prompt 双响应可构造 DPO 偏好对并进 dpo 数据集（版本台账可溯）——`buildDistillPairs`（同 promptId teacher+local 配对，teacher=chosen / local=rejected）+ `distillPairsToRecords`（dpo 列映射 prompt/chosen/rejected 进 IngestRecord），经 dataset-builder 既有 dpo 分支落 `dataset_version` 台账（hash+样本数+配置可溯，复用 v1.4.2 版本管理面零新增）；测试：配对正确 / 无 teacher 跳过 / 双响应不同内容保真
- [x] workflow 产物文件进 corpus 第六源（过 sortify 闸门，敏感档拦截留本地）——`SampleSource` 增 `workflow-artifact`；`extractWorkflowArtifacts` 递归收集 `data/fde/<ent>/deliverables/**` 下 .md/.txt；`ArtifactGateFn` 注入分拣闸（缺省内置保守闸 `DEFAULT_ARTIFACT_GATE`——core 不反向 import train，闸门真正实现由 train 侧 `applyPreClassification` 接线，见第八章）；`AggregationResult.artifactGatedCount` 计数（测试：六源聚合 / 闸门拦截计数 / 非白名单扩展名跳过）
- [x] router 过站 usage 入 cost 台账（cost_query 可查，按模型/时段聚合）——`usageToCostEntry`（promptTokens+completionTokens → CostLedgerEntry，单价外部化）；MCP tool 落盘 `data/<ent>/cost-ledger.jsonl` append-only（append-only 不重写）；测试：入账金额正确 / append 形态（既有行不动）
- [x] key 维度 harness 四件可用：按 key 聚合计量可查 / 过站行为 HMAC 挂链 / 异常检测告警（阈值外部化）/ 处置建议推送（执行回 router 侧）——`aggregateByKeyUsage`（key→用量/费用聚合）+ `detectKeyAnomalies`（突增检测，`DEFAULT_KEY_ANOMALY_THRESHOLDS` 阈值外部化对齐 dataset-validator 模式）+ `formatKeyDisposition`（处置建议文本——暂停/降配额，判定与建议在引擎）；审计挂链在 router_session_push tool 侧（keyHarnessSnapshot 五元组摘要入 audit.jsonl HMAC）；执行动作回 router 侧——引擎不碰 key 生命周期；测试：聚合正确 / 突增检出 / 阈值外部化生效 / 处置建议格式
- [x] `npm test` 全绿——train 711 / mcp 238（router-session-push 10 用例：schema fail-closed / 幂等 / 落盘 / 脱敏 / cost / HMAC / TOOLS=104 锁）/ core 526，`tools/check/test-count.sh` 判定全绿

### 交付实况（2026-09-15 批 5）

- **schema 严格面**：zod `.strict()` 拒 unknown 字段——exporter 侧字段拼错当场拒绝，防脏数据入库（fail-closed 而非静默忽略）
- **切窗语义**：滚动窗口（非切分不重叠）——windowTurns=8/stride=8 时每窗 8 轮步进 8；stride < windowTurns 时窗口重叠（长 session 上下文保持），纯函数可回放
- **幂等键 = sessionId**：同 sessionId 重复推送拒绝（返 already-persisted），幂等窗口内 exporter 重试不产生重复语料
- **[sofagent] 前缀**：tool 内日志全部 `[sofagent] router-session-push:` 前缀——daemon/audit 消费方可按前缀过滤
- **TOOLS 103→104**：`router_session_push` 注册（roles:['ops']，required:['raw']）——全 14 落点同步（README 双语/SKILL 双文件/API/WIKI/ARCHITECTURE 五域图 C8/HANDBOOK/GEMINI/AGENTS/.cursor/umbrella 双文件/regression-checklist + gen-api-tools NAME_TO_MODULE 拍板 train 域），批 3 的 9bdb9c1d 式样

### 扩展：会话作用域续接五元组（商业平台机制对账 · 2026-09-08 排入）

> 📋 **背景**：商业平台机制文档定义了数字员工会话续接边界（对标 wemux scoped session continuation，商业待拍板项已吸收暂定方案）——换模型/换执行节点/换数字员工后旧会话还能不能续？引擎侧此前零排期，随第七章（session 承接主题章）一并落地。

- **五元组作用域归属**：会话按（执行器 + 员工身份 + 模型 + 工作目录 + 运行时）归属——五元组完全匹配才可续接
- **宁可不续**：作用域不匹配时降级为**摘要交接**（较早摘要 + 最近消息 + 最新结论），绝不让数字员工带着错配上下文继续跑——错配续接比重新开始危险
- **续接判定为纯函数**：五元组比对可单测、可回放，与调度判定双层（v1.4.8 第八章纪律批）同一设计哲学
- **验收追加**：五元组匹配续接 / 任一元组不匹配走摘要交接（注入测试双向断言）；摘要交接内容含三要素（较早摘要 + 最近消息 + 最新结论）

## 八、敏感识别升级：检测器插槽 + 敏感度分类器 + NER 外挂协议（2026-09-08 排入 · 自原 v1.5.3 前移 + 新编排）

> 📋 **背景**：现分拣闸（v1.4.6）是 5 组正则模式匹配，认不出「不含手机号/金额形态的客户名单」；现脱敏（v1.4.4 三类红名单）语义类靠人工录入实体名单。语义级识别需要模型推理——但引擎层纪律**只做管线不做模型推理**（分层架构：推理属模型层/企业侧）。解法：**插槽架构**——引擎建通用检测器接口与调度管线，内置正则检测器（零依赖默认档），外挂检测器（企业 NER 服务/Ollama 本地小模型/商业平台 NER API）经 endpoint 协议接入。敏感度分类器与 NER 脱敏**共用同一套检测器插槽**（一次建架构、两个消费面）。
>
> **可嵌入性（2026-09-08 补注）**：检测器插槽**设计为可独立消费的组件，不绑死在引擎进程内**——企业自建的模型网关/代理（如 LiteLLM 一类网关底座）可直接集成同一份分类器与检测器，在流量侧做 PII 过滤与敏感度分流。**网关侧与引擎侧共用一份规则定义，不各写一套**——避免判定口径分裂（同类教训见 v1.5.3 第一章双规则引擎统一）。注意分工：网关管「模型说了什么」（流量侧），约束层管「Agent 做了什么」（行为侧工具调用/git 变更/意图上下文），两者互补不替代。

### 交付

| 组件 | 能力 |
|------|------|
| **通用 Detector 接口** | `detect(text) → spans[]`（实体区间+类型+置信度）——检测器注册表 + 管线调度 + span 聚合去重 + 占位符替换 + 「哪个检测器命中了什么」入审计链 |
| **L0 内置正则检测器** | 现有 REDACTION_PATTERNS + sorting-gate 敏感模式原位升级为插槽实现（零依赖默认档，开箱能用） |
| **L1 企业专名词典检测器**（2026-09-08 补排 · **本版最高优先级**） | 从企业已有结构化数据自动生成实体名单：知识库 entity/concept 名录、客户/供应商/项目代号表（CSV/Excel 经 data-ingest 接入）→ 生成词典检测器。**零模型、零算力、零部署**——制造业客户的敏感数据（客户名/产品代号/工艺参数）多已在企业既有名录里，覆盖率常优于通用 NER（通用 NER 不认识企业内部代号），投入产出比最高 |
| **L2 外挂 NER 检测器** | 两类实现走同一接口：① 企业侧 NER 服务经 endpoint 配置接入（HTTP：文本→spans）② **本地模型检测器适配器**——用一体机本地模型（Ollama/vLLM 已注册端点）以结构化输出抽取实体，一体机开箱即用、客户零额外部署。引擎调度、外挂推理，置信度透传给消费方定档 |
| **Presidio 实体类型 schema 对齐** | 对齐 Microsoft Presidio 的实体类型命名（PERSON/EMAIL/PHONE 等）与两段式（analyze / anonymize）API 形态——**借模式不移植代码**（Presidio 依赖 Python/spaCy，进不了 Node 引擎与客户机房）；生态既有配置可直接迁移，降低对接与学习成本 |
| **敏感度实时分类器** | 文本→档位（公开/内部/敏感）——session 承接（第七章）与上云分拣（v1.4.6 sorting-gate）的共用输入前置件：先分类、再路由，分类结果随 routeReason 入审计留痕。分类策略 = 内置规则档（关键词+正则+词典）+ 外挂模型档（置信度透传，对接 GB/T 35273 分级叙事） |
| **redactor 管线升级** | v1.4.4 通用脱敏管线的 sanitizeFn 注入点（预留于 dataset-builder）正式消费检测器插槽——脱敏链从「三类红名单」升级为「L0/L1/L2 三层检测器管线」 |
| **置信度三层与 fail-closed** | 高置信（名单+模型一致）→ 直接替换；中置信（单检测器命中）→ 替换 + 记审计；低置信（弱正则模糊命中）→ 只标记不替换，交人审（消费 v1.5.0 数据集审阅卡）。外挂 NER 不可达时降级内置档（L0+L1），**不静默放行** |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/core/src/export/detector-registry.ts` | 新建 | 通用 Detector 接口 + 注册表 + 管线调度 + span 聚合 + 置信度三层 |
| `engine/core/src/export/detector-regex.ts` | 新建 | L0 内置正则检测器（现有模式原位升级） |
| `engine/core/src/export/detector-glossary.ts` | 新建 | L1 企业专名词典检测器（名录接入 + 词典构建 + 匹配） |
| `engine/core/src/export/detector-remote.ts` | 新建 | L2 外挂 NER 客户端（endpoint 配置 + 本地模型端点复用 + 置信度透传） |
| `engine/core/src/export/detector-presidio-schema.ts` | 新建 | Presidio 实体类型 schema 与两段式 API 形态对齐 |
| `engine/core/src/export/sensitivity-classifier.ts` | 新建 | 敏感度分类器（规则档 + 模型档两态） |
| `engine/core/src/export/redactor.ts` | 修改 | 消费检测器插槽（三类红名单升级为三层管线） |
| `engine/train/src/sorting-gate.ts` | 修改 | 分类结果作为分拣输入前置件 |
| `engine/core/src/__tests__/detector-registry.test.ts` | 新建 | 单测（插槽注册/span 聚合/置信度三层/审计留痕） |
| `engine/core/src/__tests__/detector-glossary.test.ts` | 新建 | 单测（名录接入/词典构建/命中与误报） |

### 验收标准（✅ 已交付，见下交付实况）

- [x] 检测器插槽可注册 L0 正则 / L1 词典 / L2 外挂 NER 三类实现，span 聚合去重正确——`DetectorRegistry`（register/unregister 重名拒绝 fail-closed / runPipeline 逐检测器调度）；`aggregateSpans` 纯函数：同区间合并 + corroboredBy 佐证链（score +0.1）+ 重叠跨族保留双方 + start 升序输出；测试 33 用例（注册/重名拒/管线聚合/佐证加成/三层分档/applySpans 从后往前替换不偏移）
- [x] **L1 企业专名词典可用**：企业名录（CSV/Excel/知识库 entity）接入后生成的词典能命中内部代号（零模型依赖）——`buildGlossaryFromRecords`（data-ingest 产物 → 词典，首列术语+别名列可配）+ `buildGlossaryFromEntityNames`（知识库 entity/concept 名录 → 词典）+ `createGlossaryDetector`（termToRegex：中文直接匹配 + ASCII 词边界断言——防「Acme」误命中「AcmeCorp」类子串）；测试 12 用例（中英命中/词边界/多别名/空词典零 span/score 0.9 档）
- [x] L2 本地模型检测器在一体机已注册端点上开箱可用（结构化输出抽实体，客户零额外部署）——`prefetchRemoteSpans`（HTTP 文本→spans，fetch 注入零真实网络）三形态 fail-closed 上抛：不可达/非 2xx/结构不符（**不静默放行**）；`createPrefetchedRemoteDetector` 包装预取结果（同步 detect 零二次网络）；`createRemoteDetector` 的 detect() 直接上抛——异步面强制走 prefetch 组合（防绕过 fail-closed 降级登记）；`validateRemoteSpans` 校验区间合法性（start<end/不越界/类型枚举）
- [x] Presidio 实体类型命名与两段式 API 形态对齐（生态配置可迁移）——`detector-presidio-schema.ts`：PresidioEntityType 枚举（PERSON/PHONE_NUMBER/EMAIL_ADDRESS/…对齐 Presidio 命名）+ `toPresidioType` 内部标签→Presidio 标签映射 + `recommendedOperator` 三档处置（replace→高置信替换/mask→中置信+审计/redact→低置信标记）+ `toPresidioResults`（span→Presidio analysis 结果形态——analyze 段对齐；anonymize 段由 redactor 消费面承担）
- [x] 敏感度分类器输出公开/内部/敏感三档 + 依据入审计链（routeReason）——`classifySensitivity`：sensitive（模型/词典任一高置信命中）→ internal（INTERNAL_KEYWORDS「内部/仅限/保密」类关键词）→ public；`SensitivityDecision.routeReason` 逐档说明判定依据 + `modelLevel` 透传（外挂模型档结论入链）；`decidedBy` rule/model 双态可溯
- [x] 置信度三层生效（高替换/中替换+记审计/低只标记交人审）；外挂 NER 不可达时 fail-closed 降级 L0+L1——`DEFAULT_CONFIDENCE_THRESHOLDS {high:0.9, medium:0.6, low:0}`：`tierOf(score)` 三档（≥0.9 高 / ≥0.6 中 / 其余低）；`PipelineResult` 含 tier 分档结果（高直接替换 / 中替换+corroboredBy 入审计 / 低只标记 span 不替换交人审）；L2 三形态故障注入测试全部 fail-closed 上抛（不可达/非 2xx/结构不符）——降级路径 = 消费方捕获后重组 L0+L1 管线，不静默放行
- [x] redactor 管线经插槽产出的脱敏结果与 v1.4.4 三类红名单结果向后兼容（回归锁）——`redact()` 第三参 `opts.detectorRegistry` 注入点：无注入 = v1.4.4 原位 entities 字面替换路径**零变化**（既有 export.test.ts 6 用例不动全过——回归锁）；有注入 = span 管线替换（占位符 `{GLOSSARY:N}`/`{PII:type:N}` 序号不含原文——防占位符二次泄漏）；format 类内置不回退
- [x] `npm test` 全绿——core 526（detector-registry 33 + detector-glossary 12 + 既有全量）；sorting-gate 前置件 `applyPreClassification`（sensitive 直接拦 / public·internal 走既有正则判定——分层不越权，格式面仍由 SENSITIVE_PATTERNS 把关，五标签原位不动）train 侧 19 用例全绿

### 交付实况（2026-09-15 批 5）

- **插槽优先序**：L1 词典最高（零模型零部署——企业名录常优于通用 NER 认内部代号）→ L0 正则内置 → L2 外挂 NER 佐证；三层不互斥，`runPipeline` 全跑后 `aggregateSpans` 聚合（跨层佐证加成 score+0.1，corroboredBy 留证）
- **L0 插槽化零回退**：REDACTION_PATTERNS 实数 14 条（13 密钥族 + 1 PII 手机号——P0-2 注释「13 检测→13 脱敏（+1 PII）」，批 5 实测核对）经 `createL0RegexDetector` 插槽化——score 分档：密钥 0.95 / 手机 0.8 / 裸 40 位十六进制 0.5（弱形态低置信只标记）；`createL0SensitiveDetector` 五标签版供 sorting-gate 语义（SENSITIVE_PATTERNS 五标签 :50 原位不动）
- **占位符序号化**：插槽态占位符 `{GLOSSARY:0}`/`{PII:PHONE:1}` 用自增序号——初版占位符含命中词（`{GLOSSARY:锐达科技}`）在测试 not.toContain('锐达科技') 暴露二次泄漏，收口为序号
- **sorting-gate 前置件语义**：`applyPreClassification` 只做「分类 sensitive → 直接拦」与「public/internal → 交既有正则」的分诊——不替代 SENSITIVE_PATTERNS 格式面判定（公开档里的密钥形态仍被正则拦住——分层不越权）
- **红色回归锁**：redactor 无插槽注入路径与 v1.4.4 完全一致（既有 6 用例零改动全过）——升级不破坏存量脱敏消费方

## 九、权重灰度发布 AB（2026-09-08 排入 · 自原 v1.5.3 前移）

> 📋 **背景**：session 承接（第七章）→ 蒸馏 → 新 LoRA 权重后，怎么安全上线？全量切新权重 = 拿生产流量赌训练质量。本件交付灰度面：新旧 LoRA 适配器按流量比例灰度（复用 v1.3.5 run_ab_test 基础设施），灰度指标回退保护对齐 v1.4.5 持续后训练回退闸。依赖项全部已交付：run_ab_test（v1.3.5）/ 权重部署+rollback-weights（v1.4.4）/ 持续后训练回退（v1.4.5）。

### 交付

| 组件 | 能力 |
|------|------|
| **权重灰度 AB** | 新旧 LoRA 适配器按流量比例分流服务（如 95% 旧 / 5% 新），灰度期指标（正确率/拒绝率/成本）对比可查 |
| **回退保护** | 灰度指标劣化超阈 → 自动回退全量旧权重（复用 rollback-weights）+ 回退事件入审计链 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/train/src/weight-canary.ts` | 新建 | 权重灰度分流 + 指标对比 + 回退判定 |
| `engine/train/src/__tests__/weight-canary.test.ts` | 新建 | 单测（分流比例/劣化判定/回退触发） |

### 验收标准（✅ 已交付，见下交付实况）

- [x] 新旧权重可按配置比例分流服务，指标对比可查——`routeRequest`（key hash 稳定分流——同 key 恒定落同一臂，canonicalJson 后 sha256 取模，比例外部化 CanaryConfig.canaryPercent）+ `accumulateMetrics`（correct/total/rejected/costTokens 四维累积）+ `deriveRates`（正确率/拒绝率/成本率三率推导）
- [x] 灰度指标劣化超阈自动回退全量旧权重（回退事件 HMAC 留痕）——`judgeDeterioration` 三维度独立判定（正确率降幅/拒绝率增幅/成本增幅，`DEFAULT_DETERIORATION_THRESHOLDS` 外部化）+ minSampleSize 噪声保护（样本不足不判劣化——防小样本假回退）；`runCanaryCheck` 劣化 → 调 rollbackWeights（签名 `(modelName)=>{ok,message}` 复用 v1.4.4 rollback-weights 语义，注入式零真实部署）+ `RollbackAuditPayload` HMAC 留痕（canonicalJson 确定性序列化——同输入恒同签名，可验证）
- [x] `npm test` 全绿——weight-canary 16 用例：分流比例（80% 分位稳定性 / 边界值）/ 三维度劣化独立触发 / minSampleSize 噪声保护 / 回退调用与留痕 / 阈值外部化

### 交付实况（2026-09-15 批 5）

- **hash 分流保稳定**：同请求 key（sessionId/agentId 等业务键）恒定落同臂——灰度期用户体验连续，不因重试换模型
- **rollbackWeights 注入式复用**：签名对齐 v1.4.4 rollback-weights（`{ok, message}` 双态）——测试注入 mock 验证调用与失败传播，生产接线由 train-continuous 灰度位驱动
- **rollbackWeights P2-18 人审注释**（bugfix 批顺带确认项）：train-continuous.ts :309 区域已核——注释在位，无异常
- **HMAC canonicalJson**：留痕 payload 走 canonical JSON（键排序定序）——审计链验签可复现，非 opaque blob

## 十、执行侧模型清单上报 + 执行时 skill 快照（商业平台机制对账 · 2026-09-08 排入）

> 📋 **背景**：商业平台待拍板项定义了两项执行侧机制（对标 wemux 执行级 Skill 物化与模型清单上报，商业三件套已吸收暂定方案），引擎侧零排期——随本版（设备接入与数据承接主题）一并落地。两项共同方向：**执行侧如实上报、平台只展示不代理**（与「平台永不执行业务代码」红线同向）。

### 交付

| 组件 | 能力 |
|------|------|
| **执行侧模型清单上报** | 设备/引擎侧扫描本机可用模型（注册表 + 本地推理端点探测）后随心跳上报模型清单——BYOK 企业的 workflow 模型绑定下拉列表数据源，平台只做展示与绑定，**不预置、不代理密钥**（商业平台消费，对齐商业红线「平台永不执行」） |
| **执行时 skill 快照** | 每次执行前打包当次可见的 skill 版本（`runtimeSkillPackages`），执行结束即清理——**不做常驻预同步**。理由：常驻同步导致同一 workflow 两次执行用不同 skill 版本（不可复现）、设备侧残留越积越多（数据主权漏洞）、版本漂移无审计起点（对标 wemux 执行级 Skill 物化，商业 prd 红线 8 已吸收） |
| **快照审计锚点** | 快照打包清单（skill 名 + 版本 + sha256）挂进该次执行的审计记录——执行回放时「当时用的是哪个版本的哪个 skill」可精确回答 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/daemon/src/model-inventory.ts` | 新建 | 本机模型扫描（注册表 + 探测）+ 心跳携带清单字段 |
| `engine/orchestrator/src/exec/skill-snapshot.ts` | 新建 | 执行前打包 / 执行后清理 / 清单挂审计 |
| `engine/daemon/src/__tests__/model-inventory.test.ts` | 新建 | 单测（扫描/上报/清单过滤） |
| `engine/orchestrator/src/__tests__/skill-snapshot.test.ts` | 新建 | 单测（打包/清理/无常驻断言/审计锚点） |

### 验收标准

- [x] 心跳携带模型清单，`device_list` 可查每台设备的可用模型
- [x] 执行前快照打包、执行后清理（文件系统断言零残留）
- [x] 两次执行间更新 skill，第二次执行用的是新版本（快照清单可证）
- [x] 快照清单（名/版本/sha256）进审计记录，回放可查
- [x] `npm test` 全绿（daemon / orchestrator 两包，批 4 实测）

> **交付实况（2026-09-14 批 4 · 第三项字段消费）**：① `model-inventory.ts` 双源扫描——注册表（`@sofagent/orchestrator/model-registry` subpath，real-provider 同款先例；retired 剔除、缺失=合法空、损坏→降级原因不抛错）+ 端点探测（默认 Ollama `/api/tags`，AbortController 2s 超时 best-effort）；合并按 name 去重 registry 优先；上报条目五字段**刻意不含密钥**（name/endpoint/clientType/source/status）；② `DeviceRecord` 增 `availableModels` / `runtimeSkillPackages` 两可选字段（null=未上报，显式 null=清空，数组=整组替换，undefined=保留原值——三态语义）；③ 心跳事件流 summary 带 `models=N skills=N` 计数（回放可查上报时点）；④ `skill-snapshot.ts`（orchestrator exec/）——打包落 `{dataDir}/skill-snapshots/<sessionId>/`（复制保相对路径 + manifest 落盘 + emitDecision 审计锚点 kind=ORCHESTRATION/moment=ACT，evidence 逐条 `name@version sha256`）；清理只删本会话目录（幂等、不动兄弟会话）；**版本判定用 mtime ISO + 内容 sha256 前 16 位**（无版本化系统时以内容变更时点为版本）；⑤ 测试 daemon 18 用例（扫描三态/探测五形态/合并去重/心跳字段三态/事件流计数）+ orchestrator 11 用例（收集/打包/审计锚点/清理零残留/幂等/兄弟隔离/版本可证/损坏 manifest）——共 29 新用例。

## 十一、连接器注册/发现（G5b · MCP `connector_list`）

> 📋 **迁移说明（2026-09-12 拍板）**：本章原称「v1.4.8 承载」（商业平台机制文档 §9.1 G5b）。实况核对：v1.4.8 交付的是**插件来源白名单 + app_tool_policy**（管「从哪装」「装了能调什么」），连接器注册/发现作为独立接口未随版落地，v1.4.8 开发日志亦未收录本章。经用户拍板**整体迁入 v1.4.9**——与第一章 G9 设备注册同属「注册面」，共享 Ed25519 身份与来源白名单机制。

### 定位

> 商业平台 §3.2 存量系统对接的连接器生态诉求：第三方连接器（数据库 / REST / SaaS 适配器）的上架、发现与接入管理。与插件来源白名单的分工——**白名单管「来源可不可信」（准入闸门），本章管「有哪些、怎么发现」（目录面）**。

### 交付

| 组件 | 能力 |
|------|------|
| **`connector_list` MCP tool** | 连接器发现：按类型（db / rest / saas）+ 主机 + 能力标签过滤，返回连接器清单与接入形态 |
| **第三方连接器注册** | 连接器注册表（来源 + 能力声明 + 归属租户），准入复用 v1.4.8 插件来源白名单（Git URL / 主机 / 本地路径） |
| **与 G5a 的分工** | G5a（v1.4.2 已交付）管「数据源怎么读数」，本章管「连接器怎么被发现和注册」——读数面与目录面分离，不重复实现接入 |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/mcp/src/tools/connector-list.ts` | 新建 | `connector_register` + `connector_list` 双 tool（注册面 + 目录面一文件收口，scope `connectors-only` 分列声明） |
| `engine/audit/src/cli/plugin-gate.ts` | 扩展 | 连接器准入复用插件来源白名单校验（`registerConnector` / `listConnectors` / `getConnector` / `loadConnectorPolicy`，+286 行） |
| `engine/audit/src/public-api.ts` | 扩展 | 连接器面 4 函数 + 3 类型 `@public` 导出（mcp 经公开面消费，不穿 dist 子路径） |
| `engine/mcp/src/__tests__/connector-workflow-tools.test.ts` | 新建 | 集成单测（注册/发现/过滤/租户隔离/分列断言，与 T5 合流 16 用例） |
| `engine/audit/src/__tests__/plugin-gate.test.ts` | 扩展 | +21 用例（白名单三形态 / null 策略全拒 / 词法 / 重名 / 注册表损坏故障注入） |

### 验收标准

- [x] `connector_list` 可按类型 / 主机 / 能力标签过滤
- [x] 来源不在白名单的连接器注册被拒（fail-closed）
- [x] 连接器清单按租户隔离（跨租户不可见）
- [x] `npm test` 全绿（audit 1186 / mcp 228 / orchestrator 1354，13 包 4639 全绿）

> **交付实况（2026-09-14 批 3）**：① 注册表落 `<dataDir>/config/connectors.json`（损坏 → 空表 + console.warn 留证，fail-open 仅影响可见性不影响准入）；② **null 策略显式拒绝**——`policy.yml` 缺失/损坏时 `registerConnector` 直接返回 `source-not-allowed`（无白名单依据 = 无放行依据，与 T2/T3 设备白名单同源语义；不走 `validatePluginSource` 的「空 allowlist = 单机放行」插装分支——连接器准入是 opt-in）；③ 与 tool-registry **分列铁律**：`connector_list` 只读连接器注册表，绝不扫 TOOLS 数组，测试断言连接器条目无 handler/inputSchema/roles 字段；④ 实际新增 2 tool（`connector_register` + `connector_list`）——注册动作需独立 MCP 入口，否则「注册可发现」验收无生产调用点。

---

## 十二、workflow 模板导出/导入 + 血缘追踪（G1 · MCP `workflow_export` / `workflow_import`）

> 📋 **迁移说明（2026-09-12 拍板）**：本章原称「v1.4.7 候选（待拍板）」（商业平台机制文档 §9.1 G1）。实况核对：v1.4.7 与 v1.4.9 开发日志均未收录，ROADMAP 无版本承载，`workflow_export` / `workflow_import` 代码零实现——属**掉出版本序列**。经用户拍板**迁入 v1.4.9**，与第十一章同版收口「注册 / 导出面」。

### 定位

> 商业平台 §五**跨企业 fork** 的引擎侧载体：workflow 模板导出成可分发形态 → 第三方导入 → 血缘回流。与前序交付的关系——v1.4.7 已交付 workflow 容器 + schema + CRUD（G14），本章补的是**跨企业的导出 / 导入与血缘**：同租户内的创建 / 更新已由 G14 覆盖，本章解决「模板离开本企业后如何保持血缘可追溯」。

### 交付

| 组件 | 能力 |
|------|------|
| **`workflow_export` MCP tool** | 五件套完整导出（workflow.yml + 本体数据 + MD 家族）+ 血缘元数据（源企业 / 源版本 / fork 层级） |
| **`workflow_import` MCP tool** | 模板导入：schema 校验门 + 血缘回流（记录导入来源，形成 fork 谱系）+ 与本地本体数据的合并策略 |
| **血缘元数据 schema** | fork 谱系的机器可读记录（与 G13 PR 元数据、G4 绩效导出同构消费） |

### 涉及文件（预估）

| 文件 | 改动 | 说明 |
|------|------|------|
| `engine/mcp/src/tools/workflow-export.ts` | 新建 | `workflow_export` tool（五件套组装 + 跨租户剥离 + manifest sha256 完整性锚） |
| `engine/mcp/src/tools/workflow-import.ts` | 新建 | `workflow_import` tool（三闸：结构 → 完整性 → schema，落地 + 本体合并 + 血缘回流） |
| `engine/orchestrator/src/workflow/lineage.ts` | 新建 | 血缘事件流（append-only `lineage.jsonl` + fork 谱系回溯 + 复合键环防护，~200 行） |
| `engine/orchestrator/src/workflow/index.ts` | 扩展 | lineage 11 符号深 barrel 导出（mcp 只经导出面，包边界不反向） |
| `engine/orchestrator/src/__tests__/workflow-lineage.test.ts` | 新建 | 13 用例（往返 / 幂等键 / 坏行故障注入 / fork 链 / import 链 / 混合链 / 环防护） |
| `engine/mcp/src/__tests__/connector-workflow-tools.test.ts` | 新建 | 集成 16 用例（五件套 / 剥离 / 全私域拒绝 / 往返 / 三闸 / 冲突 / 本体合并，与 T4 合流） |

### 验收标准

- [x] 导出五件套完整（workflow.yml + 本体数据 + MD 家族）且带血缘元数据
- [x] 导入必过 schema 校验门（非法模板拒绝）
- [x] 导入后血缘可回溯到源企业 / 源版本（fork 谱系可查）
- [x] 跨租户导入不泄漏源企业的 private / result-only 节点（联动 G6 可见性）
- [x] `npm test` 全绿（orchestrator 1354 / mcp 228，含 lineage 13 + 集成 16 新用例）

> **交付实况（2026-09-14 批 3）**：① 五件套 = workflow.yml + ontology-entities.json + MD 家族（think.md + knowledge/lessons-missteps.md **按实收录**，无则不塞空文件）+ manifest（sha256 逐项摘要）+ lineage.json（`chainFromBundle` 组装）；② **manifest 完整性锚**——digest 必须与 bundle 内实际序列化形态**逐字节一致**（两端同用 `JSON.stringify(entities, null, 2)`，pretty-print 漂移即 integrity-mismatch 拒绝）；③ 跨租户剥离**双保险**——export 侧 `stripForCrossTenant` 剔除 private/result-only 节点 + 清理悬空 depends_on，import 侧再扫（跨企业包内私域节点 = 包构造异常，整包拒绝 code=private-leak，防手工构造包绕过）；④ 本体合并**本地优先**——同名 entity 不覆盖（导入补缺不替换），新增落 `ontology/entities/`；⑤ 血缘回流 `appendLineageEvent(type=import)` 落 `{dataDir}/workflow-store/lineage.jsonl`（独立文件，不占 trunk/branch 命名空间），`traceLineage` 以 `${workflowId}@${enterprise}` 复合键做环防护与去重。

---

## 十三、bugfix 修复批（已实施 · 2026-09-14）

> ⚠️ **本章为「已实施」修复批，非规划目标；上方一~十二章仍为规划态。** 本章逐条记录本轮 bugfix 批**已落地、当场可验证**的缺陷修复与门禁加固，写法为「问题 → 解决方案」，不含未实施项。

### 显式披露

> **自动演化路径恢复为行为恢复**：`isEvolveAvailable()` 原先探活 `evolve-gate`（全仓无此可执行）恒返回 `false`，把 native 默认路径整体挡死；修好探活即打开该路径，并暴露自指删除缺陷（`runEvolve` 曾把 `candidateDir`/`targetDir` 同传输入路径 ⇒ adopt 自指 copy 必 ENOENT 且 live 文件被删）。本版一并修掉。**属行为恢复：此前不可用 → 现可用**，发版入 CHANGELOG 索引时须并入本条。

### 行为恢复：自动演化路径

**问题**：探活面与调用面**对不上真实面却各自看起来正常**——探活 `evolve-gate`（全仓无此可执行）恒 false，调用 `skillopt-sleep`（外部兼容层真实名）；native 默认路径被恒 false 的探活整体挡死，**从未真正打开过**。打开后立即暴露一级数据丢失缺陷：`runEvolve` 把 `candidateDir`/`targetDir` 同传输入路径 ⇒ adopt 先 `rmSync(targetDir)` 再 `copyFileSync(candidateDir, targetDir)`，而两者同一路径 ⇒ **删源再拷已删的源** = `ENOENT` + **live 文件永久丢失**。

**解决方案**：
- 探活与调用**同源为单一常量** `LEGACY_GATE_CLI = 'skillopt-sleep'`；native 模式内置实现零外部依赖，`isEvolveAvailable()` 恒 true，不再因「外部 CLI 不在场」自降级（cli 模式仍真执行外部二进制看退出码）。
- `native-gate` adopt 改**无损替换** `replaceAtomically(src, dst)`：同路径 ⇒ no-op（不变式①）；异路径 ⇒ 先写**目标同目录**临时文件再 `renameSync`（同文件系统内原子替换，不变式②），消灭「目标已删、新内容未到」窗口。
- `runEvolve` 候选落 **staging 临时副本**（`mkdtempSync`）——技能目录只多出 `.gate-bak` / `.gate-history.json`；调用面加**防数据丢失锁**（L1 live 仍在 / L2 内容 ∈ {候选, 原文} / L3 adopt 后 `.gate-bak` 仍在），任一打破即 fail-loud 并尝试 `recoverLiveIfLost()` 从备份恢复。
- 顶层静态 `import { runNativeGate }` 替换原 `require('./native-gate')`——原写法在 vitest ESM 转换下不存在 ⇒ 该分支**直接抛错、从未被任何测试执行**（旧断言只查 `typeof === 'boolean'`，属零覆盖掩护，非「断言太弱」）；改静态 import 后回归锁才真跑。

### 门禁自愈（G-1 / G-2）

**问题**：`check-docs` 第 13 步接线断言**空转**——提取为空即静默跳过 ⇒ 恒绿（免费绿灯）；覆盖度行口径不统一、静默跳过不可见；发版窗口 `check-version` 非严格模式且 SOP 无 SKIP 逐条裁决步。

**解决方案**：§13 接线断言空转根治；覆盖度行统一范式 + 静默跳过显式化；发版窗口 `check-version` 默认 `--strict`，SOP 增「SKIP 逐条裁决」步。

### 审计树写侧（P0-1）

**问题**：hook 场景 `treeSha` 记的是「当前 HEAD 的 tree」而非「**即将生成的提交**的 tree」⇒ post-commit 对账拿不到同一 key，回声丢失。

**解决方案**：hook 场景改记「即将生成的提交」tree，post-commit 对账按**三重键回声**恢复；acceptance S411 场景压缩 51→27 行，控在 4300 行警戒线内。

### 脱敏表补族（P0-2）

**问题**：`AIza`（Google API key）/ `xox`（Slack token）/ JWT / 裸 40 位十六进制 **4 族在「检测表」里有、在「脱敏表」里缺** ⇒ 检出却未脱敏。

**解决方案**：脱敏表补 4 族、对齐检测表 + 回归锁（core 448→449）。

### 代码面修复批（P1）

| 项 | 问题 | 解决方案 |
|---|------|---------|
| P1-1 | `compactIfNeeded` 自动上下文压缩**零生产调用点**（写了不接线） | 接线（env 门控 + 休眠等价锁：未设 env 时注入体量逐字节相同） |
| P1-2 | 跨包相对引用越包（分发形态下解不出） | 新增守卫 + 豁免台账，顺带抓修 3 处 `package.json` 越包引用真缺陷 |
| P1-3 | train webhook SSRF：`http://` 端点被静默拒发 | 改用 `@sofagent/audit` 权威实现 |
| P1-4 | custom 层**写入根与读取根不一致**（写进 A 读自 B） | 两处读写根对齐（harness 39→43） |
| P1-5 | A/B 方案 C 约束链**读空静默降级** | 读空改 fail-loud |
| P1-7 | ROADMAP 版本头错版 + WIKI「下一版」自相矛盾 | 根治 + §15 升为阻断 |
| P1-9 | 用户文案报一个**不存在的工具名** + 无关 PyPI 指引 | 收口为「外部 CLI 兼容层缺位」，活区陈旧模块名一并对齐 |
| P1-10 | A1 未消费 `DiffFile.status` ⇒ 补救 commit 被硬阻断 | 按变更方向分级（`deleted` → WARN） |
| P1-12 | `watch.yml` 无有效路径仍报「已启动」（假绿） | 文件监听启动行按**实际 watcher 数**分流 |
| P1-13 | doctor 版本修复提示不分安装形态（npm 形态提示 `bash install.sh`） | 按安装形态分流 |
| P1-14 | 知识库旧路径 `.sofagent/knowledge` 活区残留 | 收口 + 双形态门禁（含**跨行分离**形态） |
| P1-15 | 引擎崩溃退出码与 cli-quick「非 git 仓库 ⇒ 3」撞码 | 崩溃退出码独立为 4；HANDBOOK 退出码枚举补全 3/4 两态 |

### 验证载体映射（coverage 对账口径）

> 本章为「问题 → 解决方案」叙述章，行为锁**不以 acceptance 场景为唯一载体**——各条目按修复形态落到四类验证面（对齐 S343「批次级代表锚点」先例：有回归风险的条目落场景，其余复用单测/门禁，覆盖完整性按本表审计）：

| 条目 | 验证载体 | 类型 |
|---|---|---|
| 行为恢复（evolve） | `evolve.test.ts` +126 / `native-gate.test.ts` +28 行（`1f58e73a`） | 单测 |
| G-1/G-2（§13 接线断言 / --strict） | `check-guards` 门禁链 + SOP SKIP 裁决步（`dfda11f1`） | 门禁 |
| P0-1（treeSha 三重键回声） | **S411** 场景三态断言 | 场景 |
| P0-2（脱敏表 4 族） | core 回归锁 448→449 + S239 脱敏断言族同源复用 | 单测+场景复用 |
| P1-1（compactIfNeeded 接线） | `load-chain-wiring.test.ts` +131 行（`cfda0bb9`） | 单测 |
| P1-2（越包相对引用） | `check-cross-package-relative.mjs` 320 行守卫（`bc529ed8`） | 门禁 |
| P1-3（webhook SSRF） | `train-webhook-ssrf.test.ts` +146 行（`437af051`） | 单测 |
| P1-4 + P1-5（读写根 + 约束链） | `ab-runner-constraint-chain.test.ts` +108 行（`d3158d17`） | 单测 |
| P1-7（ROADMAP/WIKI 口径） | `check-version.sh` §15 阻断 +32 行判据（`0c529688`） | 门禁 |
| P1-9（evolve CLI 文案） | 活区五文档 grep 零残留（`fc98f0cf`） | 门禁 |
| P1-10（DiffFile.status 分级） | **S412** 场景断言 | 场景 |
| P1-12（watcher 数分流） | `daemon.test.ts` +94 行（`d6dd76e8`） | 单测 |
| P1-13（doctor 形态分流） | `doctor.test.ts` +55 行（`b10eaf65`） | 单测 |
| P1-14（知识库旧路径） | `check-legacy-knowledge-path.mjs` 99 行双形态门禁（`541d8090`/`1f58e73a`） | 门禁 |
| P1-15（崩溃退出码 4） | `cli-crash-exit-code.test.ts` +109 行（`cdfc5251`） | 单测 |
| 门禁基础设施批（G-3/G-6/G-7/G-12/G-13/G-16/家目录双实现） | `ce600b26`/`28b5a2e3`/`1f58e73a` 门禁面 | 门禁 |

### 门禁基础设施批

- **G-3**：`check-test-count` 占位判据收窄到 `head-10` + 冻结分支补 SKIPS 记账；冻结判据收窄到**同一窗口** + 窗口抽为显式口径。
- **G-6**：`check-test-count` 两处守卫恒不执行（免费绿灯）⇒ 死锚改活。
- **G-7**：静默 catch 基线改**内容级定键**（原行号键随行移漂移，属假红来源）。
- **G-12**：`check-tool-health.sh` 未接入 pre-push（前序批次真实漏网）⇒ 接入。
- **G-13**：`check-test-count` README 断言块缺 `head -1` + 四数字空提取静默跳过 ⇒ 提取点健壮化。
- **G-16**：pre-push 检查项清单注释 ⊆ 实际调用的轻量断言（判据「清单 ⊆ 实现」；**提取为空 ⇒ FAIL**，拒绝假绿）。
- **家目录口径双实现守卫**：harness 为零依赖纪律核心包（`dependency-direction.yml` 定 `allow: []`）**不能 import core**，被迫本地重实现 `resolveHomeDir`；重复可接受、**口径漂移不可见不可接受** ⇒ 新增 `check-home-resolution-parity.mjs` 断言两侧同输入同输出，差异由 `DIFF_REGISTRY` 登记（**差异消失也判红**——「统一」必须是显式动作，而非某次重构的副产品）。

### 审计链定位面

**问题**：黄色 `unverifiable` verdict 的 8 个置位点**共用同一段静态文案** ⇒ 传 `(undefined)` 与传具体坏输入，输出**逐字相同**（不可定位到具体记录）。

**解决方案**：`UnverifiableReason`（8 原因码）+ 逐条定位串（**全量序号** + 时间戳 + `prevHash` 前缀 + 原因码）+ 校验窗口说明；序号加回 `maxEntries` 窗口偏移 ⇒ 窗口一变不会让同一坏记录显示成不同序号。`status` 的判定时机与取值**零改动**。

### 文档门面批

README 双语能力段升版 + 三条数字声称守卫补位；测试数声称按 `tools/check/test-count.sh` 实测回填（沿革见附录账）。

### P2 批（择要 25 条 · 文档与门面细项）——已实施收口

本批 P2 条目已按性质分组落地，逐条映射如下（末列为落盘凭证）：

| 组 | 条目 → 解决方案 | 落地 |
|---|---|---|
| 文档门面 | **P2-1** CHANGELOG 的 v1.4.7 索引行日期由 09-13 改回 **09-11**（并由门禁 §14 补「非顶版行已发版日期不得随发版批变动」断言） | `87c39b85` |
| | **P2-2** `SECURITY.md` 的租户隔离表述改为如实：「查询侧 v0 已随 v1.4.7 交付；**写入侧隔离尚未落地**」 | 同上 |
| | **P2-9** `CONTRIBUTING.md` 头部版本由 v1.4.6 升到 v1.4.8 | 同上 |
| | **P2-10** `LIMITATIONS.md` 的单行考古倾倒拆段（结论留在正文、沿革细节移入本日志附录） | 同上 |
| | **P2-11** README 正文里的内部对账口径（「版本时点数字」「N = 13 主包口径」）移出对外正文 | 同上 |
| 写入口安全 | **P2-3** `createTrainJob` 补 `isSafePathSegment(enterpriseId / jobId)`——与 `getJobGuarded` 同源守卫；**写入口此前无段校验（读入口有）**，`../` 构造可逃逸 | `08343d30` |
| | **P2-4** `cloud-exec` 的 `remoteJobId` 过 `isSafePathSegment` 后再拼入 ssh 远端命令——`ssh` 会把多段 argv 拼回远端 shell 执行，未校验即构成远端命令注入；submit / status / artifacts / cancel **四动作入口各一次** | 同上 |
| 命令语义 | **P2-5** 同名 `sofagent` 二分语义说明：npm 裸名总包 = 审计薄转发、`install.sh` 安装态 = 完整入口（含 `status` / `web` / `dashboard`），补 PATH 判别法与「并存时显式用全名」 | `b2d67cb7` |
| 路径 | **P2-6** 清 `playbook/federation-e2e.md` / `.mjs` 里的维护者绝对路径，改 `$(pwd)` 缺省推导（代码侧本就正确，故零行为变更） | `43e58fee` |
| 发布面 | **P2-7** 九个核心包补 `prepublishOnly`（值取 audit / mcp / rules 现役先例全文，保证与前例逐字可比） | `3c5f7834` |
| | **P2-23** `sofagent-load-chain` 的 `files` 补 `!dist/**/*.test.*` 排除臂（`npm pack --dry-run`：6 文件 10.2kB → **4 文件 8.8kB**） | 同上 |
| CLI 披露 | **P2-8** 两入口 `--help` 补退出码语义；并**纠正此前的不实措辞**——「quick 与完整引擎同口径」不成立：**3 为非 git 仓库且属 quick 专属，完整引擎把该处境记为 2** | `16a6bb0d` + `dc39877c` |
| | **P2-13** quick 的「N 条跳过」提示提取为单一常量（改前 PASS / 非 PASS 两分支各写一份字面量），并补「**git diff 硬证据类规则已全量执行，跳过项非漏检**」的定性 | `16a6bb0d` |
| 承诺如实 | **P2-16** HANDBOOK 的报告推送承诺改为如实（**日报实装** / 周·月·季报排期中 / 扩容预警未实现），并**同步 `check-docs` §13 的锚定串**——旧锚定串随措辞漂移失配，该断言当场报红（守卫未静默空转） | `ff738ac2` + `49a190ac` |
| 架构图 | **P2-17** 五域分桶重算为 SSOT 自洽（**域和 = 图题 = registry 实数 = 95**；改前域和 80 ≠ 图题 95）；图内各子项补全工具名清单使之可人工复核；并新增分桶自洽断言，后经**重新定谳**扩为两条：「域和 = 图题 = registry」+「**每域域内子项「×N」之和 == 该域题号**」。⚠️ 此处**刻意不列逐域数值**——分桶定义可随重新定谳变动，把逐域值写进正文会与 SSOT 形成第二真值源；**一律以 `check-docs.sh` 该断言的当轮实测为准** | `2e6fd0cf` + `25e2d281` |
| 注释对账 | **P2-18** `model-registry` 的版本级回滚人审语义注释改为如实——**免人审是版本级与模型级的既定差异，不是「对齐 rollbackModel」**（模型级恰因翻转活动模型而强制人审）；文件头人审语义清单补两行 | `cdcadc13` |
| 安装 | **P2-20** symlink 被普通目录占用而降级为复制时，写 `STALE-COPY.md` **持久标记**（说明以本源为准 + 给同步命令）；四平台分支（openclaw / workbuddy / cursor / claude）共用**单一 helper** | `e87f77fc` |
| 术语 | **P2-21**「中间件」词性统一为**品类定位词**：ARCHITECTURE 范围声明 + 术语对照表补行、PHILOSOPHY 两处、WIKI 术语表补行 | `61552c2f` |
| 输出语言 | **P2-22** HANDBOOK 补披露「**CLI 全部输出为中文**，i18n / `--lang en` 均未排期」——把「没有 i18n」从沉默事实变为可检索披露 | 同上 |
| 发布前门禁 | **P2-24** 删除 `install.sh --with-memory` 的假绿 `sed` 块——目标字面量全仓零命中、且 `config-loader` 的 `memory_sync` 是对象非布尔 ⇒ 该 `sed` 恒 no-op 却打印成功 | `c4bae5dc` |
| 量纲对账 | **P2-25** VALIDATION 与 README 双语补「**能力型 harness 数据**」限定词——该数据提升的是任务得分与 token 效率，与治理型约束层的可靠性收益**非同一量纲** | `49a190ac` |
| 降密 | **P2-26** FORGE 在对外主文档首现处补消歧脚注（**项目内部自迭代工具链，非产品能力**） | `b2d67cb7` |
| 新门禁 | **P2-27** 新增 `tools/check/doc-discipline.sh`（内部工单 / 审查代号 + 本机私有路径两面判定，含扫描面失明防护）并清存量代号，接入 pre-push | `3ef9cb56` |

### 未实施项（逐条写去向，不留悬空「待办」）

| 条目 | 去向 | 理由（实测 / 裁定） |
|---|---|---|
| **P2-12** GitHub repo topics 含内部缩写、缺品类词 | 不在本仓可实现 | topics 是 GitHub 仓库设置里的**仓外元数据**，代码批无改动面；须在仓库设置页手工改（`check-storefront.sh` 只取 `.topics \| length`，**只验数量不验内容**） |
| **P2-14** README 体量与「该不该装」表位置 | 评估后不列入本版 | 视觉调整非缺陷；且动 README 会连带触发双语 parity 与 A 层预算核算，收益不抵风险 |
| **P2-15** 三个收录 badge 仍为 `PR open` | 不由本仓决定 | badge 状态取决于**外部平台**的 PR 合并进度，属运营跟进，本仓无对应改动面 |
| **P2-19** 审计链锚点写入 fail-open | 经裁定**不改本批**（理由按下段实测修正） | 见下 |

### P2-19 实测复核（与原始记载不符，以实测为准）

原记载为「链头锚点写入 fail-open ⇒ 需降级为至少 WARN 级用户可见输出」。**实测不支持该前提**——WARN 级告警**早已在位**，且读侧另有可见面；fail-open 亦非疏漏而是**刻意的设计取舍**。

**（i）降级告警已存在**（非待补项）：

- `engine/audit/src/audit-history.ts:499` — `} catch (e) {`
- `engine/audit/src/audit-history.ts:500-501` 注释原文：「fail-open：锚点写入失败不阻断审计主链路（与 appendHistory 既有取舍一致），仅告警——本次追加后尾部截断检测退化为「无锚点 = 跳过」的旧行为」
- `engine/audit/src/audit-history.ts:502` — `console.warn(\`[sofagent] 链头锚点写入失败（尾部截断检测本次不可用）: ${e instanceof Error ? e.message : String(e)}\`)`
- 同族另有 `engine/audit/src/audit-history.ts:601`：`console.warn(\`[sofagent] 链头锚点删除失败（请手动清理，否则可能误报历史截断）: …\`)`

即 `console.warn`（stderr，默认可见）已在位，「至少 WARN 级用户可见输出」这一要求**已满足**。

**（ii）读侧另有可见面，且「跳过」只针对文件不存在**：

- `engine/core/src/audit-history.ts:307-310` 注释原文：「finding-02 尾部截断防护——链头锚点校验（读侧）。… 判定顺序：**不可读 → unverifiable（黄）**；条数不足/锚位哈希不符 → tampered（红）」
- `engine/core/src/audit-history.ts:242-244` 注释原文：「锚点文件不存在时跳过，向后兼容。诚实边界：能同时重写 history.jsonl 与锚点文件的攻击者仍可伪造一致状态——锚点属防篡改证据强化而非密码学保证」

⇒ 锚点**存在但不可读**会落 `unverifiable`（黄，用户可见）；只有**文件不存在**（含本项 fail-open 后的退化态）才跳过，且这是为兼容锚点引入之前的历史而设。

**（iii）升 FAIL 会阻断审计主链**：该 `catch` 位于 `appendHistory` 的**追加路径内**——抛出即让**每次审计记录的写入**失败，等于以「丢审计留痕」换「锚点可用」，方向与「留痕优先」的核心取舍相反；且 fail-open 的退化是**有界**的（退回锚点引入前的旧行为，非静默数据丢失）。

⇒ 裁定：**不改本批**。若后续要提升该场景的可见性，方向应是读侧区分「锚点从未存在」与「锚点曾存在但已丢失」，而非把写侧改为 FAIL。

### 显式披露

1. **P2-21 / P2-22 两条的 commit message 未引用条目编号**（`61552c2f` 的 message 以内容描述代替编号）。两条的**内容均已落盘**，仅编号未在 message 中标注，故在本表内补齐编号以免上游对账时判为「缺项」。
2. **`61552c2f` 内含一段归属错位的改动**：并行维护者的「中文行内直引号统一」工作（ARCHITECTURE 36 行 / PHILOSOPHY 38 行 / LIMITATIONS 10 行含引号行）**被 `61552c2f` 一并提交**，未单独成 commit。裁决：**不重写历史**——内容零丢失，仅署名错位；重写会与并行写入者撞车。
3. 同一并行维护者自身 `a11122d3` 的 message 声称调整「156 处（ARCHITECTURE 59 / PHILOSOPHY 66 / DEVELOPMENT 14 / LIMITATIONS 12 / SECURITY 6）」，而**该 commit 实际只含 `SECURITY.md` 7 行 + `docs/DEVELOPMENT.md` 20 行**（`git show --numstat --format="" a11122d3` 实测）。两处记载不一致的事实如实记录，处置同上（不重写历史）。


---

## 十四、阶段四审查体系分发（A/B/C 三类 → 四文档）+ 收敛批

> 输入：fresh-eyes run-02 终态（round-05 findings：P0×2 / P1×6 / P2×8 / 人工介入×1 / 观察×6）+ 一~十二章交付面 + 十三章 bugfix 批。分类纪律：A 每新功能至少一条 / B 每修复一条 / C 只进校准不加检查项。
> **收敛纪律（本版追加）**：本阶段先做加法后做收敛——最终以「零上调警戒线 + 净减行数」收口，不把新面变成永久增量。

### A 类 → regression-checklist（**零新增维** · 真实归并 4 处 + 1 处解散）

| 处置 | 内容 | 依据 |
|---|---|---|
| **解散 #143**（原「v1.4.9 新交付面防复发」） | 三条断言分别移入 #51（验链双实现差分锁）/ #16（diff 主路径 fail-closed 对称）/ #110（计数解析 locale 无关 + 吞数判红）；另两条（五新 tool 注册、插槽符号）**删除**——已被 B 类场景 S413/S414/S416 覆盖 | 三判据①：新维度做的事已被既有维度/场景覆盖 ⇒ 不该新增编号 |
| **归并 #142 → #128** | fresh-eyes 修复批防复发族（v1.4.8 批）内容逐字移入 | 三判据②聚簇 |
| **归并 #129 → #128** | 同族 v1.4.4 批（19 项）内容逐字移入 | 三判据②聚簇 |
| **归并 #109 → #101** | check-docs 双面（预算超标 + 锚点扫描降级）合族 | 三判据② |
| **归并 #106 → #110（降为一行引用）** | 该维度内容即转跑 `check-test-count.sh`（已被门禁覆盖）⇒ 保留引用一行 | 三判据① |

**行数账**：1974 → **1947**（净减 27 行）；**警戒线零上调**（1950 保持不变，v1.4.8 的 1950 值沿用）。头部翻牌：**90 维 · 编号 1-141 · 51 编号已归并删除**。

### B 类 → acceptance-test.sh（四场景 · 场景数 339→343 · 折壳 2 处）

| 场景 | 行为锁 | 来源 |
|---|---|---|
| **S410（接线复活）** | Release body 卫生——本版 changelog 链接在位 + 零流程元说明 | 阶段三发现：该断言块位于文件尾 `exit` 之后 = **不可达死代码**（从未执行、无 pass/fail 上报） |
| **S413** | G9 设备接入——device 四 tool 注册 + 注册表五导出 + `/health` 三态判定（空巡检 fail-closed=dead） | 第一章 |
| **S414（原 S415 折入）** | v1.4.9 **数据承接面**——G5b 连接器（无策略 fail-closed / 非法声明 / 清单形态）**+** G1 血缘（事件形态 / 坏行计入 / 追溯数组 / 双 tool 注册） | 第十一、十二章 |
| **S416** | 第八章 敏感识别插槽——DetectorRegistry 四方法 + tierOf 三档 + L0 检测器形态 | 第八章 |
| ~~S417~~ → **折入 S379** | 计数解析链 locale 无关 + 吞数判红（与 S379「门禁自身 fail-loud」同族） | 阶段三存量修复（`7fadec7c`） |

**行数账**：4300 → 4381（分发）→ 4374（收敛：折壳 2 处 + 摘除不可达死代码 17 行）→ **4422**（阶段五 P0-3 补测 S418-S424 七场景，断言本体抽入 acceptance-node-probes.js 探针库——壳层 4 行/场景，对齐 S101/S156 先例）；警戒线 4375 → **4500**（v1.4.9 阶段五 P0-3 七场景实体 + 探针库化收敛后定线，行数 4422 ≤ 4500）。
**场景数三源同步**：acceptance 头部 / ROADMAP SSOT 口径段（最大场景号 S412→S416）/ DEVELOPMENT / LIMITATIONS 两处 / 本日志发布检查清单（339→343）。阶段五 P0-3 补测后再同步五处（343→350，最大场景号 S424），见阶段五批次记录。

### C 类 → fresh-eyes-calibration.md（v1.4.9 校准收编 5 条）

门禁「提取为空」≠「合规为空」· 自动化提交的大范围同构替换须核对 SSOT · 死断言与断言可达性 · 改断言必须重跑全量 · 结构性协议升级类 finding 归 C 不归 A/B（含 H1「链头哈希未入签名输入 ⇒ 删条目/重排/截断不可检测」的协议 v3 决策与 O1-O6 观察项留档）。

### 收敛批（本阶段收口 · 用户拍板「不要一直无限地往上去加」）

| 项 | 分发态 | 收敛态 | 手段 |
|---|---|---|---|
| checklist 行数 | 1973 | **1947** | 解散 #143 + 归并 4 处 + 注释压缩 |
| checklist 维数 | 94 | **90** | 归并 5 处（含 1 处解散） |
| acceptance 行数 | 4381 | **4374** | 折壳 2 处 + 摘除死代码 17 行 |
| acceptance 场景数 | 345 | **343** | S417→S379 · S415→S414（断言零删减）；阶段五 P0-3 补测后 **350**（S418-S424 七新场景，S417/S415 空洞号保留） |
| 警戒线 | 1985 / 4400 | **1950 / 4375** | 上调撤回，按「先归并对销」执行 |

> 残留说明：acceptance 净增 74 行为五新场景实体（339→343 场景 + 每场景断言密度），既有 339 场景经文案重复扫描**零冗余壳**（实测无重复 pass 文案）——进一步收敛需人工逐对复核老场景，非机械可做，留待后续版本按需处理。

---

## 十五、阶段五 P0-3 coverage 补测批（已实施 · 2026-09-16）

release-gate 20260916-01 复验裁定 FAIL 后的唯一遗留阻塞项：changelog 九章零场景锚点 + 四章部分锚。按 S330/S341/S342/S343 先例补行为实测场景（S418-S424 七条 + S413 扩心跳断言），场景数 343→350。**代表锚点：S418-S424 + S413 扩（commit `99718b0c`）**。

| 场景 | 行为锁 | 锚定章 |
|---|---|---|
| **S418** | G10 授权读取——参数缺失拒（invalid-params + [sofagent] 前缀）+ 未注册拒（reason ∈ 门禁四态） | 第二章 |
| **S419** | G11 上行通道——invalid-params + isError 形态 + 未注册拒 | 第三章 |
| **S420** | installer 五步标题/诊断四字段 + enqueue→心跳捎带→claim 往返（先心跳后入队——T12 在线判定） | 第四+五章 |
| **S421** | 派单语义——离线拒派 + reassign 落同能力在线备机（reassignedTo 对账）+ hold 挂起告警回调（ok:false + held-for-alarm 是本意形态） | 第六章 |
| **S422** | 蒸馏偏好对——配对方向 + 缺源 skipReasons 计数 + qualityScore 择优 + toRecords 衔接 | 第七章 |
| **S423** | 权重灰度 AB——同 key 确定性分流 + 0/100 端点 + 劣化判定附原因 + 无劣化对照 | 第九章 |
| **S424** | 模型清单——retired 过滤 + schema 非法降级原因（version≠1 才降级；缺文件=合法空态）+ 心跳 availableModels 捎带 | 第十章 |
| S413 扩断言 | 心跳半边：未注册拒绝 reason ∈ {not-registered, invalid-identity, revoked} + 拒绝留痕后 verifyDeviceEventsChain 可验 | 第一章 |

**工程形态**：断言本体抽入 `playbook/acceptance-node-probes.js` 探针库（v1.2.1 同构手法），shell 侧每场景 4 行调用壳——行数警戒线经探针库化收敛后定线 **4375→4500**（当前 4422）。

**签名纪律**（施工实锤，探针库头注释已记）：`registerDevice(identity, {kind, capabilities}, dataDir)` 三位置参形态与 `reportHeartbeat/enqueue/claim/reassign` 的 `opts.dataDir` 形态并存——传错形态 = invalid-params 假红；`generateAgentIdentity` 的 agentId 是随机 UUID，对账用返回值；hold 模式返回 ok:false + reason:held-for-alarm（挂起即本意）。

---

## 十六、release-gate run-02 修复批（已实施 · 2026-09-16）

run-02 verdict=FAIL（阻塞唯一来源 C-P0-1：workflow_export/import dist 直调零场景锁）。本批按 verdict §5 放行条件逐项闭环。**代表锚点：S425（C-P0-1）+ S426（C-P1-8）（commit `df06f0fd`）**。

**C-P0-1 解除（S425 新场景）**：补 G1 模板分发 dist 直调往返——导出落盘（五件套按实收录 + private 剥离计数 + 悬空 depends_on 清理）→ 导入回读（落地 v1 新 id + 节点 id/agent/task 逐项一致 + owner 落地者）→ 血缘回溯锚（lineageTrace ≥2 层且源企业/源版本对账）+ 六态拒收（duplicate-id / integrity-mismatch 篡改 / schema-gate 非法模板 / empty-after-strip 全私空包 / invalid-input / bundle-corrupt）+ tool-registry 双注册断言（S192/S398 先例路径，源码 SSOT）。场景数 350→**351**→**352**（+S426 审查体系四文档结构锁，C-P1-8 闭环；五处同批同步：acceptance 头部 / ROADMAP SSOT 段 / DEVELOPMENT / LIMITATIONS 两处 / 本日志检查清单）。

**trunk 构造纪律**：导入校验门与 CRUD 同一 zod schema（workflowCreateSchema）——node 必含 `id`/`agent`/`task` 三非空 string（visibility ∈ open/private/result-only）；导出面 readTrunk 只要求 nodes 为数组（导出不拦），往返成立的前提是 trunk 用合法 CRUD 形态构造。

**P0-1 / P0-2 去向留痕（run-02 C-P2-9 回写）**：模块十五编号「P0-3」系 release-gate run-01 三项 P0 之一——P0-1（install.sh sha256 钉值与 HEAD 不自洽）与 P0-2（发布拓扑序未含 engine/train）**不归本修复批**，按 run-01 verdict 修复指引排「阶段九 tag 前」批次闭环（P0-1 先修注释再重算钉值；P0-2 固化进 09-publish SOP 对账命令），与 C-P1-1 / C-P1-2 同期收口——编号不悬空。

**本批 P1 闭环清单**：C-P1-3（S417 去向注记回写 S379 壳）/ C-P1-4（S419 扩成功路径四断言）/ C-P1-5（S422 扩五元组+交接三要素+router 伴生三态）/ C-P1-6（S416 扩分类器三档+NER 协议三态）/ C-P1-7（S424 扩 skill 快照三态五断言）/ C-P1-8（S426 新场景：四文档结构锁）。

---

## 十七、release-gate run-03 修复批（已实施 · 2026-09-16）

run-03 verdict=FAIL（阻塞来源 C-P0-1：G11 数据上行通道主链路零场景覆盖；三 P1 中两项经零信任复验定谳为失真指控——S424 skill 快照五断言与 S417 双处去向注记均在位，根因是 run-02 修复批改探针未同步场景壳标题，coverage 审查按 precheck 标题对账）。本批闭环真实缺口。**代表锚点：S419 扩（C-P0-1 六断言）+ S418 扩（C-P1-1 放行侧）+ S418/S419/S424 三处壳标题同步（commit `649098fa`）**。

**C-P0-1 解除（S419 扩 tool 入口 happy-path）**：补 MCP tool 入口串联——采集声明 opt-in（saveDeviceUploadPolicy）→ deviceDataPush 放行入队（ok/queued/seq/auditLogged 四断言）→ 声明外类别拒（category-not-declared）→ 目的地不符拒（destination-mismatch）→ WAL 密文复核（明文 0.42 不落盘）→ 审计计量留痕（decision-log evidence 含 seq/delivery 六元组）。隔离形态：SOFAGENT_DATA 环境变量 + 模块级缓存刷新（dataDir/密钥常量 require 时固化，与无密钥面同手法）。

**C-P1-1 闭环（S418 扩放行侧）**：真实身份注册（generateAgentIdentity Ed25519 验签通过）→ 白名单声明（saveDeviceDataPolicy allowedDirs）→ 白名单内读取成功（content 一致 + auditLogged=true）→ 白名单外拒（边界字符 share-dir-secret 防绕过）。

**失真解除（壳标题同步）**：S418/S419/S424 三场景壳标题补全 run-02 已落的探针断言面（S419 WAL 四断言 / S424 快照五断言 / S418 放行侧）——coverage 审查按 acceptance-test.sh 场景标题对账，探针扩写不同步标题 = 「零覆盖」假红。S416/S422 壳标题 run-02 已同步，无需再动。

**R-P1-1 闭环（维度 101 引号缺陷）**：Markdown 说明行「**子项（归并自原 #109…」误落 bash 代码块内被执行（command not found）——移出代码块为独立小节；LIMIT_B 回显全角括号改半角斜杠格式（消除 locale 坏字节）；两个子检查补显式 ✅/❌ 判定标记（对齐维度脚本三铁律）。

**run-03 失真指控根因（工程纪律沉淀）**：探针断言扩写必须同批同步场景壳标题——coverage 审查的数据源是 acceptance-test.sh 的 scenario 标题行，与探针库断言本体是两套口径；只改探针不改标题，下一轮闸门必报「零覆盖」假红。S417 去向注记实为双处落盘（changelog B 类表 + S379 壳注释），coverage 只对账场景索引标题故未见。

**run-04 P1-2 降级留档（RC-2 闭环）**：verdict P1-2 指控「S419 四段链路仅前两段有锚点」经实测为 driver 标题截断假红——`release-gate-driver.mjs` 场景标题正则 `{0,120}` 上限把 S419 壳标题（超 120 字符）的「tool 入口 happy-path（加密上行+审计计量）」段截掉，coverage 对账时判后两段无锚点；实测探针库 s419 的 deviceDataPush happy-path 四断言与 decision-log evidence 六元组断言均在位（commit `649098fa`），四段链路全覆盖成立。verdict RC-2 备注的降级条件（「若截断段实际含后两段断言则降级 P2」）满足，P1-2 维持降级为 P2。

---

## 发版闸门场景锚定登记（阶段五 · 逐项结论）

> 本节登记 v1.4.9 发版闸门（release-gate-loop · 2026-09-16 run-01→run-04 闭环链）对 coverage 面的锚定核查结论，供阶段六准入与后续版本追溯。run-01 曾被主 session 零信任复验推翻（loop-end 直写 PASS 而 verdict §7 仍 FAIL），经 P0-3 补测批（S418-S424）→ run-02 修复批 → run-03 修复批 → run-04 四轮收敛，终态 **CONDITIONAL PASS（0 P0 / 4 P1 / 9 P2）**。

| 模块 | 闸门核查结论 | 依据 / 处置 |
|---|---|---|
| 三 · G11 数据上行通道 | 四段链路全覆盖（曾判「仅前两段」） | run-04 P1-2 定谳：driver `parseAcceptanceScenarios` 正则 `{0,120}` 上限截断 S419 壳标题（实测 221 字符）致 coverage 判后段零锚点，系**假红**；探针实测 `acceptance-node-probes.js` s419() 四段断言在位（拒绝路径 / WAL 加密 / tool happy-path / decision-log evidence），降级 P2，根因已修（上限 120→400，`af698dd9`）。 |
| 十三 · bugfix 修复批 | 仅首尾锚点 → 补映射口径闭环 | run-04 P1-3：补「验证载体映射（coverage 对账口径）」表（15 条目 × 单测/门禁/场景三类，每条 `git show --stat` 实测锚定 commit）——对齐 S343/S281「批次级代表锚点」先例（`3d5061ce`）。 |
| 十五/十六/十七 · 闸门修复批三段 | 状态字样失实 → 已闭环 | run-04 P1-4：三段均补代表锚点（S418-S424 / S425+S426 / S419扩+S418扩，施工 commit 锚定）+「（施工）」→「（已实施）」；全文施工态字样清零（L235/L731 描述性用法保留）。 |
| 附 · install.sh sha256 钉值 | **未到期**（阶段九 tag 前强制） | run-04 P1-1 → RC-3：钉值窗口期维持 v1.4.8 tag 值自洽（`101bfa62`）；bump 后重算基线，回填与 URL bump 同 commit（RC-4 → RC-3 时序）。 |

*登记依据：`~/.sofagent/data/forge-runs/release-gate-loop/2026-09-16/run-04/verdict.md` §三（P1 清单）/ §四（RC 表）。*

### 附：闸门 P2 观察项结论登记（9 项继承）

| 编号 | 项目 | 结论 |
|---|---|---|
| P2-1 | WIKI 指针 / 95 计数（维度 131） | 待证实漂移，随阶段六文档 bump 复核。 |
| P2-2 | post-commit hook 对账首 commit 缺空树兜底（维度 98） | 边缘形态，循 S222 空树锚路径补齐。 |
| P2-3 | DSH 依赖锁 `0.1.2-rc.1`（维度 103） | 在档漂移（`d24dc15a` 显式升级），0.1.2 正式版上架后回切。 |
| P2-4 | 维度 80 脚本 exit=1 语义 | 已按公约归一化；建议补显式 ❌ 输出。 |
| P2-5 | config.yml / daemon plist 环境跳过（维度 7/20） | 符合跳过语义；企业部署路径（维度 14）不受影响。 |
| P2-6 | coverage meta S417 现役区段缺去向注记 | **已收口**：S379 壳标题补「（原 S417 折入——计数解析链 locale 无关 + 吞数判红）」括注，与 S415 先例（S414 壳标题同款形态）对齐——coverage 对账数据源是场景索引标题行，注记入标题即对账可见。 |
| P2-7 | 模块一/二/十一/十二 tool 计数对账锚缺失 | 历史漂移 S172→S270→S348；本版新增 tool 较多，补「registry 总数 = SKILL/HANDBOOK 口径」对账锚点。 |
| P2-8 | 场景 4/41/96 info 级「未确认审计记录」 | **RC-5 已收口（预期行为留档）**：定性 = TMP_REPO 场景上下文预期，非对账漏报——`mktmp_repo()` 只建裸仓（无 `--init`、无 hook 装配），commit 走系统 git 从未产生审计记录，post-commit 对账在真实 HOME history 查无此 commit 报 info 属 fail-safe 观察如实上报；镜像对照 S411（显式装配 + 隔离 HOME）断言「干净 commit 不含『未确认审计记录』」PASS，证明装配环境对账精确——两场景是同一对账机制的正确两面。不升级。 |
| P2-9 | 场景 34c mock webhook 终止噪声 | 不影响判定，建议 trap 静默清理。 |

*结论依据：`~/.sofagent/data/forge-runs/release-gate-loop/2026-09-16/run-04/verdict.md` §三 P2 表。*

---

## 已知工程债与待裁定（阶段五收口登记）

> 登记交付前复核发现的、**非本批引入**但需后续处理的门禁级工程债。

| 债项 | 状态 | 说明 |
|---|---|---|
| 检查器锚点指向已删实现（维度 110） | ✅ 本版已修 | 「审计通过」锚自 `config-template.ts`（已删）改指 `hooks/post-commit` 唯一源；acceptance 锚改字面量（原 shell 展开绝对路径假红）。 |
| checkConflict 语义漂移（维度 25 子项 d） | ✅ 本版已修 | P1-14 修复后知识库走全局路径，裸 `process.cwd()` 读到本机真实知识库（78 孤儿项）→ 假红；改 `SOFAGENT_HOME` 隔离测优雅降级（对齐 S274 手法）。 |
| driver 场景标题截断上限（coverage 假红根因） | ✅ 本版已修 | 正则 `{0,120}` → `{0,400}`（`af698dd9`）；注释勘误 S419=221 / 最长 S376=236 字符（`a6fcf4c1`）。判长基准 = 全仓实测，禁凭汇报转写。 |
| test-count.sh locale 吞数 | ✅ 前批已修 | macOS 非 UTF-8 locale 下 `illegal byte sequence` 整段输出为空 ⇒ 解析链静默吞数；13 处 `LC_ALL=C` + 「有输出却提不出汇总 = 红」判据（`7fadec7c`）。 |
| install.sh lib 6 哈希回填 | ⏳ 阶段九 | RC-3：bump 后重算基线，与 URL bump 同 commit 回填（自 v1.4.8 tag 起有改动，不可沿用）。 |
| P2-8 审计对账 info 输出定性 | ✅ 已收口 | RC-5 定性 = TMP_REPO 预期行为留档（见上 P2 表）；非对账漏报，不升级。 |
| P2-6 S417 场景账沿革注记 | ✅ 已收口 | S379 壳标题补「原 S417 折入」括注对齐 S415 先例（见上 P2 表）。 |
| 维度 130 sha256 窗口态分支 | ✅ 本版已固化 | 窗口期 grep v1.4.9 钉值维持 v1.4.8 tag 值 + CHANGELOG ⏳ 标注消红；禁止提前回填 HEAD 哈希（URL 未切 = 校验必炸）。 |

---

## 与后续版本的依赖

| 后续版本 | 依赖 v1.4.9 的什么 |
|---------|-------------------|
| v1.5.0 | 数据集审阅人审面（治理 KPI 面板消费第七/八章数据集与分拣脱敏记录）· 合规报告导出（lineage 引用本章审计链） |
| v1.5.1 | G9 设备注册/心跳（G12 OTA 升级面向已注册设备，心跳离线挂起等待）+ G11 上行通道（升级后版本清单回传） |
| v1.5.4 | session 承接面（路由决策回流的消费端——routeReason 全链路）· 敏感度分类器（敏感度路由的输入前置件已就位，v1.5.4 只做调用侧分流） |
| v2.x | 设备联邦完整版（跨设备 workflow 分片执行 + 数据不出设备的联合计算 + 一体机本地推理与审计滞留回传合体——对齐 v2.0.0 离线 USB 节点）· router 伴生 exporter（v2.x 离线节点的网关侧组件） |
| **商业平台（模型层）** | ① 设备面板数据源（G9 注册/心跳）② 一体机/本地数据的脱敏语料供给（G10 → v1.4.4 语料导出管线衔接模型层训练）③ 一体机本地推理结论回传 / 节点计量摘要云端回传（G11 上行通道——脱敏 + 加密 + 断点续传出设备）④ session 蒸馏素材源（第七章 exporter 持续后训练数据回流——模型层数据飞轮入口） |

---

## 发布检查清单（汇总）

> 发版门禁总表——与各章「验收标准」是两个层次：验收标准 = 功能层面做没做到；本清单 = 发布层面能不能发版。**取证时间：2026-09-17（阶段六定稿批实测）**；阶段九发布前更新取证时间。
> ⚠️ 标「阶段九」的项须在 bump 后方可打勾（版本号段全部属此）。

### 通用门禁（2026-09-16 阶段三实测）
- [x] `npm run build` exit 0（全仓包 + 插件基座全量重建）
- [x] `npm test` 全绿（**4,805** tests / 13 包 workspace 口径）
- [x] `check-version.sh` 全绿（**119/119 项** · 2 警告为合法窗口态 · 1 降级跳过；SSOT = 1.4.9 实测已就绪——bump 已于阶段六 81f9d84c 执行。计数口径 = 脚本「检查通过: N/N 项」的检查项计数，2026-09-17 阶段八复验实跑值）
- [x] `check-test-count.sh` 全绿（**14 处对账自洽**）
- [x] `check-docs.sh` 全绿（A 7425/7450 · B 9337/9500 · E 3833/4000 · A+B 16762/17000）
- [x] `check-review-system.sh` 全绿（**13 项 ✅ · 0 警告**）
- [x] `check-unwired-exports.sh`（豁免 0）· `check-template-drift.sh`（6/6）· `check-storefront.sh` · `dependency-direction.sh`（14 包方向合法）全绿
- [x] `check-seam-contract.mjs` 全绿（WARN 0 / SKIP 0）· `check-guards.sh` 全绿（违规 0 / 警告 0）· `check-guard-fail-loud.sh` 四项故障注入全部报红（守卫失明必失声）
- [x] **阶段八复验（bump 后全量 · 2026-09-17）**：`pre-push-check.sh` **32 通过 / 1 警告 / 0 失败**（警告 = FORGE 已豁免用例 + tag 窗口态）· `check-silent-catch.mjs` 新增 0（304 条基线全命中）· `doc-discipline.sh` 0 违规 · `check-storefront.sh` FAIL=0 · `check-docs.sh` A 7445/7450 · B 9348/9500 · E 3916/4000 · A+B 16793/17000
- [x] **bump（阶段六）新暴露漂移 4 项——已修复复绿**：① pre-commit hook 头留 v1.4.8（bump 脚本原无 hook 处理步骤 → 已补 9c 段 + worktree 正负成对实测）② 插件生成段 8 文件留 v1.4.8（7 个 cordis.patch.yml 头 + 桥包 optionalDependencies 兄弟依赖；生成段唯一生产方 = `gen-plugin-manifests.mjs`，bump 后必跑）③ 13 处新增关键路径空 catch 未登记（按检查器豁免标记补「为何可静默」显式理由，零行为变更）④ `docs/API.md` 版本头 `v1.4.8（已发版）` 滞留待发版窗口（bump 不认「版本：vX.Y.Z」形态；该行此前仅对账工具数、版本号字段无门禁 → §22b 已扩版本号对账断言，正负成对实测：篡改即报红 exit 1）
- [x] `acceptance-test.sh` 全绿（**433/433 检查实例 · 352 场景 · SKIP 0 · EXIT 0**，run-04 闸门实测口径；沿革：阶段四收敛批 343 → P0-3 补测 350 → run-02 +S426 352）
- [x] shellcheck 零 error 零 warning（CI 同口径 `-S warning -s bash -e SC2034 -e SC1090 -e SC1091`；本轮改动 3 脚本实测 0 warning）

### 质量循环
- [x] fresh-eyes-loop：**run-01 aborted-signal**（环境缺 `SOFAGENT_LLM_C/D`，env 模板已补）+ **run-02 正常终态**（5 轮加权收敛 · P0=0 / P1=0）；两 P0（验链 prevHash 短路绕过 / diff 主路径 fail-open）与 P1×5 已回流修复
- [x] run-02 终态 P2 残留（八条 DEFER）**已全部修复收口**：diff-parser 三补（stderr maxBuffer / spill 失败 fail-closed / 文案逃生门）· dashboard 两补（404 双态 / 假时间戳）· umbrella engines 统一 · audit vitest 配置 `.mts` · **验链双实现差分一致性测试**（首跑即抓 core `no-prevhash` 分支漏修并把 core 侧对齐 kernel F01 语义）
- [x] release-gate-loop：run-01 假 PASS 被主 session 零信任复验推翻（loop-end 直写 PASS 而 verdict §7 仍 FAIL）→ P0-3 补测批（S418-S424）→ run-02/run-03 修复批 → **run-04 终态 CONDITIONAL PASS（0 P0 / 4 P1 / 9 P2）**；RC-1/RC-2 已闭环（映射表 + 去施工态 + P1-2 假红定谳），RC-3/RC-5 阶段九 tag 前收口；复验三件套全过

### 版本号（**阶段九 bump 后**打勾）
- [x] SSOT 版本号 bump 1.4.8→1.4.9（`bump-version.sh`）——2026-09-17 阶段八复验实测：root/audit package.json = 1.4.9（bump 已于阶段六 81f9d84c 执行；条目锚误抬已由 f73d7c45 双通道豁免根治 + f20daa1e 源锚恢复）
- [x] 文档头日期统一 + ROADMAP「现在在哪」同步——实测 9 个 docs 文档头 + SECURITY.md 均「v1.4.9 · 2026-09-17」且带「⏳ 待发版」标注（06-doc-finalize 第⑥条坑位无残留）；ROADMAP「现在在哪：v1.4.9（已交付）」
- [x] 发版状态三件套核对（WIKI 状态表 / ROADMAP / CHANGELOG 行尾）——三处同口径「⏳ 待发版」；CHANGELOG 索引行行尾预写漂移已修正（96fc1e68）

### 章节验收
- [x] 一~十三章验收标准 100% 勾选（**55 项**，实测 0 项未勾）
- [x] 关键数字跨文档一致（测试数 4805 / 规则数 24 / acceptance 场景数 352——S165 动态对账；沿革：阶段四收敛批 343 → P0-3 补测 350 → run-02 +S426 352，五处同批同步）
- [x] 发版闸门场景锚定登记（阶段五，见上方专段——run-01 推翻 → run-04 CONDITIONAL PASS 链 + P2 九项结论）
- [x] 已知工程债与待裁定登记（阶段五收口，见上方专段——4 已修 / 3 阶段九收口 / 1 窗口态固化）

### 阶段三发现并修复（非 fresh-eyes 来源 · 存量缺陷）
- [x] **版本口径分裂**：`a0fd04b2`（FORGE auto-commit · b-fix round-3）把 76 文件版本号抬到 1.4.9 而根 SSOT 未动 ⇒ 按实测惯例（v1.4.8 开发期全仓停 1.4.7、发版时统一 bump）回退，`4f906b3b`
- [x] **门禁吞数（真因被掩盖）**：`test-count.sh` 的 ANSI 剥离链在 macOS 非 UTF-8 locale 下遇多字节日志报 `illegal byte sequence`、整段输出为空 ⇒ 整包测试数被静默跳过，门禁只当「文档数字漂移」（S165 长期报「缺测试数 3604」= 4805 − audit 1201）⇒ 13 处改 `LC_ALL=C` + 新增「有输出却提不出汇总 = 解析失败（红）」判据，`7fadec7c`
- [x] **过时断言两处**：S274 双形态路由只认「三锚已重置」（场景 cwd 恒为仓外）· S384 registry 计数硬编码 95（实测 104）⇒ 双形态各断言 + 计数改下界，`cf745c8d`
- [x] **测试数声称回填** 4787→4805（README 双语 / WIKI / ARCHITECTURE / LIMITATIONS 共 8 处；沿革账保留历史值），`d5ce83dc`

---

## Release Notes · v1.4.9

> 面向用户的发布说明。**本节存在性 = 阶段六定稿必备项**（06 §铁律十一：缺该段 = 定稿不完整，打回）。GitHub Release body 与本段**同源同构**——生成动作 = **阶段八步骤六随发布 prompt 同批备草稿**（落 `~/Desktop/release-note-vX.Y.Z-body.md`，供作者放行前过目）+ **阶段九步骤七复核落终版**；按 [09-publish.md 三道工序](../releasing/09-publish.md) 生成并自检，**禁止把本段直接复制当 body**。

📡 引擎从「单机审计」长成「**多设备数据承接层**」——设备注册/心跳/派单一条链（G9）、授权读取 + 加密上行 + 审计计量一条链（G10/G11）、router 过站 session 承接一条链（T7），外加敏感识别三层插槽（T8）、权重灰度 AB（T9）、连接器注册与 workflow 模板血缘（G5b/G1）；MCP 工具面 95 → **104**。

**TL;DR (EN):** The engine now brokers multi-device data: device registry/heartbeat/dispatch (G9), authorized reads + encrypted uploads with audit metering (G10/G11), and router session ingestion for distillation (T7) — plus a 3-tier sensitive-detection slot architecture (T8), weight canary AB (T9), and connector/workflow-template lineage (G5b/G1). MCP tools 95 → 104. 4805 tests · 104 MCP tools · MIT.

## ⚡ Install / Upgrade

    # 新装（五形态任选：FDE 插件 / Skill / MCP / CLI / GitHub Action）
    curl -fsSL https://raw.githubusercontent.com/KongFangXun/sofagent/refs/tags/v1.4.9/install.sh | bash

    # 已装用户升级到本版
    npx @sofagent/audit@v1.4.9 --doctor

    # 一条命令装全四大功能包（裸名总包）
    npm i -g sofagent

    # GitHub Action（PR 自动审计）
    uses: KongFangXun/sofagent@v1.4.9

## 🔨 核心变更

### 📡 设备接入与数据面（G9/G10/G11）
- 设备注册/发现/心跳（Ed25519 身份 + 注册表 + 心跳新鲜度判定 + 任务捎带/回执进审计链）
- 数据目录白名单授权读取（`device_data_query`——平台拉数据必经授权门）+ 采集声明 opt-in 上行（`device_data_push`——WAL 加密暂存 / 明文不落盘 / 断点续传 / 审计计量 evidence 落盘）
- 派单语义：在线才派单 + 掉线改派/挂起告警（路由算法留给 v1.5.1 事件总线）

### 🧠 训练数据飞轮（T7/T8/T9）
- router 过站 session 承接：`router_session_push` schema fail-closed + 签名验签 + 切窗滚动 + 脱敏贯通 + cost 台账
- 敏感识别三层插槽（L0 内置正则 / L1 词典 / L2 NER 外挂协议）——分类器与脱敏管线共用一套架构
- 权重灰度发布 AB：hash 稳定分流 + 三维度劣化独立触发 + 噪声保护 + 回退 HMAC 留痕

### 🔌 平台接口面（G5b/G1）
- 连接器注册/发现（白名单三形态 + null 策略全拒 + 租户隔离）
- workflow 模板导出/导入 + 血缘追踪（事件流往返 / 环防护 / 完整性校验 / 私域泄漏拒绝）

### 🛠 工程效能（T6/T10）
- installer skill 四步装机引导（逐步 rollback + doctor 判据 + 验签 fail-closed）
- 执行侧模型清单双源扫描随心跳上报 + 执行时 skill 快照（打包/清理/审计锚点）

### 🔒 BugFix（上版遗留）
- 13 项修复：train webhook SSRF / fs-watch 启动行分流 / 引擎崩溃退出码独立为 4 / 审计链黄色 verdict 定位面 等（明细见开发日志）

## ✅ 质量验证

| 检查项 | 结果 |
|------|:--:|
| npm test | 4805 tests 全绿 ✅ |
| acceptance-test | 433/433 passed · SKIP: 0 · EXIT: 0 ✅ |
| shellcheck | 零 error 零 warning ✅ |
| check-version | 119/119 全绿 ✅ |
| 回归检查 | 90 维度 ✅ |
| release-gate | verdict=CONDITIONAL PASS（0 P0；RC-3/RC-5 已 tag 前收口）✅ |
| fresh-eyes | 单盲四角色两轮闭环（P0=0 / P1=0 终态）✅ |

## ⚠️ 破坏性变更

无公开包 API 破坏性变更。内部：`@sofagent/daemon` 巡检器 re-export shim 化（真实现迁至 core `federation.ts`），直接 deep-import 巡检器路径的调用方需改走包根导出。

## 🔗 深入了解

| 想看什么 | 链接 |
|---|---|
| 全部变更明细 | [CHANGELOG](https://github.com/KongFangXun/sofagent/blob/main/CHANGELOG.md) |
| 上手指南 | [HANDBOOK](https://github.com/KongFangXun/sofagent/blob/main/docs/HANDBOOK.md) |
| 有问题 / 晒场景 | [Discussions](https://github.com/KongFangXun/sofagent/discussions) |
| npm 包 | `@sofagent/audit` · 共 15 包（[npm](https://www.npmjs.com/~sofagent)） |

---

## 附录：测试与场景账沿革

> v1.4.9 P2-10 迁入：原 `docs/LIMITATIONS.md` 三处单行 2000+ 字符的「考古倾倒」长行
> （原 :414 测试账 4921 字符 / 原 :497 CI 覆盖账 2940 字符 / 原 :498 acceptance 场景账 2588 字符）
> 在此归档为可读列表；LIMITATIONS 原位只留当前值结论与指针。**本附录是沿革账、不是当值声称**——
> 当值以 `tools/check/test-count.sh` 实跑为准（LIMITATIONS 的当前值声称由 `check-test-count.sh` 门禁对账）。

### 测试账（审计核心 + 全 workspace · v1.4.1 起）

- v1.4.1 批次 2937→3178 +241，v1.4.2 批 +171（bugfix 批 +24 全部为 audit 包回归用例 878→902（H-01 三层防线 / H-02 密钥四类 / H-03 空白折叠 / G-01 基线 / G-07 webhook 脱敏）+ dev 批 +147（数据管道 53 / eval 闭环与环境 45 / dry-run 与报告 28 / FDE 六引擎 21，orchestrator 1295→1442）+ A2 data-URI 豁免三用例 902→905
- data-sovereignty data-URI 豁免两用例 905→907
- 四轮深挖 core data-paths 五用例 369→374
- orchestrator 占位豁免三用例 1442→1445
- eval 桥接两用例 1445→1447
- v1.4.3 批 +138：audit +12（stats 聚合）/ orchestrator +126（train-analyze 35
- train-monitor 22
- train-diagnose 26
- train-sandbox 14
- post-training-workflow 11
- env-anticheat 18）至 audit 919 / orchestrator 1648）+ 安全回归批 +34（engineer 路径守卫，orchestrator 1573→1607，总数 3507→3541）+ L4 修复器路径守卫批 +41（orchestrator 1607→1648，总数 3541→3582）+ v1.4.4 审查批 #73 占位重写 +12（daemon cron 1→5 / harness constraints 1→4 / think-generator 1→3 / daemon inspectors 1→4，总数 3582→3594）+ v1.4.3 阶段五 run-02 闭环批一 P0-3 +6（doctor Ontology 完整性检查 6 用例，core 374→380，总数 3594→3600）+ 审查修复批 +11（config-loader cost 透传契约 2 用例 core 380→382
- webhook IPv6 SSRF 防护 6 用例
- audit-log 落盘脱敏 3 用例，audit 919→928，总数 3600→3611）+ 出站防护与原子写契约批 +8（daemon webhook 推送 SSRF 守卫 5 用例
- push-target 2 用例 287→294
- eval 原子写契约 1 用例 32→33，总数 3611→3619）+ 同批形式循环依赖清理删除 loop-audit-history.test.ts −6（orchestrator 1648→1642，净 3619 持平）+ v1.4.4 审查批 stats E 系列规则码回归 +1（audit 928→929，总数 3619→3620）+ v1.4.4 审查修复批 +15（quick A9 range 回归 4 用例
- audit-history 降级增强 3 用例 audit 929→936
- verify 三哨兵三态 6 用例 core 382→388
- daemon 错误日志轮转 2 用例 294→296，总数 3620→3635）+ v1.4.4 开发批 +109（audit 936→976：decision-query 因果链与先例检索 6 用例
- 语料导出三件套 34 用例；core 388→393：doctor daemon 守护感知 5 用例；orchestrator 1642→1697：artifact-register 13 用例
- train-compare 11 用例
- 语料导出配套；daemon 296→302：daemon 退出码落盘与 doctor 感知 6 用例；mcp 154→157：corpus-export 协议面 3 用例，总数 3635→3744）+ v1.4.5 修复批 +100：audit 978→1030（webhook SSRF DNS 复验 / A2 加固 / stats 口径 / AgentShield 等回归与新增 +52）、core 393→416（config 签名旅程 / doctor hooksPath / config-loader 契约 +23）、daemon 302→325（cron 分层巡检接线 / daemon-health 版本 / dream-cycle 守卫 +23）、orchestrator 1703→1705（node-executor 降级假绿 / pluginMeta 版本 +2）、mcp 158→158（±0），总数 3753→3853（基线为 git tag v1.4.4 逐包实测 978/393/302/1703/158；早期记录 3744 起点与 976/1697/157/358 为开发批时点口径，2026-09-05 checkout 实测修正）
- S2 遗留清零批 +3（appendHistory 深扫脱敏行为锁×3，audit 1030→1035），总数 3853→3858
- 第一批后训模块 +107（train-serve 21 / train-compliance 19 / train-continuous 12 / retention-policy 15 / train-deliverable 20 / daemon continuous-training 9
- train-archive 11）+ 第七章二/三批 +20（orchestrator evolution-feedback-loop 5
- tool-evolution 7、mcp evolution-dynamic-tool 5、daemon 归并 −2 修正）＝ 4015（OpenClaw 4 插件用例走根 `npm test --workspaces` 统一执行，不计入 12 包口径）+ 第四批收尾 +28（fde-session-mgr 17
- mcp fde-interview-init 2
- 既有对齐 9）+ fresh-eyes 收编批 +4（finding-04/05/07 retention 三用例
- finding-08 audit-history 豁免收窄一用例，audit 1035→1036 / orchestrator 1842→1845）+ fresh-eyes 收口批 +6（链头锚点 history-chain-head 五用例
- 创世条目指纹比对一用例，audit 1036→1042）＝ 4055）+ v1.4.7 批次 M cli-quick fail-loud 三形态（audit 1042→1046）+ v1.4.6 批 +47（train multi / train cloud / sorting gate / data push 四新面，orchestrator 1845→1892，总数 4055→4102
- DSH rc.1 适配批 +6（orchestrator 1892→1898），4102→4108）+ 边界收缩批 −20（模板资产迁离删 3 / trainEnvInit 删 6 / model-downloader 删 17，orchestrator 1898→1878，总数 4108→4088）+ v1.4.7 批次 I 静默降级两态（daemon 372→374 / mcp 165→167，总数 4088→4092）+ 批次 I builtin-agents/worktree 观测与 ab-runner fail-loud 回归（orchestrator 1878→1880，daemon 374 持平，总数 4092→4094）+ 批次 M/G 测试增补收口（总数 4094→4107）+ v1.4.7 开发批 +142（G7+G14+G6 多租户/CRUD/可见性 24298c0a、G2 能力缺口 0171426f、上岗 prompt abc3f932、G13 PR 生命周期 a52a6ab4、G4 绩效 12df6c25、章十 audit-reducer 3ca7b534、章九 USB 6de0a906、章十二云训练 a62a4dc2、章十三 data_push 6fd67f50、章十四静态加密 7d969348、章十五 repo-hash 隔离 7d181083——audit 1046→1090 / core 416→429 / mcp 167→190 / daemon 374→414 / orchestrator 1880→1932，总数 4107→4279）+ v1.4.8 bugfix 批 +10（audit 1090→1099，总数 4279→4289）+ v1.4.8 开发批 +89（plugin-gate 15
- tool-gate app 策略 9
- escalation 11
- quota-gate 8
- formations 10
- load-chain 压缩 9
- model-preference 7
- exec-discipline 16
- native-gate 4——audit 1099→1143 / core 429→448 / orchestrator 1932→1974 / harness 30→39 / evolve 33→37，总数 4289→4407）+ 深模块审查修复批 净减 9（audit 规则元数据断言集中减 20 / orchestrator loop 归位与出口契约增 3 / daemon 巡检器金名单增 6 / mcp 知识页收编与 schema 守卫增 2——分包终值 audit 1123 / orchestrator 1977 / daemon 420 / mcp 193）+ A2 规则修复批 增 3（函数调用 RHS 豁免回归锁——audit 1123→1126）；审计链内核与跨链回归批 增 20（审计链内核用例 audit +15 / 跨链用例 orchestrator +5）后为 audit 1143 / 总数 4429（末端还有 train 拆包与 A21 回归批 +8：新增 train 包 647 / orchestrator 迁出 641 / audit 1141→1143，逐批明细见 v1.4.8 开发日志）+ v1.4.9 bugfix 批二 P0-2 脱敏表补族回归锁 +1（core 448→449，总数 4429→4430）+ v1.4.9 bugfix 批三 P1-3 train webhook SSRF 合并 +7（train 647→654）+ P1-12 fs-watch 启动行分流 +5（daemon 420→425）+ P1-13 doctor 版本提示按安装形态分流 +2（core 449→451）+ P1-15 引擎崩溃退出码独立为 4 +4（audit 1143→1147），总数 4430→4448；实测见 `tools/check/test-count.sh`，flaky 复跑机制内置，以脚本判定为准，与 pre-push-check 一致

### CI 覆盖账（同一账链的例行摘要口径）

- v1.4.1 批次 2937→3178 +241，v1.4.2 批 +171，v1.4.3 批 +138（audit +12 / orchestrator +126）：bugfix 批 +24（audit 878→902）+ dev 批 +147（orchestrator 1295→1442）+ 安全回归批 +34（engineer 路径守卫，orchestrator 1573→1607，总数 3507→3541）+ L4 修复器路径守卫批 +41（orchestrator 1607→1648，总数 3541→3582）+ v1.4.4 审查批 #73 占位重写 +12（总数 3582→3594）+ v1.4.3 阶段五 run-02 闭环批一 P0-3 +6（core 374→380，总数 3594→3600）+ 审查修复批 +11（core 380→382
- audit 919→928，总数 3600→3611）+ 出站防护与原子写契约批 +8（daemon 287→294
- eval 32→33，总数 3611→3619）+ 同批删除 loop-audit-history.test.ts −6（orchestrator 1648→1642，总数 3619 持平）+ v1.4.4 审查修复批 +15（audit 929→936 / core 382→388 / daemon 294→296，总数 3620→3635）+ v1.4.4 开发批 +109（audit 936→976 / core 388→393 / orchestrator 1642→1697 / daemon 296→302 / mcp 154→157，总数 3635→3744）+ v1.4.5 修复批 +100（audit 978→1030 / core 393→416 / daemon 302→325 / orchestrator 1703→1705 / mcp 158→158，总数 3753→3853——git tag 实测基线，早期 3744/976/1697/157/358 为开发批时点口径已修正）+ S2 遗留清零批 +3（audit 1030→1035，总数 3853→3858）+ 第一批后训模块 +107
- 第七章二/三批 +20（进化采样桥/L4 工具管线/动态桥）+ 第四批收尾 +28（fde-session-mgr 17
- mcp fde-interview-init 2
- 既有对齐 9），总数 4015→4045）+ fresh-eyes 收编批 +4（finding-04/05/07 retention 三用例
- finding-08 audit-history 豁免收窄一用例，audit 1035→1036 / orchestrator 1842→1845），总数 4045→4049）+ fresh-eyes 收口批 +6（链头锚点 history-chain-head 五用例
- 创世条目指纹比对一用例，audit 1036→1042），总数 4049→4055）+ v1.4.6 批 +47（train multi / train cloud / sorting gate / data push 四新面，orchestrator 1845→1892，总数 4055→4102）+ DSH rc.1 适配批 +6（orchestrator 1892→1898，总数 4102→4108）+ 边界收缩批 −20（orchestrator 1898→1878，总数 4108→4088）+ v1.4.7 批次 I 静默降级两态（daemon 372→374 / mcp 165→167，4088→4092）+ 批次 I builtin-agents/worktree 观测回归（orchestrator 1878→1880，4092→4094）+ 批次 M/G 测试增补（4094→4107）+ v1.4.7 开发批 +142（多租户 v0 / workflow CRUD / 可见性 / 能力缺口 / 上岗 prompt / PR 生命周期 / 绩效 / audit-reducer / USB 烧录 / 云训练通道 / data_push / 静态加密接线 / repo-hash 隔离——audit 1046→1090 / core 416→429 / mcp 167→190 / daemon 374→414 / orchestrator 1880→1932，总数 4107→4279）+ v1.4.8 bugfix 批 +10（audit 1090→1099，总数 4279→4289）+ v1.4.8 开发批 +89（总数 4289→4407）+ 深模块审查修复批 净减 9（audit 规则元数据断言集中 / orchestrator loop 归位与出口契约 / daemon 巡检器金名单 / mcp 知识页收编与 schema 守卫——分包终值 audit 1123 / orchestrator 1977 / daemon 420 / mcp 193）+ A2 规则修复批 增 3（audit 1123→1126）；审计链内核与跨链回归批 增 20 后为 audit 1143 / 总数 4429（末端还有 train 拆包与 A21 回归批 +8，逐批明细见 v1.4.8 开发日志；分包账见「测试覆盖范围」节）+ v1.4.9 bugfix 批二 P0-2 脱敏表补族回归锁 +1（core 448→449），总数 4429→4430
- v1.4.9 bugfix 批三 P1-3 train webhook SSRF 合并 +7（train 647→654）+ P1-12 fs-watch 启动行分流 +5（daemon 420→425）+ P1-13 doctor 版本提示按安装形态分流 +2（core 449→451）+ P1-15 引擎崩溃退出码独立为 4 +4（audit 1143→1147），总数 4430→4448
- v1.4.9 bugfix 批三续 P1-4 custom 层读写根对齐 +4（harness 39→43 / constraints 4 用例）+ P1-5 A/B 约束链读空 fail-loud +3（ab-test 17→20）+ P1-1 自动压缩接线 +5（harness 43→48 / load-chain-wiring 5 用例）+ P1-10 A1 按变更方向分级 +8（audit 1147→1155），总数 4448→4468；全绿，详见上方「测试覆盖范围」节，实测见 `tools/check/test-count.sh`，与 pre-push-check 一致
- v1.4.9 bugfix 批四 G-17 native gate adopt 自指删 live 文件回归锁 +8（evolve 37→45：`runEvolve` 数据丢失四态 4 用例（live 不得消失 / 候选落 staging 临时副本 / 备份面补救 / 输入不存在不伪造成功）+ `isEvolveAvailable` G-11 按模式判 2 用例（native 恒 true / cli 探活真执行外部二进制且看退出码）+ `native-gate` 自指 adopt 与 `.gate-tmp-*` 收尾 2 用例）+ G-5 审计链黄色 verdict 定位面 +3（audit 1155→1158：两种不同坏输入 ⇒ 输出必须不同 / 校验窗口截断仍报全量序号 / 原因码与条数可见），总数 4468→4479；全绿，实测见 `tools/check/test-count.sh`，与 pre-push-check 一致
- v1.4.9 批 1（G9/G10/G11 设备注册与数据面）+44：audit 1147→1167（device-register 白名单/词法/重名/租户隔离/注册表损坏 + device-data 授权/加密往返 20 用例）/ daemon 425→476（心跳捎带回执·fs-watch 设备离线巡检·/health 三态端点·上行 WAL 断点续传 51 用例）/ mcp 193→212（device_register/device_list/device_data_query/device_data_push 四 tool 协议面 19 用例），总数 4479→4523；全绿
- v1.4.9 批 3（G5b 连接器 + G1 workflow 模板与血缘）+50：audit 1167→1186（plugin-gate 连接器面 21 用例——白名单三形态注册/拒绝、null 策略全拒、词法、重名、租户隔离、分列断言、注册表损坏故障注入）/ orchestrator 1341→1354（workflow-lineage 13 用例——事件流往返、幂等键、坏行故障注入、fork 链、import 链、混合链、环防护、buildBundle/chainFromBundle）/ mcp 212→228（connector-workflow-tools 集成 16 用例——注册/拒绝审计、过滤隔离分列、五件套、剥离、全私域拒绝、全链往返、schema 门、完整性、私域泄漏、冲突、本体合并、TOOLS 接线 103），总数 4523→4639（其中 v1.4.9 批 1 后 bugfix 中间值 4448→4523 已并入上段与本段合并口径）；全绿，实测见 `tools/check/test-count.sh`，与 pre-push-check 一致
- v1.4.9 批 4（T6 installer skill + T10 第三项模型清单与 skill 快照）+39：daemon 476→504（model-inventory 18 用例——注册表扫描三态（正常/缺失合法空/损坏降级/版本非法）、端点探测五形态（正常/非 2xx/超时/结构不符/连接拒绝）、合并去重 registry 优先、心跳字段三态（数组整组替换/显式 null 清空/undefined 保留原值）、事件流计数 models=N skills=N；installer-skill 10 用例——四步标题齐/诊断五字段模板/逐步 rollback/doctor 判据/验签 fail-closed 转述/policy 语义/上游闭环/装完即注册联动/行数预算 ≤200）/ orchestrator 1354→1365（skill-snapshot 11 用例——递归收集字典序排除非 md、打包保相对路径、emitDecision 审计锚点 evidence 断言、mtime+sha256 版本可证、空 root 走完流程、清理零残留、幂等、兄弟会话隔离、损坏 manifest 返 null、清理后 decision-log 仍在），总数 4639→4678；全绿（daemon 45 files 504 / orchestrator 91 files 1365），实测见 `tools/check/test-count.sh`，与 pre-push-check 一致
- v1.4.9 批 5（T7 session 承接 + T8 敏感识别三层插槽 + T9 权重灰度 AB）+109（core +45 / train +54 / mcp +10）：core 481→526（detector-registry 33 用例——注册重名拒/管线调度/aggregateSpans 同区间合并+佐证加成 0.1+跨族保留+升序/tierOf 三档/applySpans 从后往前替换；detector-glossary 12 用例——CSV/Excel 名录接入/entity 名录接入/中文直接匹配/ASCII 词边界/多别名/空词典）+ train 657→711（批前基线实测 657——ledger 前值 654 系批 1-4 期间 3 用例未入账，本批一并校准；session-ingest 28 用例——schema 八字段 fail-closed 三形态/切窗滚动+重叠/角色映射四态/续接五元组双向断言+摘要三要素/handoff/exporter 签名验签/distill 配对跳过/usage→cost/key 四件；weight-canary 16 用例——hash 稳定分流/三维度劣化独立触发/minSampleSize 噪声保护/回退调用+HMAC 留痕；sorting-gate 追加 applyPreClassification 4 用例——sensitive 直拦/public·internal 走正则/分层不越权/五标签回归锁；dataset-builder 追加 chat 形态 6 用例——messages 解析/非法 JSON 跳过/脱敏贯通/defaultSanitize 递归/validator 兼容/sft 回归锁）+ mcp 228→238（router-session-push 10 用例——schema fail-closed/幂等拒重复/落盘路径/脱敏/cost 台账 append/HMAC 签名字段/TOOLS=104 锁），总数 4678→4787；全绿（core 38 files 526 / train 36 files 711 / mcp 34 files 238），实测见 `tools/check/test-count.sh`，与 pre-push-check 一致

### acceptance 场景账（v1.4.2 起）

- v1.4.2 阶段三 S333-S339 七场景增量 265→272
- 存量清零 S340 272→273
- 章六补测 S341 273→274
- 章五零覆盖补测 S342/S343 274→276
- 阶段十二回写 S344 276→277
- v1.4.3 bugfix F-03 行为锁 S345 277→278
- v1.4.3 阶段三 S346-S348 三场景增量 278→281：审计聚合 CLI 行为实测/反作弊基线三防线锚点/训练监控三 tools 注册面
- 阶段五 coverage 补测 S349-S351 三场景增量 281→284：训练沙箱三约束行为实测/训练需求推导行为实测/后训练 workflow 模板解析
- v1.4.3 阶段五 run-02 闭环 S352-S355 四场景增量 284→288：DSH 执行深化三步锚点/train_diagnose 行为实测/入口导览与 onboarding 断层走查/存量清扫零残留
- run-04 coverage 闭环 S356 288→289：doctor Ontology 完整性检查锚点，补十三章零覆盖 P0-1
- run-05 coverage 闭环 S357-S358 两场景增量 289→291：审计聚合触发率数值实测/train_status 行为实测，S347 同批补四形态映射锁
- 闸门 run-05 P1 批 S359 291→292：过时承诺排期化/悬空引用补锚点/三态退出码防复发
- 闸门 run-06 误报批 S360 292→293：规则数 24 双口径锚点/维度 9 探针 A+E 全口径/PASS 场景级断言输出/S165 标题去 158 残留
- v1.4.3 阶段十二回写 S361 293→294：lock 零本地部署树路径防复发锚点
- v1.4.4 闸门 run-01 判断层 P0-1 闭环 S362/S363/S365-S370 九场景增量 294→303：v1.4.4 十模块验收——补章九收编锚点 S371——语料导出 27 编号位/方法论三锚点+脱敏闭环/权重部署哈希红线/产物注册人审语义/对比训练 ROI 排序/因果链回溯+先例打分+HMAC 篡改判定/CI 供应链四锚点/章七十收口八锚点（原 S364 corpus_export 双入口对账真实归并入 S348——同版归并对销 1 处解锁 acceptance 警戒线同版上调，断言零删减）+ 闸门 run-06 coverage 闭环 S372 303→304：章十一阶段四 B 类行为锁补测批四测试文件在位锚
- 五代表断言锚
- v1.4.5 第七章二/三 S373/S374 归并对销净 +1（S265 归并入 S264
- S366 归并入 S369，断言零删减）304→304：反哺闭环端到端（真实采样数据→harvest→jury→promote 链路级）/L4 工具层自进化全流程（候选→扫描→人审→注册→invoke 可调+静态计数不漂移）+ 阶段四 S375 304→305：train 五新面行为锁（deliverable 打包+HMAC verify 篡改拒绝/compliance PII findings+provenance 台账/retention symlink 拒绝保留源/serve 三 tools 注册面）+ v1.4.6 阶段三 S376/S377 305→307：train multi/cloud 行为锁
- v1.4.6 追加交付 S378 307→308：npm 裸名总包 umbrella 行为锁
- 流程加固批 S379-S382 308→312：防线失明自检/分支收编标记对账/空 diff message 类审计/driver 冻结窗口锁
- v1.4.6 阶段四防膨胀批归并 2 处 312→310：S367 归并入 S369（章四对比训练三断言并入章三+六场景壳）+ S332 归并入 S331（双 manifest 与 bump 通配守卫合族），断言零删减
- v1.4.6 边界收缩批 S383 310→311：边界收缩四批行为锁（删除符号导出面零残留/TrainExecutor 隔离/四场景判定语义与参考模板在位/外部装载面可用，coverage 缺口闭环），断言零删减
- v1.4.7 商业平台接口批 S384-S387 四场景 311→315：G 系列工具面五新 tool/PR 域收口真判定/云通道接线装配面/daemon 接线收口（release-gate coverage 闭环）+ run-02 verdict 路径 A 五模块锚点 S388-S392 315→320：G6 可见性/G7 租户隔离/G8 模板/上岗 prompt/G14 CRUD——九场景零锚两轮收口
- run-03 闭环批 S393-S398 320→326：G2/G4 语义层
- 批 J/质量循环修复批锚
- USB 往返
- 四原则扫描
- run-04 RC-1 收口 S399/S400 326→328：批 G scheduler 执行链
- 批 O L2 巡检观测性
- v1.4.8 阶段四 A 类分发 S401-S409 九场景 328→337：策略门×2/行为分级×2/成本压缩×2/模型进化/纪律/安全豁免（九新面各 1 锚点，D7 欠账兑付））+ v1.4.9 bugfix 批二 P0-1 回归锁 S411 337→338：hook 场景 treeSha 跨阶段错配三态锁（干净 commit 回声 / soft-reset 换料不命中 / 相邻提交各自命中）+ v1.4.9 bugfix 批三 P1-10 回归锁 S412 338→339：A1 按 DiffFile.status 方向分级（沙盒 git rm .env 补救 commit 退出码 ≤1
- 保留「已移除…历史仍在」指引）
