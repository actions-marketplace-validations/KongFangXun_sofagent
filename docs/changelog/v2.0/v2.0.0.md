# v2.0.0 开发日志 — S1M 宣告大版本（FDEing × S1A 身份切换·S1A = S1M + Harness）

> 📋 **已排期。** 前置依赖：v1.9.0（**四阶段收口达阈——本版硬前置门**：判据 schema 定稿 / ECE 达阈 / DecisionChannel 实现 + 离线推理 + 权重分发通路 / 三原语补概率 + 四层性能标准落数，任一未达阈则本版不启动——[v1.9.0 §七](../v1.9/v1.9.0.md) 两条分支的第一分支）+ v1.6.0（§三 理念升级驱动的文档重构——本版全站口径对齐章的母本；§四 判定底座的边界——本版对外叙事的边界 SSOT）+ v1.8.0（§六 自带件形态成立——本版宣告的形态基础）
> ⚠️ **尚未实现。** 以下内容为版本目标，不代表当前可用能力（开发排期见 ROADMAP）。
> 状态：已排期 · 2026-08-29（v1.5.0 系列规划首版落盘，时为数据主权大版本）· **2026-09-22 定位重写（用户拍板）：V2.0.0 交付一个决策模型——把项目从「审计引擎 harness」升级为「决策模型」，USB 节点合体降级为分支功能（原两章迁出）** · **2026-09-22 二次拍板：V2.0 目标 =「基于 FDEing 的能力，打造一个 harness 与决策模型」**（公式后经多轮定稿为 FDEing × S1A，S1A = S1M + Harness——见 §一宣告行）**；且 V2.0 起版本全部是 S1M 的迭代域，V2.0 前的能力不放 V2.0 后的版本去迭代（网络出口治理面与 AI 节点已按归位再分配落 [v1.5.2](../v1.5/v1.5.2.md) / [v1.5.4](../v1.5/v1.5.4.md)，见 §五）** · 原 2026-09-16 收编的网络出口治理面（Guardian 启发）内容零丢失，仅版本归属变更 · 2026-08-29 前移拍板（模型路由 / Vault / 多实例表决前移 v1.5.4）与本版无依赖关系，随数据主权定位一并归档
> 状态（续）：已排期 · **2026-09-22 用户拍板（探索方向清仓 + 兼容层退役清单「加」）：新增第六章「兼容层退役清单」——登记在仓兼容层（L1 未就位兜底 / 判定源降级路径 / `Choice` 纯布尔兼容路径 / 旧命令 shim / DSH fallback 保险绳）并逐条挂可机械观测的退役触发条件，同时立「新增兼容层须同批登记本章」纪律；本章为登记与排期纪律（非功能），不违反宣告版零新能力纪律**
> 状态（三续）：已排期 · **2026-09-22 用户拍板（问「已开发好的内容在 V2.0.0 有没有要退役的」）：新增第七章「已交付能力面的归属与退役清单」——三因子归属分类 + 定位漂移退役候选（具名带证据与触发条件）+ 边界补划界三条 + V2.0 后版本域冲突面登记（活文档 9 处 `v2.x` 承诺逐条裁定）；立「新增能力须标三因子归属」「真删三条件」两条纪律。本章为退役登记与纪律（非功能），不新增任何判定能力**
> 状态（四续）：已排期 · **2026-09-22 用户四次拍板（同日）**：① **退役基调**——「该删就删，不要关闭」：浏览器四件套与 MLflow exporter 从关档候选改退役删除（「历史版本在 git 上，需要者自取」，保项目纯洁性）；② **USB 复盘改判**——源码复读证明骨架与新形态兼容（验签 / 密钥驻内存 / 便携 env / 零残留），USB 从「真删」改「**重定位为 DSH 节点 + 判定件的交付介质**」（机器 + USB = 无头判定节点），两档交付排进 [v1.8.0 §五](../v1.8/v1.8.0.md) 离线介质导入扩容（A 安装器档 + B 便携运行时档）；③ **A 表评估框架升级**——V1.x 定位是「harness 层 + plug-in 功能」，V2.0 主干升级为 S1M（General Decision Model）、其余皆插件，已交付面（107 tools / 82 能力位 / 13 模块包）按「插件还是死重」统一评估

## 定位

> 🏁 **S1M 宣告大版本**——V2.0.0 只做一件事：**宣告身份切换**。产品从「审计引擎 harness」（FDE Harness——治理 7×24 无人值守部署的 AI 节点）升级为 **FDEing × S1A（S1A = S1M + Harness）**——「基于 FDEing 的能力，打造一个 harness 与决策模型」：FDEing 是方法论 plug-in（把 FDE 的进场判断力产品化、流程化的工作流能力），**S1M 是本体（System One Model——判据集 · 三原语 · 校准概率 · 弃权语义；命名承接判断与生成分离的行业谱系，见 §一）**，Harness 是治理环境（约束层五能力：注入·审计·回溯·沉淀·进化）。判定底座施工期（v1.6.0–v1.9.0）已把「本体」造完；本版不再新增判定能力，只做六件事：**宣告（§一）· 术语清洗（§二）· 对外叙事重定位（§三）· 原规划去向落盘（§五）· 兼容层退役清单（§六）· 已交付能力面的归属与退役清单（§七）**。**为什么身份切换值得一个独立版本**：v1.9.0 §八已论证——术语全站统一后「切换时不需要再做一轮术语清洗」，且「形式上成立却内容未达阈时宣告，等于把叙事跑在实现前面」；本版就是那个「达阈之后的宣告动作」本身。**宣告的资格链**：v1.5.x 备载体（治理环境）→ v1.6.0–v1.9.0 装本体（判定底座）→ 本版宣告「本体与载体合一」。**V2.0 后的版本域纪律**：V2.0 起全部是 S1M 的迭代域——V2.0 前的能力不放 V2.0 后的版本去迭代；V2.0 后做什么暂不定义，由 S1M 自身的产品化需求驱动。

---

## 一、身份宣告（FDEing × S1A · S1A = S1M + Harness）

> **形态归属**：`[主干]`（交付·身份宣告——本版为**宣告与口径动作**）｜ —

### 交付

| 组件 | 能力 |
|------|------|
| **S1M 首次定义（术语 SSOT 落地）** | **S1M = System One Model（系统一模型）——本仓决策模型的名字**。核心文档身份锚定段的首次定义（此前 FDE 面已有运行时展开——[FDE/README.md](../../../FDE/README.md) 强调段与 [FDE/S1M-DECISION-POINTS.md](../../../FDE/S1M-DECISION-POINTS.md) 头部，属场景侧使用；本版把定义落 [ARCHITECTURE.md](../../ARCHITECTURE.md) 身份锚定段为**全站 SSOT**，FDE 面定义性文字改指向该锚——原 [v1.9.0 §八](../v1.9/v1.9.0.md) 使用未定义的术语债本版闭合）。命名承接判断与生成分离的行业谱系（Kahneman「系统一/系统二」的快思考分类 + TypeSafe AI Jev 所代表的 System One 模型类别——不生成文字、只输出类型化决策 + 校准概率；行业锚见 [ARCHITECTURE.md](../../ARCHITECTURE.md) System One 段与 [VALIDATION.md](../../VALIDATION.md) 同名节，本版把该谱系命名为仓内术语）。语义：判定面（判据集 · 三原语 · 校准概率 · 弃权）是**本体**，约束层五种能力（注入·审计·回溯·沉淀·进化）是它**自带的执行与治理环境**——「可治理」内置在定义里（S1M 是本体 + harness 是载体，Governed 不再需要单独成词）。**为什么 S1M 而不是裸 Decision Model**：裸模型只能卖模型（越界——[v1.6.0 §四](../v1.6/v1.6.0.md) 边界判据④「不承担模型提供者角色」；裸决策模型无治理环境即不可自持——离线不可跑、判据不随仓开源）。S1M = 本体 + 自带治理环境，离线可跑、判据与权重随本仓开源、不承诺模型性能只提供通道 |
| **FDEing × S1A 宣告（公式定稿·方法论先行、制品落地）** | 身份公式定稿为 **FDEing × S1A，其中 S1A = S1M + Harness**。**FDEing 的形态（仓内 plug-in·强绑定）**：FDEing 是**住在本仓的方法论 plug-in**——形态上可插拔（文档 FDE/ + 工具 `fde_*` 为插件面，独立成篇可单独阅读传播），**关系上与 S1A 强绑定**（不是可替换的第三方插件）。**因果方向（谁为谁而生）**：不是「FDEing 为 S1A 而生」，而是 **S1A 是用 FDEing 这套方法论做出来的东西**——sofagent 项目自身就是第一个 FDEing 的完整实践（自举可核对：审计闸门 / 判据版本可举证 / 门禁故障注入自证都是 FDEing 循环的产物）；S1A 对 FDEing 做**最好的强调与适配**：判据 schema 按方法论的产出物设计、治理面对准 workflow 与本体数据、SUSTAIN 对准真实痕迹——方法论与制品互相成就，而非单向服务。**方法论的出身（诚实口径）**：FDE 是行业既有名词，FDEing 是本仓的用法；方法论骨架承接 Palantir FDE 交付纪律与既有咨询实践，本仓做的是能力化与工程化。**价值闭环（强绑定的理由）**：FDEing 的产出（判据 / 本体 / workflow / 真实痕迹）只有落进 S1A 才变成活的能力——离开 S1A，梳理成果只是咨询报告模板；反过来 S1A 的粮食也主要来自 FDEing（自举即证）。**叙事口径（三句定稿）**：① 万物皆可 FDEing（思维方式·进场语，吸引所有执行者）；② 产出只有落进 S1A 才是活的（闭环——「梳理完就走，成果是死的；落进 S1A，成果才是活的」）；③ **FDEing 是一种思维方式，S1A 是这种思维方式的能力载体**（完整主张：思维方式学的是行业，能力制品落地在本仓）。**对外被挑战时的应答（耦合不可拆的证明）**：判据 schema 唯一 SSOT 在本仓（check-docs §23 门禁断言方法论不得定义判据格式）——任何方法论产出的判据要变成可判定、可校准、可审计的能力，只有经本仓 schema 一条路；FDE/ 目录零依赖可读（传播友好）与判据落仓强绑定（能力闭环）同时成立：读是免费的，落地在本仓。**两个算子的语义**：外层乘号＝FDEing 与 S1A 缺一不可（没有 FDEing 判据荒芜、没有 S1A 成果停纸面）；内层加号＝S1M 与 Harness 同仓共生组成一个制品。旧写法「FDEing × S1M × Harness」在 changelog 历史区按原样保留 |
| **System 1 Agent 品类宣告（心智占位）** | 上行组合的**品类名**：**System 1 Agent（S1A = S1M + Harness，受 FDEing 作用）**——定义句沿用既有身份句（[FDE/README.md](../../../FDE/README.md)）：**FDE 定义场景，S1M 执行判断，Harness 保证判断被治理**。品类宣言：**不是更快的 Agent，是判断先被治理的 Agent**（Not a faster agent — an agent whose judgment is governed first）。能力四句（场景由 FDE 定义 / 判断由 S1M 执行 / 判断被 Harness 治理 / 判不了就弃权升级人）与边界三句（不替代生成模型——执行侧的脑仍是生成模型，DSH 造节点、本仓治理；不进控制回路——判定只判断、执行归代码；不做 L1 平台 / L2 框架——品类定义在 L3 约束基础设施上）。**对「Kahneman 系统一有偏」的先手回应**：正因裸 System 1 有偏，System 1 Agent 的 System 1 是**校准过的**（ECE 必测——声称 90% 把握就真的约 90%）**且被治理的**（判断进审计链，判不了必须弃权转人）——别人的 System 1 会自信地错，我们的 System 1 判不了会承认；**人是 System 2 兜底**。与 [FDE/S1M-DECISION-POINTS.md](../../../FDE/S1M-DECISION-POINTS.md) 四分类（规则代码判 / S1M 判 / 生成模型 / HITL）零冲突——品类句在四分类之上，不改变任何判定归属。**为什么占 Agent 不占 Model（占位逻辑）**：System 1 Model 的心智已被商业件（Jev）占据且其类名来自 TypeSafe AI——正面抢「模型类」心智是以短击长；**System 1 Agent = S1M + Harness（受 FDEing 作用）** 是更高一层的完整形态（模型是器官、Agent 是器官+治理环境），占据它等于同时定义了组合方式，且天然与本仓制品对齐。**与生态既有用法的区分**：Jev 生态已出现「System-1 agent」的**描述性**指称（快思考架构模式——低延迟直觉反应、绕开 CoT；亦有科研通用智能体框架占用 S1-Agent 缩写），本宣告的 System 1 Agent 是**品类定义**而非架构描述——差异在宣言句：快思考只述速度，本品类落在「判断先被治理」，速度是 S1M 的属性、治理是品类的定义性特征。对外书写用全称（System 1 Agent），**不用 S1A / S1-Agent 缩写**（后者已被生态占用）。**本版与 FDEing 动词化同批构成两项心智占位**：FDEing（动词——万物皆可 FDEing）+ System 1 Agent（品类——Agent 的 System One）。**FDEing 与本项目的耦合点（判据供需两端·缺一不可）**：FDEing 是**判据的获取方式**——梳理→判定→交付→养护把企业的「该不该/行不行/值不值」变成结构化判据与正负样例；S1M 是**判据的消费引擎**——没有 FDEing，S1M 没有粮食（判据无从产生，判定件是空转的分类头）；没有 S1M，FDEing 停在人力咨询（判断永远由人拍、无法 7×24 自运转）。判据从 FDEing 产出、按内容哈希上链、被 S1M 按版本消费——判据供需两端互为存在前提，这就是 FDEing 必须住在本仓的全部理由 |
| **名字含义升级（backronym）** | **sofagent = System One For Agents —— Agent 的 System One**。名字不改（npm 包 / tag / 全部既有标识零变动），含义回溯升级为「给 Agent 装上 System One」；「sofa + agent」是名字的谐音来历（[THANKS.md](../../THANKS.md) 名字来历段保留；**品牌是 sofagent**——谐音只是名字趣味，不是独立品牌资产） |
| **宣告的依据链** | 宣告不是口号，须引用四阶段收口证据：① 判据 schema 定稿（v1.6.0 §一/§二）② ECE 达阈（v1.7.0 §二 硬线）③ 形态三要素（v1.8.0 §六：实现 + 离线推理 + 分发通路）④ 四层性能标准落数（v1.9.0 §四）。**四件证据齐 → 宣告成立；缺任一 → 本版不启动**（与前置门同一条纪律） |

### 验收标准

- [ ] S1M 术语定义落 [ARCHITECTURE.md](../../ARCHITECTURE.md) 身份锚定段（与既有 System One 行业锚段合并，单一 SSOT）
- [ ] 全仓扫描「S1M」：定义先于使用、无第二处定义（术语 SSOT 纪律）
- [ ] System 1 Agent 品类句与能力四句 / 边界三句落叙事章（§三），与 FDE/README 身份句零冲突（同句不同层）
- [ ] 「sofagent = System One For Agents」落 [THANKS.md](../../THANKS.md) 名字来历段（谐音来历保留、正式含义升级在前）
- [ ] 宣告依据链四件证据可引用（逐项指向 v1.6.0–v1.9.0 验收项）
- [ ] `npm test` 全绿

---

## 二、全站术语清洗（v1.6.0 §三触发条件的正式实例）

> **形态归属**：`[非功能]`（文档治理·重构）｜ —

> 📋 本版是 v1.6.0 §三「理念升级驱动的文档重构」触发条件的**首次全站级实例**：身份切换改变断言基础——「sofagent 是审计引擎 harness」这句话在 README / ARCHITECTURE / SECURITY / HANDBOOK 等处的口径**同时失效**，须按该章「全站扫描该断言的全部承载面，同批改写」处置，不留新旧并存。

### 交付

| 组件 | 能力 |
|------|------|
| **断言承载面普查** | 全站扫描「审计引擎 harness / FDE Harness 身份」表述的全部落点（README 双语 / ARCHITECTURE / SECURITY / HANDBOOK / 各包 README / dashboard 文案），逐条登记——**普查清单落盘，数量如实落数** |
| **同批改写** | 按新身份统一：对外一句话 = 「sofagent 是一个自带 Harness 的 S1M——判定做本体，治理做载体」；旧身份表述改写为治理能力自述（「自带 7×24 审计与治理环境」），**不删除能力描述、只改归属口径** |
| **术语分层口径（判定 / 决策）** | 两词**不是同义替换、也非二选一**，按层分工：**身份句**用「**决策模型**」（对齐 S1M = System One Model；[ROADMAP](../../ROADMAP.md) v2.0.0 行已用「可治理的决策模型底座」；**ARCHITECTURE 术语对照表 S1M 行已按此三层口径登记**——含「不互换」判据，本版 §一验收项「身份锚定段落档」落段后以该段为全站 SSOT）；**机制句**用「**判定**」（判定面 · 判定底座 · 判定通道 · 判据集 · 判定档 · 判定四分类）。**为什么不能互换**：「决策」含行动意志、指向应用面；「判定」只出结论、指向治理面——与 [v1.6.0 §四](../v1.6/v1.6.0.md)「做马鞍、不做马场」边界同源，混用会把治理面读成应用面。**落地要求**：全站扫混合写法（如「可治理的判定底座」）统一为上述分层；双语 README 英文侧随行（decision model / judgment） |
| **双语对称** | README.md / README.en.md 同批改（漂移检查通过）；**英文侧术语 SSOT**：S1M = System One Model（全称与缩写并记）、品类名一律全称 **System 1 Agent**（**不用 S1A / S1-Agent 缩写**——后者已被生态科研框架占用，见 §一品类宣告行「与生态既有用法的区分」）、判断/判定对照 judgment（动作）/ decision model（身份）。**叙事逻辑跟版本走（机制立规）**：README 与各文档的叙事口径**随版本演进、每版开发中同步升级**——叙事公式演进链已锚进各版叙事章注记（[v1.5.2 §六](../v1.5/v1.5.2.md) 骨架注入 → [v1.5.3 §三](../v1.5/v1.5.3.md) 三域重组 → [v1.5.5 §三](../v1.5/v1.5.5.md) 全量重写 → 本版 §一公式定稿 FDEing × S1A），每版施工时按**当版时点的公式口径**校准措辞、后版不得出现与前版矛盾的旧式（历史区除外）；改写以当版 changelog 叙事章为准，README 直改须与当版叙事章同批。**changelog 深度分层（防预写规格返工）**：近两版（即将施工）可写满（目标 + 边界 + 验收 + 实现细节）；**远期版本（距今 ≥3 个 minor）保持骨架深度**——目标、边界、验收标准写足，实现细节（接口形态 / 字段枚举 / 读数阈值）等施工到跟前再补全——判据：预写细节若在施工时需要返工，即证明该细节属于施工期决策，不应预写 |
| **FDEing 动词化（心智占位）** | **FDE 是名词，FDEing 才是动词**——对外口号统一「**万物皆可 FDEing**」（原「万物皆可被 FDE」全部升级，活文档面零旧式残留；changelog 历史区按原样保留）。**口径（去首创声明）**：FDEing 是行业既有名词 FDE 的动词化用法，本仓直接使用、不做首创声明——对外只讲用法与价值（万物皆可 FDEing），不讲「谁先提出」（用户拍板：首创类声明画蛇添足，已废 README 底部小字方案）。**定义与思维方式同批登记**：FDEing = 把人去做的 FDE 工作变成可自动执行的 FDE 能力（与商业侧 BP P4「把 Engineer 做成 Engineering」同源）；思维方式三问——workflow 怎么搭 / AI 节点是什么 / 怎么让 AI 帮实现——落 README 首提位 / PHILOSOPHY / WIKI 词条 / FDE 打法位四处。**语法纪律**：凡「用 FDE」的动词位一律 FDEing——「万物皆可 FDEing」「对一个对象做 FDEing」「FDEing 一遍」；名词位仍用 FDE（FDE 岗位 / FDE 方法论 / 从 FDE 到 FDEing）。英文侧动词形态同批：to FDE / FDE'ing → **FDEing**（与 engineering 同构，读作 /ef-di-i-ing/）。**README 双语已即时直改**（不等 R5 全量重写）——「从 FDE 到 FDEing」段原位升级为口号首提位（万物皆可 FDEing + 「不限于软件」的具身外延一句）；R5 重写时该段并入理念主线，口号不动 |
| **指针可兑现** | 清洗后无指向不存在章节的指针（check-anchors 全绿）；changelog 历史区（`docs/changelog/**`）**不动**——历史沿革按原样保留 |

### 验收标准

- [ ] 断言承载面普查清单落数（位置 / 原口径 / 新口径，逐条可核对）
- [ ] 全站无新旧身份口径并存（扫描旧身份断言零残留于非历史区）
- [ ] 双语 README 对称（同批改，漂移检查通过）
- [ ] 门禁全绿：`check-docs` / `check-anchors` / `check-forms` / `check-version`
- [ ] changelog 历史区零改动（回归断言）

---

## 三、对外叙事重定位（README 能力段与叙事章）

> **形态归属**：`[主干]`（交付·对外口径）｜ —

### 交付

| 组件 | 能力 |
|------|------|
| **README 头部与能力段重写** | 「这是什么」段按新身份重写：判定底座（本体）在前、治理环境（载体）在后；能力段新增「决策模型」主段落（判定档 · 判据集 · 校准概率 · 弃权语义），审计叙事段降为「自带的治理环境」小节。**新能力段只留本版**（README 纪律：旧版能力段去 CHANGELOG 查） |
| **两项心智占位叙事（品类故事）** | README 叙事章承载两项对外心智：**万物皆可 FDEing**（动词心智——**不止企业业务：任何一条 workflow——软件流程、业务对象、物理过程（含机器人运动，见 VALIDATION 具身切面）——都可以被 FDEing 梳理成机器可判定的 workflow**）与 **System 1 Agent**（品类心智——**System 1 Model 的心智已被商业件占据，本仓占的是 System 1 Agent**：模型是器官、Agent 是完整形态，占据更高一层）。**核心叙事链：因为有了 FDEing，才知道什么样的场景需要什么样的 System 1 Agent**——FDEing 是 System 1 Agent **内置的打法能力**（三因子之一，不是另一个 Agent、不另立品类），其产出物（判据）直接流入 System 1 Agent 的判定面。**FDEing 能力的当前形态（诚实边界）**：它是一套方法论而非自主智能——**执行者无关**：人、任何 AI Agent（WorkBuddy / OpenClaw / Codex / DSH 节点皆可，FDE/ 目录零依赖 15 分钟路径即为此设计）都能执行这套方法论；判据的持续进化走 SUSTAIN 循环（真实痕迹蒸馏 → 校准复验 → 晋 trunk，人审门常在）。企业接入路径三步（对外主叙事）：**① FDEing 判断**——企业接入自己的模型，由任一执行者用本仓方法论快速完成 FDEing 判断；**② 决策模型构建**——判断落判据，快速构建企业自己的决策模型（S1M 判定件 + 校准）；**③ Harness 实现**——最后由 Harness 把判断装进可治理的执行（审计链 / 回溯 / 沉淀 / 进化）。**交付形态**：System 1 Agent 的能力以 **plug-in 家族**形态提供（插在 DeepSeek Harness 上，见 [v1.5.4](../v1.5/v1.5.4.md) cordis 兼容层）；企业用自己的 Agent 经 hub 接口调用同样成立——**品类名只有 System 1 Agent 一个，FDEing 是其能力因子，不再另立 Agent 名**。品类宣言句：**不是更快的 Agent，是判断先被治理的 Agent**（Not a faster agent — an agent whose judgment is governed first）。收尾句（sofagent = System One For Agents；sofa+agent 谐音是名字来历）：判断被治理之后，负责人躺回 sofa——不是不管，是每次点头有留痕、每次疑义可回溯、判不了必升级来问你。**与 B 步重构链的衔接**：README 理念主线已由 [v1.5.2 第六章](../v1.5/v1.5.2.md)（骨架注入 R2）与 [v1.5.5 第三章](../v1.5/v1.5.5.md)（全量重写 R5）渐进完成，本版重写为**终态校准**（A 步）——按宣告后的 S1M 术语 SSOT 对四段结构做最终口径收口，不是从零重写 |
| **边界纪律同步** | [v1.6.0 §四](../v1.6/v1.6.0.md) 四条边界判据（约束 vs 扩张 / 治理 vs 应用 / 是否对外服务 / 是否模型提供者——SSOT 见 [ARCHITECTURE · 判定底座的边界](../../ARCHITECTURE.md#判定底座的边界做马鞍不做马场)）随身份重定位同步进 README——**身份升级不等于边界扩张**，边界外清单原文保留。**口径提醒**：「不做模型托管」是判据 ④ 的**边界外项**、**不是第五条判据**——两处四条的成分必须一致（旧写法曾把边界外项提成独立判据，与 SSOT 构成两套口径） |
| **LIMITATIONS 同步** | [docs/LIMITATIONS.md](../../LIMITATIONS.md) 补「决策模型已知失灵面」小节：可被 state 内容操纵（永不进信任地基）/ 域外泛化未验证 / 校准依赖数据分布（漂移须重标）——与 v1.8.0 §六、v1.9.0 §五口径一致 |
| **判定底座的发布面** | S1M 权重上模型托管平台（[v1.8.0 §五](../v1.8/v1.8.0.md) 既定主载域：Hugging Face）与身份宣告**同一发布事件**——**镜像源（hf-mirror 等反向代理）与国产托管平台（ModelScope 魔搭）与主载域同批首发**（实网已证 HF 直连不可达、镜像是必需项非优化项，见 evidence Case 023 网络面；ModelScope 面向信创/国企等国内一等公民场景，**同批上传第二份制品**）——三通道 sha256 全部与本地构建产物一致（同一制品三扇门：HF 主载域 / hf-mirror / ModelScope）——宣告（身份成立）+ 制品（可下载 · sha256 可校验）+ 模型卡（校准依据声明 · 弃权归因 · 四层性能标准 · 判据集版本；**模板与字段规范见 [v1.8.0 §五](../v1.8/v1.8.0.md)**）三件套一次打出。**为什么合并成一次**：判定面的差异化恰恰在托管平台上**同类制品给不出的面**（审计链 · 校准依据声明 · 弃权归因）——分两次发布等于把这条叙事拆散；且宣告当天即可下载验证，比纯文字宣告可信。**前置纪律**：模型卡数字须满足 [v1.7.0 §三](../v1.7/v1.7.0.md)「对外数字可复现」与「数据集谱系声明」——没有第三方可比数据前**不写「对标」**，只报本仓口径与复现方式，并声明数据谱系。**边界相容**：权重分发不构成判据 ④ 越界（精确划界见 [v1.6.0 §四](../v1.6/v1.6.0.md)） |
| **工程可信度行** | README 工程可信度行（测试数 · 13 模块包 + 11 插件（7 DSH + 4 OpenClaw），**以发版时点现值为准**——不写死数字，写死即随测试滚动变陈旧值）数字不动——身份切换不改工程事实；V2.0.0 发版时按发版 SOP 顺手优化 README（既有纪律） |

### 验收标准

- [ ] README「这是什么」段与能力段按新身份重写（双语对称）
- [ ] **故事三句版与品类宣言句落 README 叙事章**（与 §三「两项心智占位叙事」行同文；双语对称，英文侧按 §二英文术语 SSOT——品类名全称 System 1 Agent）
- [ ] 边界四判据在 README 在位（与 v1.6.0 §四、ARCHITECTURE 一致，无第二口径）
- [ ] LIMITATIONS 补决策模型失灵面小节（三条与 v1.8.0/v1.9.0 同口径）
- [ ] 工程可信度行数字不变（回归断言）
- [ ] `npm test` 全绿

---

## 四、版本收口与 v2.0.x 补丁位约定

> **形态归属**：`[非功能]`（排期纪律）｜ —

### 交付

| 组件 | 能力 |
|------|------|
| **宣告版零新能力纪律** | 本版**不新增任何判定能力**——四阶段施工期已交付的能力面，本版只做宣告、清洗、重定位。**为什么**：宣告版塞新能力会让「身份切换」与「能力变更」混在一版，回归问题无法定位（与 v1.9.0 §二「语义正名与行为改变不同版」同族纪律） |
| **V2.0 后版本域纪律** | **V2.0 起版本全部是 S1M 的迭代域**——V2.0 前的能力不放 V2.0 后的版本去迭代（网络出口治理面与 AI 节点已按归位再分配落 [v1.5.2](../v1.5/v1.5.2.md) / [v1.5.4](../v1.5/v1.5.4.md)）。V2.0 后做什么**暂不定义**，由 S1M 自身的产品化需求驱动——不是无内容，是不预设。[v1.5.4 §五](../v1.5/v1.5.4.md) L3 插件位「V2.0 后插件可消费类型化判定 API」即属该迭代域的**首批自然候选**，与本行不冲突——「暂不定义」禁的是现在排期承诺，不禁候选登记 |
| **v2.0.x 补丁位用途约定** | v2.0.x 承接**宣告后 S1M 自身的缺陷修补与迭代**（新判据域 / 校准漂移应对 / 分发通道补强），不承接 V2.0 前遗留能力——那些必须在 V2.0 前的版本里做完。**补丁位节流**：非安全/非阻断缺陷不占补丁位（走下个 minor），防补丁位被高频消耗稀释「宣告版」的里程碑感 |
| **权重版本号独立轨道（用户拍板）** | HF / ModelScope 上的权重制品**不用 v2.0.0 式三段版本号，直接用 V2 / V2.1 / V2.2 式模型代次号**——模型代次（weights generation）与仓版本号是两条轨道：**每次代次升级 = 模型性能提升**（新判据域吸收 / 校准改进 / 基座升级），与 sofagent 代码版本无关；小迭代只在 GitHub 仓内进行（changelog + ROADMAP 照常记账），**模型性能提升到下一档时才推新代次上平台**（V2 → V2.1 → …）。模型卡标注用代次号 + 判据集版本 + ECE 读数（三者才是可追溯锚，代次号只是人读的里程碑）。与 [v1.8.0 §五](../v1.8/v1.8.0.md)「版本松耦合」（接口契约由 DecisionChannel 保证、不由版本号保证）同源——本行是它在**发布面**的落点：下载件按 `remote.期望sha256` 锚定制品，代次号不参与机器校验。**制品命名（用户拍板 + 平台惯例校准）**：HF / ModelScope 权重制品名用 **`sofa-s1m-v2`**（**全小写连字符**，sofa 保留小写=名字来历fun、非独立品牌；S1M 作模型类缩写；代次号小写 v 前缀与仓 tag v2.0.0 视觉区分）——与 HF 惯例（Qwen/Qwen2.5、BAAI/bge-* 全小写）及 ModelScope 命名习惯一致；变体扩展位保留（`sofa-s1m-v2-judge` 类）。**GitHub 发布物与 HF 发布物是两种制品，发布前必须拆分清楚**（见 §四发布面 SOP 缺口行的拆分要求）|
| **权重 license（已定稿：MIT · 用户拍板对齐 DeepSeek）** | **代码与权重同用 MIT**——对齐 DeepSeek 路线（R1 起全系 V3.1/V3.2/V4 代码+权重双 MIT，实测于 HF 仓 LICENSE 与官方公告；早期 V3 base/Coder-V2 曾用自定义 OpenRAIL 式、后弃用转 MIT）。**一致性的价值大于专利条款差异**：整个仓库一种 license（代码 / 权重 / 判据集 / 评测器四制品同 MIT），认知与合规成本最低；商用 / 微调 / 蒸馏 / 再分发全许，仅保留版权声明——与「不限制用途」的项目气质一致。模型卡「许可」字段填 MIT（[v1.8.0 §五](../v1.8/v1.8.0.md) 模型卡模板标准字段） |
| **发布面 SOP 缺口（本版前必须补）** | V2.0.0 的核心发布事件是「**S1M 权重上平台 + 镜像源 + 身份宣告**」三位一体，但现有发版 SOP（[releasing/10-distribute.md](../releasing/10-distribute.md)）只覆盖 Skill / 插件 / Marketplace / npm——**零权重发布步骤**。V2.0 施工前须补一节「权重发布」：HF 上传（模型卡含 ECE 读数与判据集版本）/ 镜像源（hf-mirror）与 ModelScope（魔搭）同批首发 / **四方 sha256 对账**（本地构建产物 = HF 制品 = hf-mirror 拉取 = ModelScope 制品，任一不符即发布中止；ModelScope 须单独上传，走同一发布事件契约）/ 事件契约（权重发布与 npm 发版 + GitHub Release 同一发布事件，失败任一则整体回滚口径）。**发布物拆分（两种制品不得混淆）**：GitHub 面发布**代码制品**（仓 tag / Release notes / npm 包 / Skill 与插件家族——全部 sofagent 命名、随仓版本走）；HF/ModelScope 面发布**权重制品**（`sofa-s1m-v2` 命名、随模型代次走——内容物=权重文件 + 模型卡 + 判据集快照 + ECE 报告，**不含代码**）。SOP 补节时须含两制品的打包清单与互不含混检查。**这不是新能力**——是既有分发通路（v1.8.0 §五）在 SOP 面的落点，属发版 SOP 补丁，挂本版 §四 |
| **V2.0 时点口径** | 重申 [v1.9.0 §九](../v1.9/v1.9.0.md)：V2.0 时点 = 「四阶段收口达阈」时点，不由版本号决定；本版启动即证明达阈已发生 |

### 验收标准

- [ ] 本版无新增判定能力（与 v1.6.0–v1.9.0 交付面对账，纯宣告面）
- [ ] v2.0.x 补丁位用途在 changelog/README 与 ROADMAP 同口径
- [ ] `npm test` 全绿

---

## 五、原规划去向落盘（内容零丢失 · 仅归属变更）

> **形态归属**：`[非功能]`（排期纪律——去向登记）｜ —

> 📋 2026-09-22 用户三次拍板（同日）：① USB 节点合体是「非常分支的功能」——V2.0.0 主体让位 S1M 宣告；② 版本边界纪律定稿——V2.0 起版本全部是 S1M 的迭代域，V2.0 前的能力不放 V2.0 后的版本去迭代；③ 归位再分配（授权主理人裁定）——两件按主题亲和分落 v1.5.2 / v1.5.4。原两项内容**零丢失**，最终去向：

| 原章 | 去向 | 理由 |
|------|------|------|
| **一、离线 USB 节点合体**（本地权重 + workflow 烧录 + 审计 + 路由四位一体，完全离线运行 + 审计滞留回传） | **[v1.5.4 第五章](../v1.5/v1.5.4.md)（AI 节点治理接入）**——按 2026-09-22 用户拍板升级表述：不再是「把引擎烧进 USB」，而是「**用 DeepSeek harness（LangChain/LangGraph 栈）快速搭建的独立 Agent 节点**」，本仓对它只做**治理接入**（审计链挂接 + 治理面注册 + 出站裁决联动），不做运行时合体 | ① USB 节点 = 建一个 Agent，属**部署形态**而非本体能力（原章自标「正交第三轴·归属不变」）② 治理接入比运行时合体**边界更干净**：DeepSeek harness 造节点（生成侧），本仓治理节点（判定与审计侧），两者互补不重叠——正是「判定只判断、执行归代码」在部署形态上的落法 ③ **版本边界纪律**：V2.0 前的能力归 V2.0 前的版本 |
| **二、网络出口治理面**（host 白名单 opt-in + 出站裁决审计挂链 + 拦截器接口——Codex Guardian 启发） | **[v1.5.2 第五章](../v1.5/v1.5.2.md)** | 纯治理件、与判定底座零依赖、可独立验收；且「管出」与判定底座的出站判定语义有天然衔接（出站裁决事件可作为判定输入源）——**排在 v1.6.0 之前恰使判定底座开工即有已就位的真实场景域**；主题亲和（审计举证面）+ 版本边界纪律同上 |

### 验收标准

- [ ] 两项内容在 v1.5.2 / v1.5.4 完整在位（网络出口治理面全文 + AI 节点治理接入全文）
- [ ] 本版与 v1.5.2 / v1.5.4 的互引指针可达（check-anchors 验证）
- [ ] `npm test` 全绿

---

## 六、兼容层退役清单（无名存续的兼容层即永久债 · 2026-09-22 用户拍板）

> **形态归属**：`[非功能]`（退役纪律·可拔契约执行面）｜ —

> 📋 **为什么需要这一章**（2026-09-22 用户拍板「加」）：身份切换之后，「V2.0 前的旧形态」与「V2.0 后的新形态」会同时在仓里活着——兼容层是过渡期的必要代价，但**兼容层没有退役条件就是永久债**：它会让后续每一次判定/审计都多一条已无人需要的分支，而没人记得它当初为什么在。本章把已有的兼容层**逐条挂上退役触发条件**，并立「新增兼容层须同批登记本章」的纪律。**与零新能力纪律的相容**：本章是登记与排期纪律（非功能），不新增任何判定能力。

### 交付

| 组件 | 能力 |
|------|------|
| **退役清单落盘** | 逐条登记在仓兼容层：出身章节 / 存在理由 / **退役触发条件** / **退役动作** / 本版处置（保留 / 可退役）/ 责任人指认面——无触发条件的兼容层不得进入清单（登记即拦截「无名存续」） |
| **退役范式统一** | 沿用 [ROADMAP · DSH-only 执行面收尾](../../ROADMAP.md)（保险绳拆除）的范式：**触发条件 = 连续 N 个完整周期零回退**，而非「将来有空时清理」；触发条件须可机械观测（有读数可查），不能是主观判断 |
| **新增登记纪律** | 自本版起，新增兼容层（兜底路径 / 降级分支 / 旧命令 shim / 旧格式兼容读）须**同批在本章登记**触发条件与动作，否则评审不通过——与 [v1.5.7 第五章](../v1.5/v1.5.7.md) 可拔契约同族（那章管能力开关，本章管过渡期拐杖） |

### 初次登记面（实测在仓的兼容层）

| 兼容层 | 出身 | 存在理由 | 退役触发条件 | 本版处置 |
|------|------|------|------|------|
| **L1 未就位兜底路径** | [v1.8.0 §一](../v1.8/v1.8.0.md) | 判定实现加载失败 / 权重缺失时退回 L0，不留空窗、不静默降级为「全部放行」 | 判定底座在主流部署形态下连续 2 个 minor 零加载失败（读数=判定源加载失败计数器，**阈值为 0**——连续 2 个 minor 无一次非零即达） | 保留（读数未建，条件未达） |
| **判定源降级路径** | [v1.9.0 §一](../v1.9/v1.9.0.md) | 判定底座不可用时退回既有确定性实现，行为与今日一致 | 底座可用率 ≥ 99.5%（读数=判定调用成功数/总判定调用数，滚动 30 天窗口）且确定性实现被完全替代（无调用方依赖旧路径——读数=旧路径调用计数器为 0） | 保留（条件未达） |
| **`Choice` 纯布尔兼容路径** | [v1.9.0 §一](../v1.9/v1.9.0.md) | 输入三问由布尔升级为可带概率时，不破坏既有调用方 | 全仓机械扫描：调用方零布尔入参 | 保留（扫描未做） |
| **旧命令 shim（12 个 `sofagent-*`）** | [v1.5.6 §一](../v1.5/v1.5.6.md) | CLI 单入口收敛的兼容期（一个 minor 版本，不 breaking） | **兼容期已届满**（v1.5.6 之后已过多个 minor） | **可执行退役**——移除 shim 与文档「兼容」小节；breaking 动作按发版 SOP 处置 |
| **DSH / LangGraph fallback 保险绳** | v1.4.3 | FORGE 执行后端从 LangGraph 切 DSH 时的回退口 | 见 [ROADMAP · DSH-only 执行面收尾](../../ROADMAP.md)（连续 2–3 个完整 run 零 fallback） | 不在本版处置（范式同源，登记为避免两处口径） |

### 验收标准

- [ ] 兼容层清单落盘，每条含「出身 / 理由 / 触发条件 / 动作 / 处置」五栏（无触发条件者不入清单）
- [ ] 触发条件全部可机械观测（有读数或可在开发期实测，非「有空清理」）
- [ ] 旧命令 shim 退役动作按发版 SOP 处置（breaking 走版本纪律，不在本版静默移除）
- [ ] 新增兼容层登记纪律在 ARCHITECTURE 或 CONTRIBUTING 有落点（单 SSOT，不重复两份清单）
- [ ] 门禁全绿：`check-docs` / `check-anchors` / `check-forms`

---

## 七、已交付能力面的归属与退役清单（定位漂移面处置 · 2026-09-22 用户拍板）

> **形态归属**：`[非功能]`（退役纪律·可拔契约执行面）｜ —

> 📋 **为什么需要这一章**：身份升级为 FDEing + S1A（S1A = S1M × Harness）之后，「已交付」不等于「仍合定位」——仓里的 107 个 MCP 工具、13 个模块包、daemon 里 1420 行 USB 运行时，都是按「审计引擎 harness」时期的路线长出来的。本章做三件事：① 给已交付能力面标**三因子归属**（服务 FDEing / S1M / harness 治理环境 哪一项，或标**无归属**）；② 把**定位漂移项**登成退役候选（挂可机械观测的触发条件）；③ 登记**V2.0 后版本域冲突面**——活文档里仍在承诺 `v2.x` 的能力，按版本边界纪律逐条裁定。**与 §六 的边界**：§六 管**过渡期拐杖**（兜底 / 降级 / shim——「不该留的临时代码」），本章管**定位漂移的能力**（「长错了方向的功能」）。
>
> 📋 **两条前置纪律（防「删繁就简」变成拍脑袋砍）**：① **退役判据须有读数**——[v1.5.7 第四章](../v1.5/v1.5.7.md) 的工具使用率遥测（`tool-usage.jsonl`）与技能 SkillOps 五维**正是为「有据退役」而立**；读数出来前本章只做**登记**，不做删除。② **归档优先于物理删**——沿用 [v1.5.7 第四章](../v1.5/v1.5.7.md)「退役候选人工确认制、归档可回滚」；真删只在**三条件同时成立**时走**发版 SOP 的 breaking 收尾**（见 E 节）。
>
> 📋 **处置基调（用户拍板「保证项目纯洁性」）**：历史版本已在 git 历史与 GitHub 上，觉得有用的用户可自取——**本仓不为「万一有人要」保留死代码**。故处置以「显式退役、该删就删」为主旋律，不再为纯定位漂移项保留关档态（历史即归档）。

### A. 三因子归属分类（已交付能力面 → 服务哪一项）

> 📋 **评估框架（用户拍板）**：V1.x 的项目定位是「harness 层 + 一组 plug-in 功能」；V2.0 主干全方位升级为 **S1M（System One Model，即 General Decision Model）**，其余功能全部作为 plug-in 插到主干上。故已交付能力面的评估问题统一为：**这个功能在新主干上是「插件」还是「死重」**——插件=服务三因子之一（FDEing 底座 / S1M 本体 / harness 治理环境），死重=与旧「审计引擎 harness」叙事绑死且新叙事下无消费方。已交付面实测：**107 个 MCP tools（tool-registry SSOT）· 82 个 `SOFAGENT_*` 能力位（v1.5.7 可拔契约 SSOT）· 13 个模块包**（三者均为**本表落盘时点值**——v1.5.2–v1.5.9 尚将交付新 tool / 能力位，发版时点以对应 SSOT 现值为准，与 §三工程可信度行同口径）——评估用「能力簇」粒度（同簇功能同判），不逐个工具点名。

| 能力簇 | 服务因子 | 判定依据 |
|---|---|---|
| FDE 六引擎（`fde_interview` → `fde_deploy`，7 tools） | **FDEing 底座** | 「把 FDE 的进场判断力产品化」——打法本体 |
| 工作流编排（18 tools）/ Agent 组织协作（7 tools） | **FDEing 底座** | 「梳理工作流 · 部署 AI 节点」 |
| 本体数据与知识资产（16 tools） | **FDEing 底座** | 业务图谱 / 本体数据是梳理产物 |
| 审计与合规（25+ 条规则（以 SECURITY SSOT 现值为准）+ 8 tools）/ 快照回溯（2） | **harness 治理环境** | 「7×24 审计每次变更」 |
| 后训流水线（16 tools）/ 评估验收（7） | **S1M 数据面 + harness**（训练编排与治理；训练器在外部） | v1.6.0–v1.8.0 全链依赖 |
| **进化面（evolve 插件 · [v1.5.8](../v1.5/v1.5.8.md) 准入门/晋级/经验池/考核 + [v1.5.9](../v1.5/v1.5.9.md) 写面审计/能力基线/透明仓/敌手自测）** | **RSI 模块独立插件**（服务三因子的自验证面） | **形态改判（执行见 [v1.5.9 第七章](../v1.5/v1.5.9.md)，本表登记三因子归属）**：进化能力从 harness 主干功能改挂 `cordis-plugin-sofagent-evolve` 插件位（DSH 七款之一，位已存在零迁移）——RSI 定位（治理与审计层非引擎）在产品形态上的落点；沉淀留 harness（数据管道），进化独立（决策管道）——沉淀产出 verified 数据、进化插件消费它做准入/晋级/自验证，飞轮「审计→沉淀→进化」中间环节不断 |
| 能力市场（`commons_*` ×6） | **harness 沉淀面**（企业内公地） | 「对外服务」判据不命中——是企业内公地 |
| 设备接入面（G9–G11，4 tools）/ 连接器面（2） | **harness 治理环境**（一体机 · 数据主权） | 数据主权承诺的落地通道 |
| 运维与可见性（14 tools） | **harness** | 治理可见性 |
| 浏览器自动化（`playwright_*` ×4） | **无直接归属**（主叙事是 UI 层审计取证） | 见 B 表 |
| **USB 完整运行时（daemon 四文件 1420 行）** | **重定位：DSH 节点交付介质**（骨架兼容，见 B 表——旧「四位一体」叙事退役，介质重定位续用） | 见 B 表 |
| MLflow exporter（orchestrator 单文件） | **弱归属**（外部实验跟踪） | 见 B 表 |

### B. 退役候选（具名 · 带证据与触发条件）

> 📋 **本轮裁定后剩余的真删面只有两组**：浏览器四件套（UI 审计窗口已关）与 MLflow exporter（量化评审窗口已关）——其余 101 tools / 全部模块包均归三因子之一或已改判重定位。**USB 四文件改判重定位不在此列**。

| 候选 | 证据 | 处置 | 触发条件 / 动作 |
|---|---|---|---|
| **USB 完整运行时** | `engine/daemon/src/usb-key.ts` / `usb-runtime.ts` / `usb-signature.ts` / `usb-detect.ts`（合计 1420 行 + 2 测试文件）。源码复读修正初判：骨架（便携打包 + 验签 fail-closed + 密钥仅驻内存 + `SOFAGENT_DATA` 全指向介质 + 拔出零残留）**与新形态完全兼容**——废弃的只是旧「四位一体」叙事（烧录 workflow + 本地权重 + 审计 + 路由全进 USB 当节点本体），不是介质本身。用户复盘拍板：**USB 重定位为「DSH 节点 + 判定件的交付介质」**——「机器 + USB = 无头判定节点」（判定件 encoder 单次前向，纯 CPU 档可跑，不挑机器） | **裁定：重定位（不退役）** | 落点 [v1.8.0 §五](../v1.8/v1.8.0.md) 离线介质导入扩容，两档：**A 安装器档**（插上即把 DSH 节点 + sofagent + 判定件装进机器，走既有 install.sh 面改造 + 同一 sha256 校验）+ **B 便携运行时档**（复用 usb-key/usb-runtime/usb-signature 骨架，打包对象换成 DSH 节点 + 判定件权重 + 配置——插上即跑、拔出零残留）。**退役删除的只有旧「四位一体」叙事**；usb-detect.ts（联邦密钥注入）仍单列随联邦面判 |
| **浏览器四件套 `playwright_*`** | [API.md](../../API.md) 自述「主叙事是 UI 层审计取证（[v1.5.7](../v1.5/v1.5.7.md) UI 审计的执行底座）」；而 UI 审计的实做窗口按版本边界纪律**已关闭** | **裁定：退役删除**（用户拍板「该删的就删掉，不要关闭」——UI 审计实做窗口已关，「日后重开即复用」不构成保留理由，历史版本可自取） | 触发条件 = [v1.5.7 第二章](../v1.5/v1.5.7.md) 可行性报告落盘（结论已预定为「不实做」）⇒ 四工具 + 叙事面移出主线，走发版 SOP breaking 收尾 |
| **MLflow exporter** | `engine/orchestrator/src/benchmark/mlflow-exporter.ts` 单文件；文档层另有「v2.x 借 MLflow agent 评估补量化评审标准」的承诺（该窗口已关闭，见 D 表 #4） | **裁定：退役删除**（同上用户拍板） | 触发条件 = [v1.9.0 第四章](../v1.9/v1.9.0.md) 四层性能标准的评审口径落定后执行删除（评审若不采用 MLflow 输出形态即删——四层标准自有落数通道） |
| **daemon 包职责过载** | [ARCHITECTURE](../../ARCHITECTURE.md) 原文：cron + fs 监听 + 文件级审计 + USB 烧录 + 联邦查询 + Dream Cycle 6 阶段 + 启动 LOOP 续跑检查 + 审计轨迹聚合巡检 + 训练孤儿巡检 + 模型清单扫描（十项挤一包） | **结构收敛候选**（不是能力退役） | 随 USB 候选处置一并做：USB 退出后剩九项，再判 Dream Cycle / 联邦查询是否另立 |

### C. 边界补划界（不退役，但须写清以免被读成越界）

| 能力 | 疑义 | 划界结论 |
|---|---|---|
| `train_serve`（推理服务生命周期） | 边界外清单含「通用模型托管」（[ARCHITECTURE](../../ARCHITECTURE.md) 判据 4） | **边界内**：本地自用推理服务（从权重目录拉起端点 + 启停记审计），不对第三方提供、不承诺性能 |
| `train_cloud`（云 VM 执行面） | 与「数据主权 + 不写云厂商适配器」的张力 | **边界内**：通用 VM + ssh 通道（**不是云厂商适配器**），opt-in 逃逸口 + 分拣闸拦敏感档上云 |
| `commons_harvest_rule`（公地日志 → 规则候选） | 是否与 S1M 判据面构成**第二套规则来源** | **须划界**：公地提炼的是 **skill 质量规则**，不是审计判据——判据 SSOT 唯一（[v1.6.0 第二章](../v1.6/v1.6.0.md) 判据 schema） |

### D. V2.0 后版本域冲突面（活文档仍在承诺 `v2.x` 的能力 · 逐条待裁定）

> 📋 版本边界纪律定稿后「V2.0 起版本全部是 S1M 的迭代域」，故活文档里的 `v2.x` 承诺**全部没有承接窗口**。本表登记 11 处（`docs/changelog/` 与 `docs/archive/` 历史冻结区不计）。**本表为 SSOT，源文档待逐条裁定后同批改——禁两套口径并存**。

| # | 落点 | 承诺内容 | 建议裁定 |
|---|---|---|---|
| 1 | [ARCHITECTURE](../../ARCHITECTURE.md) 协同层行 | 协同层（多设备 + 云端 / Agent 独立身份 / 共享上下文 / 组织记忆）→ `v2.x` | **关闭**——组织级共享记忆与「中小企业私有部署 + 数据主权」定位漂移 |
| 2 | [ARCHITECTURE](../../ARCHITECTURE.md) `v2.x` 路线段 | ① 组织级共享记忆 ② 协同层 ③ 分层模型路由 ④ 离线 USB 节点 | ①② **关闭**；③ **改「路由层本体已在位，调用侧分流归位 [v1.5.4](../v1.5/v1.5.4.md)」**（句子陈旧——`model-router.ts` 五档 + 拦截出口已实装）；④ **改指针**（已由 §五 改写） |
| 3 | [ARCHITECTURE](../../ARCHITECTURE.md) 养护行 / 路线契合段 | `v2.x`「ontology I/O schema 硬化」 | **排进 V2.0 前**——本体数据是 FDEing 底座面，有承接窗口 |
| 4 | [ARCHITECTURE](../../ARCHITECTURE.md) 养护操作化身段 | `v2.x` 借 MLflow agent 评估补量化评审标准 | **关闭**（随 B 表 MLflow 候选） |
| 5 | [HANDBOOK](../../HANDBOOK.md)「现在还干不了的事」 | 离线 USB 节点合体（v2.0.0）；租户级鉴权 / 配额 / 跨租户策略与写入侧隔离留 v1.5.x / `v2.x` | USB 部分 **改指针**（§五）；**多租户排进 V2.0 前 v1.5.x** |
| 6 | [LIMITATIONS](../../LIMITATIONS.md) 编排局限缓解段 | `v2.x` 协同层的确定性编排模块（称「根本解」） | **二择一**：排进 V2.0 前，或关闭并改述缓解面（取决于是否承认「编排依赖模型质量」需另立确定性编排层） |
| 7 | [ROADMAP](../../ROADMAP.md) v1.5.7 行 + [v1.5.7](../v1.5/v1.5.7.md) 依赖行 | UI 层审计「实做候选 `v2.x`」 | **裁定：关闭**（窗口已关，随 B 表浏览器候选）——**源文档已同批改** |
| 8 | [ROADMAP](../../ROADMAP.md) 分层模型架构段 | 「路由层可提前到 `v2.x` 做」 | **裁定：改「路由层本体已在位，调用侧分流归位 [v1.5.4](../v1.5/v1.5.4.md)」**（句子陈旧，非承诺——`model-router.ts` 五档 + 判定档 + 拦截出口已实装）——**源文档已同批改** |
| 9 | [VALIDATION](../../VALIDATION.md) 三处 | 建议 `v2.x` 引入：可视化 DAG 画布 / 把 ontology 与 audit 经 MCP 对外暴露 / ontology schema 硬化 | 逐条：**DAG 画布 → 探索方向表**（纯评估·W5）；**MCP 对外暴露 → 排进 V2.0 前**（与 [v1.5.2](../v1.5/v1.5.2.md) 对外面同族）；**schema 硬化 → 同 #3** |
| 10 | [SECURITY](../../SECURITY.md) 权限核验行 | 审计 A14 运行时阻断「列入 v2.x（ROADMAP.md）」 | **改指针**——运行时阻断按 [v1.5.4](../v1.5/v1.5.4.md) 凭证与验证域归位评估（N 实例表决 / 出站管控已在该版交付面），不占 V2.0 后窗口 |
| 11 | [VALIDATION](../../VALIDATION.md) Omnigent 段 | 「路线图尚未实现，但方向值得在 v2.x 评估框架参考」 | **改述**——评估属研究叙事非能力承诺：改「方向登记进探索方向表（W1–W5 准入判据评估）」，去掉 `v2.x` 版本指向 |

### E. 退役纪律（本章长期效力）

- **新增能力须标三因子归属**——无法归属的不进主干（与 [v1.5.7 第五章](../v1.5/v1.5.7.md) 可拔契约同族：那章管「怎么拔」，本章管「该不该长」）。
- **退役候选须挂可机械观测的触发条件**（同 §六 兼容层范式），禁「将来有空清理」式主观条件。
- **真删三条件同时成立**方可走 breaking 收尾：① 与边界外清单直接冲突 ② 已被同族新面完全替代 ③ 无对外叙事落点且无已知消费方。**「历史版本在 git 上」不构成保留理由**（用户拍板「保证项目纯洁性」——需要者从历史自取，本仓不背死代码）。
- **本章与 [v1.5.7 第四章](../v1.5/v1.5.7.md) 的关系**：那里是**数据驱动**的退役机制（遥测 + 五维 → 候选清单），本章是**定位驱动**的退役登记（归属分类 → 定位漂移候选）；两者**汇总到同一份退役候选清单**，不各立一套。

### 验收标准

- [ ] 三因子归属分类表落盘（已交付能力簇逐条有归属，或显式标「重定位」）
- [ ] 退役候选逐条带证据（文件路径 + 行数）与**可机械观测**的触发条件
- [ ] V2.0 后版本域冲突面 9 处逐条有裁定，裁定后源文档同批改（**无两套口径并存**）
- [ ] 边界补划界三条落文档（`train_serve` / `train_cloud` / `commons_harvest_rule`）
- [ ] 本章与 [v1.5.7 第四章](../v1.5/v1.5.7.md) 汇总到同一份退役候选清单（无两套清单）
- [ ] 门禁全绿：`check-docs` / `check-anchors` / `check-forms`

---

## 与后续版本的依赖

| 后续版本 | 依赖 v2.0.0 的什么 |
|------|------|
| v2.0.x | **S1M 迭代基线**——宣告后 S1M 自身的缺陷修补与迭代（新判据域 / 校准漂移应对 / 分发通道补强），不承接 V2.0 前遗留能力 |
| v3.x | 分层模型架构（企业专属小模型精调 QLoRA）——继承本版身份口径；v3.x 的「本地档执行」消费 S1M 判定档输出 |
