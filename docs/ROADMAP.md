# 路线图 · Roadmap

<p align="center"><img src="assets/sofagent.png" alt="sofagent" width="96" /></p>

> 已经做了什么、未来要去哪、哪些地方需要你的帮助。
> v1.5.2 · 2026-09-24（UTC）· ✅ 已发版 · 孔放勋
> v1.5.2（✅ 已发版 · 2026-09-24）——审计模块 · 对外面与判定语义：测试 5083→5296 · acceptance 367→373 · MCP 105→107。完整历史见 [CHANGELOG](../CHANGELOG.md)。

产品定位详见 [设计哲学](./PHILOSOPHY.md) 和 [README](../README.md)。

## 目录

- [现在在哪：v1.5.2（已发版）· 下一版 v1.5.3](#现在在哪v152已发版-下一版-v153)

- [迭代历程](#迭代历程)
- [未来去哪](#未来去哪)
- [版本规划](#版本规划)
- [行业印证](#行业印证)
- [探索方向](#探索方向)
- [分层模型架构（v3.x 远景概述）](#分层模型架构v3x-远景概述)
- [欢迎参与](#欢迎参与)
- [历史架构演进](#历史架构演进)

---

## 现在在哪：v1.5.2（已发版）· 下一版 v1.5.3

> **v1.5.1 开发完成、文档收尾完成，已于 2026-09-22 发布**——编排模块事件驱动（业务事件触发 + 理解债务应对 + 设备 OTA 远程升级 + 异常处理总线）· 任务下发通道二期 · 生产管线接线 · 审计输入双通道 · `sofagent demo` · 测试 4903→5083 · acceptance 357→367。详见 [v1.5.1 开发日志](./changelog/v1.5/v1.5.1.md)。


---

## 迭代历程

完整版本历史见 [CHANGELOG](../CHANGELOG.md)。v0.x 为实验/测试版，v1.0.0 起为正式版。

| 版本 | 核心交付 |
|------|------|
| **v1.5.2** | **🔍 审计模块 · 对外面与判定语义**：MCP audit 数据对外（audit_query 只读 + 事件订阅推送）· 约束导出与证据链外部可验（ruleset_export 双向可逆 + verify-chain 独立验签）· should-run 五问判定链 · 结论失效语义 · 出口治理面 · 事前授权补环 · DSH 插件 npm 首发（kit + 七款）| [v1.5.2](./changelog/v1.5/v1.5.2.md) |
| **v1.5.1** | **⚡ 编排模块 · 事件驱动**：业务事件触发（四源 + on: 订阅 + 死信重放）· 理解债务应对 · AI 异常总线 · 设备 OTA + 任务下发二期 · 审计输入双通道 · sofagent demo| [v1.5.1](./changelog/v1.5/v1.5.1.md) |
| **v1.5.0** | **🛡️ 治理模块 · 可见性与本体成熟**：治理 KPI 面板（Dashboard 治理 tab 六卡 + 数据集审阅 + lineage 合规报告 + 周报）· 本体数据双时态（stateAt 时点快照 + 三层渐进加载）· Ontology Validation Engine（DAG 无环 + 激活前置门 fail-closed）· 决策因果链消费 · FDE 陪跑期补全 · 跨层证据对账 trace_reconcile（三源四态）· 存量清扫收尾（退役 API 正式移除 + composeWithDeepAgents 别名下线 + audit CLI 三 shim 移除）· 命名债务清扫（`@sofagent/harness` → `@sofagent/inject`）· DSH 插件事件接线 · MCP 104→**105** · 测试 4805→**4903**（+98）· acceptance 352→357（详见 [CHANGELOG](../CHANGELOG.md)） |
| **v1.4.9** | **📡 设备接入与数据承接**：G9 设备注册/发现/心跳（Ed25519 身份 + 在线才派单/掉线改派）+ G10 数据目录白名单授权读取 + G11 采集声明 opt-in 上行（WAL 加密暂存 + 断点续传 + 审计计量）+ T7 router 过站 session 承接 + T8 敏感识别三层插槽（SDK 面）+ T9 权重灰度 AB + G5b 连接器注册 + G1 workflow 模板血缘 + T6 installer + T10 模型清单 · MCP 95→**104**（9 新）· 测试 4429→**4805**（详见 [CHANGELOG](../CHANGELOG.md)） |
| **v1.4.8** | **🔌 插件管控与工程效能**：插件来源白名单 · 应用级工具策略 · 多 Agent 协作阵型库 · 自动上下文压缩 · shell 提权分级 · 成本 quota 事前门禁 · 依赖方向架构测试 · 自研进化 gate（skillopt 更名 `@sofagent/evolve`）· 执行机制纪律批 · MCP 95→**95**（零新增）· 测试 4279→**4429**（详见 [CHANGELOG](../CHANGELOG.md)） |
| **v1.4.7** | **🔌 商业平台接口版**：读接口（G2 能力缺口 / G4 人机贡献度 / G6 节点级可见性 / G7 多租户 v0）· 写接口（G13 PR 生命周期三 tool · G14 workflow CRUD 四 tool · `trigger.schedule`）· 交付三件（workflow 烧 USB / G8 首部署 cron / 上岗 prompt）· 云训练执行收口（TrainChannel 标准接口 + ssh 通道）· 三接线（静态加密 / audit repo-hash / data_push）· audit 规约层与 PROV-O 导出· 测试 4107→**4279**（起点为换基后口径：4107 = 4088 + 19 散测；CHANGELOG 索引行记换基前值 4088）· acceptance 311→328 · MCP 84→**95**（详见 [CHANGELOG](../CHANGELOG.md)） |
| **v1.4.6** | **🚀 后训模块 · 分布式与云端**：多卡/多机训练（gpu.count/nodes + schema v2 存量兼容 + GPU 双轴拓扑 + NCCL 第八类诊断）+ 云 VM 执行面（train_cloud 注册/体检/远程 spawn + 分拣三档宁拦勿漏 + 失联止损 5min + 成本入预算）+ 标准数据推送接口（双闸，入口接线 v1.4.7）+ 边界收缩（配方外部装载/trainEnvInit 归 shell/model-downloader 删除/TrainExecutor 隔离，约 −1.2k 行）· 测试 4055→4088 · acceptance 305→311 · MCP 83→84（详见 [CHANGELOG](../CHANGELOG.md)） |
| **v1.4.5** | **🚀 后训模块 · 服务与持续**：训练推理服务 train_serve + 持续后训练三触发与回退保护 + 合规扫描闸门 train_compliance + FDE 交付包 train_deliverable + 保留策略归档 + 后训 Quickstart 十步 + 进化实证收口（真脑/台账/采样/L4 自进化）+ FDE 进场记忆目录 + 可靠性加固批（webhook SSRF/审计链截断/超时降级）· 测试 3753→4055 · acceptance 304→305 · MCP 80→83（详见 [CHANGELOG](../CHANGELOG.md)） |
| **v1.4.4** | **🚀 后训模块 · 信号与部署闭环**：训练语料导出三件套（corpus_export 79→80 + reward 骨架）+ 企业专属模型本地权重部署（manifest+sha256 篡改拒绝+rollback-weights）+ 训练产物→注册自动衔接 + 多基座对比（train compare ROI 排序）+ 决策因果链与先例检索 + CI 供应链全 SHA 固定 + 五能力叙事定稿 · 测试 3619→3753 · acceptance 294→304 · MCP 79→80（详见 [CHANGELOG](../CHANGELOG.md)） |
| **v1.4.3** | **🚀 后训模块 · 运行与需求 + 审计聚合指标**：训练监控 + GPU 队列（train_status/train_list）· 失败诊断 train_diagnose 七类 · 训练沙箱 + 设备打包 · 需求推导 + RL 配方模板 · 后训练 workflow 模板 · 审计聚合指标 --stats · 训练反作弊基线 · 存量清扫五件（含 checkHistoryChainIntegrity 退役公告）· doctor 补 Ontology 完整性 · 测试 3349→3619 · acceptance 276→294 · MCP 76→79 |
| **v1.4.2** | **🚀 后训模块 · 数据与评估 + FDE Harness 层**：数据管道（多源接入 + 质量闸门 + 脱敏）· dataset_version 版本台账 · eval 闭环 · 环境管理 + train_doctor · dry-run 与算力外推 · 训练报告 · FDE 六件（fde_interview/classify/quantify/derive/distill/deploy）· IM 桥 dsh-im · 测试 3202→3349 · acceptance 271→276 · MCP 67→76 |
| **v1.4.1** | 🚂 后训模块 · 地基 八大块（train-job 编排 + train_job 审计 HMAC 链 + enterpriseId 隔离 + 可复现指纹 + 权重 HMAC 签名 + 中断回收 + 崩溃恢复 + 安全基线）+ train_submit + 阶段 0 Metal reward 收敛验证 + 双栈契约/训练安全基线文档 + SKILL 体系重构 · 测试 2981→3222（+241）· MCP 66→67 |
| **v1.4.0** | 📊 Web 工作明细页 + 图谱栏 + 成本审计（cost_query）+ DSH 插件 9 款 + OpenClaw 插件 4 款 + Dashboard 产品化 + 联邦查询 E2E + MLflow + Agentic Browser + 工具角色分层 + MCP 自动配置 · 测试 2903→2981 · MCP 61→66 |
| **v1.3.x**（10 版） | **运行时审计闭环 + L1-L3 组织协作 + 引擎接口外化 + 自进化种子**：v1.3.0 运行时审计最小闭环（激活链 SUSTAIN 收尾）→ v1.3.1 Durable Execution + Benchmark → v1.3.2 Onboard Agent → v1.3.3 L2 团队协作 → v1.3.4 L3 组织能力市场 + 编排/执行分离 → v1.3.5 MCP 自进化 + instinct→skill 自动进化 → v1.3.6 引擎接口外化完整版（模型层前置 · MCP 52→60）→ v1.3.7 SubAgent 完整沙箱 + AgentShield + 行业 overlay → v1.3.8 代理网关硬边界 + 数据静态加密 + Durable L3 → v1.3.9 AST 规则引擎 + meta-harness + FORGE driver 切 DSH（详见 [CHANGELOG](../CHANGELOG.md)） |
| **v1.2.x**（10 版） | **激活链 ACTIVATE→ORCHESTRATE→EXECUTE 全线打通 + 约束层叙事统一 + 三个入口产品**：v1.2.0 物理结构大重构（/sofagent/→/engine/）→ v1.2.5 激活链 Phase 1 + A20-A23 规则 → v1.2.7 编排模块增强（StateGraph + Session Goals）→ v1.2.9 FORGE 短任务化 + npx CLI/规则市场/GitHub Action 三入口 + 约束层叙事重构（详见 [CHANGELOG](../CHANGELOG.md)） |
| **v1.1.x**（10 版） | **编排模块从 ao → LangGraph + 多设备联邦 + Dream Cycle 知识进化**：v1.1.0 包结构纯度重构（12 包独立）→ v1.1.3 LangGraph StateGraph 直接编排 → v1.1.7 Dream Cycle 6 阶段 + 知识健康巡检 → v1.1.8 安全层加密 + 联邦查询 → v1.1.9 产品叙事收敛（FDE Agent）+ USB 完整运行时（详见 [CHANGELOG](../CHANGELOG.md)） |
| **v1.0.x**（10 版） | **审计模块奠基 + AI 知识库实现 + 双节点架构**：v1.0.0 正式版发布（Agent 审计工具，2026-07-10）→ v1.0.5 Ontology 统一层 + Work模板市场 → v1.0.7 双节点架构 + ao 退役 → v1.0.8 FDE Agent 自进化 + 文件系统审计 → v1.0.9 二进制审计 + MCP compose tool（详见 [CHANGELOG](../CHANGELOG.md)） |

> ℹ️ v1.4.8 / v1.4.9 两行未单列 acceptance 增量——两版合计 acceptance 328→352，逐版拆分未留档，按诚实原则留空；v1.5.0 起恢复逐版记录。

## 未来去哪

> 以下是**方向**，不是承诺。没实测过的事标「不知道」。

**终局**：企业不再需要 FDE。AI 节点部署后自主运行，审计模块持续盯变更，编排模块自动纠偏，知识库自我积累——人只需要偶尔看一眼 dashboard 确认一切正常。我们做的不是给企业装 AI，是让企业忘了我们的存在。

> 连创造 AI 的人都在公开表达方向失控的不安（DeepMind 创始人 Hassabis 2026 年访谈）——「AI 可以被管住」的 Harness 中间件不是选配，是刚需。

**为什么是现在——转折点的三信号**：单一信号不够，三信号同时成熟才构成真正的范式转折点：

| 信号 | 维度 | 内容 |
|------|------|------|
| 供给侧 | AI Coding 成本趋零 | FDE 借 AI Coding 1 天出 Demo，瓶颈从技术能力转向**业务抽象能力**（能否把 SOP 拆成 Agent 工作流） |
| 治理侧 | Agent IAM 组织身份 | Agent 有工号/权限/审计/全生命周期管理，从「工具」变「员工」，才能进生产环境 |
| 能力侧 | 协同飞轮持续进化 | 每次人工纠正/确认/追问回流为结构化学习信号，越用越懂企业 |

**现实验证**：工作流主语从「人」迁移到「Agent」——SOP 拆为 Agent 工作流、给 Agent 派工号、人工纠正回流为学习信号——三信号同时成熟的落地案例。

sofagent 的定位正卡在这个转折点上：审计模块（治理侧）+ Ontology（能力侧）+ 开源 MIT（供给侧）——三信号缺一不可，单独做任何一个都不够。

**供给侧技术拼图已齐**（NVIDIA 2025.7 判断：模型/微调框架/示范方案/沙盒/工具链全部就绪）——sofagent 的 `install.sh` + FDE 四阶段示范方案直接给一套可以跑的蓝图，不是告诉企业"该做"。

两条路径：**FDE 驻场部署**（传统中小企业，FDE 进场→四阶段十二步流程→交付→撤离）和 **开发者自部署**（开源社区，git clone→install.sh→审计→CI 集成）。

设备端形态：安装时可配置 Agent 平台（OpenClaw / WorkBuddy 等），审计结果通过 MCP server 推到企业协同平台。**数据主权在设备**——所有记忆、日志、决策记录永不离开本地。

> 以下是方向落地为版本的具体拆解。v3.x 长期架构骨架见下方「探索方向」。

---

## 版本规划

> 以下带状态版本表为权威源；各版本子项定义见各行「日志」列链接的规划文件。

### 排期准入判据

> **每版交付章上限 10 章**（v1.5.0 已冻结的章节不追溯；自 v1.5.1 起适用）。超限章**优先后移到后续版本**（用版本序列承接，不轻易退回探索方向表——先把版本排满再谈砍）；后移顺序按下方权重从低到高。

| 权重 | 来源 | 判据 |
|:--:|------|------|
| **W1** | 具名消费方诉求 | 指认得出消费方——G 编号 / 平台层或模型层接口表 / 真实客户诉求 |
| **W2** | 数据飞轮链路缺口 | 一体机链路（session→分拣→脱敏→数据集→训练→灰度）上缺失的那一环 |
| **W3** | 已交付能力的缺陷与债务 | 存量红、命名债、假绿、跨仓漂移、悬空承诺 |
| **W4** | 外部对照研究收编 | 须能回答「服务于 W1 或 W2 的哪一条」；答不出按 W5 处置 |
| **W5** | 内部灵感 / 远景 / 纯评估报告 | 默认进探索方向表，不占版本章；已排者超限时优先移出 |

**两条落地纪律**：① **收编须指认消费方**——进 changelog 的每章须能指认 W1/W2/W3 至少一项，不能指认的移入探索方向表；② **不设冷却期**——外部研究的吸收节奏由维护者按当期上下文人工裁定，登记（[VALIDATION](./VALIDATION.md)）与承诺（changelog）分离的原则不变。

### 规划版本

> 🔴 **阻塞项占位纪律**：任何 🔴 采购 / 合规阻塞项必须在下表占据一个**明确的版本单元格**（标注具体版本号），不得仅写在散文备注里。

| 版本 | 状态 | 核心交付 | 日志 |
|------|:--:|------|:--:|
| **v1.4.8** | ✅ 已发版（2026-09-13） | **🔌 插件管控与工程效能**：插件来源白名单（Git URL/主机/本地路径 + 托管 hook 独裁）· 应用级工具策略（app×tool 白名单矩阵，fail-closed）· 多 Agent 协作阵型库（六阵型）· 自动上下文压缩（加载链超 3% 预算触发）· shell 提权分级（dangerous 走 HITL）· 成本 quota 事前门禁 · 依赖方向架构测试（14 包边界[build 序列口径：13 模块包 + load-chain，不含 umbrella；workspace 总量 26 口径见 [WIKI §六](./WIKI.md#六当前状态)] + CI 强制）· workflow 节点级模型偏好绑定 · 技能按模型分级门控 · 自研进化 gate 验证器 + skillopt 重构更名 @sofagent/evolve（进化四角色合拢完整自进化循环）· 跨 harness 协作阵型注册表（ACP Registry，接入仅评估不承诺）· **执行机制纪律批（商业平台机制对账：调度判定双层纯函数主判 / Git 能力降级矩阵三态×两隔离 / 多 Agent 并发 Git 纪律 / 作用域显名禁裸 id）** | [日志](./changelog/v1.4/v1.4.8.md) |
| **v1.4.9** | ✅ 已发版（2026-09-17） | **📟 设备接入与数据承接版（多设备 Harness 中间层 + router 伴生数据面）**：G9 设备注册/发现与心跳（Ed25519 身份码 + 验签 fail-closed）· G10 设备侧数据面授权读取（目录白名单默认空 opt-in + 脱敏联动 + 读取审计入 HMAC 链）· G11 数据上行通道（采集声明 opt-in → WAL 暂存 → 断点续传 → 加密上行）· **G5b 连接器注册/发现（自 v1.4.8 迁入：connector_list + 第三方连接器注册——v1.4.8 实交付插件来源白名单 + app_tool_policy，连接器注册面未随版落地）** · **G1 workflow 模板导出/导入 + 血缘（自 v1.4.7 候选迁入：五件套导出 + fork 谱系回流——逐版对账确认掉出版本序列）** · installer skill（prompt 驱动裸机自动安装）· 任务下发通道一期——心跳捎带 · 在线才派单 / 掉线改派语义（G9 验收标准补全）· 跨设备任务路由评估（依赖 v1.5.1 事件总线，评估不实做）· **session 承接与 router 伴生（自原 v1.5.3 前移改写：router 基于开源自建、约束层不自研 router 本体，交付伴生 exporter 协议——过站 session 多轮样本模型 + 蒸馏偏好对构造 + 工作产物采集五源→六源 + usage 入 cost 台账 + key 维度 harness 四件：按 key 计量/审计挂链/异常检测/处置建议）**· **敏感识别升级（自原 v1.5.3 前移 + 三层检测器新编排：L0 内置正则 / L1 企业专名词典（最高优先级·零模型）/ L2 外挂 NER（含一体机本地模型适配器）+ Presidio 实体类型 schema 对齐；敏感度分类器与脱敏共用插槽 + 置信度三层 fail-closed）**· **权重灰度发布 AB（自原 v1.5.3 前移：新旧 LoRA 按流量比例灰度 + 劣化自动回退）**——一体机数据飞轮（session→分拣→脱敏→数据集→训练→灰度上线）在 v1.5.0 前整链成型；「平台不直连设备，数据一律过约束层」；**设备侧 /health 健康巡检端点（三态分层）+ 会话作用域续接五元组（宁可不续走摘要交接）+ 执行侧模型清单上报 + 执行时 skill 快照（打包/清理/审计锚点）**；商业侧规划独立于本仓库维护 | [日志](./changelog/v1.4/v1.4.9.md) |
| **v1.5.3** | 📋 规划中 | **🔍 审计模块 · 规则引擎统一与自测**：双规则引擎统一（消除 ARCHITECTURE 悬空承诺——tool-level 3 条与 git-diff 24 条收敛为单一规则引擎 + 两种触发时机）· 审计规则自测 schema（Codex execpolicy 启发——24 条规则强制携带 match/notMatch 正负样例，加载时断言 fail-closed + FAIL 报文附替代建议 + 多规则取最严）· A24 交付物落点审计规则（+1 条成 25——白名单 opt-in 事前拦截，出生即带正负样例；全仓计数落点同批升级）。 · **🧰 运维面 · doctor 修复闭环**：检查 → 一键修复（`doctor --refresh` 备份 + 一键重置到默认 + 前后 diff 报告，全程审计留痕）· **@sofagent/skillopt 旧包退役收尾**（npm deprecate 收尾，自 v1.4.8 承接——registry 实查 deprecated 未落、全仓 workspaces 零条目，本版补做；breaking 随重置语义一并收，承诺落点经地基期拆版顺延至本版） · **🔍 审计模块 · 规则面地基补两章**：判决类记录成对完整（扫全仓判决类记录类型，只记单边即报红——实例修正不等于类修正）+ 审计范围语义一等公民化（`AuditScope` 显式对象，规则不再自调 git 取范围）。**形态归属**：主干 4 章（规则自测 schema / A24 交付物落点规则 / doctor 修复闭环 / 判决类记录成对完整）+ 非功能 3 章（双规则引擎统一=重构，不新增能力面；ARCHITECTURE 三域重构——B 步 R3；审计范围语义一等公民化 / AuditScope 重构=重构）+ 通道成分 1（refresh 的备份与重置执行动作）。**本版由旧 v1.5.4、旧 v1.5.5 撤并而来** | [日志](./changelog/v1.5/v1.5.3.md) |
| **v1.5.4** | 📋 规划中 | **⚡ 执行模块 · 路由决策链（自 v2.0.0 前移）**：模型路由层（云端规划 / 本地执行分层路由 + 敏感度 fail-closed + routeReason 可解释——敏感度分类输入前置件与 session 承接 / 权重灰度 AB 已前移 [v1.4.9](./changelog/v1.4/v1.4.9.md)，本版做调用侧分流）· 本地槽位排队与判定分层（私有部署一体机形态消费方[W1]——本地并发槽位信号量 3–5 + 排队超时按合规升级云端 + 判定分层 L0 声明式映射 / L1 embedding 分类 / L2 难例兜底[判定不占本地主模型槽] + 决策下达面[补 v1.4.9 承接面的反向通道，构成双向闭环]）。 · **⚡ 执行模块 · 凭证与验证**：凭证隔离 Vault（Agent 代码碰不到 token + 轮换吊销 + **凭证范围声明**产出——闭合审计面凭证范围盲区的证据面）· 多实例自验证（N 实例多数表决 + 分歧路由 HITL，复用 run_ab_test） · **🤝 执行模块 · AI 节点治理接入（2026-09-22 归位再分配迁入）**：外部 Agent 节点纳入治理面——治理面注册（Ed25519 身份 + 编排栈声明（LangChain/LangGraph）+ 事件端点 + 能力面声明）+ 事件挂接（复用 v1.5.1 事件总线，规则判定逻辑零改动）+ 出站管控消费（节点出站过 [v1.5.2](./changelog/v1.5/v1.5.2.md) 出口治理白名单裁决——首个实装消费方）+ workflow 引用契约（已注册节点可被引用为 workflow 节点类型，治理状态前置检查）+ **cordis 兼容层**（主干=Hub 的 USB 口：事件桥+services 桥暴露给第三方 cordis 插件 + 插件可用性预检四段探针（随宿主版本复检）+ L1直挂/L2挂hub/L3挂hub+S1M 三层插件位 + **四层增值链**（白得审计/进化护栏/飞轮增值/判定服务）+ **兼容面向接口集不面向具体插件**（宿主公开服务面为对照全集，不为具体插件写特例））；节点本体用 DeepSeek harness 加 plugin 构建，本仓不做运行时合体、只做判定与审计侧 · **🧾 执行模块 · 授权与凭证对账**：授权三元素（谁批的 / 批了什么）× 凭证范围声明（发了什么范围）逐项比对——范围超集 / 时效超期 / 缺授权三类错位判越权风险并挂链留痕。**形态归属**：主干 5 章（路由判定 / 本地槽位排队与判定分层 / 多实例表决 / AI 节点治理接入 / 授权面与凭证面对账）+ 通道 1 章（凭证 Vault——凭证托管 / 轮换 / 吊销是通用轮子，换 HashiCorp Vault 价值不受损，「Agent 代码碰不到 token」的拦截约束留主干）+ 非功能 1 章（PHILOSOPHY × FDE 叙事章——B 步 R4）——旧 v1.5.6 两章同属一条决策链的相邻段，同版交付保依赖闭环。**本版由旧 v1.5.6、旧 v1.5.7 撤并而来** | [日志](./changelog/v1.5/v1.5.4.md) |
| **v1.5.5** | 📋 规划中 | **⚡ 执行模块 · 全节点执行状态机**：SKILL.state 收编（P+Σt+ot 三输入确定性合并，O(1) prompt O(T) token + 「轨迹可弃行为可溯」审计闸门前置——全面推广不试点，全局降级开关兜底）。 · **⚡ 执行模块 · Tool search 按需加载**：工具不全量注入（OpenAI Agents API 启发——按节点域 + 任务语义检索注入子集 + 分步按需 + 命中率与 token 曲线度量；mid-turn steering 运行中纠偏随本版评估）。**形态归属**：主干 2 章（全节点状态机 / tool search 按需加载）+ 非功能 1 章（README 理念主线全量重写——B 步 R5）——旧 v1.5.8 重件独立成版（协议层改造面覆盖全部长任务节点）；旧 v1.5.9 与 [v1.5.4](./changelog/v1.5/v1.5.4.md) 同属调用侧上下文供给。**本版由旧 v1.5.8、旧 v1.5.9 撤并而来** | [日志](./changelog/v1.5/v1.5.5.md) |
| **v1.5.6** | 📋 规划中 | **🧭 执行模块 · CLI 单入口收敛**：13 bin → `sofagent <域> <动作>`（实测 12 个 `sofagent-*` + 裸名主入口已在位，补域路由 + 旧命令 shim 化，文档面同批收敛，兼容期不 breaking）。 · **🔍 审计模块 · 数据生命周期治理**：`~/.sofagent/data` 增长面收敛（memory 事实二级分层 + 归档轮转 / 审计链历史段归档双锚点——归档 ≠ 删除 / 遗留备份接管 / doctor data 健康项）。**形态归属**：主干 1 章（数据生命周期治理）+ 非功能 2 章（CLI 单入口=重构 13 bin → 1；运维与安全文档注入——B 步 R6）+ 通道成分 1（归档轮转与遗留清理**执行动作**）。**本版由旧 v1.5.10、旧 v1.5.13 撤并而来** | [日志](./changelog/v1.5/v1.5.6.md) |
| **v1.5.7** | 📋 规划中 | **🔍 审计模块 · 覆盖面扩展**：SMB 场景审计扩展（数据处理 / 报表生成——数值勾稽 / 来源可溯 / 口径一致三规则 + `DecisionKind` 加 `DATA_PRODUCT`[⚠️ `@public` 契约] + SMB onboarding 模板 + 治理 KPI 复用）· **浏览器四件套退役执行**（可行性报告落盘即触发 v2.0.0 §七 B 表预定裁定：registry 注销+计数 107→103 落点同批+breaking 公告）· UI 层审计前置评估（多模态截图证据可行性报告——按版本边界纪律 `v2.x` 承接窗口已关闭，实做与否待报告结论，裁定见 [v2.0.0 §七](./changelog/v2.0/v2.0.0.md)）· OWASP Agentic Top 10 补条（按版补 1-2 条 ASI 规则走向 10/10）+ **规则语料双分类映射**（OWASP ASI + MITRE ATLAS 双编号回填）。 · **🔍 审计模块 · 能力面治理与可拔契约**：能力面使用率治理（SkillOps 五维 + 低价值技能退役候选 + 技术债台账 + 工具使用率遥测 `tool-usage.jsonl` 本地无上报 + `stats --tools` 三视图）· 可拔契约与主干能力清单（`engine/capabilities.json` SSOT + 82 个 `SOFAGENT_*` 按「功能门控 / 配置参数」分流 + 级联校验 exit 2 + 关档硬线扩主干侧 + `list_capabilities` 可拔能力地图 + **形态标注守卫** `tools/check/check-forms.mjs`——声明面与守卫同批立） · **🔍 审计模块 · 对外与输入面三章**：国标 Agent 审计对位（标准跟踪登记 + 对位差距表，不预设对齐动作、不转述标准文本）+ 决策日志升为审计输入面正式一路（已记 15 类可问责决策、规则判定面此前零消费，比新造采集器便宜）+ ACS YAML 策略引擎兼容性评估（表达面 / 分叉风险 / 两态结论，结论须早于 v1.6.0 判据 schema 定稿）。**形态归属**：主干 5 章（SMB 场景审计扩展 / OWASP 补条 / 能力面使用率治理 / **可拔契约与主干能力清单** / 决策日志作为审计输入源）+ 非功能 4 章（UI 层审计前置评估=可行性报告，本版不实做；VALIDATION 与 WIKI 生态叙事注入——B 步 R7，R1 清单至此全部销账；国标 Agent 审计对位=标准跟踪与差距表，不实做对齐动作；ACS YAML 策略引擎=策略表达面兼容性评估，不实做引擎）+ 外部成分 1（SMB onboarding 模板——数据类）——旧 v1.5.12 两章是同一件事的两半（哪些能力值得退役 / 退役怎么拔）。**本版由旧 v1.5.11、旧 v1.5.12 撤并而来** | [日志](./changelog/v1.5/v1.5.7.md) |
| **v1.5.8** | 📋 规划中 | **⚡ 进化模块 · 准入门与晋级判据（RSI 三算子对位）**：进化准入判据（检验-生成差距——域验证器三档 deterministic/model-judge/human-only 分流 + 未登记 fail-closed 按 human-only）· 三层晋级判据（注入租用 → skill 半持久 → 微调摊销——频次 × 存续 × 验证三输入纯函数 + 晋级台账 + promote-train 只提示人审 + 三层可逆回退）。 · **⚡ 进化模块 · 数据面与入池质检**：经验池接训练管道（**hooks 零外部调用·评估只在显式命令·结果只排序不自动执行** + instinct 池 → 数据集 → TrainChannel 断链打通——`instinct` 第五数据源 + 血缘锚点 + 来源真实性标记 + 租户隔离，一体机飞轮 Data 面自供给）· 出题考核器（instinct 反向生成考核题 + 双成功才 `verified` 入池 + 考核态进 scorer 置信度——Data-RSI 归因→出题→考核补齐） · **🧪 进化模块 · 训练期治理**：训练期策略集 opt-in + 命中轨迹按规则严重级折算惩罚权重进 reward 判据（承接 v1.4.4 已有映射，不另立第三套）+ 留痕（哪个规则集版本影响了哪批数据）；本仓只做治理与信号、训练循环仍归模型层，与准入门**互补不互替**。**形态归属**：主干 5 章（准入判据 / 晋级判据 / 经验池管道的入池资格判定 / 出题考核器 / RL 训练治理）+ 通道成分 1（经验池管道的数据集构造与 TrainChannel 提交**执行动作**）——旧 v1.5.14 两章为进化的两面门、准入章**不可拔**（进化安全边界），域验证器三档是考核器分流的依据；旧 v1.5.15 考核章**不可拔**（经验池的质检门）。**本版由旧 v1.5.14、旧 v1.5.15 撤并而来** | [日志](./changelog/v1.5/v1.5.8.md) |
| **v1.5.9** | 📋 规划中 | **⚡ 进化模块 · 写面审计与能力基线**：自进化写面的审计覆盖（第三轮补盲：prompt / memory 两面补留痕——权重面 v1.4.4 manifest 与 skill 面 eval-gate 已覆盖，唯独自我改进最大的两个写面无硬证据，审计失明）· 能力基线版本线（四层状态快照成链——v1.4.9 执行级快照管单次可复现，本版管跨时间可追溯，出事能回答「当时它是怎么做事的」）。 · **⚡ 进化模块 · 配置面契约与元验证**：加载链配置面契约（四层配置字段 SSOT + 字段所有权断言 + 越界检测——与 [v1.5.7](./changelog/v1.5/v1.5.7.md) 可拔契约同族，判据不是「能配置」而是「配置面无死角」）· 验证器自身老化检测（元验证——进化越久验证集越失真，越跑门越松；老化只提示不阻断） · **🥊 审计可信度的攻防两面（两项合流）**：敌手自测（三类攻击面成对用例——篡改 history / 伪造 HMAC 链 / 绕过 ToolGate；被打穿进回归清单、未穿透进 anti-cases）+ 透明仓（治理原始记录含误报与翻案公开，受误报率 <5% 红线与人工确认双控，不含租户数据与凭证）。**形态归属**：主干 5 章（写面审计覆盖 / 能力基线版本线 / 配置面契约 / 验证器老化检测 / 透明仓与敌手自测）+ 插件 1 章（**进化面插件化——evolve 升格 RSI 独立模块**：形态收口放能力收齐后身份宣告前，沉淀留 harness·进化独立插件，挂载位迁移零代码搬移）+ 非功能 1 章（FDE Handbook 升格——GUIDE 六段生命周期展开，B 步 R8）——**2026-09-22 归位再分配（维护者裁定）**：网络出口治理面迁 [v1.5.2 第五章](./changelog/v1.5/v1.5.2.md)（审计举证面亲和）、AI 节点治理接入迁 [v1.5.4 第五章](./changelog/v1.5/v1.5.4.md)（路由决策链面亲和），两章全文零丢失迁出，本版回归进化模块四主题——旧 v1.5.16 写面审计章**不可拔**（审计地基）；旧 v1.5.17 与 [v1.5.7](./changelog/v1.5/v1.5.7.md) 可拔契约同族，判据不是「能配置」而是「配置面无死角」。**本版由旧 v1.5.16、旧 v1.5.17 撤并而来** | [日志](./changelog/v1.5/v1.5.9.md) |
| **v1.6.0** | 📋 规划中 | **🧭 判定底座 · 阶段一（判据与数据地基）· 判据 schema 定稿**：三原语类型化（`Choice` 带全候选分布 / `Score` 带区间 / `Noul` 带真值概率）+ 问句集限定（类型越界拒绝）+ 多问并行形态预留 + 24–25 条审计规则逐条重述为「判据 + 证据」（复用 [v1.5.3](./changelog/v1.5/v1.5.3.md) 正负样例格式，零格式分叉）+ 判据 schema 加载校验 fail-closed + **判据质量 lint**（硬项 fail-closed：正负样例矛盾 / 证据不可复现 / **概率来源不可追溯** / **问句混装多判断**（拆原子问句由代码加权合成）；软项只提示不阻断：可校准性 / 判据重复度）+ **判据间依赖显式化**（前置判据输出为后置输入，有依赖串行、不靠推断）+ **判据重述五项增补**（① 判定化范围收敛——**纯函数型常数留代码、不判定化**，其缺陷以「记系数来源」修正；② **调用级风险绑定**——风险属「工具＋参数」而非规则，判据须能表达参数条件升档；③ **防线归属标注**——该不该做 / 数据能否出去 / 最多能碰什么，三防线拆开不混装；④ **档位五态** ALLOW / ASK / **ABSTAIN** / DENY / SKIP，**选项表弃权位**（强制选择必含「无合适项→转人工」第四选项，与 ABSTAIN 分立互补）+ **弃权独立归因**不塞进 ASK；⑤ **三处结构盲区登记**——凭证范围 / 动作授权状态 / 序列累积效应，登记为盲区而非新增规则、**条数一条不动**（前两者折进既有规则语义层 `A22-2` / `A17-2`，后者随探索方向 mandate 补环推进；**盲区即证据面缺口**——事实不可观测时加规则只会得到永远弃权或永远放行的假规则）、**并指认既有归属**）+ **判据集版本可举证**（内容哈希随判定落 decision-log——「按哪版判的」须可答）+ **门槛纪律**（按原语分档 / 绑定分布须重标 / 健康度两极端告警——门槛设错只静默失效）+ 理念升级驱动的文档重构机制立规（触发条件 / 分层口径 / 版本关系纪律）+ **判定底座边界声明**（做马鞍不做马场——四判据 + 边界外清单 + 边界内六项落点）。**边界**：不训练、不接线、不出权重；decision-log / HMAC 链 schema 零改动。**前置门**：判据不可用则不启动后续版本。 · **🧱 判定底座 · 阶段一 · 数据集三元组与三源采集**：`(state, questions, answers)` 三元组格式 + **概率目标表达**（分布/区间/真值概率——硬标签训不出校准，格式层预留）+ `label.source` 三档（`deterministic`/`model-judge`/`human`，对齐 [v1.5.8](./changelog/v1.5/v1.5.8.md) 域验证器口径）+ history/HITL/PR 三源采集器 + 来源真实性门（未建链/合成来源 fail-closed 拒收，防自证污染）+ 租户隔离 + 去重与**跨集泄漏防护**（按轨迹粒度切分；另防**回放泄漏**——切分点之后产生的信息不得进入切分点之前的输入）+ 采集可观测 + **九类困难样本采集点**（弃权记录 / 上下文取舍 / 路由对比 / 措辞不变性 / OOD / 漂移探测 / 对抗 / 对齐分歧 / 出站裁决）+ **四道采集质量门**（来源真实性 / 跨集泄漏 / 标签噪声对账 / 校准标注）+ **主动学习四类选样**（不确定 / 分歧 / 代表 / 漂移优先；中文优先与人审优先纪律）+ **集分档**（候选集 / 刻度集 / 免调集三集分工不混用；候选集**冻结即单调**、样本**私有锚定**防厂商评测集污染；**门槛是分布的函数**——换基座或数据漂移须重标门槛）。 · **🧪 判定底座 · 阶段一收口 · 编码器基座与零样本基线**：**encoder-only** 基座接入（非生成路线）+ 类型化判定头（choice/score/noul 分头共享编码器）+ 多问并行（一次编码多头出答案）+ 判定不占本地主模型槽位（行为锁）+ 零新增运行时组件 + 零样本评测器（逐判据准确率 + **失败模式分类**）+ 校准现状探测（分桶可靠图 + ECE）+ **中文零样本基线**（中英分列落数不合并 / 判据集中文版语义等价非机翻 / 失败模式按语言分报）+ **分布内外分列**（演示任务族 vs 从未训练的任务类型，落差约 24 个百分点的量级须显式落数）+ **可复算产物随数交付**（脚本 + 结果文件入仓；单次运行 / 私有数据口径显式标注）+ **基线如实落数不修饰**。**产物性质**：这是基线不是成果——阶段一累计产物为「判据+数据+基线」三件内部资产，对外口径零变化。**形态归属**：主干 8 章（三原语类型化 / 24 条规则判据重述 / 判定底座边界声明 / 数据集三元组格式 / 三源采集器与九类补充采集点 / 采集质量门与主动学习选样（含出站裁决源——消费 v1.5.2 §五导出） / 编码器与判定头接线 / 零样本基线落数 + **已知失灵面主动落数**（算术/计数/日期/间接问句/干扰上下文）+ **校准「已验证/设计目标」分档表述**）+ 非功能 1 章（文档重构机制立规）+ 通道成分 1（采集与落盘的执行动作）。**本版由旧 v1.6.0、旧 v1.6.1、旧 v1.6.2 撤并而来** | [日志](./changelog/v1.6/v1.6.0.md) |
| **v1.7.0** | 📋 规划中 | **🎯 判定底座 · 阶段二（模型与校准）· RLCD 训练复现**：严格适当评分规则（`Noul` 用 log score / `Choice` 用 log score + spherical / `Score` 用 RPS 保序）+ 策略梯度训练回路（**无人类偏好模型、无外部教师模型**——奖励全来自评分规则）+ **非对称惩罚**（报极端概率而判错重罚 / 报温和概率而判错轻罚——诚实报概率是唯一不被惩罚的策略，不确定输入下置信度被主动拉低以让调用方有条件走 fallback）+ 消费概率目标而非硬标签 + `label.source` 档位加权 + 云端全参数微调 + 本地 LoRA 档两条路径都交付（本地档为交付形态下限非备选，可用性须实测落数）+ TrainChannel 提交（不写云厂商适配器）+ 训练可复现清单。 · **📐 判定底座 · 阶段二 · 校准（可用性硬门槛）**：分桶温度拟合（粒度可配）+ 三原语分别校准（`score` 档**保序**）+ 校准集隔离（独立于训练集，按轨迹/租户划分）+ 校准参数独立制品（与权重解耦）+ 三态对照（零样本/训练后/校准后）+ **ECE 达阈为验收硬线**（未达阈本版不通过）+ **校准判据纪律从设计承诺落地为门禁**（决策路径上未标注校准依据的阈值 → 断言失败）+ 校准漂移监测（只提示不阻断）+ 既有置信度普查登记 + **弃权语义独立化**（弃权为一等输出 / 0.5 是真值概率非「中等可信」+ **门槛不是 0.5 且按原语分档**（`noul` / `choice` / `score` 置信剖面各异，`score` 消费**整条分布**；起步锚点取自**生产分布实际峰位**——门槛错位会让门控**静默失效**而非报错） / 三源分别留痕 / 弃权率可观测 / **门控健康度四联红旗**：ASK 率趋零 · 获批率恒 100% · 审批时延 1–2 秒 · 人均审批量异常高）+ **弃权之外的稳定化重读**（逐问句看熵、超阈带新噪声重读取平均；默认关闭且校准须同口径重测）+ **校准上线前置门**（ECE 未达阈时弃权默认开启且不可关闭；新增判据域弃权带默认最宽）。 · **🧮 判定底座 · 阶段二收口 · 三原语概率面与基线对照**：三原语完整输出面（choice 分布+熵 / score 区间+序 / noul 概率+**四态映射**）+ 建议动作**不具执法力**（执行权归既有规则与审计层）+ **证据面不扩大**（只提高已获证据利用率，不新增采集）+ 输出直落 decision-log + 三态对照落数 + **判定质量与概率可信度分列报告**（禁合并单一分数）+ 失败模式复扫（已解决/部分改善/未改善）+ 阶段二收口判据（双达阈才启动阶段三）。**形态归属**：主干 6 章（训练目标与评分规则 / 校准实现 / 校准判据纪律落地 / 弃权语义与校准上线前置门 / 三原语概率面完整化 / 对照基线与阶段二收口）。**本版由旧 v1.7.0、旧 v1.7.1、旧 v1.7.2 撤并而来** | [日志](./changelog/v1.7/v1.7.0.md) |
| **v1.8.0** | 📋 规划中 | **🔌 判定底座 · 阶段三（接线与分发）· DecisionChannel 真实实现**：以阶段一/二产物作 `DecisionChannel` **默认实现**（填 [v1.5.4](./changelog/v1.5/v1.5.4.md) 留空）+ 契约可插拔测试（换实现不动契约）+ **L1 判定层从「恒返回未就位」切换为真实可用**（**保留未就位兜底路径**——不留空窗、不静默放行）+ 云端 provider 默认关闭（opt-in 留痕）+ 失败语义 fail-closed（失败退 L0）+ 判定结果落 decision-log（含概率+四态+routeReason）+ 判定不占主模型槽位（行为锁）+ **判定面输入契约**（state 构造唯一入口 / 最小可判定前缀 / 定序去噪 / 协议结构保留 / 超限不静默截断 / 输入与留痕同源**但记指纹而非原样 payload** / **写时锚定且可脱离原模型回放** / **动作输入取结构化参数向量**·shell 包装可拆则拆、拆不动整串从严 / **判定件出站脱敏**·敏感字段判定前摘除、编码后发送面先解码再筛、敏感输入不发送转本地判）+ **问句标识不上行**（判据名与问句 id 不进模型）+ **影子运行门**（先影子跑、与既有处置对账后再启用）。 · **🖥️ 判定底座 · 阶段三 · 一体机推理接入**：**并行约束解码**（同状态全部问句一次前向出答案，KV cache 广播 + 单次前向填充全部字段）+ **类型约束解码**（解码层保证类型正确，与 schema 层双重保证）+ 独立问句并行/有依赖问句串行（依赖显式声明，不自动推断）+ 判定独立槽位通道（不与主模型抢槽）+ 离线可用（不联络外部服务）+ **实现路线不绑定**（契约由 DecisionChannel 保证，并行约束解码 / 离散扩散去噪 / 零微调均属实现面，契约测试以接口为准；**外部实证**：同期生态在不足两天内出现**六个架构互不相同的实现**却全部收敛到同一条接口——**护城河不在模型架构**，在契约、校准与判据质量）+ **超词表上界的选项集分块**（分块打分 + 末轮归一 + 「以上都不是」槽，候选集不静默截断）+ **单 token 可映射性硬前提**（零微调路线要求答案标签恰好编码为 1 个词表 token，多 token 标签走「逐 token 概率连乘」或「整串概率合并」变体——构造期断言，非运行期兜底）+ **职责频率分层**（延迟即职责边界，判定永不进控制回路与安全反射；无 GPU 的 CPU 档为最低可用档）+ **路线选型四道筛**（顺序即优先级：信创与离线 → 边界不做模型托管 → 审计概率来源可追溯 → 一体机耦合零新增运行时。结论：**主力**免训练占位槽 logits 直读分布（零训练 / 零新增运行时 / 审计链最短 / 语言能力随部署方基座可配）；**辅助**窄域小模型打分头（未校准前不得作为概率消费）；**条件启用**离散扩散（按硬件档，信创 CPU 档不启用）；**明确不做**逐客户训权重（越界而非效果问题）；主力路线的**校准缺口须由校准层补齐**，不得以「零样本强」代替「校准强」）+ 延迟/吞吐/并发劣化曲线落数 + **路由判定档（`decision-model`）从插槽接通为可达**（不进自动矩阵 / 绝不落云端生成分支 / 可观测）。 · **📦 判定底座 · 阶段三收口 · 权重分发通路与自带件形态成立（含叙事章）**：`weights-manifest` 补 `remote` 字段（**纯增量**，既有五环零破坏）+ 下载件（断点续传/重试/进度，接续 v1.4.2 排期）+ **sha256 篡改检测 fail-closed**（不匹配拒绝启用并留痕）+ 来源白名单（防供应链投毒）+ 多版本并存与回滚（复用既有 `model_switch`）+ **三层分发物**（代码包 / 规则资产 / 模型制品，权重版本与代码版本松耦合——契约由 `DecisionChannel` 保证非版本号）· **自带件形态成立**（实现+离线推理+分发通路三要素齐备，本版**首次**具备对外叙事资格；仍不得宣称准确率——证据链属 v1.9.0）。**形态归属**：主干 6 章（DecisionChannel 实现落地 / 判定面输入契约 / 并行约束解码 / 路由判定档接通 / 权重分发通路 / 自带件形态成立）。**本版由旧 v1.8.0、旧 v1.8.1、旧 v1.8.2 撤并而来** | [日志](./changelog/v1.8/v1.8.0.md) |
| **v1.9.0** | 📋 规划中 | **🧩 判定底座 · 阶段四（消费与收口）· 既有判定面收敛与假概率正名**：三原语既有实现补概率（`fde_classify` 补全候选分布 / `evaluate`+`quality-judge` 补可信区间 / `shouldAllow` 并列输出概率而**布尔执行语义零改动**）+ 判定源统一路由到判定底座（**降级路径保留**，底座不可用时行为与今日一致）+ 24–25 条审计规则判定面**零改动** + **假概率正名**（实测普查：引擎既有置信度数值无一是校准概率——硬编码常数正名为**启发式权重**、枚举映射数值保留**序数等级**语义；**全仓 `probability` 字段零命中**）+ 数值携带语义标记（概率/权重/序数）+ **不静默改行为**。**边界**：本版产出是「诚实」而非「更强」。 · **📊 判定底座 · 阶段四 · 判据面扩展与四层性能标准（含叙事章）**：判据面从审计规则扩至 **workflow / 本体数据 / 路由判定**三域 + **决策模式词表查漏**（对照业界已收敛分类法逐模式对查——重试控制/并行扇出等无落点者逐个裁定） + 各域校准状态显式声明（未校准域输出**不进决策路径**）+ 未验证域（开放式/主观/长链推理）显式声明不宣称能力+ **判定不适用面**（要生成散文 → 生成模型；需多轮推理或工具调用 → Agent；**决策是结构化输入的纯函数、能用代码直接写出 → 去写代码**；十毫秒级延迟预算 → 小分类器。判定的适用面是「**输入没法写死、但答案空间可以写死**」）+ **三层级联**（判定管内环做出带概率的判断 → 确定性代码处理它能处理的 → 生成模型只接难例；生态共识「判定只判断，执行归代码」——门槛、风险否决与执行动作一律留在确定性代码） + **四层性能标准**（判据级准确率 / 校准级 ECE / **分流级三段门控真实错误率** / 端到端延迟吞吐）+ **MLflow exporter 退役执行**（评审口径落定后删除，非 MCP 工具计数不受影响）**分层落数分列报告**（禁压成单一分数）+ **确定性基线先行**（正则/词表/TF-IDF 先落数——判定件只在基线到不了的档才有存在理由）+ **第四方对照**（高能力本地档 / 大参数云端档 / 云端快档 / 同类开源判定件，同判据集同评测器，四维并列，第三方数字标注口径）+ **静默失败监控**（四探针：输出分布漂移 / 概率分布异常 / 弃权率突变 / 输入长度-延迟相关——超阈告警不阻断但必须可见）+ **判定使用度量**（采纳率 / ASK / DENY / 被人工推翻率——「无人消费」与「被推翻」必须可区分 / **门控四联红旗**：ASK 率趋零 · 获批率恒 100% · 审批时延 1–2 秒 · 人均审批量异常高）+ **设计有效性与运行有效性分列**（判据齐全 ≠ 实际判过；**运行证据缺失本身即一条 finding**）+ **压缩留痕完整性**（压缩前的判据输入引用可还原，摘要不能替代）+ **公开独立实测存档**（独立机构实测商业判定件：777 次判断 < 0.7 s、成本约四分之一美分、**准确率比前沿模型低一档**；开源复刻作者在一体机同款机型上的对照：扩散路线 198/201 vs 商业件 191/201——两条评测集均系对方自建，只并列不排名）+ **分布内外分列与不利结论照录** + **对手格式破坏率的通道偏置说明**（两位数百分比的格式失败源于对照通道不对称——判定件结构由契约保证、对手结构靠提示与重试维持；对手方适配器**自承该对照并不公平**，不写这条会把格式失败误读为能力差距）+ **多源数字冲突逐源并列禁拼合**（同一发布物在不同来源间出现过十个百分点级差异，各源各自是有效读数、拼合出的第三个数不是任何一源的读数）+ **能力边界实例存档**（单据与表单分类：261 份表单严格准确率 100%、单页约千分之一美元、比原生成管线便宜三十余倍；高频决策回路：每约三百毫秒一次带概率判断且执行全留代码——两者评测集系对方自建，只并列不排名）+ **同集可比对**（唯一同集同口径的一对公开数字：某公开评测严格公共子集 343 对上商业件 86.9% vs 开源复刻 76.7%）+ **可复算产物**（每个数字附脚本与结果文件，或标注单次运行口径）+ **判定调用密度**（每任务判定次数，**须区分串行调用与打包请求**）+ **打包并发的成本杠杆**（同状态多问句须打包为一次请求——五次串行合并后延迟降约五倍、成本降约一个数量级；这是判定层最大的单点成本优化）+ **成本主体是输入而非输出**（账单几乎全由输入 token 构成，着力点是问句与 state 的尺寸而非压缩输出——贵的是提示词，不是模型）。**纪律**：不比通用 benchmark。 · **🏁 判定底座 · 施工期收口与全站口径对齐（含叙事章）**：四阶段收口判据逐项核对（达阈/未达阈如实标注）+ 两条分支（全部达阈 → 进 V2.0；任一未达 → **施工期内增补补丁位**，不带着未达阈项进 V2.0）+ 全站口径对齐（术语统一 / 判定面定位 / 边界如实 / 制品架构表述）· **版本弹性机制落盘**：阶段内可增补补丁位（`vX.Y.1`）不挤后续阶段；新工作面**插入新阶段**（如 `v1.10.x`）保持原编号不重排；**V2.0 时点不锁死**（由「四阶段收口达阈」决定，不由版本号决定）。**不做**：产品身份切换（属 V2.0，本版只铺平口径）。**形态归属**：主干 8 章（三原语既有实现补概率 / 假概率正名 / 判据面扩展 / 四层性能标准 / 第四方对照 / 静默失败监控与判定使用度量 / 施工期收口判据 / 全站口径对齐）+ 非功能 1 章（版本弹性机制）。**本版由旧 v1.9.0、旧 v1.9.1、旧 v1.9.2 撤并而来** | [日志](./changelog/v1.9/v1.9.0.md) |
| **v2.0.0** | 📋 规划中 | **🏁 S1M 宣告大版本（FDEing × S1A 身份切换·S1A = S1M + Harness）**：**S1M 首次定义**（System One Model 系统一模型——本仓决策模型之名，判定面为本体、约束层五能力为自带治理环境，闭合 [v1.9.0 §八](./changelog/v1.9/v1.9.0.md) 使用的术语债）+ **System 1 Agent 品类宣告**（三因子组合的品类名——宣言「不是更快的 Agent，是判断先被治理的 Agent」，与 FDEing 动词化同批构成两项心智占位）+ **名字含义升级 backronym**（sofagent = System One For Agents——名字零改动，沙发谐音降为品牌彩蛋）+ 身份宣告章（三因子各归其位：**FDEing**=能力底座（FDE 进场判断力产品化）/ **S1M**=本体（决策模型）/ **harness**=治理环境；宣告依据链四件：判据 schema 定稿 / ECE 达阈 / 形态三要素 / 四层性能标准——缺任一不启动）+ **全站术语清洗**（「审计引擎 harness」断言承载面普查 + 同批改写——[v1.6.0 §三](./changelog/v1.6/v1.6.0.md) 触发条件首次全站级实例，双语对称，changelog 历史区不动）+ 对外叙事重定位（README「这是什么」与能力段按新身份重写——判定底座在前、治理环境在后；边界四判据同步；LIMITATIONS 补决策模型失灵面）+ **宣告版零新能力纪律** + 原规划去向落盘（**网络出口治理面与 AI 节点已按归位再分配分落 [v1.5.2](./changelog/v1.5/v1.5.2.md) / [v1.5.4](./changelog/v1.5/v1.5.4.md)**——版本边界纪律：V2.0 前的能力不放 V2.0 后的版本去迭代）+ **V2.0 后版本域纪律**（V2.0 起全部是 S1M 的迭代域，V2.0 后做什么暂不定义、由 S1M 自身的产品化需求驱动）。**前置门：v1.9.0 四阶段收口达阈（硬门）** · **兼容层退役清单**（登记在仓兼容层——L1 未就位兜底 / 判定源降级路径 / `Choice` 纯布尔兼容 / 旧命令 shim / DSH fallback 保险绳，逐条挂**可机械观测**的退役触发条件；立「新增兼容层须同批登记」纪律；旧命令 shim 兼容期届满列可执行退役）+ **已交付能力面退役清单**（三因子归属分类——已交付能力簇逐条归 FDEing / S1M / harness 治理环境或显式标无归属；定位漂移候选具名挂触发条件（USB 完整运行时 / 浏览器四件套 / MLflow exporter / daemon 包职责过载）；边界补划界三条；V2.0 后版本域冲突面 9 处逐条裁定；立「新增能力须标三因子归属」与「真删三条件」纪律）。**发布面（S1M 权重上平台）**：三通道同批首发（Hugging Face 主载域 + hf-mirror 镜像 + ModelScope 魔搭）· **四方 sha256 对账**（本地构建 = HF = hf-mirror = ModelScope，任一不符即发布中止）· 权重代次号独立轨道（V2/V2.1 式，非 v2.0.0 三段号，与仓版本松耦合）· 制品双拆分（GitHub = 代码制品、随仓版本；HF/ModelScope = 权重制品 `sofa-s1m-v2`、随代次）· 发版 SOP 须补「权重发布」节（权重发布与 npm 发版 + GitHub Release 同一发布事件）· **权重 license 定稿 MIT**（对齐 DeepSeek——代码/权重/判据集/评测器四制品同 MIT）。**形态归属**：主干 2 章（身份宣告 / 对外叙事重定位）+ 非功能 5 章（全站术语清洗 / 版本收口与补丁位约定 / 原规划去向落盘 / 兼容层退役清单 / 已交付能力面的归属与退役清单） | [日志](./changelog/v2.0/v2.0.0.md) |


### 判定底座施工期（v1.6.0 – v1.9.0 · 四阶段四版）

> **起点与依据**：判定底座（可治理的决策模型底座）的施工从 **v1.6.0** 起，四个阶段各一版（原十二细版按「同一阶段 = 同一工作面群」归并）。**为何分阶段**：判定底座是一个**模型**——其开发量落在「判据 / 数据 / 训练 / 校准 / 接线 / 分发 / 收敛 / 验证」多个独立工作面，任一面的实际工作量都可能超出预估；版本颗粒度粗会让单版内容失控，或让「还没做完」被挤成「先宣告再补」。

| 阶段 | 版本 | 主题 | 该阶段出口条件 |
|:--:|------|------|------|
| 一 | v1.6.0 | 判据与数据地基——让概率有训练目标 | 判据 schema 定稿 + 数据集可校验 + 零样本基线落数 |
| 二 | v1.7.0 | 模型与校准——让概率变得有意义 | **ECE 达阈** + 判定质量与概率可信度双达阈 |
| 三 | v1.8.0 | 接线与分发——让概率可用 | DecisionChannel 实现 + 一体机离线推理 + 权重分发通路 |
| 四 | v1.9.0 | 消费与收口——让概率进入既有判定面 | 三原语补概率 + 假概率正名 + 四层性能标准落数 |

**版本弹性机制**（防止「后面版本不够」而挤版）：

- **阶段内开发量超出** → 在该阶段内**增补补丁位**（`vX.Y.1`），不挤进后续阶段（阶段内主题一致、依赖连续，增补成本最低）
- **本版交付后发现缺陷需修补** → 在该 minor 内开补丁位（`vX.Y.1`），不挤后续 minor
- **出现新识别工作面** → 在阶段之间**插入新阶段**（如 `v1.10.x`），**保持原四阶段编号不重排**（编号稳定便于引用，避免全仓版本号重写）
- **V2.0 时点不锁死** → V2.0 在「四阶段收口达阈」时点到来，**不由版本号决定**；未达阈则增补版本而非放宽标准。**V2.0.0 已定位为 S1M 宣告版**（身份切换 FDEing × S1A（S1A = S1M + Harness），宣告版零新能力；**版本边界纪律**——V2.0 起版本全部是 S1M 的迭代域，V2.0 前的能力不放 V2.0 后的版本去迭代，网络出口治理面与 AI 节点已按归位再分配分落 v1.5.2 / v1.5.4）
- **增补须有依据** → 每次增补须有明确的未达阈项或新识别工作面，「感觉还没做完」不构成依据
- **不允许**：把多个阶段的工作挤进一版；或为「按计划进 V2.0」而放宽达阈标准

> 📖 各版完整交付与验收标准见 [changelog 目录](./changelog/README.md)；设计依据（三原语对位、权重分发与制品架构、判定面边界）见维护者侧判定底座设计文档（仓外）。

> 维护者口径（场景数 SSOT、加载链预算跟踪）已迁 [DEVELOPMENT](./DEVELOPMENT.md)——路线图只讲方向，不讲内部对账细节。当前值 373（最大场景号 S448；SSOT = `playbook/acceptance-test.sh` 头部声明，逐条对账见 [DEVELOPMENT](./DEVELOPMENT.md)）。

---

## 行业印证

> 完整行业对标（DeerFlow / Omnigent / DataFlow / OpenWorker / OpenFDE / a16z 七法则 / Graph Engineering / 5 阶段风险收敛）统一见 [VALIDATION](./VALIDATION.md)。以下仅保留与版本规划直接相关的结论。

**落地纪律**：行业对标均为「用行业术语框定已有/规划能力」，不新增能力范围。外部框架是设计启发 + 开源借力，非依赖引入。

**热度信号**：2025-2026 硅谷「AI 自进化 / Loop」成为最热关键词（斯坦福 2025 秋季自进化公开课），其「工具调用 + 验证器 + 评审器 + 编排器」四件套与激活链（ACTIVATE→ORCHESTRATE→EXECUTE→SUSTAIN）逐件对位——激活链不是追热点，是提前踩中趋势。方法论印证见 [VALIDATION · Verifier 才是瓶颈](./VALIDATION.md#verifier-才是瓶颈) 与 [VALIDATION · 循环系统的鲁棒性](./VALIDATION.md#循环系统的鲁棒性四类故障与六要素)。

---

## 探索方向

> 探索方向 = 想到了但还没排进具体版本的方向。已交付的见[迭代历程](#迭代历程)，已排期的见[版本规划](#版本规划)。

> 📋 **探索方向清仓纪律（评审拍板「逐项裁定、排进 V2.0 前」）**：**版本边界纪律**（V2.0 起版本全部是 S1M 的迭代域，V2.0 前的能力不放 V2.0 后的版本去迭代）意味着**凡是 V2.0 前的治理面能力，窗口只在 v1.5.x–v1.9.0，逾期即永久出局**——而探索方向表的定义是「还没排进具体版本」，长期挂着等价于默认丢弃。故凡是**属于 V2.0 前治理面能力的方向，一律逐项裁定：排进具体版本章，或显式关闭**（不留「长期挂着」第三态）。已裁定去向：事前授权 mandate → [v1.5.2](./changelog/v1.5/v1.5.2.md) · 判决类记录成对完整与 AuditScope 重构 → [v1.5.3](./changelog/v1.5/v1.5.3.md) · 授权面与凭证面对账 → [v1.5.4](./changelog/v1.5/v1.5.4.md) · 国标 Agent 审计对位 / 决策日志作为审计输入源 / ACS YAML 策略引擎评估 → [v1.5.7](./changelog/v1.5/v1.5.7.md) · RL 训练治理 → [v1.5.8](./changelog/v1.5/v1.5.8.md) · 透明仓与敌手自测合流 → [v1.5.9](./changelog/v1.5/v1.5.9.md)；关闭两项：docs 文体归位（自述声明式已落地）、HITL 结论作判定层标注数据（已由 [v1.6.0 §六](./changelog/v1.6/v1.6.0.md)「HITL 采集」承接）。**改判不等于触发条件已满足**：多数项的原触发条件仍未达，改判理由是窗口消失（不等了），各章内逐条写明。留在表内的条目按其自述的「不提前抽象 / 无版本单元格」纪律继续留存——**它们不是 V2.0 前的治理面能力，不受本纪律约束**。

| 方向 | 一句话 |
|------|------|
| **自带净水设备的水龙头（v3.x+ 远景）** | Subagent 支持挂载外部精调小模型（约束层提供路由与加载插槽），零投喂、本地推理、离线可用 |
| **数据 schema 迁移管道（Omarchy migrations/ 启发）** | `~/.sofagent/data/` 数据格式演进的机制兜底：按版本号顺序执行迁移脚本 + 幂等可重跑（`sofagent doctor --migrate` 或升级时自动执行）。**不提前建管道**（无提前抽象纪律）——v1.5.0 trace 新数据源已埋 `schemaVersion` 前置件，首个破坏性 schema 变更真实出现时再触发评估 |
| **大文档三档拆分（概念/决策/清单）** | ARCHITECTURE/PHILOSOPHY 类大文档按「概念/决策/清单」三档拆分重组，控制单文档认知负载——归文档优化专项（无版本单元格，触发时机=下一次大规模文档新增时） |
| **DSH-only 执行面收尾（保险绳拆除）** | FORGE 执行后端 v1.4.3 起全 step 默认 DSH，LangGraph 仅作 fallback 保险绳（`FORGE_FRESH_EYES_BACKEND=langgraph` 回退口 + langgraph-backend.ts + audit-middleware 包裹路径）。DSH 发布非 alpha 稳定版且连续 2–3 个完整 run 零 fallback 时，删除兜底面收尾为 DSH-only——driver 侧质量门控（熔断/产物门控/宽限窗）是产品逻辑不随迁，留在 driver。alpha 期实测已两遇破坏性变更（0.1.2-alpha.3→rc.1 砍 `Session.events`，防御见 snapshotSessionEvents 双形态兼容），保险绳成本低、拆除不急 |
| **统一改进调度器（RSI 组合层 · 各面成熟后再议）** | 失败信号→改进面路由的统一调度器（该改 Harness 还是改 Model 由调度器判）。裁定不进 [v1.5.8](./changelog/v1.5/v1.5.8.md)（马场——经验池/准入/晋级/考核四面未熟前是过度抽象，组合层的价值随被组合面的成熟而增长）。触发条件：[v1.5.8](./changelog/v1.5/v1.5.8.md) 四章交付且各面有真实运行数据后评估 |
| **证据驱动分组（GEE 式 evolve 升级）** | instinct→skill 聚合从 2-gram 浅分组升级为证据驱动（工具调用直方图 / 高频命令 / 重复纠正聚合后再判哪些模式成 skill——RSI-Harness GEE 思路，轨迹数据 execution-backends/trajectory.ts 已有）。裁定马鞍但价值中、独立可做——不占版本格，触发条件=下一次 evolve 模块返工窗口 |
| **进化件退役修剪面（pruner · 进化的第三道门）** | [v1.5.8](./changelog/v1.5/v1.5.8.md) 准入管「进得来」、晋级管「升得上去」，**没人管「留下来的还值不值」**——行业实证：无复杂度惩罚项时 harness 自进化**单调膨胀**（每条留下的编辑都永久吃 token），带 pruner（删「太小 / 太贵 / 不再有用」的变更）后 token 降 30% 且 OOD 泛化反升（[RRSI](https://arxiv.org/abs/2609.24972)）。对位落点：skill 台账按「最近 N 次调用零命中 + 无 solves 溯源」产出退役建议（只提示人审，对齐 promote-train 同款纪律）；非自动删除——与「三层可逆回退」对称，是回退的常态化面。**马鞍**（进化面的治理动作，非引擎）；不占版本格，触发条件=v1.5.8 交付后随晋级台账一并评估 |
| **停环判据（何时停止进化迭代）** | v1.5.8 有准入（进得来）与晋级（升得去），无「何时该停」——生态建议停止规则：优化分数持续上升而留出集 / 正交度量停滞或失稳时应停止迭代（继续只提高代理目标被利用的概率，与作弊率观测位互为表里）。来源为厂商技术博客未标日期（权威性低），登记备查、落盘前须回溯原始出处。**马鞍**；不占版本格，触发条件=v1.5.8 交付后随作弊率观测位一并评估 |
| **harness token 效率四机制（SoL-Pi 观察）** | 生态自动搜索发现的四个 harness 机制——Action Fusion（动作+命令合并为一次请求）/ Online Context Compact（计划步完成时按成本门压缩）/ ObservationPack（大输出归档换句柄）/ Evidence-Preserving Reducer（低成本模型压缩构建日志+确定性校验）——token 流量降 44.7–49%、API 成本降约 1/3，且冻结 harness 跨后端迁移成立（[SoL-Pi](https://arxiv.org/abs/2609.20519)）。与本仓 [v1.5.2](./changelog/v1.5/v1.5.2.md) 判定面输入契约（state 构造/最小可判定前缀/超限不静默截断）同域。**观察登记不实做**——属成本优化非治理必需，触发条件=一体机场景对 token 成本敏感度上升时再评估哪些机制可收编进判定面输入契约 |
| **回放式评估（已完成轨迹即模拟器 · 审计链的评估面复用）** | 把 HMAC 审计链上已完成的执行轨迹当**精确回放模拟器**：候选改法在历史轨迹上离线重放打分，零新增执行（行业实证：探索树重放使 agent 调用降 1.7×–162×，[Dream-RSI](https://arxiv.org/abs/2609.14858)）。两条硬纪律随迁：① **回放只许看已揭示前缀子树**——禁偷看未揭示分数与最优解、禁硬编码赢家路径（否则评估面自己被污染）；② **候选集永远包含现役策略** ⇒ 数学上保证不劣于现状（比「严格优于历史最佳」更保守的兜底）。本仓审计链 + decisions 落痕天然是这份原料——评估器消费审计链，**不做改进调度器本体**（定位不变）。**马鞍**；不占版本格，触发条件=v1.5.9 第四章评估器解耦落地后再评估（评估器先在位，回放才有消费方） |
| **动态判定面收窄（Jev 生态启发）** | 判定请求的**候选面按上下文动态收窄**——只把此刻真正可选的项与目标交给判定模型，而非永远给全集（生态实现：`browser-use/jev-ultrafast` 的动态动作空间索引）。收益是判定面与误判面同步变小；**代价是同一问句在不同收窄下的概率不可比**——收窄后的置信度与门控阈值须重新对齐，否则消费的是另一个问题的概率。**不提前抽象**：三原语问句集按工作流节点静态限定（[v1.6.0 第一章](./changelog/v1.6/v1.6.0.md)「问题集限定」）已够用；触发条件=出现「候选面随 state 变化」的真实判定场景（如可用工具集按权限动态浮动），且校准面能给出收窄后的可比口径 |
| **投机式预判（Jev 生态启发）** | 一次往返**投机性地问出多个可能目标**、按最终操作结果丢弃其余（生态实现：`browser-use/jev-ultrafast` 的多目标一次往返）——用一次判定的成本换掉串行等待，代价是算多余的份。与 [v1.9.0 第四章](./changelog/v1.9/v1.9.0.md)「打包并发的成本杠杆」同族（都指向「别串行问」），但多一层「预先猜后续路径」。**边界（与打包的划界，2026-09-22 登记）**：打包问的是**已定**的多问（同状态一次前向），投机问的是**未定**的候选路径——① 后者的多余成本**不得混入** [v1.9.0 第四章](./changelog/v1.9/v1.9.0.md) 判定调用密度与成本基线的读数；② 立项前须先答「多问是否共享同一 state」——不共享则打包不成立、成本杠杆不适用，需另算账。**不提前抽象**：打包问句已覆盖「同状态并行」这一层；触发条件=出现「判定延迟成为端到端瓶颈、且后续路径可枚举」的真实场景，届时**先量猜测命中率**再决定是否立项 |
| **单机全流程（训练 → 推理 → 后训练不出本机）** | 目标是「一台普通笔记本跑完全流程」：用户从 GitHub 取本仓与基座权重，在本机完成 LoRA 后训练、本机推理判定、本机继续积累迭代——**数据不出机、模型可替换、后训练权在用户手里**。**三块地基已在**：[PHILOSOPHY · 远期演化愿景](./PHILOSOPHY.md#远期演化愿景从内置小模型到自动化企业后训模块) 的「本地小模型可跑」、[v1.7.0 第一章](./changelog/v1.7/v1.7.0.md)「本地 LoRA 档为交付形态下限」、[v1.8.0 第三章](./changelog/v1.8/v1.8.0.md)「最低可用档 · 无独立 GPU 的单机」。**对外口径先立死三条**：① 「能跑」按**可用内存档位**说（可训规模由可用内存决定，不由机型品牌决定）② 「单机」须**分清推理侧与训练侧**（生态实测：推理侧笔记本档已可直接服务，训练侧小档已跑通、大档仍在云卡）③ 基座选型必答「**目标硬件上有没有快核**」（生态实测：同尺寸换用无该硬件快核的架构后慢 3–7 倍）。落数见 [v1.9.0 第四章](./changelog/v1.9/v1.9.0.md) 端到端档位分列。无版本单元格、不排期，触发条件=首个真实用户在单机上走通「后训练 → 判定服务」闭环并留下可复算读数 |
| **运行期宿主装配（通道章宿主侧收尾）** | v1.5.1 的三条通道章（G12 设备 OTA / 任务下发通道二期 / `sofagent demo`）交付的是**契约与执行侧**，宿主侧装配不在该版落点，留下三项悬空：① **签名者信任锚**——`verifyDeliverySignature` 只证「摘要由信封**自带公钥**签出」的自洽性，全链无平台公钥 pin / 无 principal 白名单 / 无设备注册表绑定（[SECURITY](../SECURITY.md#g12-设备远程下发面升级--内容下发--任务推送) G12 ① 已如实披露）② **订阅登记调用方归属**——`registerDeviceSubscriptions` / `deviceTaskReceipt` 零生产调用方，需定「`device_register` 成功后调用」还是「设备进程启动时注册」（含重启幂等语义）③ **死信批量排空器**——`listDeadLetters()` 聚合枚举已在位，缺的是批量**动作**入口；排空器须逐条确认，沿用 `replayDeadLetter` 的「非 retryable 需显式 `force`」守卫。**触发条件 = 首次接真实传输通道**：第①项在该时点由「设计期已知缺口」直接变为「已暴露面」（当前三条下发面均为注入端口、外部不可达），须同批落地；②③ 可与①同批，也可在设备侧生命周期语义定稿后单独落地。归因 **W3**（已交付能力的缺陷与债务 · 悬空承诺）｜ 无版本单元格 |
| **敌手自测（audit 红队）** | 定期以攻击者视角尝试绕过审计——篡改 `history.jsonl` / 伪造 HMAC 链 / 绕过 ToolGate，结果进 [anti-cases](./evidence/anti-cases/README.md)。审计系统的可信度靠「被打过而没被打穿」证明；与「透明仓」触发条件（第三方首次独立审查）天然合流。无版本单元格、不排期 |
| **MCP tool 名前缀化（破坏性，多 server 撞名风险驱动）** | 107 个 tool 现状 verb_noun 无前缀（v1.5.2 已统一唯一前缀残留 sofagent_compose → compose，见 API.md 工具命名策略）。多 server 共存挂载时通用词可能撞名、宿主去重改名后无法归因；整体前缀化属破坏性变更，评估后另定版本。无版本单元格、不排期 |


> 📖 DeerFlow / OpenFDE 方法论印证见 [VALIDATION](./VALIDATION.md)。

> 以下「分层模型架构」为探索方向的核心技术骨架概述。当前版本约束层未涉及，v3.x 才启动。

## 分层模型架构（v3.x 远景概述）

核心驱动力 = **数据主权**（企业数据进 API key 大模型 = 一定被拿去训练）。三层模型 + Harness 路由：云端档负责规划推理 → 翻译成标准化指令 → 本地档执行工作流判定与多步 workflow → 本地管道档跑固定管道（模板/格式/字段提取）。敏感数据只在本地处理，通用知识才走云端。**具体模型规格不写死在文档层**——由业务窄域宽度、延迟预算与客户硬件决定，路由层只认抽象档位。**约束层只做模型路由（`model-router.ts` 已有云端两档 / 本地两档插槽 + 判定档 + 拦截出口），通用大模型精调属模型层非开源范围；决策模型为约束层自有底座，随本仓开源。** 路由层本体已在位、不依赖精调模型（调用侧分流归位 [v1.5.4](./changelog/v1.5/v1.5.4.md)）；**外部 AI 节点治理接入见 [v1.5.4 第五章](./changelog/v1.5/v1.5.4.md)**（不做「引擎烧进 USB」的运行时合体，改为 DeepSeek harness 造节点 + LangChain/LangGraph 编排 + 本仓治理接入——版本边界纪律下归位 V2.0 前）；v3.x-v4.x+ 剩企业专属小模型精调（QLoRA distill 轻量化）。完整技术骨架（Mermaid 图 + 选型表 + 实现难度）见 [PHILOSOPHY · 远期演化愿景](./PHILOSOPHY.md#远期演化愿景从内置小模型到自动化企业后训模块)。

---

## 欢迎参与

| 你能做的事 | 时间 | 说明 |
|------|:--:|------|
| 跨平台测试 | 30 min | 你有 Codex / Hermes / Claude Code？装一下告诉我们 |
| 补充 FAQ | 20 min | 你踩了什么坑？直接改 [HANDBOOK](./HANDBOOK.md) 的「排查与自定义」节 |
| 文档翻译 | 1-2 h | 英文翻译对社区意义巨大 |
| 第三方证据 | 1 周 | 装完用一周，填 [docs/evidence/evidence.md](./evidence/evidence.md) |
| 安全审计 | 不限 | 给 SECURITY.md 较真 |
| 企业场景反馈 | 30 min | 你们团队怎么用 Agent？直接开 Issue |

> [CONTRIBUTING.md](../CONTRIBUTING.md)

---

## 历史架构演进

编排模块从 ao → DeepAgents → LangGraph 的升级史（v1.2.0 起 FORGE loop 已完全弃用 deepagents，改用 createReactAgent；历史编排模块的 DeepAgents 调度原型见 v1.1.8 changelog）、Ontology 从实体关联到本体数据的渐进构建、外部框架对标（Palantir/gbrain/WeKnora/Runta）、Loop Engineering 全栈对照等详见 **[ARCHITECTURE.md](./ARCHITECTURE.md)** 的「架构设计决策的行业锚点」+「编排收敛与 A/B 测试」+「本体数据 = GitHub 生长树」章节，以及各版本 **[开发日志](./changelog/)**。

> 📖 多设备同步方案见 [多设备同步指南](./guides/multi-device-sync.md)。

> 📖 loop-engineering 启发方向的去向：FDE 节点注册表 + Worktree 隔离已交付（v1.3.5 / v1.3.6），理解债务已排期（v1.5.1），quota 事前门禁 + 依赖方向测试已交付（v1.4.8）。来源链接见 [cobusgreyling/loop-engineering](https://github.com/cobusgreyling/loop-engineering)（MIT 开源）。
