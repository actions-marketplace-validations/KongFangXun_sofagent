# docs/reports · 一次性扫描快照区

本目录承载**一次性扫描快照**产物（如 `commons-retire-scan-*.json` 退役候选自扫）——非周期性报表、非交付物。

- 产物来源：tools 扫描任务落盘（文件内 `action` 字段自述来源与语境）
- 保留策略：按需保留、不承诺时效——快照反映落盘时点状态，过期不回填
- 归档去向：历史快照可移 `docs/archive/`，不强制
