# 工作流节点文档模板（以 sofagent "识别 AI 节点" 为案例）

> 本文件既是格式参考，也是一份真实案例。
> FDE 在 §5 补充本体信息（frontmatter），§7 完成运行配置（正文）。每个 🔄/⚡ 节点一份。
> 节点文档同时服务三个消费者：**人读**（看懂这个节点是什么）+ **编排模块读**（sofagent-orchestrator compose 拆任务）+ **加载链读**（被动注入知识库）。
> 实际产出路径：`nodes/[节点名].md`

```yaml
---
# §5 本体信息（FDE 补充——人和机器都读）
title: "[节点业务名称]"
type: entity
domain: "[域路径]"
relations:
  has_many: [关联节点ID列表]
  belongs_to: "[上游节点ID]"
knowledge-domain:
  include: ["*"]    # 谁能读这个节点的知识
  exclude: []       # 谁不能读
constraints: {}     # 硬约束边界
---
```

---

## 识别 AI 节点 · fde-node-3

| 属性 | 值 |
|------|-----|
| 分类 | 🔄 自动执行 |
| 所属岗位 | FDE |
| 负责人 | <负责人姓名> |

### 五要素

- **输入**：工作流节点图（来自 §4 梳理，结构化表格——每节点五要素齐全）
- **输出**：节点分类清单（🔄/⚡/👤 + 判定理由），推到飞书表格
- **原耗时**：每次 1 小时（10 个节点 × 6 分钟），每个企业 1 次
- **痛点**：判定标准因人而异——不同 FDE 对同一个节点可能判出不同分类
- **AI 后**：Agent 读节点图 → 三问判定法自动分类 → 输出清单。FDE 只 review 边界情况

### 判定理由

三问判定法，规则明确可执行：
1. 输入能从系统自动取到吗？
2. 处理规则能写清楚吗？（新人照着能做对）
3. 输出能自动推到人手里吗？

三个全「是」→ 🔄，两个「是」→ ⚡，否则 → 👤。

sofagent 自身的这个节点就是 🔄——输入是结构化的节点图、规则是固定的三问、输出是表格推送。

### 价值数据

| 指标 | 数值 |
|------|------|
| 当前成本 | 岗位年薪 × 12.5% 工时占比（1h/天 ÷ 8h 工作日） |
| AI 后成本 | 0.2h（review 边界情况） |
| 年节省 | 80% 时间释放 |
| 回本周期 | < 1 月 |

### 运行配置

| 配置项 | 值 |
|--------|-----|
| sofagent 版本 | full（编排 + 审计） |
| 审计周期 | daily |
| 审计重点规则 | A3, A7 |
| 检查点抽检率 | 10% |
| 质检员 | <质检员姓名> |
| 不合格处理 | 标记 → 反馈主人 → 记录异常 → 触发 Skill 优化 |
| 企业 Skill | `skills/fde-识别AI节点/SKILL.md`（注入了三问判定法 + 行业案例库） |
| Skill 迭代 | 开启自动迭代（检查点不合格触发，A/B 测试 3 轮，连续 2 次胜出才 promote） |
| Skill 当前版本 | 1.0（初始版本，节点跑起来后基于 eval.md 自动迭代） |

### 运行状态（三层实体确认）

- [x] 📄 文档层：本文档已创建（`nodes/fde-识别AI节点.md`）——人读 + 编排模块读
- [x] 🧠 Skill 层：企业 Skill 已创建（`skills/fde-识别AI节点/SKILL.md`）并注入——AI 读
- [x] 🔴 运行层：sub-agent session 已创建、已跑通——活的
- [x] 节点主人已确认会用
- [x] 检查点已配置
- [x] 已纳入审计周报
